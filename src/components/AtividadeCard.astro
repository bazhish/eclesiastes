---
import ContentTypeIcon from './ContentTypeIcon.astro';
const { id } = Astro.props as { atividade: unknown; id: string };
---
<section class="resource-detail" aria-labelledby={`${id}-title`}>
  <header><ContentTypeIcon tipo="roteiro" label={false} /><h2 id={`${id}-title`}>Material da Aula</h2></header>
  <div class="resource-detail__message">A resposta ainda não foi divulgada.</div>
</section>
