---
{"materia": "Banco de dados", "materiaSlug": "banco-de-dados", "bimestre": 3, "semana": 16, "ordem": 10, "aula": "a1", "titulo": "SGBD Relacional · Aula 1", "atividade": {"titulo": "Atividades Práticas", "enunciado": "Índices devem ser criados nas colunas usadas com frequência em filtros, junções e ordenações, depois de medir a consulta. Exemplo: `CREATE INDEX idx_vendas_cliente_data ON vendas (id_cliente, data_venda);`. O comando `EXPLAIN SELECT * FROM vendas WHERE id_cliente = 101;` mostra se o plano consegue usar o índice. Índices demais ocupam espaço e tornam `INSERT`, `UPDATE` e `DELETE` mais caros, portanto precisam ser justificados por consultas reais.\n\nEm uma base relacional, também é necessário manter chaves primárias, estrangeiras, integridade referencial e transações. A equipe deve testar consultas com dados representativos, monitorar tempo de resposta e revisar índices que não são utilizados.", "resposta": "Índices devem ser criados nas colunas usadas com frequência em filtros, junções e ordenações, depois de medir a consulta. Exemplo: `CREATE INDEX idx_vendas_cliente_data ON vendas (id_cliente, data_venda);`. O comando `EXPLAIN SELECT * FROM vendas WHERE id_cliente = 101;` mostra se o plano consegue usar o índice. Índices demais ocupam espaço e tornam `INSERT`, `UPDATE` e `DELETE` mais caros, portanto precisam ser justificados por consultas reais.\n\nEm uma base relacional, também é necessário manter chaves primárias, estrangeiras, integridade referencial e transações. A equipe deve testar consultas com dados representativos, monitorar tempo de resposta e revisar índices que não são utilizados.", "conteudo": "Índices devem ser criados nas colunas usadas com frequência em filtros, junções e ordenações, depois de medir a consulta. Exemplo: `CREATE INDEX idx_vendas_cliente_data ON vendas (id_cliente, data_venda);`. O comando `EXPLAIN SELECT * FROM vendas WHERE id_cliente = 101;` mostra se o plano consegue usar o índice. Índices demais ocupam espaço e tornam `INSERT`, `UPDATE` e `DELETE` mais caros, portanto precisam ser justificados por consultas reais.\n\nEm uma base relacional, também é necessário manter chaves primárias, estrangeiras, integridade referencial e transações. A equipe deve testar consultas com dados representativos, monitorar tempo de resposta e revisar índices que não são utilizados.", "fonte": "gabarito/Banco de dados/3° bimestre/Semana 16/semana-16-respostas.md#atividades-praticas", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
