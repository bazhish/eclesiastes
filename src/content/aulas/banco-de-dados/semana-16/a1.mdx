---
{"materia": "Banco de dados", "materiaSlug": "banco-de-dados", "bimestre": 3, "semana": 16, "ordem": 10, "aula": "a1", "titulo": "SGBD Relacional · Aula 1", "atividade": {"enunciado": "Uma empresa de tecnologia precisa gerenciar clientes, pedidos e produtos com integridade referencial. O modelo possui `clientes`, `pedidos`, `produtos` e `itens_pedido`; as chaves estrangeiras devem impedir pedidos sem cliente e itens sem pedido ou produto.", "resposta": "### Criação das tabelas\n\nO arquivo de solução cria as quatro tabelas com as chaves primárias e estrangeiras solicitadas. `pedidos.id_cliente` referencia `clientes.id_cliente`; `itens_pedido.id_pedido` referencia `pedidos.id_pedido`; e `itens_pedido.id_produto` referencia `produtos.id_produto`.\n\n### Integridade referencial com ON DELETE e ON UPDATE\n\nEm `pedidos`, a regra `ON DELETE RESTRICT` impede a exclusão de um cliente que ainda possui pedidos. Em `itens_pedido`, `ON DELETE CASCADE` para o pedido exclui automaticamente os itens de um pedido removido, evitando itens órfãos. Para cliente e produto, `ON UPDATE CASCADE` propaga uma eventual atualização de identificador para as tabelas relacionadas. Essas regras preservam a coerência entre os registros sem que a aplicação precise corrigir as referências manualmente.\n\n### Inserção de dados\n\nForam inseridos três clientes, cinco produtos, dois pedidos e dois itens em cada pedido. Todos os valores de `id_cliente`, `id_pedido` e `id_produto` usados nos relacionamentos existem nas respectivas tabelas.\n\n### Consulta de integridade\n\n```sql\nSELECT pe.id_pedido, c.nome, p.nome_do_produto, i.quantidade\nFROM pedidos AS pe\nJOIN clientes AS c ON c.id_cliente = pe.id_cliente\nJOIN itens_pedido AS i ON i.id_pedido = pe.id_pedido\nJOIN produtos AS p ON p.id_produto = i.id_produto;\n```\n\nA consulta retorna cada pedido com o cliente, o produto e a quantidade correspondente, comprovando que as referências foram gravadas de forma consistente.\n\n### Desafio — exclusão de cliente com pedidos\n\n```sql\nDELETE FROM clientes WHERE id_cliente = 1;\n```\n\nO comando falha porque o cliente 1 possui o pedido 10 e a chave estrangeira de `pedidos` usa `ON DELETE RESTRICT`. Esse resultado é desejado: sem a restrição, a exclusão poderia deixar pedidos que apontam para um cliente inexistente. Para excluir o cliente de forma intencional, seria necessário remover ou reatribuir seus pedidos, ou alterar conscientemente a regra de exclusão conforme a política do negócio.", "conteudo": "## Contexto\n\nUma empresa de tecnologia precisa gerenciar clientes, pedidos e produtos com integridade referencial. O modelo possui `clientes`, `pedidos`, `produtos` e `itens_pedido`; as chaves estrangeiras devem impedir pedidos sem cliente e itens sem pedido ou produto.\n\n## Tarefas e respostas\n\n### Criação das tabelas\n\nO arquivo de solução cria as quatro tabelas com as chaves primárias e estrangeiras solicitadas. `pedidos.id_cliente` referencia `clientes.id_cliente`; `itens_pedido.id_pedido` referencia `pedidos.id_pedido`; e `itens_pedido.id_produto` referencia `produtos.id_produto`.\n\n### Integridade referencial com ON DELETE e ON UPDATE\n\nEm `pedidos`, a regra `ON DELETE RESTRICT` impede a exclusão de um cliente que ainda possui pedidos. Em `itens_pedido`, `ON DELETE CASCADE` para o pedido exclui automaticamente os itens de um pedido removido, evitando itens órfãos. Para cliente e produto, `ON UPDATE CASCADE` propaga uma eventual atualização de identificador para as tabelas relacionadas. Essas regras preservam a coerência entre os registros sem que a aplicação precise corrigir as referências manualmente.\n\n### Inserção de dados\n\nForam inseridos três clientes, cinco produtos, dois pedidos e dois itens em cada pedido. Todos os valores de `id_cliente`, `id_pedido` e `id_produto` usados nos relacionamentos existem nas respectivas tabelas.\n\n### Consulta de integridade\n\n```sql\nSELECT pe.id_pedido, c.nome, p.nome_do_produto, i.quantidade\nFROM pedidos AS pe\nJOIN clientes AS c ON c.id_cliente = pe.id_cliente\nJOIN itens_pedido AS i ON i.id_pedido = pe.id_pedido\nJOIN produtos AS p ON p.id_produto = i.id_produto;\n```\n\nA consulta retorna cada pedido com o cliente, o produto e a quantidade correspondente, comprovando que as referências foram gravadas de forma consistente.\n\n### Desafio — exclusão de cliente com pedidos\n\n```sql\nDELETE FROM clientes WHERE id_cliente = 1;\n```\n\nO comando falha porque o cliente 1 possui o pedido 10 e a chave estrangeira de `pedidos` usa `ON DELETE RESTRICT`. Esse resultado é desejado: sem a restrição, a exclusão poderia deixar pedidos que apontam para um cliente inexistente. Para excluir o cliente de forma intencional, seria necessário remover ou reatribuir seus pedidos, ou alterar conscientemente a regra de exclusão conforme a política do negócio.\n\n## Código e verificação\n\n[`codigo/aula-1/solucao.sql`](codigo/aula-1/solucao.sql) contém a criação do cenário, inserções, consulta e o teste de exclusão. Veja a execução em [`codigo/aula-1/README.md`](codigo/aula-1/README.md).\n\n**Verificação:** simulação técnica para MySQL 8.0+; o último `DELETE` deve retornar erro de chave estrangeira, como previsto no desafio.", "fonte": "gabarito/Banco de dados/3° bimestre/Semana 16 - SGBD Relacional/Gabarito - Roteiro Prático Aula 1.md", "artefato": {"nome": "Artefatos da Aula 1", "href": "artefatos/banco-de-dados/semana-16/a1.zip", "arquivos": 2}}, "quizzes": []}
---
