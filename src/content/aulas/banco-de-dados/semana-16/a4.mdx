---
{"materia": "Banco de dados", "materiaSlug": "banco-de-dados", "bimestre": 3, "semana": 16, "ordem": 40, "aula": "a4", "titulo": "SGBD Relacional · Aula 4", "atividade": {"enunciado": "A FastTech precisa simplificar relatórios que juntam vendas, clientes e produtos. A atividade pede uma vista para o relatório de vendas e a simulação de uma vista materializada no MySQL, que não possui esse recurso nativamente.", "resposta": "## Contexto\n\nA FastTech precisa simplificar relatórios que juntam vendas, clientes e produtos. A atividade pede uma vista para o relatório de vendas e a simulação de uma vista materializada no MySQL, que não possui esse recurso nativamente.\n\n## Parte 1 — Criação da vista simples\n\nAs tabelas `clientes`, `produtos` e `vendas` são criadas e preenchidas com dados de demonstração no script. A vista solicitada é:\n\n```sql\nCREATE VIEW relatorio_vendas AS\nSELECT\nc.nome AS cliente,\np.nome AS produto,\nv.data_venda,\nv.quantidade\nFROM vendas AS v\nJOIN clientes AS c ON c.id = v.id_cliente\nJOIN produtos AS p ON p.id = v.id_produto;\n```\n\nPara visualizar seu resultado:\n\n```sql\nSELECT * FROM relatorio_vendas;\n```\n\n## Parte 2 — Consulta usando a vista\n\n```sql\nSELECT *\nFROM relatorio_vendas\nWHERE quantidade > 10;\n```\n\nA consulta reaproveita a junção já encapsulada pela vista; portanto, o relatório fica menor e menos sujeito a divergências entre consultas semelhantes.\n\n## Parte 3 — Vista materializada simulada\n\nNo MySQL, a simulação pode ser feita com uma tabela que armazena o resultado calculado:\n\n```sql\nCREATE TABLE relatorio_vendas_materializada AS\nSELECT\nc.nome AS cliente,\np.nome AS produto,\nv.data_venda,\nv.quantidade,\np.preco\nFROM vendas AS v\nJOIN clientes AS c ON c.id = v.id_cliente\nJOIN produtos AS p ON p.id = v.id_produto\nWHERE p.preco > 100;\n```\n\n```sql\nSELECT * FROM relatorio_vendas_materializada;\n```\n\nEssa tabela melhora a leitura de um relatório frequente, mas representa uma fotografia dos dados no momento em que foi criada.\n\n## Parte 4 — Atualização da vista materializada simulada\n\nApós uma nova venda, a tabela precisa ser atualizada. A solução da atividade é recriá-la a partir da consulta original:\n\n```sql\nDROP TABLE relatorio_vendas_materializada;\nCREATE TABLE relatorio_vendas_materializada AS\nSELECT\nc.nome AS cliente, p.nome AS produto, v.data_venda, v.quantidade, p.preco\nFROM vendas AS v\nJOIN clientes AS c ON c.id = v.id_cliente\nJOIN produtos AS p ON p.id = v.id_produto\nWHERE p.preco > 100;\n```\n\nEm um sistema real, essa atualização pode ser programada ou controlada por procedimento, conforme o intervalo de atualização aceitável para o relatório.\n\n## Código e verificação\n\nO cenário completo está em [`codigo/aula-4/solucao.sql`](codigo/aula-4/solucao.sql); instruções de execução e resultado esperado estão em [`codigo/aula-4/README.md`](codigo/aula-4/README.md).\n\n**Verificação:** simulação técnica para MySQL 8.0+. A vista deve listar as vendas com seus nomes associados; a tabela materializada deve mostrar somente produtos com preço superior a 100.", "conteudo": "## Contexto\n\nA FastTech precisa simplificar relatórios que juntam vendas, clientes e produtos. A atividade pede uma vista para o relatório de vendas e a simulação de uma vista materializada no MySQL, que não possui esse recurso nativamente.\n\n## Parte 1 — Criação da vista simples\n\nAs tabelas `clientes`, `produtos` e `vendas` são criadas e preenchidas com dados de demonstração no script. A vista solicitada é:\n\n```sql\nCREATE VIEW relatorio_vendas AS\nSELECT\n    c.nome AS cliente,\n    p.nome AS produto,\n    v.data_venda,\n    v.quantidade\nFROM vendas AS v\nJOIN clientes AS c ON c.id = v.id_cliente\nJOIN produtos AS p ON p.id = v.id_produto;\n```\n\nPara visualizar seu resultado:\n\n```sql\nSELECT * FROM relatorio_vendas;\n```\n\n## Parte 2 — Consulta usando a vista\n\n```sql\nSELECT *\nFROM relatorio_vendas\nWHERE quantidade > 10;\n```\n\nA consulta reaproveita a junção já encapsulada pela vista; portanto, o relatório fica menor e menos sujeito a divergências entre consultas semelhantes.\n\n## Parte 3 — Vista materializada simulada\n\nNo MySQL, a simulação pode ser feita com uma tabela que armazena o resultado calculado:\n\n```sql\nCREATE TABLE relatorio_vendas_materializada AS\nSELECT\n    c.nome AS cliente,\n    p.nome AS produto,\n    v.data_venda,\n    v.quantidade,\n    p.preco\nFROM vendas AS v\nJOIN clientes AS c ON c.id = v.id_cliente\nJOIN produtos AS p ON p.id = v.id_produto\nWHERE p.preco > 100;\n```\n\n```sql\nSELECT * FROM relatorio_vendas_materializada;\n```\n\nEssa tabela melhora a leitura de um relatório frequente, mas representa uma fotografia dos dados no momento em que foi criada.\n\n## Parte 4 — Atualização da vista materializada simulada\n\nApós uma nova venda, a tabela precisa ser atualizada. A solução da atividade é recriá-la a partir da consulta original:\n\n```sql\nDROP TABLE relatorio_vendas_materializada;\nCREATE TABLE relatorio_vendas_materializada AS\nSELECT\n    c.nome AS cliente, p.nome AS produto, v.data_venda, v.quantidade, p.preco\nFROM vendas AS v\nJOIN clientes AS c ON c.id = v.id_cliente\nJOIN produtos AS p ON p.id = v.id_produto\nWHERE p.preco > 100;\n```\n\nEm um sistema real, essa atualização pode ser programada ou controlada por procedimento, conforme o intervalo de atualização aceitável para o relatório.\n\n## Código e verificação\n\nO cenário completo está em [`codigo/aula-4/solucao.sql`](codigo/aula-4/solucao.sql); instruções de execução e resultado esperado estão em [`codigo/aula-4/README.md`](codigo/aula-4/README.md).\n\n**Verificação:** simulação técnica para MySQL 8.0+. A vista deve listar as vendas com seus nomes associados; a tabela materializada deve mostrar somente produtos com preço superior a 100.", "fonte": "gabarito/Banco de dados/3° bimestre/Semana 16 - SGBD Relacional/Gabarito - Roteiro Prático Aula 4.md", "arquivos": [{"caminho": "README.md", "linguagem": "markdown", "conteudo": "# Como executar\r\n\r\n- **Linguagem:** SQL para MySQL 8.0+\r\n- **Dependências:** MySQL 8.0+ e um usuário com permissão para criar o banco, as tabelas e os objetos usados no arquivo.\r\n- **Execução:** `mysql -u SEU_USUARIO -p < solucao.sql`\r\n- **Teste:** execute as consultas `SELECT`, `EXPLAIN` ou verificações comentadas no próprio arquivo.\r\n- **Resultado esperado:** criação de uma base de demonstração e retorno dos resultados descritos no gabarito. Use uma instância de teste, pois o script recria tabelas próprias.\r\n"}, {"caminho": "solucao.sql", "linguagem": "sql", "conteudo": "DROP DATABASE IF EXISTS vistas_a4;\r\nCREATE DATABASE vistas_a4 CHARACTER SET utf8mb4;\r\nUSE vistas_a4;\r\nCREATE TABLE clientes (id INT AUTO_INCREMENT PRIMARY KEY,nome VARCHAR(100),email VARCHAR(100)) ENGINE=InnoDB;\r\nCREATE TABLE produtos (id INT AUTO_INCREMENT PRIMARY KEY,nome VARCHAR(100),preco DECIMAL(10,2)) ENGINE=InnoDB;\r\nCREATE TABLE vendas (id INT AUTO_INCREMENT PRIMARY KEY,id_cliente INT,id_produto INT,data_venda DATE,quantidade INT,\r\n  FOREIGN KEY(id_cliente) REFERENCES clientes(id),FOREIGN KEY(id_produto) REFERENCES produtos(id)) ENGINE=InnoDB;\r\nINSERT INTO clientes(nome,email) VALUES ('Ana','ana@exemplo.com'),('Bruno','bruno@exemplo.com');\r\nINSERT INTO produtos(nome,preco) VALUES ('Teclado',180),('Cabo',30),('Monitor',1200);\r\nINSERT INTO vendas(id_cliente,id_produto,data_venda,quantidade) VALUES (1,1,'2026-08-01',12),(1,2,'2026-08-02',2),(2,3,'2026-08-03',1);\r\nCREATE VIEW relatorio_vendas AS\r\n  SELECT c.nome AS cliente,p.nome AS produto,v.data_venda,v.quantidade FROM vendas v\r\n  JOIN clientes c ON c.id=v.id_cliente JOIN produtos p ON p.id=v.id_produto;\r\nSELECT * FROM relatorio_vendas;\r\nSELECT * FROM relatorio_vendas WHERE quantidade > 10;\r\nCREATE TABLE relatorio_vendas_materializada AS\r\n  SELECT c.nome AS cliente,p.nome AS produto,v.data_venda,v.quantidade,p.preco FROM vendas v\r\n  JOIN clientes c ON c.id=v.id_cliente JOIN produtos p ON p.id=v.id_produto WHERE p.preco > 100;\r\nSELECT * FROM relatorio_vendas_materializada;\r\nDROP TABLE relatorio_vendas_materializada;\r\nCREATE TABLE relatorio_vendas_materializada AS\r\n  SELECT c.nome AS cliente,p.nome AS produto,v.data_venda,v.quantidade,p.preco FROM vendas v\r\n  JOIN clientes c ON c.id=v.id_cliente JOIN produtos p ON p.id=v.id_produto WHERE p.preco > 100;\r\n"}]}, "quizzes": [], "tipo": "roteiro"}
---
