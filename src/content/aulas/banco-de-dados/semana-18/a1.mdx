---
{"materia": "Banco de dados", "materiaSlug": "banco-de-dados", "bimestre": 3, "semana": 18, "ordem": 10, "aula": "a1", "titulo": "Clausulas Select · Aula 1", "atividade": {"enunciado": "## Tarefas e respostas\n\n### Criem a tabela vendas com os campos solicitados.\n\n```sql\nCREATE TABLE vendas (\nid_venda INT PRIMARY KEY AUTO_INCREMENT,\nvendedor VARCHAR(100) NOT NULL,\nproduto VARCHAR(100) NOT NULL,\ndata_venda DATE NOT NULL,\nvalor DECIMAL(10,2) NOT NULL\n);\n```\n\n### Criem uma consulta com ROW_NUMBER() e PARTITION BY para numerar as vendas de cada vendedor.\n\n```sql\nSELECT\nvendedor,\nproduto,\ndata_venda,\nvalor,\nROW_NUMBER() OVER (\nPARTITION BY vendedor\nORDER BY data_venda, id_venda\n) AS sequencia_venda_do_vendedor\nFROM vendas\nORDER BY vendedor, sequencia_venda_do_vendedor;\n```\n\n`PARTITION BY vendedor` reinicia a contagem para cada vendedor. A ordenação por data e identificador torna a sequência estável quando há duas vendas no mesmo dia.\n\n## Código e verificação\n\nO cenário com inserções de exemplo e a consulta estão em [`codigo/aula-1/solucao.sql`](codigo/aula-1/solucao.sql), com instruções em [`codigo/aula-1/README.md`](codigo/aula-1/README.md).", "resposta": "### Criem a tabela vendas com os campos solicitados.\n\n```sql\nCREATE TABLE vendas (\nid_venda INT PRIMARY KEY AUTO_INCREMENT,\nvendedor VARCHAR(100) NOT NULL,\nproduto VARCHAR(100) NOT NULL,\ndata_venda DATE NOT NULL,\nvalor DECIMAL(10,2) NOT NULL\n);\n```\n\n### Criem uma consulta com ROW_NUMBER() e PARTITION BY para numerar as vendas de cada vendedor.\n\n```sql\nSELECT\nvendedor,\nproduto,\ndata_venda,\nvalor,\nROW_NUMBER() OVER (\nPARTITION BY vendedor\nORDER BY data_venda, id_venda\n) AS sequencia_venda_do_vendedor\nFROM vendas\nORDER BY vendedor, sequencia_venda_do_vendedor;\n```\n\n`PARTITION BY vendedor` reinicia a contagem para cada vendedor. A ordenação por data e identificador torna a sequência estável quando há duas vendas no mesmo dia.", "conteudo": "## Tarefas e respostas\n\n### Criem a tabela vendas com os campos solicitados.\n\n```sql\nCREATE TABLE vendas (\n    id_venda INT PRIMARY KEY AUTO_INCREMENT,\n    vendedor VARCHAR(100) NOT NULL,\n    produto VARCHAR(100) NOT NULL,\n    data_venda DATE NOT NULL,\n    valor DECIMAL(10,2) NOT NULL\n);\n```\n\n### Criem uma consulta com ROW_NUMBER() e PARTITION BY para numerar as vendas de cada vendedor.\n\n```sql\nSELECT\n    vendedor,\n    produto,\n    data_venda,\n    valor,\n    ROW_NUMBER() OVER (\n        PARTITION BY vendedor\n        ORDER BY data_venda, id_venda\n    ) AS sequencia_venda_do_vendedor\nFROM vendas\nORDER BY vendedor, sequencia_venda_do_vendedor;\n```\n\n`PARTITION BY vendedor` reinicia a contagem para cada vendedor. A ordenação por data e identificador torna a sequência estável quando há duas vendas no mesmo dia.\n\n## Código e verificação\n\nO cenário com inserções de exemplo e a consulta estão em [`codigo/aula-1/solucao.sql`](codigo/aula-1/solucao.sql), com instruções em [`codigo/aula-1/README.md`](codigo/aula-1/README.md).", "fonte": "gabarito/Banco de dados/3° bimestre/Semana 18 - Clausulas Select/Gabarito - Roteiro Prático Aula 1.md", "artefato": {"nome": "Artefatos da Aula 1", "href": "artefatos/banco-de-dados/semana-18/a1.zip", "arquivos": 2}}, "quizzes": []}
---
