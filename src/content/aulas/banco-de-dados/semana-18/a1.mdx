---
{"materia": "Banco de dados", "materiaSlug": "banco-de-dados", "bimestre": 3, "semana": 18, "ordem": 10, "aula": "a1", "titulo": "Cláusulas Select · Aula 1", "atividade": {"titulo": "Atividades Práticas", "enunciado": "Funções de janela calculam valores mantendo as linhas do resultado. Exemplo para numerar vendas por cliente, da mais recente à mais antiga:\n\n```sql\nSELECT id_cliente, id_venda, data_venda,\n       ROW_NUMBER() OVER (PARTITION BY id_cliente ORDER BY data_venda DESC) AS posicao\nFROM vendas;\n```\n\nElas são úteis para ranking, comparação, totais acumulados e seleção do registro mais recente por grupo. `PARTITION BY` reinicia o cálculo em cada cliente, categoria ou outra divisão lógica; sem ela, o cálculo usa todo o conjunto retornado.", "resposta": "Funções de janela calculam valores mantendo as linhas do resultado. Exemplo para numerar vendas por cliente, da mais recente à mais antiga:\n\n```sql\nSELECT id_cliente, id_venda, data_venda,\n       ROW_NUMBER() OVER (PARTITION BY id_cliente ORDER BY data_venda DESC) AS posicao\nFROM vendas;\n```\n\nElas são úteis para ranking, comparação, totais acumulados e seleção do registro mais recente por grupo. `PARTITION BY` reinicia o cálculo em cada cliente, categoria ou outra divisão lógica; sem ela, o cálculo usa todo o conjunto retornado.", "conteudo": "Funções de janela calculam valores mantendo as linhas do resultado. Exemplo para numerar vendas por cliente, da mais recente à mais antiga:\n\n```sql\nSELECT id_cliente, id_venda, data_venda,\n       ROW_NUMBER() OVER (PARTITION BY id_cliente ORDER BY data_venda DESC) AS posicao\nFROM vendas;\n```\n\nElas são úteis para ranking, comparação, totais acumulados e seleção do registro mais recente por grupo. `PARTITION BY` reinicia o cálculo em cada cliente, categoria ou outra divisão lógica; sem ela, o cálculo usa todo o conjunto retornado.", "fonte": "gabarito/Banco de dados/3° bimestre/Semana 18/semana-18-respostas.md#atividades-praticas", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
