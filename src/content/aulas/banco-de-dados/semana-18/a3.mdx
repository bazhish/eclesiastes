---
{"materia": "Banco de dados", "materiaSlug": "banco-de-dados", "bimestre": 3, "semana": 18, "ordem": 30, "aula": "a3", "titulo": "Clausulas Select · Aula 3", "atividade": {"enunciado": "Combinar nomes de clientes e fornecedores removendo duplicidades com `UNION`, mantendo-as com `UNION ALL`, contar linhas e identificar nomes repetidos.", "resposta": "`UNION` elimina linhas duplicadas no resultado combinado. `UNION ALL` preserva todas as linhas e é mais adequado quando a origem de cada ocorrência importa. No conjunto proposto, João Silva aparece nas duas tabelas; por isso, surge uma vez com `UNION` e duas vezes com `UNION ALL`. A contagem agrupada sobre a união identifica nomes presentes mais de uma vez.", "conteudo": "## UNION e UNION ALL\n\n## Enunciado\n\nCombinar nomes de clientes e fornecedores removendo duplicidades com `UNION`, mantendo-as com `UNION ALL`, contar linhas e identificar nomes repetidos.\n\n## Resposta\n\n`UNION` elimina linhas duplicadas no resultado combinado. `UNION ALL` preserva todas as linhas e é mais adequado quando a origem de cada ocorrência importa. No conjunto proposto, João Silva aparece nas duas tabelas; por isso, surge uma vez com `UNION` e duas vezes com `UNION ALL`. A contagem agrupada sobre a união identifica nomes presentes mais de uma vez.\n\n## Código entregue\n\n[`codigo/aula-3/solucao.sql`](codigo/aula-3/solucao.sql)\n\n**Verificação:** simulação técnica. O ambiente local não possui servidor ou cliente MySQL instalado; o código foi conferido para a sintaxe e a semântica do MySQL 8.0+. Antes de usar em produção, execute-o em uma instância de teste.\n\n## Fonte técnica\n\n- https://dev.mysql.com/doc/refman/8.4/en/union.html", "fonte": "gabarito/Banco de dados/3° bimestre/Semana 18 - Clausulas Select/Gabarito - Roteiro Prático Aula 3.md", "arquivos": [{"caminho": "README.md", "linguagem": "markdown", "conteudo": "# Como executar\r\n\r\n- **Linguagem:** SQL para MySQL 8.0+\r\n- **Dependências:** MySQL 8.0+ e um usuário com permissão para criar o banco, as tabelas e os objetos usados no arquivo.\r\n- **Execução:** `mysql -u SEU_USUARIO -p < solucao.sql`\r\n- **Teste:** execute as consultas `SELECT`, `EXPLAIN` ou verificações comentadas no próprio arquivo.\r\n- **Resultado esperado:** criação de uma base de demonstração e retorno dos resultados descritos no gabarito. Use uma instância de teste, pois o script recria tabelas próprias.\r\n"}, {"caminho": "solucao.sql", "linguagem": "sql", "conteudo": "DROP DATABASE IF EXISTS uniao_a3;\r\nCREATE DATABASE uniao_a3 CHARACTER SET utf8mb4;\r\nUSE uniao_a3;\r\nCREATE TABLE clientes (id INT PRIMARY KEY,nome VARCHAR(100),cidade VARCHAR(100),email VARCHAR(150));\r\nCREATE TABLE fornecedores (id INT PRIMARY KEY,nome VARCHAR(100),cidade VARCHAR(100),email VARCHAR(150));\r\nINSERT INTO clientes VALUES (1,'João Silva','São Paulo','joao@email.com'),(2,'Maria Santos','Rio de Janeiro','maria@email.com'),(3,'Pedro Lima','São Paulo','pedro@email.com');\r\nINSERT INTO fornecedores VALUES (1,'João Silva','São Paulo','joao@fornece.com'),(4,'Ana Costa','Salvador','ana@fornece.com'),(5,'Carlos Nunes','Recife','carlos@fornece.com');\r\nSELECT nome FROM clientes UNION SELECT nome FROM fornecedores;\r\nSELECT nome FROM clientes UNION ALL SELECT nome FROM fornecedores;\r\nSELECT COUNT(*) AS total_linhas FROM (SELECT nome,email FROM clientes UNION ALL SELECT nome,email FROM fornecedores) AS contatos;\r\nSELECT nome,COUNT(*) AS ocorrencias FROM (SELECT nome FROM clientes UNION ALL SELECT nome FROM fornecedores) AS nomes\r\n  GROUP BY nome HAVING COUNT(*) > 1;\r\n"}]}, "quizzes": [], "tipo": "roteiro"}
---
