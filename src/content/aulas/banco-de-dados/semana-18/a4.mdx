---
{"materia": "Banco de dados", "materiaSlug": "banco-de-dados", "bimestre": 3, "semana": 18, "ordem": 40, "aula": "a4", "titulo": "Clausulas Select · Aula 4", "atividade": {"enunciado": "Criar consultas que classifiquem valor de venda, desempenho de vendedores e preço de produtos com `CASE`.", "resposta": "A expressão `CASE` aplica as faixas solicitadas na ordem correta: acima de 1.000 é Alta; de 500 a 1.000 é Média; abaixo de 500 é Baixa. Para vendedores, o total é calculado antes da classificação; e para produtos, as faixas são Caro acima de 100, Moderado de 50 a 100 e Barato abaixo de 50.", "conteudo": "## CASE para classificação de vendas\n\n## Enunciado\n\nCriar consultas que classifiquem valor de venda, desempenho de vendedores e preço de produtos com `CASE`.\n\n## Resposta\n\nA expressão `CASE` aplica as faixas solicitadas na ordem correta: acima de 1.000 é Alta; de 500 a 1.000 é Média; abaixo de 500 é Baixa. Para vendedores, o total é calculado antes da classificação; e para produtos, as faixas são Caro acima de 100, Moderado de 50 a 100 e Barato abaixo de 50.\n\n## Código entregue\n\n[`codigo/aula-4/solucao.sql`](codigo/aula-4/solucao.sql)\n\n**Verificação:** simulação técnica. O ambiente local não possui servidor ou cliente MySQL instalado; o código foi conferido para a sintaxe e a semântica do MySQL 8.0+. Antes de usar em produção, execute-o em uma instância de teste.\n\n## Fonte técnica\n\n- https://dev.mysql.com/doc/refman/8.4/en/flow-control-functions.html", "fonte": "gabarito/Banco de dados/3° bimestre/Semana 18 - Clausulas Select/Gabarito - Roteiro Prático Aula 4.md", "arquivos": [{"caminho": "README.md", "linguagem": "markdown", "conteudo": "# Como executar\r\n\r\n- **Linguagem:** SQL para MySQL 8.0+\r\n- **Dependências:** MySQL 8.0+ e um usuário com permissão para criar o banco, as tabelas e os objetos usados no arquivo.\r\n- **Execução:** `mysql -u SEU_USUARIO -p < solucao.sql`\r\n- **Teste:** execute as consultas `SELECT`, `EXPLAIN` ou verificações comentadas no próprio arquivo.\r\n- **Resultado esperado:** criação de uma base de demonstração e retorno dos resultados descritos no gabarito. Use uma instância de teste, pois o script recria tabelas próprias.\r\n"}, {"caminho": "solucao.sql", "linguagem": "sql", "conteudo": "DROP DATABASE IF EXISTS case_a4;\r\nCREATE DATABASE case_a4 CHARACTER SET utf8mb4;\r\nUSE case_a4;\r\nCREATE TABLE clientes (id_cliente INT PRIMARY KEY,nome VARCHAR(100));\r\nCREATE TABLE vendedores (id_vendedor INT PRIMARY KEY,nome VARCHAR(100));\r\nCREATE TABLE produtos (id_produto INT PRIMARY KEY,nome VARCHAR(100),preco DECIMAL(10,2));\r\nCREATE TABLE vendas (id_venda INT PRIMARY KEY,id_cliente INT,id_vendedor INT,valor DECIMAL(10,2));\r\nINSERT INTO clientes VALUES(1,'Ana'),(2,'Bruno'); INSERT INTO vendedores VALUES(1,'Carla'),(2,'Diego');\r\nINSERT INTO produtos VALUES(1,'Monitor',1200),(2,'Teclado',80),(3,'Cabo',20);\r\nINSERT INTO vendas VALUES(1,1,1,1200),(2,1,1,9000),(3,2,2,700),(4,2,2,4000);\r\nSELECT c.nome,v.valor,CASE WHEN v.valor>1000 THEN 'Alta' WHEN v.valor>=500 THEN 'Média' ELSE 'Baixa' END AS categoria_venda\r\n  FROM vendas v JOIN clientes c ON c.id_cliente=v.id_cliente;\r\nSELECT ve.nome,SUM(v.valor) total_vendas,CASE WHEN SUM(v.valor)>10000 THEN 'Excelente' WHEN SUM(v.valor)>=5000 THEN 'Bom' ELSE 'Regular' END AS desempenho\r\n  FROM vendedores ve JOIN vendas v ON v.id_vendedor=ve.id_vendedor GROUP BY ve.id_vendedor,ve.nome;\r\nSELECT nome,preco,CASE WHEN preco>100 THEN 'Caro' WHEN preco>=50 THEN 'Moderado' ELSE 'Barato' END AS categoria_preco FROM produtos;\r\n"}]}, "quizzes": [], "tipo": "roteiro"}
---
