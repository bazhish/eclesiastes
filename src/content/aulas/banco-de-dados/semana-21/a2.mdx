---
{"materia": "Banco de dados", "materiaSlug": "banco-de-dados", "bimestre": 3, "semana": 21, "ordem": 20, "aula": "a2", "titulo": "SGBD Relacional · Aula 2", "atividade": {"enunciado": "Criar TechTradeDB, aplicar índice composto em vendas, comparar o plano de consulta e aplicar particionamento temporal corretamente, explicando vantagens e limites.", "resposta": "O índice composto `(id_produto, data_venda)` atende a consulta que filtra primeiro pelo produto e depois pelo intervalo de datas; `EXPLAIN` permite verificar seu uso. A sintaxe do roteiro para `CREATE INDEX ... PARTITION BY` não é válida no MySQL: o particionamento é definido na tabela. Em InnoDB, toda chave única, inclusive a primária, deve incluir a coluna de particionamento. Por isso a solução cria `vendas` com chave primária composta por `id_venda, data_venda` e usa `PARTITION BY RANGE COLUMNS(data_venda)`. Particionar só faz sentido com grande volume e consultas que filtram a chave de partição; não substitui a escolha correta de índices.", "conteudo": "## Indexação avançada\n\n## Enunciado\n\nCriar TechTradeDB, aplicar índice composto em vendas, comparar o plano de consulta e aplicar particionamento temporal corretamente, explicando vantagens e limites.\n\n## Resposta\n\nO índice composto `(id_produto, data_venda)` atende a consulta que filtra primeiro pelo produto e depois pelo intervalo de datas; `EXPLAIN` permite verificar seu uso. A sintaxe do roteiro para `CREATE INDEX ... PARTITION BY` não é válida no MySQL: o particionamento é definido na tabela. Em InnoDB, toda chave única, inclusive a primária, deve incluir a coluna de particionamento. Por isso a solução cria `vendas` com chave primária composta por `id_venda, data_venda` e usa `PARTITION BY RANGE COLUMNS(data_venda)`. Particionar só faz sentido com grande volume e consultas que filtram a chave de partição; não substitui a escolha correta de índices.\n\n## Código entregue\n\n[`codigo/aula-2/solucao.sql`](codigo/aula-2/solucao.sql)\n\n**Verificação:** simulação técnica. O ambiente local não possui servidor ou cliente MySQL instalado; o código foi conferido para a sintaxe e a semântica do MySQL 8.0+. Antes de usar em produção, execute-o em uma instância de teste.\n\n## Fonte técnica\n\n- https://dev.mysql.com/doc/refman/8.4/en/partitioning-limitations-partitioning-keys-unique-keys.html", "fonte": "gabarito/Banco de dados/3° bimestre/Semana 21 - SGBD Relacional/Gabarito - Roteiro Prático Aula 2.md", "arquivos": [{"caminho": "README.md", "linguagem": "markdown", "conteudo": "# Como executar\r\n\r\n- **Linguagem:** SQL para MySQL 8.0+\r\n- **Dependências:** MySQL 8.0+ e um usuário com permissão para criar o banco, as tabelas e os objetos usados no arquivo.\r\n- **Execução:** `mysql -u SEU_USUARIO -p < solucao.sql`\r\n- **Teste:** execute as consultas `SELECT`, `EXPLAIN` ou verificações comentadas no próprio arquivo.\r\n- **Resultado esperado:** criação de uma base de demonstração e retorno dos resultados descritos no gabarito. Use uma instância de teste, pois o script recria tabelas próprias.\r\n"}, {"caminho": "solucao.sql", "linguagem": "sql", "conteudo": "DROP DATABASE IF EXISTS TechTradeDB; CREATE DATABASE TechTradeDB CHARACTER SET utf8mb4; USE TechTradeDB;\r\nCREATE TABLE produtos(id_produto INT PRIMARY KEY,nome_produto VARCHAR(100),categoria VARCHAR(100),preco DECIMAL(10,2));\r\nCREATE TABLE clientes(id_cliente INT PRIMARY KEY,nome_cliente VARCHAR(100),cidade VARCHAR(100),estado CHAR(2));\r\nCREATE TABLE vendas(\r\n  id_venda INT NOT NULL,id_produto INT NOT NULL,id_cliente INT NOT NULL,data_venda DATE NOT NULL,quantidade INT NOT NULL,valor_total DECIMAL(10,2) NOT NULL,\r\n  PRIMARY KEY(id_venda,data_venda), KEY idx_produto_data(id_produto,data_venda)\r\n) ENGINE=InnoDB\r\nPARTITION BY RANGE COLUMNS(data_venda)(\r\n  PARTITION p2022 VALUES LESS THAN ('2023-01-01'),PARTITION p2023 VALUES LESS THAN ('2024-01-01'),\r\n  PARTITION p2024 VALUES LESS THAN ('2025-01-01'),PARTITION pmax VALUES LESS THAN (MAXVALUE)\r\n);\r\nINSERT INTO produtos VALUES(101,'Notebook','Informática',3500); INSERT INTO clientes VALUES(1,'Ana','São Paulo','SP');\r\nINSERT INTO vendas VALUES(1,101,1,'2024-06-10',1,3500),(2,101,1,'2024-11-10',1,3500);\r\nEXPLAIN SELECT * FROM vendas WHERE id_produto=101 AND data_venda BETWEEN '2024-01-01' AND '2024-12-31';\r\n"}]}, "quizzes": [], "tipo": "roteiro"}
---
