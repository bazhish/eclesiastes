---
{"materia": "Banco de dados", "materiaSlug": "banco-de-dados", "bimestre": 3, "semana": 15, "ordem": 40, "aula": "a4", "titulo": "SGBD Relacional · Aula 4", "atividade": {"titulo": "Atividade Prática - Aula 4: Operações CRUD", "enunciado": "```sql\n-- SELECT: vendas do cliente 101\nSELECT v.id_venda, v.data_venda, iv.id_produto, iv.quantidade, iv.preco_unitario\nFROM vendas AS v\nLEFT JOIN itens_venda AS iv ON iv.id_venda = v.id_venda\nWHERE v.id_cliente = 101;\n\n-- INSERT: novo fornecedor\nINSERT INTO fornecedores (nome, contato, endereco)\nVALUES ('GlobalTech', 'contato@globaltech.com', 'Av. das Indústrias, 500');\n\n-- UPDATE: endereço do fornecedor 201\nUPDATE fornecedores\nSET endereco = 'Av. das Indústrias, 550'\nWHERE id_fornecedor = 201;\n\n-- DELETE: venda cancelada 303\nDELETE FROM itens_venda WHERE id_venda = 303;\nDELETE FROM vendas WHERE id_venda = 303;\n```\n\nAntes de `UPDATE` ou `DELETE`, eu executaria um `SELECT` com o mesmo `WHERE`, confirmaria a quantidade de registros afetados e usaria transação quando a operação envolver mais de uma tabela. Também manteria backup, permissões mínimas e nunca executaria comando de alteração sem condição `WHERE` quando a intenção for alterar somente um registro.", "resposta": "```sql\n-- SELECT: vendas do cliente 101\nSELECT v.id_venda, v.data_venda, iv.id_produto, iv.quantidade, iv.preco_unitario\nFROM vendas AS v\nLEFT JOIN itens_venda AS iv ON iv.id_venda = v.id_venda\nWHERE v.id_cliente = 101;\n\n-- INSERT: novo fornecedor\nINSERT INTO fornecedores (nome, contato, endereco)\nVALUES ('GlobalTech', 'contato@globaltech.com', 'Av. das Indústrias, 500');\n\n-- UPDATE: endereço do fornecedor 201\nUPDATE fornecedores\nSET endereco = 'Av. das Indústrias, 550'\nWHERE id_fornecedor = 201;\n\n-- DELETE: venda cancelada 303\nDELETE FROM itens_venda WHERE id_venda = 303;\nDELETE FROM vendas WHERE id_venda = 303;\n```\n\nAntes de `UPDATE` ou `DELETE`, eu executaria um `SELECT` com o mesmo `WHERE`, confirmaria a quantidade de registros afetados e usaria transação quando a operação envolver mais de uma tabela. Também manteria backup, permissões mínimas e nunca executaria comando de alteração sem condição `WHERE` quando a intenção for alterar somente um registro.", "conteudo": "```sql\n-- SELECT: vendas do cliente 101\nSELECT v.id_venda, v.data_venda, iv.id_produto, iv.quantidade, iv.preco_unitario\nFROM vendas AS v\nLEFT JOIN itens_venda AS iv ON iv.id_venda = v.id_venda\nWHERE v.id_cliente = 101;\n\n-- INSERT: novo fornecedor\nINSERT INTO fornecedores (nome, contato, endereco)\nVALUES ('GlobalTech', 'contato@globaltech.com', 'Av. das Indústrias, 500');\n\n-- UPDATE: endereço do fornecedor 201\nUPDATE fornecedores\nSET endereco = 'Av. das Indústrias, 550'\nWHERE id_fornecedor = 201;\n\n-- DELETE: venda cancelada 303\nDELETE FROM itens_venda WHERE id_venda = 303;\nDELETE FROM vendas WHERE id_venda = 303;\n```\n\nAntes de `UPDATE` ou `DELETE`, eu executaria um `SELECT` com o mesmo `WHERE`, confirmaria a quantidade de registros afetados e usaria transação quando a operação envolver mais de uma tabela. Também manteria backup, permissões mínimas e nunca executaria comando de alteração sem condição `WHERE` quando a intenção for alterar somente um registro.", "fonte": "gabarito/Banco de dados/3° bimestre/Semana 15/semana-15-respostas.md#atividade-pratica-aula-4-operacoes-crud", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
