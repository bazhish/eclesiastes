---
{"materia": "Banco de dados", "materiaSlug": "banco-de-dados", "bimestre": 3, "semana": 15, "ordem": 40, "aula": "a4", "titulo": "SGBD Relacional · Aula 4", "atividade": {"enunciado": "A TechSolutions possui uma estrutura de banco de dados normalizada e precisa garantir que as operações diárias de inserção, consulta, atualização e exclusão sejam executadas de maneira eficiente e precisa. A equipe deve implementar e testar as operações CRUD nas tabelas da empresa. Há novos fornecedores e vendas a registrar, um endereço de fornecedor a corrigir, um preço sujeito a alteração e uma venda cancelada a excluir.", "resposta": "### 4.1. SELECT — Consultar os dados\n\n**Tarefa:** faça uma consulta SQL para visualizar todas as vendas feitas pelo cliente identificado pelo ID 101.\n\n**Resposta:**\n\n```sql\nSELECT v.id_venda, v.data_venda, iv.id_produto, iv.quantidade, iv.preco_unitario\nFROM vendas AS v\nLEFT JOIN itens_venda AS iv ON iv.id_venda = v.id_venda\nWHERE v.id_cliente = 101\nORDER BY v.data_venda, v.id_venda;\n```\n\n### 4.2. INSERT — Inserir novos registros\n\n**Tarefa:** insira o fornecedor GlobalTech, com contato `contato@globaltech.com` e endereço `Av. das Indústrias, 500`.\n\n**Resposta:**\n\n```sql\nINSERT INTO fornecedores (id_fornecedor, nome, contato, endereco)\nVALUES (201, 'GlobalTech', 'contato@globaltech.com', 'Av. das Indústrias, 500');\n```\n\nO identificador `201` foi usado porque a tarefa seguinte referencia esse fornecedor. Em uma tabela de produção, ele pode ser gerado automaticamente, desde que o identificador retornado seja usado no `UPDATE` posterior.\n\n### 4.3. UPDATE — Atualizar registros\n\n**Tarefa:** atualize o endereço do fornecedor de ID 201 para `Av. das Indústrias, 550`.\n\n**Resposta:**\n\n```sql\nUPDATE fornecedores\nSET endereco = 'Av. das Indústrias, 550'\nWHERE id_fornecedor = 201;\n```\n\n### 4.4. DELETE — Excluir registros\n\n**Tarefa:** exclua a venda de ID 303, que foi cancelada.\n\n**Resposta:**\n\n```sql\nSTART TRANSACTION;\nDELETE FROM itens_venda WHERE id_venda = 303;\nDELETE FROM vendas WHERE id_venda = 303;\nCOMMIT;\n```\n\nOs itens são excluídos antes da venda para respeitar a chave estrangeira. Se a tabela tivesse sido criada com `ON DELETE CASCADE`, a exclusão dos itens poderia ser feita automaticamente; ainda assim, a transação continua adequada para permitir `ROLLBACK` antes da confirmação.\n\n### Que precauções você recomendaria para evitar erros ao executar operações como DELETE e UPDATE?\n\nAntes de alterar ou excluir, deve-se executar um `SELECT` com a mesma cláusula `WHERE` para confirmar os registros atingidos. A condição deve usar uma chave ou outro critério específico; nunca se deve executar `UPDATE` ou `DELETE` sem `WHERE` quando a intenção é alterar apenas parte da tabela. Em operações relevantes, use uma transação, verifique a quantidade de linhas afetadas e só execute `COMMIT` após conferir o resultado. Também é recomendável manter backup recente, restringir permissões de escrita e testar o script em banco de homologação.", "conteudo": "## Contexto\n\nA TechSolutions possui uma estrutura de banco de dados normalizada e precisa garantir que as operações diárias de inserção, consulta, atualização e exclusão sejam executadas de maneira eficiente e precisa. A equipe deve implementar e testar as operações CRUD nas tabelas da empresa. Há novos fornecedores e vendas a registrar, um endereço de fornecedor a corrigir, um preço sujeito a alteração e uma venda cancelada a excluir.\n\n## Tarefas e respostas\n\n### 4.1. SELECT — Consultar os dados\n\n**Tarefa:** faça uma consulta SQL para visualizar todas as vendas feitas pelo cliente identificado pelo ID 101.\n\n**Resposta:**\n\n```sql\nSELECT v.id_venda, v.data_venda, iv.id_produto, iv.quantidade, iv.preco_unitario\nFROM vendas AS v\nLEFT JOIN itens_venda AS iv ON iv.id_venda = v.id_venda\nWHERE v.id_cliente = 101\nORDER BY v.data_venda, v.id_venda;\n```\n\n### 4.2. INSERT — Inserir novos registros\n\n**Tarefa:** insira o fornecedor GlobalTech, com contato `contato@globaltech.com` e endereço `Av. das Indústrias, 500`.\n\n**Resposta:**\n\n```sql\nINSERT INTO fornecedores (id_fornecedor, nome, contato, endereco)\nVALUES (201, 'GlobalTech', 'contato@globaltech.com', 'Av. das Indústrias, 500');\n```\n\nO identificador `201` foi usado porque a tarefa seguinte referencia esse fornecedor. Em uma tabela de produção, ele pode ser gerado automaticamente, desde que o identificador retornado seja usado no `UPDATE` posterior.\n\n### 4.3. UPDATE — Atualizar registros\n\n**Tarefa:** atualize o endereço do fornecedor de ID 201 para `Av. das Indústrias, 550`.\n\n**Resposta:**\n\n```sql\nUPDATE fornecedores\nSET endereco = 'Av. das Indústrias, 550'\nWHERE id_fornecedor = 201;\n```\n\n### 4.4. DELETE — Excluir registros\n\n**Tarefa:** exclua a venda de ID 303, que foi cancelada.\n\n**Resposta:**\n\n```sql\nSTART TRANSACTION;\nDELETE FROM itens_venda WHERE id_venda = 303;\nDELETE FROM vendas WHERE id_venda = 303;\nCOMMIT;\n```\n\nOs itens são excluídos antes da venda para respeitar a chave estrangeira. Se a tabela tivesse sido criada com `ON DELETE CASCADE`, a exclusão dos itens poderia ser feita automaticamente; ainda assim, a transação continua adequada para permitir `ROLLBACK` antes da confirmação.\n\n### Que precauções você recomendaria para evitar erros ao executar operações como DELETE e UPDATE?\n\nAntes de alterar ou excluir, deve-se executar um `SELECT` com a mesma cláusula `WHERE` para confirmar os registros atingidos. A condição deve usar uma chave ou outro critério específico; nunca se deve executar `UPDATE` ou `DELETE` sem `WHERE` quando a intenção é alterar apenas parte da tabela. Em operações relevantes, use uma transação, verifique a quantidade de linhas afetadas e só execute `COMMIT` após conferir o resultado. Também é recomendável manter backup recente, restringir permissões de escrita e testar o script em banco de homologação.\n\n## Código e verificação\n\nO script completo, com estrutura mínima de teste, dados de demonstração, as quatro operações e consultas de verificação, está em [`codigo/aula-4/solucao.sql`](codigo/aula-4/solucao.sql). As dependências e o modo de execução estão em [`codigo/aula-4/README.md`](codigo/aula-4/README.md).\n\n**Verificação:** simulação técnica. O ambiente local não possui uma instância MySQL disponível. O script foi revisado para MySQL 8.0+; execute-o em uma base de teste, pois ele cria o banco `techsolutions_crud` e insere dados de demonstração.", "fonte": "gabarito/Banco de dados/3° bimestre/Semana 15 - SGBD Relacional/Gabarito - Roteiro Prático Aula 4.md", "artefato": {"nome": "Artefatos da Aula 4", "href": "artefatos/banco-de-dados/semana-15/a4.zip", "arquivos": 2}}, "quizzes": []}
---
