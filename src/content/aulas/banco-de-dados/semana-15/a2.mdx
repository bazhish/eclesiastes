---
{"materia": "Banco de dados", "materiaSlug": "banco-de-dados", "bimestre": 3, "semana": 15, "ordem": 20, "aula": "a2", "titulo": "SGBD Relacional · Aula 2", "atividade": {"enunciado": "A empresa TechSolutions está crescendo rapidamente e enfrenta desafios na organização de seus dados, que envolvem informações sobre clientes, produtos, vendas e fornecedores. Atualmente, esses dados estão armazenados de maneira desestruturada, o que dificulta consultas precisas e relatórios. A tarefa é criar um diagrama Entidade-Relacionamento (ER) que modele os dados de maneira eficiente e fazer a normalização para evitar redundâncias e inconsistências.", "resposta": "### Quais são as principais entidades e seus atributos que vocês esperam modelar no contexto da TechSolutions (clientes, produtos, vendas, fornecedores)?\n\nAs entidades principais são:\n\n- `Cliente`: `id_cliente`, `nome`, `email`, `telefone` e `endereco`.\n- `Produto`: `id_produto`, `nome`, `descricao`, `preco` e `estoque`.\n- `Venda`: `id_venda`, `data_venda`, `id_cliente` e `id_vendedor`.\n- `Fornecedor`: `id_fornecedor`, `razao_social`, `cnpj`, `email` e `telefone`.\n\nPara registrar os produtos de cada venda, é necessária ainda a entidade associativa `Item_Venda`, com `id_item_venda`, `id_venda`, `id_produto`, `quantidade` e `preco_unitario`. A compra de produtos de fornecedores pode ser registrada em `Produto_Fornecedor`, com as duas chaves estrangeiras e o custo de aquisição.\n\n### Como a normalização pode ser útil para evitar duplicação de dados em sistemas como o da TechSolutions?\n\nA normalização separa cada fato em seu lugar apropriado. Na 1FN, os campos recebem valores atômicos: em vez de guardar vários produtos em uma única coluna de venda, cada produto fica em uma linha de `Item_Venda`. Na 2FN, os atributos dependem da chave completa da tabela; por exemplo, o nome do produto não deve ser repetido em cada item de venda, pois depende de `id_produto`. Na 3FN, atributos que dependem de outra informação não-chave também são separados; os dados do fornecedor ficam em `Fornecedor`, e o produto apenas guarda a referência necessária. Assim, uma alteração de e-mail do cliente ou de preço cadastral do produto é feita em um único local.\n\n### Desenhem um esboço inicial de um diagrama ER para a TechSolutions, incluindo pelo menos quatro entidades e seus relacionamentos principais.\n\n```mermaid\nerDiagram\nCLIENTE ||--o{ VENDA : realiza\nVENDA ||--|{ ITEM_VENDA : contem\nPRODUTO ||--o{ ITEM_VENDA : integra\nFORNECEDOR ||--o{ PRODUTO_FORNECEDOR : fornece\nPRODUTO ||--o{ PRODUTO_FORNECEDOR : possui\n\nCLIENTE {\nINT id_cliente PK\nVARCHAR nome\nVARCHAR email UK\n}\nVENDA {\nINT id_venda PK\nDATE data_venda\nINT id_cliente FK\n}\nITEM_VENDA {\nINT id_item_venda PK\nINT id_venda FK\nINT id_produto FK\nINT quantidade\nDECIMAL preco_unitario\n}\nPRODUTO {\nINT id_produto PK\nVARCHAR nome\nDECIMAL preco\nINT estoque\n}\nFORNECEDOR {\nINT id_fornecedor PK\nVARCHAR razao_social\nVARCHAR cnpj UK\n}\nPRODUTO_FORNECEDOR {\nINT id_produto FK\nINT id_fornecedor FK\nDECIMAL custo_aquisicao\n}\n```\n\nO relacionamento entre `Cliente` e `Venda` é de um para muitos. `Venda` e `Produto` têm relação muitos para muitos, resolvida por `Item_Venda`. `Produto` e `Fornecedor` também podem ter relação muitos para muitos, resolvida por `Produto_Fornecedor`.", "conteudo": "## Contexto\n\nA empresa TechSolutions está crescendo rapidamente e enfrenta desafios na organização de seus dados, que envolvem informações sobre clientes, produtos, vendas e fornecedores. Atualmente, esses dados estão armazenados de maneira desestruturada, o que dificulta consultas precisas e relatórios. A tarefa é criar um diagrama Entidade-Relacionamento (ER) que modele os dados de maneira eficiente e fazer a normalização para evitar redundâncias e inconsistências.\n\n## Respostas da etapa 2\n\n### Quais são as principais entidades e seus atributos que vocês esperam modelar no contexto da TechSolutions (clientes, produtos, vendas, fornecedores)?\n\nAs entidades principais são:\n\n- `Cliente`: `id_cliente`, `nome`, `email`, `telefone` e `endereco`.\n- `Produto`: `id_produto`, `nome`, `descricao`, `preco` e `estoque`.\n- `Venda`: `id_venda`, `data_venda`, `id_cliente` e `id_vendedor`.\n- `Fornecedor`: `id_fornecedor`, `razao_social`, `cnpj`, `email` e `telefone`.\n\nPara registrar os produtos de cada venda, é necessária ainda a entidade associativa `Item_Venda`, com `id_item_venda`, `id_venda`, `id_produto`, `quantidade` e `preco_unitario`. A compra de produtos de fornecedores pode ser registrada em `Produto_Fornecedor`, com as duas chaves estrangeiras e o custo de aquisição.\n\n### Como a normalização pode ser útil para evitar duplicação de dados em sistemas como o da TechSolutions?\n\nA normalização separa cada fato em seu lugar apropriado. Na 1FN, os campos recebem valores atômicos: em vez de guardar vários produtos em uma única coluna de venda, cada produto fica em uma linha de `Item_Venda`. Na 2FN, os atributos dependem da chave completa da tabela; por exemplo, o nome do produto não deve ser repetido em cada item de venda, pois depende de `id_produto`. Na 3FN, atributos que dependem de outra informação não-chave também são separados; os dados do fornecedor ficam em `Fornecedor`, e o produto apenas guarda a referência necessária. Assim, uma alteração de e-mail do cliente ou de preço cadastral do produto é feita em um único local.\n\n### Desenhem um esboço inicial de um diagrama ER para a TechSolutions, incluindo pelo menos quatro entidades e seus relacionamentos principais.\n\n```mermaid\nerDiagram\n    CLIENTE ||--o{ VENDA : realiza\n    VENDA ||--|{ ITEM_VENDA : contem\n    PRODUTO ||--o{ ITEM_VENDA : integra\n    FORNECEDOR ||--o{ PRODUTO_FORNECEDOR : fornece\n    PRODUTO ||--o{ PRODUTO_FORNECEDOR : possui\n\n    CLIENTE {\n        INT id_cliente PK\n        VARCHAR nome\n        VARCHAR email UK\n    }\n    VENDA {\n        INT id_venda PK\n        DATE data_venda\n        INT id_cliente FK\n    }\n    ITEM_VENDA {\n        INT id_item_venda PK\n        INT id_venda FK\n        INT id_produto FK\n        INT quantidade\n        DECIMAL preco_unitario\n    }\n    PRODUTO {\n        INT id_produto PK\n        VARCHAR nome\n        DECIMAL preco\n        INT estoque\n    }\n    FORNECEDOR {\n        INT id_fornecedor PK\n        VARCHAR razao_social\n        VARCHAR cnpj UK\n    }\n    PRODUTO_FORNECEDOR {\n        INT id_produto FK\n        INT id_fornecedor FK\n        DECIMAL custo_aquisicao\n    }\n```\n\nO relacionamento entre `Cliente` e `Venda` é de um para muitos. `Venda` e `Produto` têm relação muitos para muitos, resolvida por `Item_Venda`. `Produto` e `Fornecedor` também podem ter relação muitos para muitos, resolvida por `Produto_Fornecedor`.", "fonte": "gabarito/Banco de dados/3° bimestre/Semana 15 - SGBD Relacional/Gabarito - Roteiro Prático Aula 2.md", "arquivos": []}, "quizzes": [], "tipo": "roteiro"}
---
