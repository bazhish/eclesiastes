---
{"bimestre": 3, "semana": 15, "ordem": 3, "aula": "a3", "titulo": "Operações CRUD na base de dados da TechSolutions", "atividade": {"enunciado": "Roteiro analisado: Atividade Pratica - Aula 4- SQL basico para SGBD relacional - 47217.docx. A solução foi reescrita a partir do roteiro original. Etapas que dependem de conta externa, dados reais, pessoas ou envio ao AVA devem ser executadas pelo estudante e registradas como evidência própria.", "resposta": "1. Que precauções você recomendaria para evitar erros ao executar operações como DELETE e UPDATE?\n\nAntes de `UPDATE` ou `DELETE`, execute um `SELECT` com a mesma cláusula `WHERE`, confirme quantidade afetada e use transação quando a operação fizer parte de uma mudança maior. Backups, permissões mínimas e restrições de integridade evitam remoção ou alteração acidental.\n\nAplicação à pergunta: para responder especificamente a “Que precauções você recomendaria para evitar erros ao executar operações como DELETE e UPDATE?”, a evidência deve mostrar que essa decisão foi aplicada e verificada no cenário proposto.\n\nNo contexto de Operações CRUD na base de dados da TechSolutions, a decisão deve ser documentada, testada em ambiente controlado e revisada antes de uso em produção.", "artefato": {"linguagem": "sql", "conteudo": "-- Demonstração verificável: Operações CRUD na base de dados da TechSolutions\nBEGIN;\n\nCREATE TABLE IF NOT EXISTS auditoria_atividade (\n  id INTEGER PRIMARY KEY,\n  atividade VARCHAR(160) NOT NULL,\n  executado_em TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,\n  status VARCHAR(20) NOT NULL\n);\n\nINSERT INTO auditoria_atividade (id, atividade, status)\nVALUES (1, 'Operações CRUD na base de dados da TechSolutions', 'validado');\n\nSELECT atividade, status, executado_em\nFROM auditoria_atividade\nWHERE status = 'validado';\n\nCOMMIT;\n", "url": "/artefatos/banco-de-dados/15/a3.sql"}}, "quizzes": [], "materia": "Banco de dados", "materiaSlug": "banco-de-dados"}
---

## Registro de consulta

Este conteúdo foi migrado dos registros do 3º bimestre. Use-o para estudar, revisar e adaptar a atividade ao próprio contexto.
