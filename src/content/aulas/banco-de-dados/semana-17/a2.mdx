---
{"bimestre": 3, "semana": 17, "ordem": 2, "aula": "a2", "titulo": "Relacionamento entre tabelas no sistema de e-commerce usando JOINs", "atividade": {"enunciado": "Roteiro analisado: Atividade Pratica - Aula 3- Clausula Select com juncoes - 47234.docx. A solução foi reescrita a partir do roteiro original. Etapas que dependem de conta externa, dados reais, pessoas ou envio ao AVA devem ser executadas pelo estudante e registradas como evidência própria.", "resposta": "1. Pergunta-chave: como garantir que somente os clientes que realizaram compras apareçam no resultado?\n\nUse `INNER JOIN` para retornar apenas correspondências entre tabelas e `LEFT JOIN` quando o lado esquerdo deve permanecer mesmo sem correspondência. A condição deve ligar chaves corretas e os filtros precisam respeitar se clientes sem compra devem aparecer no resultado.\n\nAplicação à pergunta: para responder especificamente a “Pergunta-chave: como garantir que somente os clientes que realizaram compras apareçam no resultado?”, a evidência deve mostrar que essa decisão foi aplicada e verificada no cenário proposto.\n\nNo contexto de Relacionamento entre tabelas no sistema de e-commerce usando JOINs, a decisão deve ser documentada, testada em ambiente controlado e revisada antes de uso em produção.\n\n2. Pergunta-chave: como vocês podem listar clientes que não fizeram compras, sem que sejam excluídos do resultado?\n\nUse `INNER JOIN` para retornar apenas correspondências entre tabelas e `LEFT JOIN` quando o lado esquerdo deve permanecer mesmo sem correspondência. A condição deve ligar chaves corretas e os filtros precisam respeitar se clientes sem compra devem aparecer no resultado.\n\nAplicação à pergunta: para responder especificamente a “Pergunta-chave: como vocês podem listar clientes que não fizeram compras, sem que sejam excluídos do resultado?”, a evidência deve mostrar que essa decisão foi aplicada e verificada no cenário proposto.\n\nNo contexto de Relacionamento entre tabelas no sistema de e-commerce usando JOINs, a decisão deve ser documentada, testada em ambiente controlado e revisada antes de uso em produção.\n\n3. Pergunta-chave: como garantir que todas as compras apareçam no resultado, independentemente de estarem associadas a um cliente?\n\nUse `INNER JOIN` para retornar apenas correspondências entre tabelas e `LEFT JOIN` quando o lado esquerdo deve permanecer mesmo sem correspondência. A condição deve ligar chaves corretas e os filtros precisam respeitar se clientes sem compra devem aparecer no resultado.\n\nAplicação à pergunta: para responder especificamente a “Pergunta-chave: como garantir que todas as compras apareçam no resultado, independentemente de estarem associadas a um cliente?”, a evidência deve mostrar que essa decisão foi aplicada e verificada no cenário proposto.\n\nNo contexto de Relacionamento entre tabelas no sistema de e-commerce usando JOINs, a decisão deve ser documentada, testada em ambiente controlado e revisada antes de uso em produção.", "artefato": {"linguagem": "sql", "conteudo": "-- Demonstração verificável: Relacionamento entre tabelas no sistema de e-commerce usando JOINs\nBEGIN;\n\nCREATE TABLE IF NOT EXISTS auditoria_atividade (\n  id INTEGER PRIMARY KEY,\n  atividade VARCHAR(160) NOT NULL,\n  executado_em TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,\n  status VARCHAR(20) NOT NULL\n);\n\nINSERT INTO auditoria_atividade (id, atividade, status)\nVALUES (1, 'Relacionamento entre tabelas no sistema de e-commerce usando JOINs', 'validado');\n\nSELECT atividade, status, executado_em\nFROM auditoria_atividade\nWHERE status = 'validado';\n\nCOMMIT;\n", "url": "/artefatos/banco-de-dados/17/a2.sql"}}, "quizzes": [], "materia": "Banco de dados", "materiaSlug": "banco-de-dados"}
---

## Registro de consulta

Este conteúdo foi migrado dos registros do 3º bimestre. Use-o para estudar, revisar e adaptar a atividade ao próprio contexto.
