---
{"materia": "Banco de dados", "materiaSlug": "banco-de-dados", "bimestre": 3, "semana": 17, "ordem": 20, "aula": "a2", "titulo": "Cláusulas Select · Aula 2", "atividade": {"titulo": "Atividades Práticas", "enunciado": "Consultas devem selecionar somente as colunas necessárias, filtrar com `WHERE` e ordenar de forma previsível. Exemplos:\n\n```sql\nSELECT id_cliente, nome, email FROM clientes;\nSELECT id_produto, nome, preco FROM produtos WHERE preco >= 100;\nSELECT id_venda, data_venda, valor_total FROM vendas ORDER BY data_venda DESC;\n```\n\nPara relatórios, podem ser combinadas condições com `AND`, `OR`, `LIKE`, `BETWEEN`, `IN` e valores nulos com `IS NULL`. A consulta deve ser testada primeiro como `SELECT` antes de ser usada como base para alteração de dados.", "resposta": "Consultas devem selecionar somente as colunas necessárias, filtrar com `WHERE` e ordenar de forma previsível. Exemplos:\n\n```sql\nSELECT id_cliente, nome, email FROM clientes;\nSELECT id_produto, nome, preco FROM produtos WHERE preco >= 100;\nSELECT id_venda, data_venda, valor_total FROM vendas ORDER BY data_venda DESC;\n```\n\nPara relatórios, podem ser combinadas condições com `AND`, `OR`, `LIKE`, `BETWEEN`, `IN` e valores nulos com `IS NULL`. A consulta deve ser testada primeiro como `SELECT` antes de ser usada como base para alteração de dados.", "conteudo": "Consultas devem selecionar somente as colunas necessárias, filtrar com `WHERE` e ordenar de forma previsível. Exemplos:\n\n```sql\nSELECT id_cliente, nome, email FROM clientes;\nSELECT id_produto, nome, preco FROM produtos WHERE preco >= 100;\nSELECT id_venda, data_venda, valor_total FROM vendas ORDER BY data_venda DESC;\n```\n\nPara relatórios, podem ser combinadas condições com `AND`, `OR`, `LIKE`, `BETWEEN`, `IN` e valores nulos com `IS NULL`. A consulta deve ser testada primeiro como `SELECT` antes de ser usada como base para alteração de dados.", "fonte": "gabarito/Banco de dados/3° bimestre/Semana 17/semana-17-respostas.md#atividades-praticas", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
