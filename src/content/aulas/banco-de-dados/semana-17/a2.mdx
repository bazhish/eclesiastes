---
{"materia": "Banco de dados", "materiaSlug": "banco-de-dados", "bimestre": 3, "semana": 17, "ordem": 20, "aula": "a2", "titulo": "Clausulas Select · Aula 2", "atividade": {"enunciado": "Com base na tabela `compras`, devem ser usados `SUM`, `AVG` e `COUNT` para obter indicadores de vendas e clientes.", "resposta": "### Calculem o valor total das vendas realizadas até hoje usando SUM.\n\n```sql\nSELECT SUM(valor) AS total_vendas\nFROM compras;\n```\n\n### Quantos clientes únicos fizeram compras no sistema usando COUNT?\n\n```sql\nSELECT COUNT(DISTINCT id_cliente) AS clientes_unicos\nFROM compras;\n```\n\n### Calculem o valor médio gasto por compra usando AVG.\n\n```sql\nSELECT AVG(valor) AS valor_medio_por_compra\nFROM compras;\n```\n\n### Contem quantas compras foram feitas em um mês específico usando WHERE.\n\n```sql\nSELECT COUNT(*) AS compras_em_agosto\nFROM compras\nWHERE data_compra >= '2026-08-01'\nAND data_compra < '2026-09-01';\n```\n\nO intervalo com início inclusivo e fim exclusivo seleciona todo o mês sem depender do horário armazenado em `data_compra`.", "conteudo": "## Situação-problema\n\nCom base na tabela `compras`, devem ser usados `SUM`, `AVG` e `COUNT` para obter indicadores de vendas e clientes.\n\n## Respostas\n\n### Calculem o valor total das vendas realizadas até hoje usando SUM.\n\n```sql\nSELECT SUM(valor) AS total_vendas\nFROM compras;\n```\n\n### Quantos clientes únicos fizeram compras no sistema usando COUNT?\n\n```sql\nSELECT COUNT(DISTINCT id_cliente) AS clientes_unicos\nFROM compras;\n```\n\n### Calculem o valor médio gasto por compra usando AVG.\n\n```sql\nSELECT AVG(valor) AS valor_medio_por_compra\nFROM compras;\n```\n\n### Contem quantas compras foram feitas em um mês específico usando WHERE.\n\n```sql\nSELECT COUNT(*) AS compras_em_agosto\nFROM compras\nWHERE data_compra >= '2026-08-01'\n  AND data_compra < '2026-09-01';\n```\n\nO intervalo com início inclusivo e fim exclusivo seleciona todo o mês sem depender do horário armazenado em `data_compra`.\n\n## Código e verificação\n\nOs dados de exemplo e todas as consultas estão em [`codigo/aula-2/solucao.sql`](codigo/aula-2/solucao.sql), com execução em [`codigo/aula-2/README.md`](codigo/aula-2/README.md).", "fonte": "gabarito/Banco de dados/3° bimestre/Semana 17 - Clausulas Select/Gabarito - Roteiro Prático Aula 2.md", "artefato": {"nome": "Artefatos da Aula 2", "href": "artefatos/banco-de-dados/semana-17/a2.zip", "arquivos": 2}}, "quizzes": []}
---
