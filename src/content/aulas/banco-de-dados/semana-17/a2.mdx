---
{"materia": "Banco de dados", "materiaSlug": "banco-de-dados", "bimestre": 3, "semana": 17, "ordem": 20, "aula": "a2", "titulo": "Clausulas Select · Aula 2", "atividade": {"enunciado": "Com base na tabela `compras`, devem ser usados `SUM`, `AVG` e `COUNT` para obter indicadores de vendas e clientes.", "resposta": "### Calculem o valor total das vendas realizadas até hoje usando SUM.\n\n```sql\nSELECT SUM(valor) AS total_vendas\nFROM compras;\n```\n\n### Quantos clientes únicos fizeram compras no sistema usando COUNT?\n\n```sql\nSELECT COUNT(DISTINCT id_cliente) AS clientes_unicos\nFROM compras;\n```\n\n### Calculem o valor médio gasto por compra usando AVG.\n\n```sql\nSELECT AVG(valor) AS valor_medio_por_compra\nFROM compras;\n```\n\n### Contem quantas compras foram feitas em um mês específico usando WHERE.\n\n```sql\nSELECT COUNT(*) AS compras_em_agosto\nFROM compras\nWHERE data_compra >= '2026-08-01'\nAND data_compra < '2026-09-01';\n```\n\nO intervalo com início inclusivo e fim exclusivo seleciona todo o mês sem depender do horário armazenado em `data_compra`.", "conteudo": "## Situação-problema\n\nCom base na tabela `compras`, devem ser usados `SUM`, `AVG` e `COUNT` para obter indicadores de vendas e clientes.\n\n## Respostas\n\n### Calculem o valor total das vendas realizadas até hoje usando SUM.\n\n```sql\nSELECT SUM(valor) AS total_vendas\nFROM compras;\n```\n\n### Quantos clientes únicos fizeram compras no sistema usando COUNT?\n\n```sql\nSELECT COUNT(DISTINCT id_cliente) AS clientes_unicos\nFROM compras;\n```\n\n### Calculem o valor médio gasto por compra usando AVG.\n\n```sql\nSELECT AVG(valor) AS valor_medio_por_compra\nFROM compras;\n```\n\n### Contem quantas compras foram feitas em um mês específico usando WHERE.\n\n```sql\nSELECT COUNT(*) AS compras_em_agosto\nFROM compras\nWHERE data_compra >= '2026-08-01'\n  AND data_compra < '2026-09-01';\n```\n\nO intervalo com início inclusivo e fim exclusivo seleciona todo o mês sem depender do horário armazenado em `data_compra`.\n\n## Código e verificação\n\nOs dados de exemplo e todas as consultas estão em [`codigo/aula-2/solucao.sql`](codigo/aula-2/solucao.sql), com execução em [`codigo/aula-2/README.md`](codigo/aula-2/README.md).", "fonte": "gabarito/Banco de dados/3° bimestre/Semana 17 - Clausulas Select/Gabarito - Roteiro Prático Aula 2.md", "arquivos": [{"caminho": "README.md", "linguagem": "markdown", "conteudo": "# Como executar\r\n\r\n- **Linguagem:** SQL para MySQL 8.0+\r\n- **Dependências:** MySQL 8.0+ e um usuário com permissão para criar o banco, as tabelas e os objetos usados no arquivo.\r\n- **Execução:** `mysql -u SEU_USUARIO -p < solucao.sql`\r\n- **Teste:** execute as consultas `SELECT`, `EXPLAIN` ou verificações comentadas no próprio arquivo.\r\n- **Resultado esperado:** criação de uma base de demonstração e retorno dos resultados descritos no gabarito. Use uma instância de teste, pois o script recria tabelas próprias.\r\n"}, {"caminho": "solucao.sql", "linguagem": "sql", "conteudo": "DROP DATABASE IF EXISTS agregacao_a2;\r\nCREATE DATABASE agregacao_a2 CHARACTER SET utf8mb4;\r\nUSE agregacao_a2;\r\nCREATE TABLE clientes (cliente_id INT PRIMARY KEY,nome VARCHAR(100),email VARCHAR(150));\r\nCREATE TABLE compras (compra_id INT PRIMARY KEY,cliente_id INT NOT NULL,valor_total DECIMAL(10,2) NOT NULL,data_compra DATE NOT NULL,\r\n  FOREIGN KEY(cliente_id) REFERENCES clientes(cliente_id));\r\nINSERT INTO clientes VALUES (1,'Ana','ana@exemplo.com'),(2,'Bruno','bruno@exemplo.com'),(3,'Carla','carla@exemplo.com');\r\nINSERT INTO compras VALUES (1,1,100,'2024-09-02'),(2,1,250,'2024-09-20'),(3,2,80,'2024-09-25'),(4,3,500,'2024-10-01');\r\nSELECT SUM(valor_total) AS valor_total_vendas FROM compras;\r\nSELECT COUNT(DISTINCT cliente_id) AS clientes_unicos FROM compras;\r\nSELECT AVG(valor_total) AS valor_medio_por_compra FROM compras;\r\nSELECT COUNT(*) AS compras_setembro_2024 FROM compras\r\n  WHERE data_compra >= '2024-09-01' AND data_compra < '2024-10-01';\r\n"}]}, "quizzes": [], "tipo": "roteiro"}
---
