---
{"materia": "Banco de dados", "materiaSlug": "banco-de-dados", "bimestre": 3, "semana": 17, "ordem": 900, "aula": "r1", "titulo": "Cláusulas Select · Registro de Aula", "atividade": {"titulo": "Registro de Aula", "enunciado": "Aprendi que `SELECT` recupera dados, `WHERE` filtra registros e `ORDER BY` organiza o resultado. Consultas claras e com colunas específicas facilitam a leitura e podem reduzir dados processados.", "resposta": "Aprendi que `SELECT` recupera dados, `WHERE` filtra registros e `ORDER BY` organiza o resultado. Consultas claras e com colunas específicas facilitam a leitura e podem reduzir dados processados.", "conteudo": "Aprendi que `SELECT` recupera dados, `WHERE` filtra registros e `ORDER BY` organiza o resultado. Consultas claras e com colunas específicas facilitam a leitura e podem reduzir dados processados.", "fonte": "gabarito/Banco de dados/3° bimestre/Semana 17/semana-17-respostas.md#registro-de-aula", "arquivos": []}, "tipo": "registro", "quizzes": []}
---
