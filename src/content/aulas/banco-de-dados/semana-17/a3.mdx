---
{"materia": "Banco de dados", "materiaSlug": "banco-de-dados", "bimestre": 3, "semana": 17, "ordem": 30, "aula": "a3", "titulo": "Clausulas Select · Aula 3", "atividade": {"enunciado": "## Tarefas e respostas\n\n### Utilizem INNER JOIN para exibir nome do cliente, e-mail e valor total de cada compra realizada.\n\n```sql\nSELECT c.nome, c.email, co.valor_total\nFROM clientes AS c\nINNER JOIN compras AS co ON co.id_cliente = c.id_cliente\nORDER BY c.nome, co.id_compra;\n```\n\n`INNER JOIN` retorna somente os clientes que possuem uma compra correspondente.\n\n### Utilizem LEFT JOIN para exibir todos os clientes e o valor das compras, com NULL para quem ainda não comprou.\n\n```sql\nSELECT c.nome, c.email, co.valor_total\nFROM clientes AS c\nLEFT JOIN compras AS co ON co.id_cliente = c.id_cliente\nORDER BY c.nome, co.id_compra;\n```\n\nQuando não há linha correspondente em `compras`, as colunas de `co` assumem `NULL`, preservando o cliente no resultado.\n\n### Utilizem RIGHT JOIN para listar todas as compras, inclusive registros sem cliente associado.\n\n```sql\nSELECT c.nome, c.email, co.id_compra, co.valor_total\nFROM clientes AS c\nRIGHT JOIN compras AS co ON co.id_cliente = c.id_cliente\nORDER BY co.id_compra;\n```\n\n`RIGHT JOIN` preserva todas as linhas da tabela à direita, `compras`; por isso, uma compra com `id_cliente` inexistente ainda aparece, com os dados de cliente nulos.\n\n## Código e verificação\n\nAs três consultas e o cenário de dados estão em [`codigo/aula-3/solucao.sql`](codigo/aula-3/solucao.sql). Consulte [`codigo/aula-3/README.md`](codigo/aula-3/README.md) para executar em MySQL 8.0+.", "resposta": "### Utilizem INNER JOIN para exibir nome do cliente, e-mail e valor total de cada compra realizada.\n\n```sql\nSELECT c.nome, c.email, co.valor_total\nFROM clientes AS c\nINNER JOIN compras AS co ON co.id_cliente = c.id_cliente\nORDER BY c.nome, co.id_compra;\n```\n\n`INNER JOIN` retorna somente os clientes que possuem uma compra correspondente.\n\n### Utilizem LEFT JOIN para exibir todos os clientes e o valor das compras, com NULL para quem ainda não comprou.\n\n```sql\nSELECT c.nome, c.email, co.valor_total\nFROM clientes AS c\nLEFT JOIN compras AS co ON co.id_cliente = c.id_cliente\nORDER BY c.nome, co.id_compra;\n```\n\nQuando não há linha correspondente em `compras`, as colunas de `co` assumem `NULL`, preservando o cliente no resultado.\n\n### Utilizem RIGHT JOIN para listar todas as compras, inclusive registros sem cliente associado.\n\n```sql\nSELECT c.nome, c.email, co.id_compra, co.valor_total\nFROM clientes AS c\nRIGHT JOIN compras AS co ON co.id_cliente = c.id_cliente\nORDER BY co.id_compra;\n```\n\n`RIGHT JOIN` preserva todas as linhas da tabela à direita, `compras`; por isso, uma compra com `id_cliente` inexistente ainda aparece, com os dados de cliente nulos.", "conteudo": "## Tarefas e respostas\n\n### Utilizem INNER JOIN para exibir nome do cliente, e-mail e valor total de cada compra realizada.\n\n```sql\nSELECT c.nome, c.email, co.valor_total\nFROM clientes AS c\nINNER JOIN compras AS co ON co.id_cliente = c.id_cliente\nORDER BY c.nome, co.id_compra;\n```\n\n`INNER JOIN` retorna somente os clientes que possuem uma compra correspondente.\n\n### Utilizem LEFT JOIN para exibir todos os clientes e o valor das compras, com NULL para quem ainda não comprou.\n\n```sql\nSELECT c.nome, c.email, co.valor_total\nFROM clientes AS c\nLEFT JOIN compras AS co ON co.id_cliente = c.id_cliente\nORDER BY c.nome, co.id_compra;\n```\n\nQuando não há linha correspondente em `compras`, as colunas de `co` assumem `NULL`, preservando o cliente no resultado.\n\n### Utilizem RIGHT JOIN para listar todas as compras, inclusive registros sem cliente associado.\n\n```sql\nSELECT c.nome, c.email, co.id_compra, co.valor_total\nFROM clientes AS c\nRIGHT JOIN compras AS co ON co.id_cliente = c.id_cliente\nORDER BY co.id_compra;\n```\n\n`RIGHT JOIN` preserva todas as linhas da tabela à direita, `compras`; por isso, uma compra com `id_cliente` inexistente ainda aparece, com os dados de cliente nulos.\n\n## Código e verificação\n\nAs três consultas e o cenário de dados estão em [`codigo/aula-3/solucao.sql`](codigo/aula-3/solucao.sql). Consulte [`codigo/aula-3/README.md`](codigo/aula-3/README.md) para executar em MySQL 8.0+.", "fonte": "gabarito/Banco de dados/3° bimestre/Semana 17 - Clausulas Select/Gabarito - Roteiro Prático Aula 3.md", "arquivos": [{"caminho": "README.md", "linguagem": "markdown", "conteudo": "# Como executar\r\n\r\n- **Linguagem:** SQL para MySQL 8.0+\r\n- **Dependências:** MySQL 8.0+ e um usuário com permissão para criar o banco, as tabelas e os objetos usados no arquivo.\r\n- **Execução:** `mysql -u SEU_USUARIO -p < solucao.sql`\r\n- **Teste:** execute as consultas `SELECT`, `EXPLAIN` ou verificações comentadas no próprio arquivo.\r\n- **Resultado esperado:** criação de uma base de demonstração e retorno dos resultados descritos no gabarito. Use uma instância de teste, pois o script recria tabelas próprias.\r\n"}, {"caminho": "solucao.sql", "linguagem": "sql", "conteudo": "USE agregacao_a2;\r\nSELECT c.nome,c.email,co.valor_total FROM clientes c\r\n  INNER JOIN compras co ON co.cliente_id=c.cliente_id;\r\nSELECT c.nome,c.email,co.valor_total FROM clientes c\r\n  LEFT JOIN compras co ON co.cliente_id=c.cliente_id;\r\nSELECT c.nome,c.email,co.compra_id,co.valor_total FROM clientes c\r\n  RIGHT JOIN compras co ON co.cliente_id=c.cliente_id;\r\n"}]}, "quizzes": [], "tipo": "roteiro"}
---
