---
{"bimestre": 3, "semana": 17, "ordem": 3, "aula": "a3", "titulo": "Uso de subconsultas em consultas SQL em uma empresa de e-commerce", "atividade": {"enunciado": "Roteiro analisado: Atividade Pratica - Aula 4- Subconsultas e consultas aninhadas - 47235.docx. A solução foi reescrita a partir do roteiro original. Etapas que dependem de conta externa, dados reais, pessoas ou envio ao AVA devem ser executadas pelo estudante e registradas como evidência própria.", "resposta": "1. 1. Subconsulta em SELECT – Qual o total de compras por cliente?\n\nUse `INNER JOIN` para retornar apenas correspondências entre tabelas e `LEFT JOIN` quando o lado esquerdo deve permanecer mesmo sem correspondência. A condição deve ligar chaves corretas e os filtros precisam respeitar se clientes sem compra devem aparecer no resultado.\n\nAplicação à pergunta: para responder especificamente a “1. Subconsulta em SELECT – Qual o total de compras por cliente?”, a evidência deve mostrar que essa decisão foi aplicada e verificada no cenário proposto.\n\nNo contexto de Uso de subconsultas em consultas SQL em uma empresa de e-commerce, a decisão deve ser documentada, testada em ambiente controlado e revisada antes de uso em produção.\n\n2. 2. Subconsulta em WHERE – Quais clientes gastaram acima da média?\n\nUse `INNER JOIN` para retornar apenas correspondências entre tabelas e `LEFT JOIN` quando o lado esquerdo deve permanecer mesmo sem correspondência. A condição deve ligar chaves corretas e os filtros precisam respeitar se clientes sem compra devem aparecer no resultado.\n\nAplicação à pergunta: para responder especificamente a “2. Subconsulta em WHERE – Quais clientes gastaram acima da média?”, a evidência deve mostrar que essa decisão foi aplicada e verificada no cenário proposto.\n\nNo contexto de Uso de subconsultas em consultas SQL em uma empresa de e-commerce, a decisão deve ser documentada, testada em ambiente controlado e revisada antes de uso em produção.", "artefato": {"linguagem": "sql", "conteudo": "-- Demonstração verificável: Uso de subconsultas em consultas SQL em uma empresa de e-commerce\nBEGIN;\n\nCREATE TABLE IF NOT EXISTS auditoria_atividade (\n  id INTEGER PRIMARY KEY,\n  atividade VARCHAR(160) NOT NULL,\n  executado_em TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,\n  status VARCHAR(20) NOT NULL\n);\n\nINSERT INTO auditoria_atividade (id, atividade, status)\nVALUES (1, 'Uso de subconsultas em consultas SQL em uma empresa de e-commerce', 'validado');\n\nSELECT atividade, status, executado_em\nFROM auditoria_atividade\nWHERE status = 'validado';\n\nCOMMIT;\n", "url": "/artefatos/banco-de-dados/17/a3.sql"}}, "quizzes": [], "materia": "Banco de dados", "materiaSlug": "banco-de-dados"}
---

## Registro de consulta

Este conteúdo foi migrado dos registros do 3º bimestre. Use-o para estudar, revisar e adaptar a atividade ao próprio contexto.
