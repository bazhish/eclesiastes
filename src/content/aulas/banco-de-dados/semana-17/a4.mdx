---
{"materia": "Banco de dados", "materiaSlug": "banco-de-dados", "bimestre": 3, "semana": 17, "ordem": 40, "aula": "a4", "titulo": "Clausulas Select · Aula 4", "atividade": {"enunciado": "Vocês trabalham no banco de dados de uma empresa de e-commerce. O gerente de vendas precisa de relatórios sobre o comportamento de compra dos clientes; as subconsultas devem fornecer totais e comparações dinâmicas.", "resposta": "### 1. Subconsulta em SELECT — Qual é o total de compras por cliente?\n\n```sql\nSELECT c.id_cliente,\nc.nome,\n(\nSELECT COALESCE(SUM(co.valor_total), 0)\nFROM compras AS co\nWHERE co.id_cliente = c.id_cliente\n) AS total_compras\nFROM clientes AS c\nORDER BY c.nome;\n```\n\nA subconsulta é correlacionada: ela é calculada para o cliente da linha atual. `COALESCE` mostra zero para clientes sem compras.\n\n### 2. Subconsulta em WHERE — Quais clientes gastaram acima da média?\n\n```sql\nSELECT c.nome, SUM(co.valor_total) AS total_compras\nFROM clientes AS c\nJOIN compras AS co ON co.id_cliente = c.id_cliente\nGROUP BY c.id_cliente, c.nome\nHAVING SUM(co.valor_total) > (\nSELECT AVG(total_por_cliente)\nFROM (\nSELECT SUM(valor_total) AS total_por_cliente\nFROM compras\nGROUP BY id_cliente\n) AS totais\n);\n```\n\nA média é calculada sobre o total já agrupado de cada cliente, e não sobre uma compra individual.\n\n### 3. Subconsulta em FROM — Relatório com total de compras por cliente acima da média geral\n\n```sql\nSELECT c.nome, totais.total_compras\nFROM clientes AS c\nJOIN (\nSELECT id_cliente, SUM(valor_total) AS total_compras\nFROM compras\nGROUP BY id_cliente\n) AS totais ON totais.id_cliente = c.id_cliente\nWHERE totais.total_compras > (\nSELECT AVG(media_base.total_compras)\nFROM (\nSELECT SUM(valor_total) AS total_compras\nFROM compras\nGROUP BY id_cliente\n) AS media_base\n)\nORDER BY totais.total_compras DESC;\n```", "conteudo": "## Situação-problema\n\nVocês trabalham no banco de dados de uma empresa de e-commerce. O gerente de vendas precisa de relatórios sobre o comportamento de compra dos clientes; as subconsultas devem fornecer totais e comparações dinâmicas.\n\n## Respostas\n\n### 1. Subconsulta em SELECT — Qual é o total de compras por cliente?\n\n```sql\nSELECT c.id_cliente,\n       c.nome,\n       (\n           SELECT COALESCE(SUM(co.valor_total), 0)\n           FROM compras AS co\n           WHERE co.id_cliente = c.id_cliente\n       ) AS total_compras\nFROM clientes AS c\nORDER BY c.nome;\n```\n\nA subconsulta é correlacionada: ela é calculada para o cliente da linha atual. `COALESCE` mostra zero para clientes sem compras.\n\n### 2. Subconsulta em WHERE — Quais clientes gastaram acima da média?\n\n```sql\nSELECT c.nome, SUM(co.valor_total) AS total_compras\nFROM clientes AS c\nJOIN compras AS co ON co.id_cliente = c.id_cliente\nGROUP BY c.id_cliente, c.nome\nHAVING SUM(co.valor_total) > (\n    SELECT AVG(total_por_cliente)\n    FROM (\n        SELECT SUM(valor_total) AS total_por_cliente\n        FROM compras\n        GROUP BY id_cliente\n    ) AS totais\n);\n```\n\nA média é calculada sobre o total já agrupado de cada cliente, e não sobre uma compra individual.\n\n### 3. Subconsulta em FROM — Relatório com total de compras por cliente acima da média geral\n\n```sql\nSELECT c.nome, totais.total_compras\nFROM clientes AS c\nJOIN (\n    SELECT id_cliente, SUM(valor_total) AS total_compras\n    FROM compras\n    GROUP BY id_cliente\n) AS totais ON totais.id_cliente = c.id_cliente\nWHERE totais.total_compras > (\n    SELECT AVG(media_base.total_compras)\n    FROM (\n        SELECT SUM(valor_total) AS total_compras\n        FROM compras\n        GROUP BY id_cliente\n    ) AS media_base\n)\nORDER BY totais.total_compras DESC;\n```\n\n## Código e verificação\n\nO cenário e as três consultas estão em [`codigo/aula-4/solucao.sql`](codigo/aula-4/solucao.sql). O arquivo [`codigo/aula-4/README.md`](codigo/aula-4/README.md) descreve a execução em MySQL 8.0+.", "fonte": "gabarito/Banco de dados/3° bimestre/Semana 17 - Clausulas Select/Gabarito - Roteiro Prático Aula 4.md", "artefato": {"nome": "Artefatos da Aula 4", "href": "artefatos/banco-de-dados/semana-17/a4.zip", "arquivos": 2}}, "quizzes": []}
---
