---
{"materia": "Banco de dados", "materiaSlug": "banco-de-dados", "bimestre": 3, "semana": 20, "ordem": 30, "aula": "a3", "titulo": "SGBD Relacional · Aula 3", "atividade": {"enunciado": "Criptografar e-mail e telefone, criar usuários admin e view_only, testar permissões, ativar e analisar logs e elaborar relatório das medidas aplicadas.", "resposta": "A tabela armazena dados criptografados em colunas binárias e os recupera com `AES_DECRYPT`. A chave de demonstração não deve ser usada em produção; a aplicação deve obter segredo de um cofre de chaves, e o ideal é usar TLS e criptografia de disco além da criptografia de coluna. `admin` recebe privilégios completos e `view_only` recebe apenas `SELECT`. O log geral é útil para teste e auditoria temporária, mas pode registrar dados sensíveis e afetar desempenho; em produção, deve haver política de retenção, acesso restrito e recurso de auditoria apropriado.", "conteudo": "## Segurança de dados sensíveis\n\n## Enunciado\n\nCriptografar e-mail e telefone, criar usuários admin e view_only, testar permissões, ativar e analisar logs e elaborar relatório das medidas aplicadas.\n\n## Resposta\n\nA tabela armazena dados criptografados em colunas binárias e os recupera com `AES_DECRYPT`. A chave de demonstração não deve ser usada em produção; a aplicação deve obter segredo de um cofre de chaves, e o ideal é usar TLS e criptografia de disco além da criptografia de coluna. `admin` recebe privilégios completos e `view_only` recebe apenas `SELECT`. O log geral é útil para teste e auditoria temporária, mas pode registrar dados sensíveis e afetar desempenho; em produção, deve haver política de retenção, acesso restrito e recurso de auditoria apropriado.\n\n## Código entregue\n\n[`codigo/aula-3/solucao.sql`](codigo/aula-3/solucao.sql)\n\n**Verificação:** simulação técnica. O ambiente local não possui servidor ou cliente MySQL instalado; o código foi conferido para a sintaxe e a semântica do MySQL 8.0+. Antes de usar em produção, execute-o em uma instância de teste.\n\n## Fonte técnica\n\n- https://dev.mysql.com/doc/refman/8.4/en/encryption-functions.html", "fonte": "gabarito/Banco de dados/3° bimestre/Semana 20 - SGBD Relacional/Gabarito - Roteiro Prático Aula 3.md", "arquivos": [{"caminho": "README.md", "linguagem": "markdown", "conteudo": "# Como executar\r\n\r\n- **Linguagem:** SQL para MySQL 8.0+\r\n- **Dependências:** MySQL 8.0+ e um usuário com permissão para criar o banco, as tabelas e os objetos usados no arquivo.\r\n- **Execução:** `mysql -u SEU_USUARIO -p < solucao.sql`\r\n- **Teste:** execute as consultas `SELECT`, `EXPLAIN` ou verificações comentadas no próprio arquivo.\r\n- **Resultado esperado:** criação de uma base de demonstração e retorno dos resultados descritos no gabarito. Use uma instância de teste, pois o script recria tabelas próprias.\r\n"}, {"caminho": "solucao.sql", "linguagem": "sql", "conteudo": "DROP DATABASE IF EXISTS seguranca_a3; CREATE DATABASE seguranca_a3 CHARACTER SET utf8mb4; USE seguranca_a3;\r\nCREATE TABLE customers(id INT AUTO_INCREMENT PRIMARY KEY,name VARCHAR(100) NOT NULL,email VARBINARY(255) NOT NULL,phone_number VARBINARY(255) NOT NULL);\r\nSET @chave_demo='NUNCA_USAR_EM_PRODUCAO';\r\nINSERT INTO customers(name,email,phone_number) VALUES\r\n  ('João Silva',AES_ENCRYPT('joao@exemplo.com',@chave_demo),AES_ENCRYPT('11999990001',@chave_demo)),\r\n  ('Maria Souza',AES_ENCRYPT('maria@exemplo.com',@chave_demo),AES_ENCRYPT('11999990002',@chave_demo)),\r\n  ('Carlos Lima',AES_ENCRYPT('carlos@exemplo.com',@chave_demo),AES_ENCRYPT('11999990003',@chave_demo));\r\nSELECT name,CAST(AES_DECRYPT(email,@chave_demo) AS CHAR(150)) AS email,CAST(AES_DECRYPT(phone_number,@chave_demo) AS CHAR(30)) AS telefone FROM customers;\r\nCREATE USER IF NOT EXISTS 'admin'@'localhost' IDENTIFIED BY 'TROCAR_POR_SEGREDO_FORTE';\r\nCREATE USER IF NOT EXISTS 'view_only'@'localhost' IDENTIFIED BY 'TROCAR_POR_SEGREDO_FORTE';\r\nGRANT ALL PRIVILEGES ON seguranca_a3.customers TO 'admin'@'localhost'; GRANT SELECT ON seguranca_a3.customers TO 'view_only'@'localhost';\r\nSHOW GRANTS FOR 'admin'@'localhost'; SHOW GRANTS FOR 'view_only'@'localhost';\r\n-- Requer privilégio administrativo; escolha caminho seguro conforme o sistema operacional.\r\nSET GLOBAL general_log = 'ON';\r\n"}]}, "quizzes": [], "tipo": "roteiro"}
---
