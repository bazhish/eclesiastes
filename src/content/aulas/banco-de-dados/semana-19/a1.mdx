---
{"materia": "Banco de dados", "materiaSlug": "banco-de-dados", "bimestre": 3, "semana": 19, "ordem": 10, "aula": "a1", "titulo": "DDL x DML · Aula 1", "atividade": {"titulo": "Atividades Práticas", "enunciado": "DDL define ou altera a estrutura do banco, por exemplo `CREATE`, `ALTER`, `DROP` e `TRUNCATE`. DML manipula os dados existentes, como `INSERT`, `UPDATE` e `DELETE`.\n\n```sql\nCREATE TABLE categorias (id_categoria INT PRIMARY KEY, nome VARCHAR(80) NOT NULL);\nALTER TABLE produtos ADD COLUMN id_categoria INT;\nINSERT INTO categorias (id_categoria, nome) VALUES (1, 'Tecnologia');\nUPDATE produtos SET id_categoria = 1 WHERE id_produto = 10;\n```\n\nAntes de DDL, é necessário revisar impacto em aplicações, dependências, permissões e backup. Antes de DML, usar transação, `WHERE` preciso e `SELECT` de conferência reduz risco de alteração indevida.", "resposta": "DDL define ou altera a estrutura do banco, por exemplo `CREATE`, `ALTER`, `DROP` e `TRUNCATE`. DML manipula os dados existentes, como `INSERT`, `UPDATE` e `DELETE`.\n\n```sql\nCREATE TABLE categorias (id_categoria INT PRIMARY KEY, nome VARCHAR(80) NOT NULL);\nALTER TABLE produtos ADD COLUMN id_categoria INT;\nINSERT INTO categorias (id_categoria, nome) VALUES (1, 'Tecnologia');\nUPDATE produtos SET id_categoria = 1 WHERE id_produto = 10;\n```\n\nAntes de DDL, é necessário revisar impacto em aplicações, dependências, permissões e backup. Antes de DML, usar transação, `WHERE` preciso e `SELECT` de conferência reduz risco de alteração indevida.", "conteudo": "DDL define ou altera a estrutura do banco, por exemplo `CREATE`, `ALTER`, `DROP` e `TRUNCATE`. DML manipula os dados existentes, como `INSERT`, `UPDATE` e `DELETE`.\n\n```sql\nCREATE TABLE categorias (id_categoria INT PRIMARY KEY, nome VARCHAR(80) NOT NULL);\nALTER TABLE produtos ADD COLUMN id_categoria INT;\nINSERT INTO categorias (id_categoria, nome) VALUES (1, 'Tecnologia');\nUPDATE produtos SET id_categoria = 1 WHERE id_produto = 10;\n```\n\nAntes de DDL, é necessário revisar impacto em aplicações, dependências, permissões e backup. Antes de DML, usar transação, `WHERE` preciso e `SELECT` de conferência reduz risco de alteração indevida.", "fonte": "gabarito/Banco de dados/3° bimestre/Semana 19/semana-19-respostas.md#atividades-praticas", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
