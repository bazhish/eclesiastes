---
{"materia": "Banco de dados", "materiaSlug": "banco-de-dados", "bimestre": 3, "semana": 19, "ordem": 20, "aula": "a2", "titulo": "DDL X DML · Aula 2", "atividade": {"enunciado": "Criar Restaurante, Menu e Pedido; adicionar preço e padrões; incluir `id_menu` e quantidade em Pedido; remover telefone; validar com `DESCRIBE` e explicar as mudanças.", "resposta": "O preço permite registrar o valor do item; `disponibilidade DEFAULT TRUE` evita que novos itens fiquem sem estado e mantém a regra padrão explícita. `id_menu` conecta o pedido ao item solicitado e `quantidade DEFAULT 1` representa o caso comum. A remoção de telefone elimina um campo sem uso. Em produção, cada alteração deve ser testada por causa de compatibilidade, tempo de bloqueio e impacto em dados existentes.", "conteudo": "## Comandos DDL no FoodPlusDB\n\n## Enunciado\n\nCriar Restaurante, Menu e Pedido; adicionar preço e padrões; incluir `id_menu` e quantidade em Pedido; remover telefone; validar com `DESCRIBE` e explicar as mudanças.\n\n## Resposta\n\nO preço permite registrar o valor do item; `disponibilidade DEFAULT TRUE` evita que novos itens fiquem sem estado e mantém a regra padrão explícita. `id_menu` conecta o pedido ao item solicitado e `quantidade DEFAULT 1` representa o caso comum. A remoção de telefone elimina um campo sem uso. Em produção, cada alteração deve ser testada por causa de compatibilidade, tempo de bloqueio e impacto em dados existentes.\n\n## Código entregue\n\n[`codigo/aula-2/solucao.sql`](codigo/aula-2/solucao.sql)\n\n**Verificação:** simulação técnica. O ambiente local não possui servidor ou cliente MySQL instalado; o código foi conferido para a sintaxe e a semântica do MySQL 8.0+. Antes de usar em produção, execute-o em uma instância de teste.\n\n## Fonte técnica\n\n- https://dev.mysql.com/doc/refman/8.4/en/alter-table.html", "fonte": "gabarito/Banco de dados/3° bimestre/Semana 19 - DDL X DML/Gabarito - Roteiro Prático Aula 2.md", "arquivos": [{"caminho": "README.md", "linguagem": "markdown", "conteudo": "# Como executar\r\n\r\n- **Linguagem:** SQL para MySQL 8.0+\r\n- **Dependências:** MySQL 8.0+ e um usuário com permissão para criar o banco, as tabelas e os objetos usados no arquivo.\r\n- **Execução:** `mysql -u SEU_USUARIO -p < solucao.sql`\r\n- **Teste:** execute as consultas `SELECT`, `EXPLAIN` ou verificações comentadas no próprio arquivo.\r\n- **Resultado esperado:** criação de uma base de demonstração e retorno dos resultados descritos no gabarito. Use uma instância de teste, pois o script recria tabelas próprias.\r\n"}, {"caminho": "solucao.sql", "linguagem": "sql", "conteudo": "DROP DATABASE IF EXISTS FoodPlusDB; CREATE DATABASE FoodPlusDB CHARACTER SET utf8mb4; USE FoodPlusDB;\r\nCREATE TABLE Restaurante (id_restaurante INT AUTO_INCREMENT PRIMARY KEY,nome VARCHAR(100) NOT NULL,endereco VARCHAR(200) NOT NULL,telefone VARCHAR(15));\r\nCREATE TABLE Menu (id_menu INT AUTO_INCREMENT PRIMARY KEY,id_restaurante INT NOT NULL,descricao VARCHAR(150) NOT NULL,disponibilidade BOOLEAN NOT NULL,\r\n  FOREIGN KEY(id_restaurante) REFERENCES Restaurante(id_restaurante));\r\nCREATE TABLE Pedido (id_pedido INT AUTO_INCREMENT PRIMARY KEY,id_restaurante INT NOT NULL,data_pedido DATE NOT NULL,status VARCHAR(20) NOT NULL,\r\n  FOREIGN KEY(id_restaurante) REFERENCES Restaurante(id_restaurante));\r\nALTER TABLE Menu ADD COLUMN preco DECIMAL(10,2) NOT NULL DEFAULT 0, MODIFY disponibilidade BOOLEAN NOT NULL DEFAULT TRUE;\r\nALTER TABLE Pedido ADD COLUMN id_menu INT NOT NULL,ADD COLUMN quantidade INT NOT NULL DEFAULT 1,\r\n  ADD CONSTRAINT fk_pedido_menu FOREIGN KEY(id_menu) REFERENCES Menu(id_menu);\r\nALTER TABLE Restaurante DROP COLUMN telefone;\r\nDESCRIBE Restaurante; DESCRIBE Menu; DESCRIBE Pedido;\r\n"}]}, "quizzes": [], "tipo": "roteiro"}
---
