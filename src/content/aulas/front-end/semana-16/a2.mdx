---
{"materia": "Front-End", "materiaSlug": "front-end", "bimestre": 3, "semana": 16, "ordem": 20, "aula": "a2", "titulo": "Animações e Pré-processadores CSS · Aula 2", "atividade": {"titulo": "Atividade Prática - Aula 2: Pré-processadores CSS", "enunciado": "Para facilitar a manutenção, organizei os estilos em arquivos SCSS. O arquivo principal importa os módulos, e o compilador gera o CSS que será usado pela página.\n\n### Estrutura proposta\n\n```text\nprojeto-login/\n  scss/\n    _variaveis.scss\n    _mixins.scss\n    _formulario.scss\n    main.scss\n  css/\n    main.css\n  index.html\n```\n\n### `scss/_variaveis.scss`\n\n```scss\n$cor-primaria: #6c63ff;\n$cor-borda: #d8d8d8;\n$raio: 4px;\n$espacamento: 10px;\n```\n\n### `scss/_mixins.scss`\n\n```scss\n@mixin foco-acessivel($cor) {\n  border-color: $cor;\n  box-shadow: 0 0 0 3px rgba($cor, 0.2);\n  outline: none;\n}\n\n@mixin botao($fundo) {\n  border: 0;\n  border-radius: $raio;\n  padding: $espacamento;\n  color: #fff;\n  background: $fundo;\n  cursor: pointer;\n}\n```\n\n### `scss/_formulario.scss`\n\n```scss\n.form-container {\n  width: min(90%, 360px);\n  padding: $espacamento * 2;\n  border-radius: $raio * 2;\n  background: #fff;\n\n  form { display: flex; flex-direction: column; gap: $espacamento; }\n\n  input {\n    padding: $espacamento;\n    border: 1px solid $cor-borda;\n    border-radius: $raio;\n\n    &:focus { @include foco-acessivel($cor-primaria); }\n  }\n\n  button { @include botao($cor-primaria); }\n}\n```\n\n### `scss/main.scss` e compilação\n\n```scss\n@use 'variaveis';\n@use 'mixins';\n@use 'formulario';\n```\n\n```bash\nnpm init -y\nnpm install --save-dev sass\nnpx sass scss/main.scss css/main.css --watch\n```\n\n**Como o uso de variáveis e mixins em pré-processadores facilita a manutenção do código CSS?**\n\nAs variáveis centralizam valores repetidos, como cores, espaçamentos e raios de borda. Os mixins encapsulam regras reutilizáveis, como o foco acessível e o estilo de botões. Assim, uma alteração é feita em um ponto e se propaga de forma consistente pelo projeto.\n\n**Quais foram as vantagens de modularizar o código em diferentes arquivos?**\n\nA separação por responsabilidade torna o código mais fácil de localizar, revisar e reaproveitar. Variáveis e mixins ficam disponíveis para vários componentes, enquanto cada componente mantém seus próprios estilos sem concentrar toda a aplicação em um arquivo grande.\n\n**Em que situações o uso de um pré-processador seria especialmente útil em projetos reais?**\n\nEle é especialmente útil em interfaces com vários componentes, temas, breakpoints e pessoas trabalhando no mesmo código. Também ajuda quando há padrões visuais reutilizáveis e necessidade de manutenção frequente, pois reduz duplicação e torna alterações globais mais confiáveis.", "resposta": "Para facilitar a manutenção, organizei os estilos em arquivos SCSS. O arquivo principal importa os módulos, e o compilador gera o CSS que será usado pela página.\n\n### Estrutura proposta\n\n```text\nprojeto-login/\n  scss/\n    _variaveis.scss\n    _mixins.scss\n    _formulario.scss\n    main.scss\n  css/\n    main.css\n  index.html\n```\n\n### `scss/_variaveis.scss`\n\n```scss\n$cor-primaria: #6c63ff;\n$cor-borda: #d8d8d8;\n$raio: 4px;\n$espacamento: 10px;\n```\n\n### `scss/_mixins.scss`\n\n```scss\n@mixin foco-acessivel($cor) {\n  border-color: $cor;\n  box-shadow: 0 0 0 3px rgba($cor, 0.2);\n  outline: none;\n}\n\n@mixin botao($fundo) {\n  border: 0;\n  border-radius: $raio;\n  padding: $espacamento;\n  color: #fff;\n  background: $fundo;\n  cursor: pointer;\n}\n```\n\n### `scss/_formulario.scss`\n\n```scss\n.form-container {\n  width: min(90%, 360px);\n  padding: $espacamento * 2;\n  border-radius: $raio * 2;\n  background: #fff;\n\n  form { display: flex; flex-direction: column; gap: $espacamento; }\n\n  input {\n    padding: $espacamento;\n    border: 1px solid $cor-borda;\n    border-radius: $raio;\n\n    &:focus { @include foco-acessivel($cor-primaria); }\n  }\n\n  button { @include botao($cor-primaria); }\n}\n```\n\n### `scss/main.scss` e compilação\n\n```scss\n@use 'variaveis';\n@use 'mixins';\n@use 'formulario';\n```\n\n```bash\nnpm init -y\nnpm install --save-dev sass\nnpx sass scss/main.scss css/main.css --watch\n```\n\n**Como o uso de variáveis e mixins em pré-processadores facilita a manutenção do código CSS?**\n\nAs variáveis centralizam valores repetidos, como cores, espaçamentos e raios de borda. Os mixins encapsulam regras reutilizáveis, como o foco acessível e o estilo de botões. Assim, uma alteração é feita em um ponto e se propaga de forma consistente pelo projeto.\n\n**Quais foram as vantagens de modularizar o código em diferentes arquivos?**\n\nA separação por responsabilidade torna o código mais fácil de localizar, revisar e reaproveitar. Variáveis e mixins ficam disponíveis para vários componentes, enquanto cada componente mantém seus próprios estilos sem concentrar toda a aplicação em um arquivo grande.\n\n**Em que situações o uso de um pré-processador seria especialmente útil em projetos reais?**\n\nEle é especialmente útil em interfaces com vários componentes, temas, breakpoints e pessoas trabalhando no mesmo código. Também ajuda quando há padrões visuais reutilizáveis e necessidade de manutenção frequente, pois reduz duplicação e torna alterações globais mais confiáveis.", "conteudo": "Para facilitar a manutenção, organizei os estilos em arquivos SCSS. O arquivo principal importa os módulos, e o compilador gera o CSS que será usado pela página.\n\n### Estrutura proposta\n\n```text\nprojeto-login/\n  scss/\n    _variaveis.scss\n    _mixins.scss\n    _formulario.scss\n    main.scss\n  css/\n    main.css\n  index.html\n```\n\n### `scss/_variaveis.scss`\n\n```scss\n$cor-primaria: #6c63ff;\n$cor-borda: #d8d8d8;\n$raio: 4px;\n$espacamento: 10px;\n```\n\n### `scss/_mixins.scss`\n\n```scss\n@mixin foco-acessivel($cor) {\n  border-color: $cor;\n  box-shadow: 0 0 0 3px rgba($cor, 0.2);\n  outline: none;\n}\n\n@mixin botao($fundo) {\n  border: 0;\n  border-radius: $raio;\n  padding: $espacamento;\n  color: #fff;\n  background: $fundo;\n  cursor: pointer;\n}\n```\n\n### `scss/_formulario.scss`\n\n```scss\n.form-container {\n  width: min(90%, 360px);\n  padding: $espacamento * 2;\n  border-radius: $raio * 2;\n  background: #fff;\n\n  form { display: flex; flex-direction: column; gap: $espacamento; }\n\n  input {\n    padding: $espacamento;\n    border: 1px solid $cor-borda;\n    border-radius: $raio;\n\n    &:focus { @include foco-acessivel($cor-primaria); }\n  }\n\n  button { @include botao($cor-primaria); }\n}\n```\n\n### `scss/main.scss` e compilação\n\n```scss\n@use 'variaveis';\n@use 'mixins';\n@use 'formulario';\n```\n\n```bash\nnpm init -y\nnpm install --save-dev sass\nnpx sass scss/main.scss css/main.css --watch\n```\n\n**Como o uso de variáveis e mixins em pré-processadores facilita a manutenção do código CSS?**\n\nAs variáveis centralizam valores repetidos, como cores, espaçamentos e raios de borda. Os mixins encapsulam regras reutilizáveis, como o foco acessível e o estilo de botões. Assim, uma alteração é feita em um ponto e se propaga de forma consistente pelo projeto.\n\n**Quais foram as vantagens de modularizar o código em diferentes arquivos?**\n\nA separação por responsabilidade torna o código mais fácil de localizar, revisar e reaproveitar. Variáveis e mixins ficam disponíveis para vários componentes, enquanto cada componente mantém seus próprios estilos sem concentrar toda a aplicação em um arquivo grande.\n\n**Em que situações o uso de um pré-processador seria especialmente útil em projetos reais?**\n\nEle é especialmente útil em interfaces com vários componentes, temas, breakpoints e pessoas trabalhando no mesmo código. Também ajuda quando há padrões visuais reutilizáveis e necessidade de manutenção frequente, pois reduz duplicação e torna alterações globais mais confiáveis.", "fonte": "gabarito/Front-End/3° bimestre/Semana 16/semana-16-respostas.md#atividade-pratica-aula-2-pre-processadores-css", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
