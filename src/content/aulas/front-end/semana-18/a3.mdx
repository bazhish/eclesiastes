---
{"materia": "Front-End", "materiaSlug": "front-end", "bimestre": 3, "semana": 18, "ordem": 30, "aula": "a3", "titulo": "CI/CD e Testes Automatizados · Aula 3", "atividade": {"titulo": "Atividade Prática - Aula 3: Testes de performance para front-end", "enunciado": "### Plano de execução\n\n1. Executar a aplicação em ambiente de teste e registrar a versão avaliada.\n2. Gerar relatório Lighthouse pelo Chrome DevTools, analisando LCP, TTI e FID/INP, além de recomendações de carregamento e acessibilidade.\n3. Criar um cenário de carga no k6 ou JMeter que percorra página inicial, lista de produtos e detalhes.\n4. Executar lotes progressivos de 50, 100, 200, 300, 400 e 500 usuários simultâneos.\n5. Registrar tempo médio, percentil 95, taxa de erros, volume de requisições e uso de recursos do servidor.\n6. Comparar os resultados, localizar gargalos e repetir os testes depois das melhorias.\n\n### Exemplo de teste de carga com k6 - `carga.js`\n\n```js\nimport http from 'k6/http'\nimport { check, sleep } from 'k6'\n\nexport const options = {\n  stages: [\n    { duration: '1m', target: 50 },\n    { duration: '2m', target: 200 },\n    { duration: '2m', target: 500 },\n    { duration: '1m', target: 0 },\n  ],\n  thresholds: {\n    http_req_failed: ['rate<0.01'],\n    http_req_duration: ['p(95)<1000'],\n  },\n}\n\nconst baseUrl = __ENV.BASE_URL || 'http://localhost:3000'\n\nexport default function () {\n  for (const rota of ['/', '/produtos', '/produtos/1']) {\n    const resposta = http.get(`${baseUrl}${rota}`)\n    check(resposta, { 'status 200': (r) => r.status === 200 })\n    sleep(1)\n  }\n}\n```\n\nExecução:\n\n```bash\nk6 run -e BASE_URL=https://ambiente-de-teste.exemplo carga.js\n```\n\n### Modelo de relatório de performance\n\n| Item avaliado | Resultado a registrar | Análise |\n| --- | --- | --- |\n| Lighthouse: LCP | valor do relatório | Identificar imagem, fonte ou recurso que atrasou o maior conteúdo. |\n| Lighthouse: TTI | valor do relatório | Verificar tarefas longas de JavaScript. |\n| Lighthouse: FID/INP | valor do relatório | Identificar atraso na resposta a interações. |\n| k6: tempo médio e p95 | valores do teste | Comparar cada patamar de usuários. |\n| k6: taxa de erros | percentual | Investigar códigos HTTP e falhas de conexão. |\n\nAs melhorias prioritárias seriam comprimir e dimensionar imagens, carregar imagens fora da tela sob demanda, dividir JavaScript em partes menores, remover dependências não usadas, aplicar cache para arquivos estáticos e evitar consultas redundantes. Métricas reais só devem ser preenchidas após executar Lighthouse e k6/JMeter contra uma aplicação acessível.", "resposta": "### Plano de execução\n\n1. Executar a aplicação em ambiente de teste e registrar a versão avaliada.\n2. Gerar relatório Lighthouse pelo Chrome DevTools, analisando LCP, TTI e FID/INP, além de recomendações de carregamento e acessibilidade.\n3. Criar um cenário de carga no k6 ou JMeter que percorra página inicial, lista de produtos e detalhes.\n4. Executar lotes progressivos de 50, 100, 200, 300, 400 e 500 usuários simultâneos.\n5. Registrar tempo médio, percentil 95, taxa de erros, volume de requisições e uso de recursos do servidor.\n6. Comparar os resultados, localizar gargalos e repetir os testes depois das melhorias.\n\n### Exemplo de teste de carga com k6 - `carga.js`\n\n```js\nimport http from 'k6/http'\nimport { check, sleep } from 'k6'\n\nexport const options = {\n  stages: [\n    { duration: '1m', target: 50 },\n    { duration: '2m', target: 200 },\n    { duration: '2m', target: 500 },\n    { duration: '1m', target: 0 },\n  ],\n  thresholds: {\n    http_req_failed: ['rate<0.01'],\n    http_req_duration: ['p(95)<1000'],\n  },\n}\n\nconst baseUrl = __ENV.BASE_URL || 'http://localhost:3000'\n\nexport default function () {\n  for (const rota of ['/', '/produtos', '/produtos/1']) {\n    const resposta = http.get(`${baseUrl}${rota}`)\n    check(resposta, { 'status 200': (r) => r.status === 200 })\n    sleep(1)\n  }\n}\n```\n\nExecução:\n\n```bash\nk6 run -e BASE_URL=https://ambiente-de-teste.exemplo carga.js\n```\n\n### Modelo de relatório de performance\n\n| Item avaliado | Resultado a registrar | Análise |\n| --- | --- | --- |\n| Lighthouse: LCP | valor do relatório | Identificar imagem, fonte ou recurso que atrasou o maior conteúdo. |\n| Lighthouse: TTI | valor do relatório | Verificar tarefas longas de JavaScript. |\n| Lighthouse: FID/INP | valor do relatório | Identificar atraso na resposta a interações. |\n| k6: tempo médio e p95 | valores do teste | Comparar cada patamar de usuários. |\n| k6: taxa de erros | percentual | Investigar códigos HTTP e falhas de conexão. |\n\nAs melhorias prioritárias seriam comprimir e dimensionar imagens, carregar imagens fora da tela sob demanda, dividir JavaScript em partes menores, remover dependências não usadas, aplicar cache para arquivos estáticos e evitar consultas redundantes. Métricas reais só devem ser preenchidas após executar Lighthouse e k6/JMeter contra uma aplicação acessível.", "conteudo": "### Plano de execução\n\n1. Executar a aplicação em ambiente de teste e registrar a versão avaliada.\n2. Gerar relatório Lighthouse pelo Chrome DevTools, analisando LCP, TTI e FID/INP, além de recomendações de carregamento e acessibilidade.\n3. Criar um cenário de carga no k6 ou JMeter que percorra página inicial, lista de produtos e detalhes.\n4. Executar lotes progressivos de 50, 100, 200, 300, 400 e 500 usuários simultâneos.\n5. Registrar tempo médio, percentil 95, taxa de erros, volume de requisições e uso de recursos do servidor.\n6. Comparar os resultados, localizar gargalos e repetir os testes depois das melhorias.\n\n### Exemplo de teste de carga com k6 - `carga.js`\n\n```js\nimport http from 'k6/http'\nimport { check, sleep } from 'k6'\n\nexport const options = {\n  stages: [\n    { duration: '1m', target: 50 },\n    { duration: '2m', target: 200 },\n    { duration: '2m', target: 500 },\n    { duration: '1m', target: 0 },\n  ],\n  thresholds: {\n    http_req_failed: ['rate<0.01'],\n    http_req_duration: ['p(95)<1000'],\n  },\n}\n\nconst baseUrl = __ENV.BASE_URL || 'http://localhost:3000'\n\nexport default function () {\n  for (const rota of ['/', '/produtos', '/produtos/1']) {\n    const resposta = http.get(`${baseUrl}${rota}`)\n    check(resposta, { 'status 200': (r) => r.status === 200 })\n    sleep(1)\n  }\n}\n```\n\nExecução:\n\n```bash\nk6 run -e BASE_URL=https://ambiente-de-teste.exemplo carga.js\n```\n\n### Modelo de relatório de performance\n\n| Item avaliado | Resultado a registrar | Análise |\n| --- | --- | --- |\n| Lighthouse: LCP | valor do relatório | Identificar imagem, fonte ou recurso que atrasou o maior conteúdo. |\n| Lighthouse: TTI | valor do relatório | Verificar tarefas longas de JavaScript. |\n| Lighthouse: FID/INP | valor do relatório | Identificar atraso na resposta a interações. |\n| k6: tempo médio e p95 | valores do teste | Comparar cada patamar de usuários. |\n| k6: taxa de erros | percentual | Investigar códigos HTTP e falhas de conexão. |\n\nAs melhorias prioritárias seriam comprimir e dimensionar imagens, carregar imagens fora da tela sob demanda, dividir JavaScript em partes menores, remover dependências não usadas, aplicar cache para arquivos estáticos e evitar consultas redundantes. Métricas reais só devem ser preenchidas após executar Lighthouse e k6/JMeter contra uma aplicação acessível.", "fonte": "gabarito/Front-End/3° bimestre/Semana 18/semana-18-respostas.md#atividade-pratica-aula-3-testes-de-performance-para-front-end", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
