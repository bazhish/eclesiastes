---
{"materia": "Front-End", "materiaSlug": "front-end", "bimestre": 3, "semana": 18, "ordem": 20, "aula": "a2", "titulo": "CI/CD e Testes Automatizados · Aula 2", "atividade": {"titulo": "Aula 2 - Testes automatizados para front-end", "enunciado": "Para o e-commerce da situação proposta, a solução é usar uma pirâmide de testes. Testes unitários com Jest verificam partes isoladas, como cálculo de total, descontos e formatação de preço. Testes de integração verificam a comunicação entre busca, carrinho e checkout. Por fim, alguns testes ponta a ponta com Cypress simulam o percurso do usuário: pesquisar produto, filtrar, adicionar ao carrinho e finalizar a compra.\n\nExemplo de teste unitário para reduzir o risco de total incorreto no checkout:\n\n```js\nimport { calcularTotal } from './carrinho'\n\ntest('calcula a soma dos itens do carrinho', () => {\n  const itens = [\n    { preco: 20, quantidade: 2 },\n    { preco: 15, quantidade: 1 },\n  ]\n  expect(calcularTotal(itens)).toBe(55)\n})\n```\n\nExemplo de teste ponta a ponta com Cypress:\n\n```js\ndescribe('checkout', () => {\n  it('atualiza o total ao adicionar um produto ao carrinho', () => {\n    cy.visit('/')\n    cy.contains('Notebook').click()\n    cy.get('[data-testid=\"adicionar-carrinho\"]').click()\n    cy.get('[data-testid=\"total-carrinho\"]').should('contain', 'R$')\n  })\n})\n```\n\nPara alinhar a equipe distribuída, eu definiria contratos entre os módulos, critérios de aceite e dados de teste comuns. Também manteria tarefas pequenas vinculadas a issues, revisão de código, integração frequente na branch principal e uma reunião breve para discutir bloqueios e mudanças que afetem outros módulos. O erro do total deve virar um caso de teste que continue sendo executado no pipeline para impedir que volte a ocorrer.", "resposta": "Para o e-commerce da situação proposta, a solução é usar uma pirâmide de testes. Testes unitários com Jest verificam partes isoladas, como cálculo de total, descontos e formatação de preço. Testes de integração verificam a comunicação entre busca, carrinho e checkout. Por fim, alguns testes ponta a ponta com Cypress simulam o percurso do usuário: pesquisar produto, filtrar, adicionar ao carrinho e finalizar a compra.\n\nExemplo de teste unitário para reduzir o risco de total incorreto no checkout:\n\n```js\nimport { calcularTotal } from './carrinho'\n\ntest('calcula a soma dos itens do carrinho', () => {\n  const itens = [\n    { preco: 20, quantidade: 2 },\n    { preco: 15, quantidade: 1 },\n  ]\n  expect(calcularTotal(itens)).toBe(55)\n})\n```\n\nExemplo de teste ponta a ponta com Cypress:\n\n```js\ndescribe('checkout', () => {\n  it('atualiza o total ao adicionar um produto ao carrinho', () => {\n    cy.visit('/')\n    cy.contains('Notebook').click()\n    cy.get('[data-testid=\"adicionar-carrinho\"]').click()\n    cy.get('[data-testid=\"total-carrinho\"]').should('contain', 'R$')\n  })\n})\n```\n\nPara alinhar a equipe distribuída, eu definiria contratos entre os módulos, critérios de aceite e dados de teste comuns. Também manteria tarefas pequenas vinculadas a issues, revisão de código, integração frequente na branch principal e uma reunião breve para discutir bloqueios e mudanças que afetem outros módulos. O erro do total deve virar um caso de teste que continue sendo executado no pipeline para impedir que volte a ocorrer.", "conteudo": "Para o e-commerce da situação proposta, a solução é usar uma pirâmide de testes. Testes unitários com Jest verificam partes isoladas, como cálculo de total, descontos e formatação de preço. Testes de integração verificam a comunicação entre busca, carrinho e checkout. Por fim, alguns testes ponta a ponta com Cypress simulam o percurso do usuário: pesquisar produto, filtrar, adicionar ao carrinho e finalizar a compra.\n\nExemplo de teste unitário para reduzir o risco de total incorreto no checkout:\n\n```js\nimport { calcularTotal } from './carrinho'\n\ntest('calcula a soma dos itens do carrinho', () => {\n  const itens = [\n    { preco: 20, quantidade: 2 },\n    { preco: 15, quantidade: 1 },\n  ]\n  expect(calcularTotal(itens)).toBe(55)\n})\n```\n\nExemplo de teste ponta a ponta com Cypress:\n\n```js\ndescribe('checkout', () => {\n  it('atualiza o total ao adicionar um produto ao carrinho', () => {\n    cy.visit('/')\n    cy.contains('Notebook').click()\n    cy.get('[data-testid=\"adicionar-carrinho\"]').click()\n    cy.get('[data-testid=\"total-carrinho\"]').should('contain', 'R$')\n  })\n})\n```\n\nPara alinhar a equipe distribuída, eu definiria contratos entre os módulos, critérios de aceite e dados de teste comuns. Também manteria tarefas pequenas vinculadas a issues, revisão de código, integração frequente na branch principal e uma reunião breve para discutir bloqueios e mudanças que afetem outros módulos. O erro do total deve virar um caso de teste que continue sendo executado no pipeline para impedir que volte a ocorrer.", "fonte": "gabarito/Front-End/3° bimestre/Semana 18/semana-18-respostas.md#aula-2-testes-automatizados-para-front-end", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
