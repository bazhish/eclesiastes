---
{"materia": "Front-End", "materiaSlug": "front-end", "bimestre": 3, "semana": 18, "ordem": 10, "aula": "a1", "titulo": "CI/CD e Testes Automatizados · Aula 1", "atividade": {"titulo": "Atividade Prática - Aula 1: Configurando e utilizando pipelines de CI/CD para aplicações front-end", "enunciado": "Configurei um workflow para executar em `push` e `pull_request` direcionados à branch `main`. O processo instala dependências de forma reprodutível, executa testes, gera o build e, somente após as verificações, publica a aplicação no GitHub Pages.\n\n### Preparação do repositório\n\n```bash\ngit init\ngit add .\ngit commit -m \"Configuração inicial do projeto\"\ngit branch -M main\ngit remote add origin <URL_DO_REPOSITORIO>\ngit push -u origin main\n```\n\n### Teste unitário de exemplo - `src/App.test.js`\n\n```js\nimport { render, screen } from '@testing-library/react'\nimport App from './App'\n\ntest('exibe o título principal da aplicação', () => {\n  render(<App />)\n  expect(screen.getByRole('heading', { name: /minha aplicação/i })).toBeInTheDocument()\n})\n```\n\n### Workflow - `.github/workflows/ci-cd.yml`\n\n```yaml\nname: CI/CD Front-End\n\non:\n  pull_request:\n    branches: [main]\n  push:\n    branches: [main]\n\npermissions:\n  contents: read\n  pages: write\n  id-token: write\n\njobs:\n  testes-e-build:\n    runs-on: ubuntu-latest\n    steps:\n      - name: Baixar código\n        uses: actions/checkout@v4\n\n      - name: Configurar Node.js\n        uses: actions/setup-node@v4\n        with:\n          node-version: 20\n          cache: npm\n\n      - name: Instalar dependências\n        run: npm ci\n\n      - name: Executar testes e cobertura\n        run: npm test -- --coverage --watchAll=false\n\n      - name: Gerar build\n        run: npm run build\n\n      - name: Enviar artefato do site\n        if: github.event_name == 'push' && github.ref == 'refs/heads/main'\n        uses: actions/upload-pages-artifact@v3\n        with:\n          path: build\n\n  deploy:\n    if: github.event_name == 'push' && github.ref == 'refs/heads/main'\n    needs: testes-e-build\n    runs-on: ubuntu-latest\n    environment:\n      name: github-pages\n      url: ${{ steps.deployment.outputs.page_url }}\n    steps:\n      - id: deployment\n        name: Publicar no GitHub Pages\n        uses: actions/deploy-pages@v4\n```\n\nUsei `npm ci` no pipeline porque ele respeita o arquivo de bloqueio de dependências e torna a instalação mais previsível. O job `deploy` depende de `testes-e-build`; portanto, não é executado quando um teste ou o build falha.\n\n**Por que é importante configurar etapas de teste automatizado em um pipeline de CI/CD, e como isso é realizado no GitHub Actions?**\n\nOs testes identificam regressões antes que o código seja integrado ou publicado. Isso reduz a chance de entregar uma funcionalidade quebrada e dá retorno rápido para a equipe. No GitHub Actions, a etapa `run: npm test -- --coverage --watchAll=false` executa os testes; se o comando retornar erro, o job falha e as etapas dependentes, como deploy, não são executadas.\n\n**Qual é a função do comando `gh-pages` no processo de deploy para o GitHub Pages, e como ele é configurado no projeto?**\n\nO pacote `gh-pages` publica o conteúdo gerado pelo build em uma branch destinada ao GitHub Pages, geralmente `gh-pages`. Uma alternativa ao workflow acima seria instalar `npm install --save-dev gh-pages` e adicionar ao `package.json` os scripts abaixo:\n\n```json\n{\n  \"homepage\": \"https://<usuario>.github.io/<repositorio>\",\n  \"scripts\": {\n    \"predeploy\": \"npm run build\",\n    \"deploy\": \"gh-pages -d build\"\n  }\n}\n```\n\nO script `predeploy` cria os arquivos estáticos e `deploy` envia a pasta `build` para a branch de publicação.\n\n**O que é o arquivo `ci-cd.yml` e como ele é estruturado para definir um pipeline de CI/CD?**\n\nÉ o arquivo YAML que descreve a automação do GitHub Actions. Ele informa os eventos que iniciam o pipeline em `on`, as permissões necessárias, os `jobs`, o ambiente em `runs-on` e as `steps` executadas em ordem. Neste caso, a sequência é checkout, instalação, testes, build, envio do artefato e deploy condicionado ao sucesso do job anterior.", "resposta": "Configurei um workflow para executar em `push` e `pull_request` direcionados à branch `main`. O processo instala dependências de forma reprodutível, executa testes, gera o build e, somente após as verificações, publica a aplicação no GitHub Pages.\n\n### Preparação do repositório\n\n```bash\ngit init\ngit add .\ngit commit -m \"Configuração inicial do projeto\"\ngit branch -M main\ngit remote add origin <URL_DO_REPOSITORIO>\ngit push -u origin main\n```\n\n### Teste unitário de exemplo - `src/App.test.js`\n\n```js\nimport { render, screen } from '@testing-library/react'\nimport App from './App'\n\ntest('exibe o título principal da aplicação', () => {\n  render(<App />)\n  expect(screen.getByRole('heading', { name: /minha aplicação/i })).toBeInTheDocument()\n})\n```\n\n### Workflow - `.github/workflows/ci-cd.yml`\n\n```yaml\nname: CI/CD Front-End\n\non:\n  pull_request:\n    branches: [main]\n  push:\n    branches: [main]\n\npermissions:\n  contents: read\n  pages: write\n  id-token: write\n\njobs:\n  testes-e-build:\n    runs-on: ubuntu-latest\n    steps:\n      - name: Baixar código\n        uses: actions/checkout@v4\n\n      - name: Configurar Node.js\n        uses: actions/setup-node@v4\n        with:\n          node-version: 20\n          cache: npm\n\n      - name: Instalar dependências\n        run: npm ci\n\n      - name: Executar testes e cobertura\n        run: npm test -- --coverage --watchAll=false\n\n      - name: Gerar build\n        run: npm run build\n\n      - name: Enviar artefato do site\n        if: github.event_name == 'push' && github.ref == 'refs/heads/main'\n        uses: actions/upload-pages-artifact@v3\n        with:\n          path: build\n\n  deploy:\n    if: github.event_name == 'push' && github.ref == 'refs/heads/main'\n    needs: testes-e-build\n    runs-on: ubuntu-latest\n    environment:\n      name: github-pages\n      url: ${{ steps.deployment.outputs.page_url }}\n    steps:\n      - id: deployment\n        name: Publicar no GitHub Pages\n        uses: actions/deploy-pages@v4\n```\n\nUsei `npm ci` no pipeline porque ele respeita o arquivo de bloqueio de dependências e torna a instalação mais previsível. O job `deploy` depende de `testes-e-build`; portanto, não é executado quando um teste ou o build falha.\n\n**Por que é importante configurar etapas de teste automatizado em um pipeline de CI/CD, e como isso é realizado no GitHub Actions?**\n\nOs testes identificam regressões antes que o código seja integrado ou publicado. Isso reduz a chance de entregar uma funcionalidade quebrada e dá retorno rápido para a equipe. No GitHub Actions, a etapa `run: npm test -- --coverage --watchAll=false` executa os testes; se o comando retornar erro, o job falha e as etapas dependentes, como deploy, não são executadas.\n\n**Qual é a função do comando `gh-pages` no processo de deploy para o GitHub Pages, e como ele é configurado no projeto?**\n\nO pacote `gh-pages` publica o conteúdo gerado pelo build em uma branch destinada ao GitHub Pages, geralmente `gh-pages`. Uma alternativa ao workflow acima seria instalar `npm install --save-dev gh-pages` e adicionar ao `package.json` os scripts abaixo:\n\n```json\n{\n  \"homepage\": \"https://<usuario>.github.io/<repositorio>\",\n  \"scripts\": {\n    \"predeploy\": \"npm run build\",\n    \"deploy\": \"gh-pages -d build\"\n  }\n}\n```\n\nO script `predeploy` cria os arquivos estáticos e `deploy` envia a pasta `build` para a branch de publicação.\n\n**O que é o arquivo `ci-cd.yml` e como ele é estruturado para definir um pipeline de CI/CD?**\n\nÉ o arquivo YAML que descreve a automação do GitHub Actions. Ele informa os eventos que iniciam o pipeline em `on`, as permissões necessárias, os `jobs`, o ambiente em `runs-on` e as `steps` executadas em ordem. Neste caso, a sequência é checkout, instalação, testes, build, envio do artefato e deploy condicionado ao sucesso do job anterior.", "conteudo": "Configurei um workflow para executar em `push` e `pull_request` direcionados à branch `main`. O processo instala dependências de forma reprodutível, executa testes, gera o build e, somente após as verificações, publica a aplicação no GitHub Pages.\n\n### Preparação do repositório\n\n```bash\ngit init\ngit add .\ngit commit -m \"Configuração inicial do projeto\"\ngit branch -M main\ngit remote add origin <URL_DO_REPOSITORIO>\ngit push -u origin main\n```\n\n### Teste unitário de exemplo - `src/App.test.js`\n\n```js\nimport { render, screen } from '@testing-library/react'\nimport App from './App'\n\ntest('exibe o título principal da aplicação', () => {\n  render(<App />)\n  expect(screen.getByRole('heading', { name: /minha aplicação/i })).toBeInTheDocument()\n})\n```\n\n### Workflow - `.github/workflows/ci-cd.yml`\n\n```yaml\nname: CI/CD Front-End\n\non:\n  pull_request:\n    branches: [main]\n  push:\n    branches: [main]\n\npermissions:\n  contents: read\n  pages: write\n  id-token: write\n\njobs:\n  testes-e-build:\n    runs-on: ubuntu-latest\n    steps:\n      - name: Baixar código\n        uses: actions/checkout@v4\n\n      - name: Configurar Node.js\n        uses: actions/setup-node@v4\n        with:\n          node-version: 20\n          cache: npm\n\n      - name: Instalar dependências\n        run: npm ci\n\n      - name: Executar testes e cobertura\n        run: npm test -- --coverage --watchAll=false\n\n      - name: Gerar build\n        run: npm run build\n\n      - name: Enviar artefato do site\n        if: github.event_name == 'push' && github.ref == 'refs/heads/main'\n        uses: actions/upload-pages-artifact@v3\n        with:\n          path: build\n\n  deploy:\n    if: github.event_name == 'push' && github.ref == 'refs/heads/main'\n    needs: testes-e-build\n    runs-on: ubuntu-latest\n    environment:\n      name: github-pages\n      url: ${{ steps.deployment.outputs.page_url }}\n    steps:\n      - id: deployment\n        name: Publicar no GitHub Pages\n        uses: actions/deploy-pages@v4\n```\n\nUsei `npm ci` no pipeline porque ele respeita o arquivo de bloqueio de dependências e torna a instalação mais previsível. O job `deploy` depende de `testes-e-build`; portanto, não é executado quando um teste ou o build falha.\n\n**Por que é importante configurar etapas de teste automatizado em um pipeline de CI/CD, e como isso é realizado no GitHub Actions?**\n\nOs testes identificam regressões antes que o código seja integrado ou publicado. Isso reduz a chance de entregar uma funcionalidade quebrada e dá retorno rápido para a equipe. No GitHub Actions, a etapa `run: npm test -- --coverage --watchAll=false` executa os testes; se o comando retornar erro, o job falha e as etapas dependentes, como deploy, não são executadas.\n\n**Qual é a função do comando `gh-pages` no processo de deploy para o GitHub Pages, e como ele é configurado no projeto?**\n\nO pacote `gh-pages` publica o conteúdo gerado pelo build em uma branch destinada ao GitHub Pages, geralmente `gh-pages`. Uma alternativa ao workflow acima seria instalar `npm install --save-dev gh-pages` e adicionar ao `package.json` os scripts abaixo:\n\n```json\n{\n  \"homepage\": \"https://<usuario>.github.io/<repositorio>\",\n  \"scripts\": {\n    \"predeploy\": \"npm run build\",\n    \"deploy\": \"gh-pages -d build\"\n  }\n}\n```\n\nO script `predeploy` cria os arquivos estáticos e `deploy` envia a pasta `build` para a branch de publicação.\n\n**O que é o arquivo `ci-cd.yml` e como ele é estruturado para definir um pipeline de CI/CD?**\n\nÉ o arquivo YAML que descreve a automação do GitHub Actions. Ele informa os eventos que iniciam o pipeline em `on`, as permissões necessárias, os `jobs`, o ambiente em `runs-on` e as `steps` executadas em ordem. Neste caso, a sequência é checkout, instalação, testes, build, envio do artefato e deploy condicionado ao sucesso do job anterior.", "fonte": "gabarito/Front-End/3° bimestre/Semana 18/semana-18-respostas.md#atividade-pratica-aula-1-configurando-e-utilizando-pipelines-de-ci-cd-para-aplicacoes-front-end", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
