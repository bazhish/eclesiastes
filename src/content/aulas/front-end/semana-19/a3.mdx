---
{"materia": "Front-End", "materiaSlug": "front-end", "bimestre": 3, "semana": 19, "ordem": 3, "aula": "a3", "titulo": "Gerenciamento de estado com Redux - actions, reducers, thunks e sagas", "atividade": {"tipo": "pratica", "enunciado": "Roteiro de Atividade Prática\n\nNome: Turma: .\n\nAtividade: Gerenciamento de estado com Redux - actions, reducers, thunks e sagas\n\nVocês estão desenvolvendo uma aplicação de lista de tarefas (to-do list) com Redux para gerenciar o estado global da aplicação. Nesta atividade, vocês vão implementar funcionalidades básicas de adicionar, remover e carregar tarefas usando actions, reducers e thunks para realizar operações assíncronas e sagas para lidar com efeitos colaterais complexos.\n\nSituação fictícia produzida pela SEDUC-SP.\n\nEspecificações\n\nConfiguração inicial do Redux\nCriem uma aplicação com create-react-app e instalem o Redux, @reduxjs/toolkit, react-redux, redux-thunk e redux-saga.\nConfigurem o Redux Store na aplicação, incluindo o middleware thunk e saga.\n\nCriar actions e reducers básicos\nNo Redux, criem um slice chamado todosSlice, com o estado inicial da lista de tarefas.\nImplementem as seguintes actions:\naddTodo – Adiciona uma nova tarefa, com título e status.\nremoveTodo – Remove uma tarefa da lista pelo ID.\nNo reducer, escrevam a lógica para modificar o estado da lista de acordo com essas ações.\n\nUsar thunks para operações assíncronas\nImplementem uma ação assíncrona usando uma thunk chamada fetchTodos que simula a carga de tarefas de uma API externa.\nNa action, façam um fetch simulado com um setTimeout para carregar uma lista de tarefas; após o carregamento, atualizem o estado no reducer.\nExibam um “Carregando...” enquanto as tarefas são carregadas.\n\nImplementar sagas para efeitos colaterais\nCriem uma saga chamada watchAddTodo; sempre que uma nova tarefa for adicionada, registrem um log no console dizendo que a tarefa foi adicionada com sucesso.\nConfigurem o saga middleware no store para ouvir a action addTodo e disparar watchAddTodo.\n\nInterface da aplicação\nNo componente principal, criem um formulário para adicionar uma tarefa com título.\nListem as tarefas no estado, exibindo título e status.\nAdicionem um botão \"Remover\" ao lado de cada tarefa para removê-la da lista.\nAdicionem um botão \"Carregar Tarefas\" para simular a carga de dados iniciais usando fetchTodos.\n\nRealizem os seguintes comandos\n\nExecutem a aplicação usando os comandos necessários para criar a aplicação React\nnpx create-react-app redux-todo-app\ncd redux-todo-app\npara depois instalar o Redux e suas dependências com\nnpm install @reduxjs/toolkit react-redux redux-thunk@2.3.0 redux-saga\n\nConfiram a seguir como vamos organizar a estrutura na pasta src:\n\nsrc/\n├── store.js\n├── todosSlice.js\n├── TodoList.js\n└── App.js\n\nCódigo de exemplo\nEm primeiro lugar, vamos configurar o store com o thunk usando reduxjs/toolkit para simplificar a criação do store.\n// src/store.js\nimport { configureStore } from '@reduxjs/toolkit';\nimport todosReducer from './todosSlice';\n\nconst store = configureStore({\nreducer: {\ntodos: todosReducer,\n},\n});\n\nexport default store;\n\nCriando o slice para tarefas (todos)\nAqui vamos definir o reducer, a action para adicionar uma tarefa e o thunk para simular uma ação assíncrona.\n// src/todosSlice.js\nimport { createSlice } from '@reduxjs/toolkit';\n\nconst todosSlice = createSlice({\nname: 'todos',\ninitialState: {\nlist: [],\n},\nreducers: {\naddTodo: (state, action) => {\nstate.list.push({ id: Date.now(), title: action.payload });\n},\n},\n});\n\nexport const { addTodo } = todosSlice.actions;\nexport default todosSlice.reducer;\n\nCriando o componente principal\nEste é o componente em que os usuários podem visualizar a lista de tarefas e adicionar novas tarefas. Vamos usar useDispatch e useSelector para interagir com o Redux.\n\n// src/TodoList.js\nimport React, { useState } from 'react';\nimport { useDispatch, useSelector } from 'react-redux';\nimport { addTodo } from './todosSlice';\n\nconst TodoList = () => {\nconst [input, setInput] = useState('');\nconst dispatch = useDispatch();\nconst todos = useSelector((state) => state.todos.list);\n\nconst handleAddTodo = () => {\nif (input.trim()) {\ndispatch(addTodo(input));\nsetInput(''); // Limpa o campo de entrada\n}\n};\n\nreturn (\n<div>\n<h1>Lista de Tarefas</h1>\n<input\ntype=\"text\"\nvalue={input}\nonChange={(e) => setInput(e.target.value)}\nplaceholder=\"Digite uma nova tarefa\"\n/>\n<button onClick={handleAddTodo}>Adicionar Tarefa</button>\n<ul>\n{todos.map((todo) => (\n<li key={todo.id}>{todo.title}</li>\n))}\n</ul>\n</div>\n);\n};\n\nexport default TodoList;\n\nIntegrando tudo no componente principal (app)\nPor último, vamos configurar o App.js para que ele forneça o store da\naplicação para todos os componentes com o Provider do Redux.\n// src/App.js\nimport React from 'react';\nimport { Provider } from 'react-redux';\nimport store from './store';\nimport TodoList from './TodoList';\n\nconst App = () => (\n<Provider store={store}>\n<TodoList />\n</Provider>\n);\n\nexport default App;\n\nExecutem a aplicação\nVocês verão a aplicação rodando. Agora, adicionem a funcionalidade de remover uma tarefa.\n\nLembrem-se de registrar cópias da tela do site funcionando com o novo recurso e enviá-las pela plataforma (AVA).", "resposta": "O roteiro não traz uma resposta escrita pronta. A entrega solicitada é prática: siga o procedimento original e registre a sua execução.", "fonte": "Material/Front-End/3° bimestre/Semana 19 - Tecnicas Avancadas de CSS e Mobile-First/Atividade Pratica - Aula 3- Gerenciamento de estado com Redux - 47430.docx"}, "quizzes": []}
---
