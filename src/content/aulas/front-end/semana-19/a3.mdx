---
{"materia": "Front-End", "materiaSlug": "front-end", "bimestre": 3, "semana": 19, "ordem": 30, "aula": "a3", "titulo": "Técnicas Avançadas de CSS e Mobile-First · Aula 3", "atividade": {"titulo": "Atividade Prática - Aula 3: Gerenciamento de estado com Redux", "enunciado": "### Instalação\n\n```bash\nnpx create-react-app redux-todo-app\ncd redux-todo-app\nnpm install @reduxjs/toolkit react-redux redux-thunk@2.3.0 redux-saga\nnpm start\n```\n\n### `src/todosSlice.js`\n\n```js\nimport { createAsyncThunk, createSlice } from '@reduxjs/toolkit'\n\nexport const fetchTodos = createAsyncThunk('todos/fetchTodos', async () => {\n  await new Promise((resolve) => setTimeout(resolve, 1000))\n  return [\n    { id: '1', titulo: 'Estudar Redux', concluida: false },\n    { id: '2', titulo: 'Criar testes', concluida: true },\n  ]\n})\n\nconst todosSlice = createSlice({\n  name: 'todos',\n  initialState: { lista: [], carregando: false },\n  reducers: {\n    addTodo: (state, action) => { state.lista.push({ id: crypto.randomUUID(), titulo: action.payload, concluida: false }) },\n    removeTodo: (state, action) => { state.lista = state.lista.filter((todo) => todo.id !== action.payload) },\n  },\n  extraReducers: (builder) => builder\n    .addCase(fetchTodos.pending, (state) => { state.carregando = true })\n    .addCase(fetchTodos.fulfilled, (state, action) => { state.carregando = false; state.lista = action.payload })\n    .addCase(fetchTodos.rejected, (state) => { state.carregando = false }),\n})\n\nexport const { addTodo, removeTodo } = todosSlice.actions\nexport default todosSlice.reducer\n```\n\n### `src/store.js`\n\n```js\nimport { configureStore } from '@reduxjs/toolkit'\nimport createSagaMiddleware from 'redux-saga'\nimport { takeEvery } from 'redux-saga/effects'\nimport todosReducer, { addTodo } from './todosSlice'\n\nfunction* registrarAdicao(action) { console.log(`Tarefa adicionada com sucesso: ${action.payload}`) }\nfunction* watchAddTodo() { yield takeEvery(addTodo.type, registrarAdicao) }\n\nconst sagaMiddleware = createSagaMiddleware()\nconst store = configureStore({\n  reducer: { todos: todosReducer },\n  middleware: (getDefaultMiddleware) => getDefaultMiddleware({ thunk: true }).concat(sagaMiddleware),\n})\nsagaMiddleware.run(watchAddTodo)\nexport default store\n```\n\n### `src/TodoList.js`\n\n```jsx\nimport { useState } from 'react'\nimport { useDispatch, useSelector } from 'react-redux'\nimport { addTodo, fetchTodos, removeTodo } from './todosSlice'\n\nexport default function TodoList() {\n  const [titulo, setTitulo] = useState('')\n  const { lista, carregando } = useSelector((state) => state.todos)\n  const dispatch = useDispatch()\n  const adicionar = () => {\n    if (!titulo.trim()) return\n    dispatch(addTodo(titulo.trim()))\n    setTitulo('')\n  }\n  return <main>\n    <h1>Lista de tarefas</h1>\n    <input value={titulo} onChange={(e) => setTitulo(e.target.value)} placeholder=\"Digite uma tarefa\" />\n    <button onClick={adicionar}>Adicionar tarefa</button>\n    <button onClick={() => dispatch(fetchTodos())} disabled={carregando}>Carregar tarefas</button>\n    {carregando && <p>Carregando...</p>}\n    <ul>{lista.map((todo) => <li key={todo.id}>{todo.titulo} - {todo.concluida ? 'concluída' : 'pendente'} <button onClick={() => dispatch(removeTodo(todo.id))}>Remover</button></li>)}</ul>\n  </main>\n}\n```\n\n### `src/App.js`\n\n```jsx\nimport { Provider } from 'react-redux'\nimport store from './store'\nimport TodoList from './TodoList'\n\nexport default function App() {\n  return <Provider store={store}><TodoList /></Provider>\n}\n```\n\n### Situação: projeto de e-commerce com prazo e membro com dificuldade\n\nEu começaria por uma conversa respeitosa e objetiva para entender quais entregas estão bloqueadas e quais informações podem ser compartilhadas sem expor a vida pessoal do colega. Em seguida, a equipe dividiria as tarefas restantes em partes pequenas, identificaria itens críticos para o lançamento e redistribuiria somente o necessário conforme as competências de cada pessoa. O planejamento seria atualizado com responsáveis, prazo curto e critérios de aceite.\n\nPara preservar a qualidade, eu manteria revisões de código, testes automatizados e integração frequente. O Redux ajuda a organizar o estado global de funcionalidades como carrinho e checkout em um fluxo previsível; com ações, reducers e testes, outra pessoa consegue continuar uma tarefa com menos risco. Se o escopo ainda não coubesse no prazo, a equipe apresentaria ao gestor uma proposta de entrega mínima viável, com as funções essenciais primeiro e melhorias planejadas para uma etapa posterior.", "resposta": "### Instalação\n\n```bash\nnpx create-react-app redux-todo-app\ncd redux-todo-app\nnpm install @reduxjs/toolkit react-redux redux-thunk@2.3.0 redux-saga\nnpm start\n```\n\n### `src/todosSlice.js`\n\n```js\nimport { createAsyncThunk, createSlice } from '@reduxjs/toolkit'\n\nexport const fetchTodos = createAsyncThunk('todos/fetchTodos', async () => {\n  await new Promise((resolve) => setTimeout(resolve, 1000))\n  return [\n    { id: '1', titulo: 'Estudar Redux', concluida: false },\n    { id: '2', titulo: 'Criar testes', concluida: true },\n  ]\n})\n\nconst todosSlice = createSlice({\n  name: 'todos',\n  initialState: { lista: [], carregando: false },\n  reducers: {\n    addTodo: (state, action) => { state.lista.push({ id: crypto.randomUUID(), titulo: action.payload, concluida: false }) },\n    removeTodo: (state, action) => { state.lista = state.lista.filter((todo) => todo.id !== action.payload) },\n  },\n  extraReducers: (builder) => builder\n    .addCase(fetchTodos.pending, (state) => { state.carregando = true })\n    .addCase(fetchTodos.fulfilled, (state, action) => { state.carregando = false; state.lista = action.payload })\n    .addCase(fetchTodos.rejected, (state) => { state.carregando = false }),\n})\n\nexport const { addTodo, removeTodo } = todosSlice.actions\nexport default todosSlice.reducer\n```\n\n### `src/store.js`\n\n```js\nimport { configureStore } from '@reduxjs/toolkit'\nimport createSagaMiddleware from 'redux-saga'\nimport { takeEvery } from 'redux-saga/effects'\nimport todosReducer, { addTodo } from './todosSlice'\n\nfunction* registrarAdicao(action) { console.log(`Tarefa adicionada com sucesso: ${action.payload}`) }\nfunction* watchAddTodo() { yield takeEvery(addTodo.type, registrarAdicao) }\n\nconst sagaMiddleware = createSagaMiddleware()\nconst store = configureStore({\n  reducer: { todos: todosReducer },\n  middleware: (getDefaultMiddleware) => getDefaultMiddleware({ thunk: true }).concat(sagaMiddleware),\n})\nsagaMiddleware.run(watchAddTodo)\nexport default store\n```\n\n### `src/TodoList.js`\n\n```jsx\nimport { useState } from 'react'\nimport { useDispatch, useSelector } from 'react-redux'\nimport { addTodo, fetchTodos, removeTodo } from './todosSlice'\n\nexport default function TodoList() {\n  const [titulo, setTitulo] = useState('')\n  const { lista, carregando } = useSelector((state) => state.todos)\n  const dispatch = useDispatch()\n  const adicionar = () => {\n    if (!titulo.trim()) return\n    dispatch(addTodo(titulo.trim()))\n    setTitulo('')\n  }\n  return <main>\n    <h1>Lista de tarefas</h1>\n    <input value={titulo} onChange={(e) => setTitulo(e.target.value)} placeholder=\"Digite uma tarefa\" />\n    <button onClick={adicionar}>Adicionar tarefa</button>\n    <button onClick={() => dispatch(fetchTodos())} disabled={carregando}>Carregar tarefas</button>\n    {carregando && <p>Carregando...</p>}\n    <ul>{lista.map((todo) => <li key={todo.id}>{todo.titulo} - {todo.concluida ? 'concluída' : 'pendente'} <button onClick={() => dispatch(removeTodo(todo.id))}>Remover</button></li>)}</ul>\n  </main>\n}\n```\n\n### `src/App.js`\n\n```jsx\nimport { Provider } from 'react-redux'\nimport store from './store'\nimport TodoList from './TodoList'\n\nexport default function App() {\n  return <Provider store={store}><TodoList /></Provider>\n}\n```\n\n### Situação: projeto de e-commerce com prazo e membro com dificuldade\n\nEu começaria por uma conversa respeitosa e objetiva para entender quais entregas estão bloqueadas e quais informações podem ser compartilhadas sem expor a vida pessoal do colega. Em seguida, a equipe dividiria as tarefas restantes em partes pequenas, identificaria itens críticos para o lançamento e redistribuiria somente o necessário conforme as competências de cada pessoa. O planejamento seria atualizado com responsáveis, prazo curto e critérios de aceite.\n\nPara preservar a qualidade, eu manteria revisões de código, testes automatizados e integração frequente. O Redux ajuda a organizar o estado global de funcionalidades como carrinho e checkout em um fluxo previsível; com ações, reducers e testes, outra pessoa consegue continuar uma tarefa com menos risco. Se o escopo ainda não coubesse no prazo, a equipe apresentaria ao gestor uma proposta de entrega mínima viável, com as funções essenciais primeiro e melhorias planejadas para uma etapa posterior.", "conteudo": "### Instalação\n\n```bash\nnpx create-react-app redux-todo-app\ncd redux-todo-app\nnpm install @reduxjs/toolkit react-redux redux-thunk@2.3.0 redux-saga\nnpm start\n```\n\n### `src/todosSlice.js`\n\n```js\nimport { createAsyncThunk, createSlice } from '@reduxjs/toolkit'\n\nexport const fetchTodos = createAsyncThunk('todos/fetchTodos', async () => {\n  await new Promise((resolve) => setTimeout(resolve, 1000))\n  return [\n    { id: '1', titulo: 'Estudar Redux', concluida: false },\n    { id: '2', titulo: 'Criar testes', concluida: true },\n  ]\n})\n\nconst todosSlice = createSlice({\n  name: 'todos',\n  initialState: { lista: [], carregando: false },\n  reducers: {\n    addTodo: (state, action) => { state.lista.push({ id: crypto.randomUUID(), titulo: action.payload, concluida: false }) },\n    removeTodo: (state, action) => { state.lista = state.lista.filter((todo) => todo.id !== action.payload) },\n  },\n  extraReducers: (builder) => builder\n    .addCase(fetchTodos.pending, (state) => { state.carregando = true })\n    .addCase(fetchTodos.fulfilled, (state, action) => { state.carregando = false; state.lista = action.payload })\n    .addCase(fetchTodos.rejected, (state) => { state.carregando = false }),\n})\n\nexport const { addTodo, removeTodo } = todosSlice.actions\nexport default todosSlice.reducer\n```\n\n### `src/store.js`\n\n```js\nimport { configureStore } from '@reduxjs/toolkit'\nimport createSagaMiddleware from 'redux-saga'\nimport { takeEvery } from 'redux-saga/effects'\nimport todosReducer, { addTodo } from './todosSlice'\n\nfunction* registrarAdicao(action) { console.log(`Tarefa adicionada com sucesso: ${action.payload}`) }\nfunction* watchAddTodo() { yield takeEvery(addTodo.type, registrarAdicao) }\n\nconst sagaMiddleware = createSagaMiddleware()\nconst store = configureStore({\n  reducer: { todos: todosReducer },\n  middleware: (getDefaultMiddleware) => getDefaultMiddleware({ thunk: true }).concat(sagaMiddleware),\n})\nsagaMiddleware.run(watchAddTodo)\nexport default store\n```\n\n### `src/TodoList.js`\n\n```jsx\nimport { useState } from 'react'\nimport { useDispatch, useSelector } from 'react-redux'\nimport { addTodo, fetchTodos, removeTodo } from './todosSlice'\n\nexport default function TodoList() {\n  const [titulo, setTitulo] = useState('')\n  const { lista, carregando } = useSelector((state) => state.todos)\n  const dispatch = useDispatch()\n  const adicionar = () => {\n    if (!titulo.trim()) return\n    dispatch(addTodo(titulo.trim()))\n    setTitulo('')\n  }\n  return <main>\n    <h1>Lista de tarefas</h1>\n    <input value={titulo} onChange={(e) => setTitulo(e.target.value)} placeholder=\"Digite uma tarefa\" />\n    <button onClick={adicionar}>Adicionar tarefa</button>\n    <button onClick={() => dispatch(fetchTodos())} disabled={carregando}>Carregar tarefas</button>\n    {carregando && <p>Carregando...</p>}\n    <ul>{lista.map((todo) => <li key={todo.id}>{todo.titulo} - {todo.concluida ? 'concluída' : 'pendente'} <button onClick={() => dispatch(removeTodo(todo.id))}>Remover</button></li>)}</ul>\n  </main>\n}\n```\n\n### `src/App.js`\n\n```jsx\nimport { Provider } from 'react-redux'\nimport store from './store'\nimport TodoList from './TodoList'\n\nexport default function App() {\n  return <Provider store={store}><TodoList /></Provider>\n}\n```\n\n### Situação: projeto de e-commerce com prazo e membro com dificuldade\n\nEu começaria por uma conversa respeitosa e objetiva para entender quais entregas estão bloqueadas e quais informações podem ser compartilhadas sem expor a vida pessoal do colega. Em seguida, a equipe dividiria as tarefas restantes em partes pequenas, identificaria itens críticos para o lançamento e redistribuiria somente o necessário conforme as competências de cada pessoa. O planejamento seria atualizado com responsáveis, prazo curto e critérios de aceite.\n\nPara preservar a qualidade, eu manteria revisões de código, testes automatizados e integração frequente. O Redux ajuda a organizar o estado global de funcionalidades como carrinho e checkout em um fluxo previsível; com ações, reducers e testes, outra pessoa consegue continuar uma tarefa com menos risco. Se o escopo ainda não coubesse no prazo, a equipe apresentaria ao gestor uma proposta de entrega mínima viável, com as funções essenciais primeiro e melhorias planejadas para uma etapa posterior.", "fonte": "gabarito/Front-End/3° bimestre/Semana 19/semana-19-respostas.md#atividade-pratica-aula-3-gerenciamento-de-estado-com-redux", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
