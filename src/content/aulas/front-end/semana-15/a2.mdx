---
{"materia": "Front-End", "materiaSlug": "front-end", "bimestre": 3, "semana": 15, "ordem": 20, "aula": "a2", "titulo": "Estilização e Performance de Páginas Web · Aula 2", "atividade": {"titulo": "Atividade Prática - Aula 2: Ferramentas de desenvolvimento front-end", "enunciado": "### Configuração inicial\n\n```bash\nnpm init -y\nnpm install --save-dev prettier eslint @babel/core @babel/cli @babel/preset-env eslint-config-prettier\n```\n\n### `.prettierrc`\n\n```json\n{\n  \"semi\": false,\n  \"singleQuote\": true,\n  \"trailingComma\": \"es5\"\n}\n```\n\n### `.eslintrc.json`\n\n```json\n{\n  \"env\": { \"browser\": true, \"es2021\": true },\n  \"extends\": [\"eslint:recommended\", \"prettier\"],\n  \"parserOptions\": { \"ecmaVersion\": \"latest\", \"sourceType\": \"module\" },\n  \"rules\": {\n    \"quotes\": [\"error\", \"single\"],\n    \"semi\": [\"error\", \"never\"]\n  }\n}\n```\n\n### `babel.config.json`\n\n```json\n{\n  \"presets\": [\"@babel/preset-env\"]\n}\n```\n\n### `src/index.js`\n\n```js\nconst saudacao = (nome) => {\n  console.log(`Olá, ${nome}!`)\n}\n\nsaudacao('Desenvolvedor')\n```\n\n### Scripts do `package.json`\n\n```json\n{\n  \"scripts\": {\n    \"format\": \"prettier --write .\",\n    \"lint\": \"eslint src\",\n    \"build\": \"babel src --out-dir dist\"\n  }\n}\n```\n\n### Execução\n\n```bash\nnpm run format\nnpm run lint\nnpm run build\n```\n\nO Prettier padroniza a formatação automaticamente, o ESLint identifica padrões problemáticos e o Babel transpila JavaScript moderno para uma versão compatível com os navegadores definidos pela configuração. Essas ferramentas tornam o código mais consistente e reduzem erros antes da publicação.", "resposta": "### Configuração inicial\n\n```bash\nnpm init -y\nnpm install --save-dev prettier eslint @babel/core @babel/cli @babel/preset-env eslint-config-prettier\n```\n\n### `.prettierrc`\n\n```json\n{\n  \"semi\": false,\n  \"singleQuote\": true,\n  \"trailingComma\": \"es5\"\n}\n```\n\n### `.eslintrc.json`\n\n```json\n{\n  \"env\": { \"browser\": true, \"es2021\": true },\n  \"extends\": [\"eslint:recommended\", \"prettier\"],\n  \"parserOptions\": { \"ecmaVersion\": \"latest\", \"sourceType\": \"module\" },\n  \"rules\": {\n    \"quotes\": [\"error\", \"single\"],\n    \"semi\": [\"error\", \"never\"]\n  }\n}\n```\n\n### `babel.config.json`\n\n```json\n{\n  \"presets\": [\"@babel/preset-env\"]\n}\n```\n\n### `src/index.js`\n\n```js\nconst saudacao = (nome) => {\n  console.log(`Olá, ${nome}!`)\n}\n\nsaudacao('Desenvolvedor')\n```\n\n### Scripts do `package.json`\n\n```json\n{\n  \"scripts\": {\n    \"format\": \"prettier --write .\",\n    \"lint\": \"eslint src\",\n    \"build\": \"babel src --out-dir dist\"\n  }\n}\n```\n\n### Execução\n\n```bash\nnpm run format\nnpm run lint\nnpm run build\n```\n\nO Prettier padroniza a formatação automaticamente, o ESLint identifica padrões problemáticos e o Babel transpila JavaScript moderno para uma versão compatível com os navegadores definidos pela configuração. Essas ferramentas tornam o código mais consistente e reduzem erros antes da publicação.", "conteudo": "### Configuração inicial\n\n```bash\nnpm init -y\nnpm install --save-dev prettier eslint @babel/core @babel/cli @babel/preset-env eslint-config-prettier\n```\n\n### `.prettierrc`\n\n```json\n{\n  \"semi\": false,\n  \"singleQuote\": true,\n  \"trailingComma\": \"es5\"\n}\n```\n\n### `.eslintrc.json`\n\n```json\n{\n  \"env\": { \"browser\": true, \"es2021\": true },\n  \"extends\": [\"eslint:recommended\", \"prettier\"],\n  \"parserOptions\": { \"ecmaVersion\": \"latest\", \"sourceType\": \"module\" },\n  \"rules\": {\n    \"quotes\": [\"error\", \"single\"],\n    \"semi\": [\"error\", \"never\"]\n  }\n}\n```\n\n### `babel.config.json`\n\n```json\n{\n  \"presets\": [\"@babel/preset-env\"]\n}\n```\n\n### `src/index.js`\n\n```js\nconst saudacao = (nome) => {\n  console.log(`Olá, ${nome}!`)\n}\n\nsaudacao('Desenvolvedor')\n```\n\n### Scripts do `package.json`\n\n```json\n{\n  \"scripts\": {\n    \"format\": \"prettier --write .\",\n    \"lint\": \"eslint src\",\n    \"build\": \"babel src --out-dir dist\"\n  }\n}\n```\n\n### Execução\n\n```bash\nnpm run format\nnpm run lint\nnpm run build\n```\n\nO Prettier padroniza a formatação automaticamente, o ESLint identifica padrões problemáticos e o Babel transpila JavaScript moderno para uma versão compatível com os navegadores definidos pela configuração. Essas ferramentas tornam o código mais consistente e reduzem erros antes da publicação.", "fonte": "gabarito/Front-End/3° bimestre/Semana 15/semana-15-respostas.md#atividade-pratica-aula-2-ferramentas-de-desenvolvimento-front-end", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
