---
{"materia": "Front-End", "materiaSlug": "front-end", "bimestre": 3, "semana": 15, "ordem": 30, "aula": "a3", "titulo": "Estilização e Performance de Páginas Web · Aula 3", "atividade": {"titulo": "Atividade Prática - Aula 3: Performance de aplicações web", "enunciado": "### Carregamento tardio de imagens\n\n```html\n<img src=\"banner-principal.jpg\" alt=\"Banner principal da página\" width=\"1200\" height=\"500\" fetchpriority=\"high\" />\n\n<img src=\"produto-1.jpg\" alt=\"Produto em destaque\" width=\"400\" height=\"400\" loading=\"lazy\" decoding=\"async\" />\n<img src=\"produto-2.jpg\" alt=\"Segundo produto em destaque\" width=\"400\" height=\"400\" loading=\"lazy\" decoding=\"async\" />\n```\n\nA imagem principal não usa `loading=\"lazy\"`, pois ela faz parte do conteúdo inicial. As imagens abaixo da primeira tela usam carregamento tardio. Os atributos `width` e `height` ajudam a evitar mudanças bruscas de layout durante o carregamento.\n\n### Minificação de CSS com PostCSS e cssnano\n\n```bash\nnpm install --save-dev postcss postcss-cli cssnano\n```\n\n### `postcss.config.js`\n\n```js\nmodule.exports = {\n  plugins: [require('cssnano')({ preset: 'default' })],\n}\n```\n\n### Script de build de CSS\n\n```json\n{\n  \"scripts\": {\n    \"build:css\": \"postcss estilos.css -o estilos.min.css\"\n  }\n}\n```\n\n```bash\nnpx postcss estilos.css -o estilos.min.css\n```\n\nNo HTML de produção, a referência passa a ser:\n\n```html\n<link rel=\"stylesheet\" href=\"estilos.min.css\" />\n```\n\n### Verificação de performance\n\n1. Abrir a página no Chrome e gerar um relatório na aba Lighthouse.\n2. Registrar os dados de performance antes e depois da minificação e do carregamento tardio.\n3. Analisar principalmente LCP, recursos bloqueadores, peso total transferido e recomendações sobre imagens.\n4. Testar o carregamento em uma janela anônima e em modo de rede limitada para observar o comportamento sem cache.\n\nO carregamento tardio reduz a transferência inicial de imagens que o usuário ainda não precisa visualizar. A minificação remove espaços, comentários e estruturas desnecessárias do CSS, diminuindo o arquivo transferido. As duas práticas devem ser validadas pelo relatório e pela experiência real de navegação, evitando aplicar otimizações que prejudiquem a imagem principal ou a acessibilidade.", "resposta": "### Carregamento tardio de imagens\n\n```html\n<img src=\"banner-principal.jpg\" alt=\"Banner principal da página\" width=\"1200\" height=\"500\" fetchpriority=\"high\" />\n\n<img src=\"produto-1.jpg\" alt=\"Produto em destaque\" width=\"400\" height=\"400\" loading=\"lazy\" decoding=\"async\" />\n<img src=\"produto-2.jpg\" alt=\"Segundo produto em destaque\" width=\"400\" height=\"400\" loading=\"lazy\" decoding=\"async\" />\n```\n\nA imagem principal não usa `loading=\"lazy\"`, pois ela faz parte do conteúdo inicial. As imagens abaixo da primeira tela usam carregamento tardio. Os atributos `width` e `height` ajudam a evitar mudanças bruscas de layout durante o carregamento.\n\n### Minificação de CSS com PostCSS e cssnano\n\n```bash\nnpm install --save-dev postcss postcss-cli cssnano\n```\n\n### `postcss.config.js`\n\n```js\nmodule.exports = {\n  plugins: [require('cssnano')({ preset: 'default' })],\n}\n```\n\n### Script de build de CSS\n\n```json\n{\n  \"scripts\": {\n    \"build:css\": \"postcss estilos.css -o estilos.min.css\"\n  }\n}\n```\n\n```bash\nnpx postcss estilos.css -o estilos.min.css\n```\n\nNo HTML de produção, a referência passa a ser:\n\n```html\n<link rel=\"stylesheet\" href=\"estilos.min.css\" />\n```\n\n### Verificação de performance\n\n1. Abrir a página no Chrome e gerar um relatório na aba Lighthouse.\n2. Registrar os dados de performance antes e depois da minificação e do carregamento tardio.\n3. Analisar principalmente LCP, recursos bloqueadores, peso total transferido e recomendações sobre imagens.\n4. Testar o carregamento em uma janela anônima e em modo de rede limitada para observar o comportamento sem cache.\n\nO carregamento tardio reduz a transferência inicial de imagens que o usuário ainda não precisa visualizar. A minificação remove espaços, comentários e estruturas desnecessárias do CSS, diminuindo o arquivo transferido. As duas práticas devem ser validadas pelo relatório e pela experiência real de navegação, evitando aplicar otimizações que prejudiquem a imagem principal ou a acessibilidade.", "conteudo": "### Carregamento tardio de imagens\n\n```html\n<img src=\"banner-principal.jpg\" alt=\"Banner principal da página\" width=\"1200\" height=\"500\" fetchpriority=\"high\" />\n\n<img src=\"produto-1.jpg\" alt=\"Produto em destaque\" width=\"400\" height=\"400\" loading=\"lazy\" decoding=\"async\" />\n<img src=\"produto-2.jpg\" alt=\"Segundo produto em destaque\" width=\"400\" height=\"400\" loading=\"lazy\" decoding=\"async\" />\n```\n\nA imagem principal não usa `loading=\"lazy\"`, pois ela faz parte do conteúdo inicial. As imagens abaixo da primeira tela usam carregamento tardio. Os atributos `width` e `height` ajudam a evitar mudanças bruscas de layout durante o carregamento.\n\n### Minificação de CSS com PostCSS e cssnano\n\n```bash\nnpm install --save-dev postcss postcss-cli cssnano\n```\n\n### `postcss.config.js`\n\n```js\nmodule.exports = {\n  plugins: [require('cssnano')({ preset: 'default' })],\n}\n```\n\n### Script de build de CSS\n\n```json\n{\n  \"scripts\": {\n    \"build:css\": \"postcss estilos.css -o estilos.min.css\"\n  }\n}\n```\n\n```bash\nnpx postcss estilos.css -o estilos.min.css\n```\n\nNo HTML de produção, a referência passa a ser:\n\n```html\n<link rel=\"stylesheet\" href=\"estilos.min.css\" />\n```\n\n### Verificação de performance\n\n1. Abrir a página no Chrome e gerar um relatório na aba Lighthouse.\n2. Registrar os dados de performance antes e depois da minificação e do carregamento tardio.\n3. Analisar principalmente LCP, recursos bloqueadores, peso total transferido e recomendações sobre imagens.\n4. Testar o carregamento em uma janela anônima e em modo de rede limitada para observar o comportamento sem cache.\n\nO carregamento tardio reduz a transferência inicial de imagens que o usuário ainda não precisa visualizar. A minificação remove espaços, comentários e estruturas desnecessárias do CSS, diminuindo o arquivo transferido. As duas práticas devem ser validadas pelo relatório e pela experiência real de navegação, evitando aplicar otimizações que prejudiquem a imagem principal ou a acessibilidade.", "fonte": "gabarito/Front-End/3° bimestre/Semana 15/semana-15-respostas.md#atividade-pratica-aula-3-performance-de-aplicacoes-web", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
