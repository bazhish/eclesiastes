---
{"materia": "Front-End", "materiaSlug": "front-end", "bimestre": 3, "semana": 17, "ordem": 10, "aula": "a1", "titulo": "Integração com APIs Externas · Aula 1", "atividade": {"titulo": "Atividade Prática - Aula 1: Consumindo uma API de dados climáticos", "enunciado": "Desenvolvi uma interface de consulta climática com as duas formas de requisição pedidas: `fetch` e Axios. A chave da API é lida de uma configuração local para não ser registrada no código-fonte.\n\n### `index.html`\n\n```html\n<!doctype html>\n<html lang=\"pt-BR\">\n  <head>\n    <meta charset=\"UTF-8\" />\n    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\" />\n    <title>Consulta climática</title>\n    <script src=\"https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js\"></script>\n    <script src=\"config.js\" defer></script>\n    <script src=\"app.js\" defer></script>\n  </head>\n  <body>\n    <main>\n      <h1>Consultar clima</h1>\n      <label for=\"city\">Cidade</label>\n      <input id=\"city\" type=\"text\" placeholder=\"Ex.: São Paulo\" />\n      <label for=\"method\">Método</label>\n      <select id=\"method\">\n        <option value=\"fetch\">Fetch</option>\n        <option value=\"axios\">Axios</option>\n      </select>\n      <button id=\"consultar\" type=\"button\">Consultar</button>\n      <section id=\"result\" aria-live=\"polite\"></section>\n      <p id=\"error\" role=\"alert\"></p>\n    </main>\n  </body>\n</html>\n```\n\n### `config.js`\n\n```js\n// Este arquivo não deve ser enviado ao repositório quando contiver uma chave real.\nwindow.OPEN_WEATHER_API_KEY = 'SUA_CHAVE_AQUI'\n```\n\n### `app.js`\n\n```js\nconst campoCidade = document.querySelector('#city')\nconst campoMetodo = document.querySelector('#method')\nconst resultado = document.querySelector('#result')\nconst erro = document.querySelector('#error')\n\nfunction criarUrl(cidade) {\n  const parametros = new URLSearchParams({\n    q: cidade,\n    appid: window.OPEN_WEATHER_API_KEY,\n    units: 'metric',\n    lang: 'pt_br',\n  })\n  return `https://api.openweathermap.org/data/2.5/weather?${parametros}`\n}\n\nfunction displayWeather(dados) {\n  resultado.innerHTML = `\n    <h2>Clima em ${dados.name}</h2>\n    <p>Temperatura: ${dados.main.temp.toFixed(1)} °C</p>\n    <p>Sensação térmica: ${dados.main.feels_like.toFixed(1)} °C</p>\n    <p>Descrição: ${dados.weather[0].description}</p>\n    <p>Umidade: ${dados.main.humidity}%</p>\n    <p>Pressão: ${dados.main.pressure} hPa</p>\n    <p>Vento: ${dados.wind.speed} m/s</p>`\n  erro.textContent = ''\n}\n\nfunction displayError(mensagem) {\n  resultado.innerHTML = ''\n  erro.textContent = `Erro: ${mensagem}`\n}\n\nfunction mensagemDaResposta(status) {\n  if (status === 404) return 'Cidade não encontrada. Confira o nome informado.'\n  if (status === 401) return 'Chave da API inválida ou ainda não ativada.'\n  if (status === 429) return 'Limite de consultas da API excedido. Tente novamente mais tarde.'\n  return 'Não foi possível consultar a API neste momento.'\n}\n\nasync function fetchWeather(cidade) {\n  try {\n    const resposta = await fetch(criarUrl(cidade))\n    if (!resposta.ok) throw new Error(mensagemDaResposta(resposta.status))\n    displayWeather(await resposta.json())\n  } catch (e) {\n    displayError(e instanceof TypeError ? 'Falha de conexão com a API.' : e.message)\n  }\n}\n\nasync function axiosWeather(cidade) {\n  try {\n    const resposta = await axios.get(criarUrl(cidade))\n    displayWeather(resposta.data)\n  } catch (e) {\n    const status = e.response?.status\n    displayError(status ? mensagemDaResposta(status) : 'Falha de conexão com a API.')\n  }\n}\n\nasync function getWeather() {\n  const cidade = campoCidade.value.trim()\n  if (!cidade) return displayError('Informe o nome de uma cidade.')\n  if (!window.OPEN_WEATHER_API_KEY || window.OPEN_WEATHER_API_KEY === 'SUA_CHAVE_AQUI') {\n    return displayError('Configure uma chave válida da OpenWeatherMap para realizar a consulta.')\n  }\n  return campoMetodo.value === 'fetch' ? fetchWeather(cidade) : axiosWeather(cidade)\n}\n\ndocument.querySelector('#consultar').addEventListener('click', getWeather)\ncampoCidade.addEventListener('keydown', (evento) => {\n  if (evento.key === 'Enter') getWeather()\n})\n```\n\n**1. Qual é a diferença entre `fetch` e Axios para realizar requisições HTTP em JavaScript?**\n\n`fetch` é uma API nativa do navegador, não exige biblioteca adicional e devolve uma `Promise`, mas a resposta HTTP deve ser testada manualmente com `response.ok`. Axios é uma biblioteca externa que já converte JSON com mais conveniência e trata respostas HTTP fora da faixa de sucesso como erro, além de oferecer recursos como interceptadores. Os dois podem ser usados com `async` e `await`.\n\n**2. Por que é importante tratar erros ao realizar requisições para uma API? Como isso pode ser feito no código?**\n\nFalhas de rede, chave inválida, cidade inexistente e limite de consultas podem ocorrer. Sem tratamento, a tela ficaria sem explicação ou poderia apresentar erro técnico ao usuário. O código usa `try/catch`, verifica `response.ok` no `fetch`, identifica o código HTTP e exibe uma mensagem clara em `displayError`.\n\n**3. O que significa o parâmetro `units=metric` na URL da API OpenWeatherMap e qual é a sua importância?**\n\nEsse parâmetro solicita dados no sistema métrico. Dessa forma, a temperatura vem em graus Celsius, que é a unidade esperada pelo público brasileiro, em vez de Kelvin, que é a unidade padrão da API.", "resposta": "Desenvolvi uma interface de consulta climática com as duas formas de requisição pedidas: `fetch` e Axios. A chave da API é lida de uma configuração local para não ser registrada no código-fonte.\n\n### `index.html`\n\n```html\n<!doctype html>\n<html lang=\"pt-BR\">\n  <head>\n    <meta charset=\"UTF-8\" />\n    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\" />\n    <title>Consulta climática</title>\n    <script src=\"https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js\"></script>\n    <script src=\"config.js\" defer></script>\n    <script src=\"app.js\" defer></script>\n  </head>\n  <body>\n    <main>\n      <h1>Consultar clima</h1>\n      <label for=\"city\">Cidade</label>\n      <input id=\"city\" type=\"text\" placeholder=\"Ex.: São Paulo\" />\n      <label for=\"method\">Método</label>\n      <select id=\"method\">\n        <option value=\"fetch\">Fetch</option>\n        <option value=\"axios\">Axios</option>\n      </select>\n      <button id=\"consultar\" type=\"button\">Consultar</button>\n      <section id=\"result\" aria-live=\"polite\"></section>\n      <p id=\"error\" role=\"alert\"></p>\n    </main>\n  </body>\n</html>\n```\n\n### `config.js`\n\n```js\n// Este arquivo não deve ser enviado ao repositório quando contiver uma chave real.\nwindow.OPEN_WEATHER_API_KEY = 'SUA_CHAVE_AQUI'\n```\n\n### `app.js`\n\n```js\nconst campoCidade = document.querySelector('#city')\nconst campoMetodo = document.querySelector('#method')\nconst resultado = document.querySelector('#result')\nconst erro = document.querySelector('#error')\n\nfunction criarUrl(cidade) {\n  const parametros = new URLSearchParams({\n    q: cidade,\n    appid: window.OPEN_WEATHER_API_KEY,\n    units: 'metric',\n    lang: 'pt_br',\n  })\n  return `https://api.openweathermap.org/data/2.5/weather?${parametros}`\n}\n\nfunction displayWeather(dados) {\n  resultado.innerHTML = `\n    <h2>Clima em ${dados.name}</h2>\n    <p>Temperatura: ${dados.main.temp.toFixed(1)} °C</p>\n    <p>Sensação térmica: ${dados.main.feels_like.toFixed(1)} °C</p>\n    <p>Descrição: ${dados.weather[0].description}</p>\n    <p>Umidade: ${dados.main.humidity}%</p>\n    <p>Pressão: ${dados.main.pressure} hPa</p>\n    <p>Vento: ${dados.wind.speed} m/s</p>`\n  erro.textContent = ''\n}\n\nfunction displayError(mensagem) {\n  resultado.innerHTML = ''\n  erro.textContent = `Erro: ${mensagem}`\n}\n\nfunction mensagemDaResposta(status) {\n  if (status === 404) return 'Cidade não encontrada. Confira o nome informado.'\n  if (status === 401) return 'Chave da API inválida ou ainda não ativada.'\n  if (status === 429) return 'Limite de consultas da API excedido. Tente novamente mais tarde.'\n  return 'Não foi possível consultar a API neste momento.'\n}\n\nasync function fetchWeather(cidade) {\n  try {\n    const resposta = await fetch(criarUrl(cidade))\n    if (!resposta.ok) throw new Error(mensagemDaResposta(resposta.status))\n    displayWeather(await resposta.json())\n  } catch (e) {\n    displayError(e instanceof TypeError ? 'Falha de conexão com a API.' : e.message)\n  }\n}\n\nasync function axiosWeather(cidade) {\n  try {\n    const resposta = await axios.get(criarUrl(cidade))\n    displayWeather(resposta.data)\n  } catch (e) {\n    const status = e.response?.status\n    displayError(status ? mensagemDaResposta(status) : 'Falha de conexão com a API.')\n  }\n}\n\nasync function getWeather() {\n  const cidade = campoCidade.value.trim()\n  if (!cidade) return displayError('Informe o nome de uma cidade.')\n  if (!window.OPEN_WEATHER_API_KEY || window.OPEN_WEATHER_API_KEY === 'SUA_CHAVE_AQUI') {\n    return displayError('Configure uma chave válida da OpenWeatherMap para realizar a consulta.')\n  }\n  return campoMetodo.value === 'fetch' ? fetchWeather(cidade) : axiosWeather(cidade)\n}\n\ndocument.querySelector('#consultar').addEventListener('click', getWeather)\ncampoCidade.addEventListener('keydown', (evento) => {\n  if (evento.key === 'Enter') getWeather()\n})\n```\n\n**1. Qual é a diferença entre `fetch` e Axios para realizar requisições HTTP em JavaScript?**\n\n`fetch` é uma API nativa do navegador, não exige biblioteca adicional e devolve uma `Promise`, mas a resposta HTTP deve ser testada manualmente com `response.ok`. Axios é uma biblioteca externa que já converte JSON com mais conveniência e trata respostas HTTP fora da faixa de sucesso como erro, além de oferecer recursos como interceptadores. Os dois podem ser usados com `async` e `await`.\n\n**2. Por que é importante tratar erros ao realizar requisições para uma API? Como isso pode ser feito no código?**\n\nFalhas de rede, chave inválida, cidade inexistente e limite de consultas podem ocorrer. Sem tratamento, a tela ficaria sem explicação ou poderia apresentar erro técnico ao usuário. O código usa `try/catch`, verifica `response.ok` no `fetch`, identifica o código HTTP e exibe uma mensagem clara em `displayError`.\n\n**3. O que significa o parâmetro `units=metric` na URL da API OpenWeatherMap e qual é a sua importância?**\n\nEsse parâmetro solicita dados no sistema métrico. Dessa forma, a temperatura vem em graus Celsius, que é a unidade esperada pelo público brasileiro, em vez de Kelvin, que é a unidade padrão da API.", "conteudo": "Desenvolvi uma interface de consulta climática com as duas formas de requisição pedidas: `fetch` e Axios. A chave da API é lida de uma configuração local para não ser registrada no código-fonte.\n\n### `index.html`\n\n```html\n<!doctype html>\n<html lang=\"pt-BR\">\n  <head>\n    <meta charset=\"UTF-8\" />\n    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\" />\n    <title>Consulta climática</title>\n    <script src=\"https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js\"></script>\n    <script src=\"config.js\" defer></script>\n    <script src=\"app.js\" defer></script>\n  </head>\n  <body>\n    <main>\n      <h1>Consultar clima</h1>\n      <label for=\"city\">Cidade</label>\n      <input id=\"city\" type=\"text\" placeholder=\"Ex.: São Paulo\" />\n      <label for=\"method\">Método</label>\n      <select id=\"method\">\n        <option value=\"fetch\">Fetch</option>\n        <option value=\"axios\">Axios</option>\n      </select>\n      <button id=\"consultar\" type=\"button\">Consultar</button>\n      <section id=\"result\" aria-live=\"polite\"></section>\n      <p id=\"error\" role=\"alert\"></p>\n    </main>\n  </body>\n</html>\n```\n\n### `config.js`\n\n```js\n// Este arquivo não deve ser enviado ao repositório quando contiver uma chave real.\nwindow.OPEN_WEATHER_API_KEY = 'SUA_CHAVE_AQUI'\n```\n\n### `app.js`\n\n```js\nconst campoCidade = document.querySelector('#city')\nconst campoMetodo = document.querySelector('#method')\nconst resultado = document.querySelector('#result')\nconst erro = document.querySelector('#error')\n\nfunction criarUrl(cidade) {\n  const parametros = new URLSearchParams({\n    q: cidade,\n    appid: window.OPEN_WEATHER_API_KEY,\n    units: 'metric',\n    lang: 'pt_br',\n  })\n  return `https://api.openweathermap.org/data/2.5/weather?${parametros}`\n}\n\nfunction displayWeather(dados) {\n  resultado.innerHTML = `\n    <h2>Clima em ${dados.name}</h2>\n    <p>Temperatura: ${dados.main.temp.toFixed(1)} °C</p>\n    <p>Sensação térmica: ${dados.main.feels_like.toFixed(1)} °C</p>\n    <p>Descrição: ${dados.weather[0].description}</p>\n    <p>Umidade: ${dados.main.humidity}%</p>\n    <p>Pressão: ${dados.main.pressure} hPa</p>\n    <p>Vento: ${dados.wind.speed} m/s</p>`\n  erro.textContent = ''\n}\n\nfunction displayError(mensagem) {\n  resultado.innerHTML = ''\n  erro.textContent = `Erro: ${mensagem}`\n}\n\nfunction mensagemDaResposta(status) {\n  if (status === 404) return 'Cidade não encontrada. Confira o nome informado.'\n  if (status === 401) return 'Chave da API inválida ou ainda não ativada.'\n  if (status === 429) return 'Limite de consultas da API excedido. Tente novamente mais tarde.'\n  return 'Não foi possível consultar a API neste momento.'\n}\n\nasync function fetchWeather(cidade) {\n  try {\n    const resposta = await fetch(criarUrl(cidade))\n    if (!resposta.ok) throw new Error(mensagemDaResposta(resposta.status))\n    displayWeather(await resposta.json())\n  } catch (e) {\n    displayError(e instanceof TypeError ? 'Falha de conexão com a API.' : e.message)\n  }\n}\n\nasync function axiosWeather(cidade) {\n  try {\n    const resposta = await axios.get(criarUrl(cidade))\n    displayWeather(resposta.data)\n  } catch (e) {\n    const status = e.response?.status\n    displayError(status ? mensagemDaResposta(status) : 'Falha de conexão com a API.')\n  }\n}\n\nasync function getWeather() {\n  const cidade = campoCidade.value.trim()\n  if (!cidade) return displayError('Informe o nome de uma cidade.')\n  if (!window.OPEN_WEATHER_API_KEY || window.OPEN_WEATHER_API_KEY === 'SUA_CHAVE_AQUI') {\n    return displayError('Configure uma chave válida da OpenWeatherMap para realizar a consulta.')\n  }\n  return campoMetodo.value === 'fetch' ? fetchWeather(cidade) : axiosWeather(cidade)\n}\n\ndocument.querySelector('#consultar').addEventListener('click', getWeather)\ncampoCidade.addEventListener('keydown', (evento) => {\n  if (evento.key === 'Enter') getWeather()\n})\n```\n\n**1. Qual é a diferença entre `fetch` e Axios para realizar requisições HTTP em JavaScript?**\n\n`fetch` é uma API nativa do navegador, não exige biblioteca adicional e devolve uma `Promise`, mas a resposta HTTP deve ser testada manualmente com `response.ok`. Axios é uma biblioteca externa que já converte JSON com mais conveniência e trata respostas HTTP fora da faixa de sucesso como erro, além de oferecer recursos como interceptadores. Os dois podem ser usados com `async` e `await`.\n\n**2. Por que é importante tratar erros ao realizar requisições para uma API? Como isso pode ser feito no código?**\n\nFalhas de rede, chave inválida, cidade inexistente e limite de consultas podem ocorrer. Sem tratamento, a tela ficaria sem explicação ou poderia apresentar erro técnico ao usuário. O código usa `try/catch`, verifica `response.ok` no `fetch`, identifica o código HTTP e exibe uma mensagem clara em `displayError`.\n\n**3. O que significa o parâmetro `units=metric` na URL da API OpenWeatherMap e qual é a sua importância?**\n\nEsse parâmetro solicita dados no sistema métrico. Dessa forma, a temperatura vem em graus Celsius, que é a unidade esperada pelo público brasileiro, em vez de Kelvin, que é a unidade padrão da API.", "fonte": "gabarito/Front-End/3° bimestre/Semana 17/semana-17-respostas.md#atividade-pratica-aula-1-consumindo-uma-api-de-dados-climaticos", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
