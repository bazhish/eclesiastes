---
{"materia": "Front-End", "materiaSlug": "front-end", "bimestre": 3, "semana": 17, "ordem": 30, "aula": "a3", "titulo": "Integração com APIs Externas · Aula 3", "atividade": {"titulo": "Atividade Prática - Aula 3: Consultas eficientes com GraphQL em APIs", "enunciado": "### Configuração\n\n```bash\nmkdir techworld-graphql\ncd techworld-graphql\nnpm init -y\nnpm install apollo-server graphql\nnode index.js\n```\n\n### `index.js`\n\n```js\nconst { ApolloServer, gql } = require('apollo-server')\n\nconst produtos = [\n  { id: '1', nome: 'Notebook', preco: 2000, estoque: 5 },\n  { id: '2', nome: 'Mouse', preco: 50, estoque: 10 },\n]\nconst clientes = [\n  { id: '1', nome: 'Alice', email: 'alice@example.com', telefone: '123456789', produtos: ['1', '2'] },\n  { id: '2', nome: 'Bob', email: 'bob@example.com', telefone: '987654321', produtos: ['2'] },\n]\n\nconst typeDefs = gql`\n  type Cliente { id: ID!, nome: String!, email: String!, telefone: String, produtos: [Produto!]! }\n  type Produto { id: ID!, nome: String!, preco: Float!, estoque: Int! }\n  type Query {\n    cliente(id: ID!): Cliente\n    produtos(estoqueMin: Int): [Produto!]!\n  }\n`\n\nconst resolvers = {\n  Query: {\n    cliente: (_, { id }) => clientes.find((cliente) => cliente.id === id),\n    produtos: (_, { estoqueMin = 0 }) => produtos.filter((produto) => produto.estoque >= estoqueMin),\n  },\n  Cliente: {\n    produtos: (cliente) => produtos.filter((produto) => cliente.produtos.includes(produto.id)),\n  },\n}\n\nnew ApolloServer({ typeDefs, resolvers }).listen().then(({ url }) => console.log(`Servidor em ${url}`))\n```\n\n### Consultas testadas no Playground\n\n```graphql\nquery ClientePorId {\n  cliente(id: \"1\") {\n    nome\n    email\n    produtos { nome preco }\n  }\n}\n```\n\n```graphql\nquery ProdutosEmEstoque {\n  produtos(estoqueMin: 5) {\n    nome\n    preco\n  }\n}\n```\n\nGraphQL é eficiente neste caso porque o cliente solicita somente os campos necessários. Na primeira consulta, por exemplo, não são transferidos telefone, identificadores ou estoque, pois eles não foram pedidos.", "resposta": "### Configuração\n\n```bash\nmkdir techworld-graphql\ncd techworld-graphql\nnpm init -y\nnpm install apollo-server graphql\nnode index.js\n```\n\n### `index.js`\n\n```js\nconst { ApolloServer, gql } = require('apollo-server')\n\nconst produtos = [\n  { id: '1', nome: 'Notebook', preco: 2000, estoque: 5 },\n  { id: '2', nome: 'Mouse', preco: 50, estoque: 10 },\n]\nconst clientes = [\n  { id: '1', nome: 'Alice', email: 'alice@example.com', telefone: '123456789', produtos: ['1', '2'] },\n  { id: '2', nome: 'Bob', email: 'bob@example.com', telefone: '987654321', produtos: ['2'] },\n]\n\nconst typeDefs = gql`\n  type Cliente { id: ID!, nome: String!, email: String!, telefone: String, produtos: [Produto!]! }\n  type Produto { id: ID!, nome: String!, preco: Float!, estoque: Int! }\n  type Query {\n    cliente(id: ID!): Cliente\n    produtos(estoqueMin: Int): [Produto!]!\n  }\n`\n\nconst resolvers = {\n  Query: {\n    cliente: (_, { id }) => clientes.find((cliente) => cliente.id === id),\n    produtos: (_, { estoqueMin = 0 }) => produtos.filter((produto) => produto.estoque >= estoqueMin),\n  },\n  Cliente: {\n    produtos: (cliente) => produtos.filter((produto) => cliente.produtos.includes(produto.id)),\n  },\n}\n\nnew ApolloServer({ typeDefs, resolvers }).listen().then(({ url }) => console.log(`Servidor em ${url}`))\n```\n\n### Consultas testadas no Playground\n\n```graphql\nquery ClientePorId {\n  cliente(id: \"1\") {\n    nome\n    email\n    produtos { nome preco }\n  }\n}\n```\n\n```graphql\nquery ProdutosEmEstoque {\n  produtos(estoqueMin: 5) {\n    nome\n    preco\n  }\n}\n```\n\nGraphQL é eficiente neste caso porque o cliente solicita somente os campos necessários. Na primeira consulta, por exemplo, não são transferidos telefone, identificadores ou estoque, pois eles não foram pedidos.", "conteudo": "### Configuração\n\n```bash\nmkdir techworld-graphql\ncd techworld-graphql\nnpm init -y\nnpm install apollo-server graphql\nnode index.js\n```\n\n### `index.js`\n\n```js\nconst { ApolloServer, gql } = require('apollo-server')\n\nconst produtos = [\n  { id: '1', nome: 'Notebook', preco: 2000, estoque: 5 },\n  { id: '2', nome: 'Mouse', preco: 50, estoque: 10 },\n]\nconst clientes = [\n  { id: '1', nome: 'Alice', email: 'alice@example.com', telefone: '123456789', produtos: ['1', '2'] },\n  { id: '2', nome: 'Bob', email: 'bob@example.com', telefone: '987654321', produtos: ['2'] },\n]\n\nconst typeDefs = gql`\n  type Cliente { id: ID!, nome: String!, email: String!, telefone: String, produtos: [Produto!]! }\n  type Produto { id: ID!, nome: String!, preco: Float!, estoque: Int! }\n  type Query {\n    cliente(id: ID!): Cliente\n    produtos(estoqueMin: Int): [Produto!]!\n  }\n`\n\nconst resolvers = {\n  Query: {\n    cliente: (_, { id }) => clientes.find((cliente) => cliente.id === id),\n    produtos: (_, { estoqueMin = 0 }) => produtos.filter((produto) => produto.estoque >= estoqueMin),\n  },\n  Cliente: {\n    produtos: (cliente) => produtos.filter((produto) => cliente.produtos.includes(produto.id)),\n  },\n}\n\nnew ApolloServer({ typeDefs, resolvers }).listen().then(({ url }) => console.log(`Servidor em ${url}`))\n```\n\n### Consultas testadas no Playground\n\n```graphql\nquery ClientePorId {\n  cliente(id: \"1\") {\n    nome\n    email\n    produtos { nome preco }\n  }\n}\n```\n\n```graphql\nquery ProdutosEmEstoque {\n  produtos(estoqueMin: 5) {\n    nome\n    preco\n  }\n}\n```\n\nGraphQL é eficiente neste caso porque o cliente solicita somente os campos necessários. Na primeira consulta, por exemplo, não são transferidos telefone, identificadores ou estoque, pois eles não foram pedidos.", "fonte": "gabarito/Front-End/3° bimestre/Semana 17/semana-17-respostas.md#atividade-pratica-aula-3-consultas-eficientes-com-graphql-em-apis", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
