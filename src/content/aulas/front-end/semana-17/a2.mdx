---
{"materia": "Front-End", "materiaSlug": "front-end", "bimestre": 3, "semana": 17, "ordem": 20, "aula": "a2", "titulo": "Integração com APIs Externas · Aula 2", "atividade": {"titulo": "Atividade Prática - Aula 2: Criando um aplicativo de chat com WebSockets", "enunciado": "### Instalação e execução\n\n```bash\nnpm init -y\nnpm install ws\nnode server.js\n```\n\nO arquivo `index.html` deve ser aberto por um servidor HTTP local, e não diretamente pelo sistema de arquivos.\n\n### `server.js`\n\n```js\nconst WebSocket = require('ws')\n\nconst wss = new WebSocket.Server({ port: 8081 })\n\nfunction enviarParaTodos(mensagem) {\n  const texto = JSON.stringify(mensagem)\n  wss.clients.forEach((cliente) => {\n    if (cliente.readyState === WebSocket.OPEN) cliente.send(texto)\n  })\n}\n\nwss.on('connection', (ws) => {\n  const id = crypto.randomUUID()\n  console.log(`Cliente conectado: ${id}`)\n  enviarParaTodos({ tipo: 'aviso', texto: 'Um usuário entrou no chat.' })\n\n  ws.on('message', (conteudo) => {\n    try {\n      const { nome, texto } = JSON.parse(conteudo.toString())\n      if (!nome?.trim() || !texto?.trim()) return\n      enviarParaTodos({ tipo: 'mensagem', nome: nome.trim(), texto: texto.trim() })\n    } catch {\n      ws.send(JSON.stringify({ tipo: 'erro', texto: 'Mensagem inválida.' }))\n    }\n  })\n\n  ws.on('close', () => {\n    console.log(`Cliente desconectado: ${id}`)\n    enviarParaTodos({ tipo: 'aviso', texto: 'Um usuário saiu do chat.' })\n  })\n  ws.on('error', (erro) => console.error('Erro no WebSocket:', erro.message))\n})\n\nconsole.log('Servidor WebSocket em execução na porta 8081')\n```\n\n### `index.html`\n\n```html\n<!doctype html>\n<html lang=\"pt-BR\">\n  <head><meta charset=\"UTF-8\" /><title>Chat em tempo real</title><link rel=\"stylesheet\" href=\"style.css\" /></head>\n  <body>\n    <main id=\"chat\">\n      <h1>Chat em tempo real</h1>\n      <input id=\"name\" placeholder=\"Seu nome\" autocomplete=\"name\" />\n      <input id=\"message\" placeholder=\"Sua mensagem\" />\n      <button id=\"send\">Enviar</button>\n      <div id=\"status\" aria-live=\"polite\"></div>\n      <div id=\"messages\" aria-live=\"polite\"></div>\n    </main>\n    <script>\n      const ws = new WebSocket('ws://localhost:8081')\n      const mensagens = document.querySelector('#messages')\n      const status = document.querySelector('#status')\n      const adicionar = (texto, classe = '') => {\n        const p = document.createElement('p')\n        p.className = classe\n        p.textContent = texto\n        mensagens.append(p)\n      }\n      ws.addEventListener('open', () => { status.textContent = 'Conectado ao chat.' })\n      ws.addEventListener('message', ({ data }) => {\n        const item = JSON.parse(data)\n        adicionar(item.tipo === 'mensagem' ? `${item.nome}: ${item.texto}` : item.texto, item.tipo)\n      })\n      ws.addEventListener('close', () => { status.textContent = 'Conexão encerrada.' })\n      ws.addEventListener('error', () => { status.textContent = 'Não foi possível conectar ao servidor.' })\n      document.querySelector('#send').addEventListener('click', () => {\n        const nome = document.querySelector('#name').value.trim()\n        const texto = document.querySelector('#message').value.trim()\n        if (!nome || !texto) return (status.textContent = 'Preencha nome e mensagem.')\n        if (ws.readyState !== WebSocket.OPEN) return (status.textContent = 'Chat desconectado.')\n        ws.send(JSON.stringify({ nome, texto }))\n        document.querySelector('#message').value = ''\n      })\n    </script>\n  </body>\n</html>\n```\n\n**Como você descreveria a importância dos WebSockets em comparação com métodos tradicionais de comunicação, como HTTP?**\n\nHTTP normalmente segue o modelo de solicitação e resposta: o cliente pede uma informação e o servidor responde. WebSocket mantém uma conexão bidirecional aberta, permitindo que o servidor envie atualizações assim que elas acontecem. Por isso é adequado para chat, acompanhamento de pedidos, jogos e notificações em tempo real. HTTP ainda é suficiente para operações pontuais, mais simples e que não exigem atualização imediata.\n\n### Discussão: monitoramento de delivery e serviços externos\n\nAlém da previsão do tempo, seriam úteis APIs de geolocalização e rotas, pagamento, notificações e mapas. A escolha precisa considerar disponibilidade, documentação, limites, custo por uso, privacidade e possibilidade de substituição. Uma API gratuita pode atender ao protótipo, mas pode ter limites e menor garantia de serviço; uma API paga tende a oferecer suporte e contrato, porém aumenta custo e dependência do fornecedor.\n\nTerceirizar desenvolvimento e contratar APIs têm em comum a dependência de uma parte externa. Em ambos os casos, é necessário definir requisitos, contrato, segurança, custos e plano de contingência. O equilíbrio consiste em terceirizar especialidades ou demandas temporárias e manter internamente conhecimento do produto, arquitetura, revisão de qualidade e decisões estratégicas. Isso reduz dependência excessiva e mantém oportunidades de formação para a equipe interna.", "resposta": "### Instalação e execução\n\n```bash\nnpm init -y\nnpm install ws\nnode server.js\n```\n\nO arquivo `index.html` deve ser aberto por um servidor HTTP local, e não diretamente pelo sistema de arquivos.\n\n### `server.js`\n\n```js\nconst WebSocket = require('ws')\n\nconst wss = new WebSocket.Server({ port: 8081 })\n\nfunction enviarParaTodos(mensagem) {\n  const texto = JSON.stringify(mensagem)\n  wss.clients.forEach((cliente) => {\n    if (cliente.readyState === WebSocket.OPEN) cliente.send(texto)\n  })\n}\n\nwss.on('connection', (ws) => {\n  const id = crypto.randomUUID()\n  console.log(`Cliente conectado: ${id}`)\n  enviarParaTodos({ tipo: 'aviso', texto: 'Um usuário entrou no chat.' })\n\n  ws.on('message', (conteudo) => {\n    try {\n      const { nome, texto } = JSON.parse(conteudo.toString())\n      if (!nome?.trim() || !texto?.trim()) return\n      enviarParaTodos({ tipo: 'mensagem', nome: nome.trim(), texto: texto.trim() })\n    } catch {\n      ws.send(JSON.stringify({ tipo: 'erro', texto: 'Mensagem inválida.' }))\n    }\n  })\n\n  ws.on('close', () => {\n    console.log(`Cliente desconectado: ${id}`)\n    enviarParaTodos({ tipo: 'aviso', texto: 'Um usuário saiu do chat.' })\n  })\n  ws.on('error', (erro) => console.error('Erro no WebSocket:', erro.message))\n})\n\nconsole.log('Servidor WebSocket em execução na porta 8081')\n```\n\n### `index.html`\n\n```html\n<!doctype html>\n<html lang=\"pt-BR\">\n  <head><meta charset=\"UTF-8\" /><title>Chat em tempo real</title><link rel=\"stylesheet\" href=\"style.css\" /></head>\n  <body>\n    <main id=\"chat\">\n      <h1>Chat em tempo real</h1>\n      <input id=\"name\" placeholder=\"Seu nome\" autocomplete=\"name\" />\n      <input id=\"message\" placeholder=\"Sua mensagem\" />\n      <button id=\"send\">Enviar</button>\n      <div id=\"status\" aria-live=\"polite\"></div>\n      <div id=\"messages\" aria-live=\"polite\"></div>\n    </main>\n    <script>\n      const ws = new WebSocket('ws://localhost:8081')\n      const mensagens = document.querySelector('#messages')\n      const status = document.querySelector('#status')\n      const adicionar = (texto, classe = '') => {\n        const p = document.createElement('p')\n        p.className = classe\n        p.textContent = texto\n        mensagens.append(p)\n      }\n      ws.addEventListener('open', () => { status.textContent = 'Conectado ao chat.' })\n      ws.addEventListener('message', ({ data }) => {\n        const item = JSON.parse(data)\n        adicionar(item.tipo === 'mensagem' ? `${item.nome}: ${item.texto}` : item.texto, item.tipo)\n      })\n      ws.addEventListener('close', () => { status.textContent = 'Conexão encerrada.' })\n      ws.addEventListener('error', () => { status.textContent = 'Não foi possível conectar ao servidor.' })\n      document.querySelector('#send').addEventListener('click', () => {\n        const nome = document.querySelector('#name').value.trim()\n        const texto = document.querySelector('#message').value.trim()\n        if (!nome || !texto) return (status.textContent = 'Preencha nome e mensagem.')\n        if (ws.readyState !== WebSocket.OPEN) return (status.textContent = 'Chat desconectado.')\n        ws.send(JSON.stringify({ nome, texto }))\n        document.querySelector('#message').value = ''\n      })\n    </script>\n  </body>\n</html>\n```\n\n**Como você descreveria a importância dos WebSockets em comparação com métodos tradicionais de comunicação, como HTTP?**\n\nHTTP normalmente segue o modelo de solicitação e resposta: o cliente pede uma informação e o servidor responde. WebSocket mantém uma conexão bidirecional aberta, permitindo que o servidor envie atualizações assim que elas acontecem. Por isso é adequado para chat, acompanhamento de pedidos, jogos e notificações em tempo real. HTTP ainda é suficiente para operações pontuais, mais simples e que não exigem atualização imediata.\n\n### Discussão: monitoramento de delivery e serviços externos\n\nAlém da previsão do tempo, seriam úteis APIs de geolocalização e rotas, pagamento, notificações e mapas. A escolha precisa considerar disponibilidade, documentação, limites, custo por uso, privacidade e possibilidade de substituição. Uma API gratuita pode atender ao protótipo, mas pode ter limites e menor garantia de serviço; uma API paga tende a oferecer suporte e contrato, porém aumenta custo e dependência do fornecedor.\n\nTerceirizar desenvolvimento e contratar APIs têm em comum a dependência de uma parte externa. Em ambos os casos, é necessário definir requisitos, contrato, segurança, custos e plano de contingência. O equilíbrio consiste em terceirizar especialidades ou demandas temporárias e manter internamente conhecimento do produto, arquitetura, revisão de qualidade e decisões estratégicas. Isso reduz dependência excessiva e mantém oportunidades de formação para a equipe interna.", "conteudo": "### Instalação e execução\n\n```bash\nnpm init -y\nnpm install ws\nnode server.js\n```\n\nO arquivo `index.html` deve ser aberto por um servidor HTTP local, e não diretamente pelo sistema de arquivos.\n\n### `server.js`\n\n```js\nconst WebSocket = require('ws')\n\nconst wss = new WebSocket.Server({ port: 8081 })\n\nfunction enviarParaTodos(mensagem) {\n  const texto = JSON.stringify(mensagem)\n  wss.clients.forEach((cliente) => {\n    if (cliente.readyState === WebSocket.OPEN) cliente.send(texto)\n  })\n}\n\nwss.on('connection', (ws) => {\n  const id = crypto.randomUUID()\n  console.log(`Cliente conectado: ${id}`)\n  enviarParaTodos({ tipo: 'aviso', texto: 'Um usuário entrou no chat.' })\n\n  ws.on('message', (conteudo) => {\n    try {\n      const { nome, texto } = JSON.parse(conteudo.toString())\n      if (!nome?.trim() || !texto?.trim()) return\n      enviarParaTodos({ tipo: 'mensagem', nome: nome.trim(), texto: texto.trim() })\n    } catch {\n      ws.send(JSON.stringify({ tipo: 'erro', texto: 'Mensagem inválida.' }))\n    }\n  })\n\n  ws.on('close', () => {\n    console.log(`Cliente desconectado: ${id}`)\n    enviarParaTodos({ tipo: 'aviso', texto: 'Um usuário saiu do chat.' })\n  })\n  ws.on('error', (erro) => console.error('Erro no WebSocket:', erro.message))\n})\n\nconsole.log('Servidor WebSocket em execução na porta 8081')\n```\n\n### `index.html`\n\n```html\n<!doctype html>\n<html lang=\"pt-BR\">\n  <head><meta charset=\"UTF-8\" /><title>Chat em tempo real</title><link rel=\"stylesheet\" href=\"style.css\" /></head>\n  <body>\n    <main id=\"chat\">\n      <h1>Chat em tempo real</h1>\n      <input id=\"name\" placeholder=\"Seu nome\" autocomplete=\"name\" />\n      <input id=\"message\" placeholder=\"Sua mensagem\" />\n      <button id=\"send\">Enviar</button>\n      <div id=\"status\" aria-live=\"polite\"></div>\n      <div id=\"messages\" aria-live=\"polite\"></div>\n    </main>\n    <script>\n      const ws = new WebSocket('ws://localhost:8081')\n      const mensagens = document.querySelector('#messages')\n      const status = document.querySelector('#status')\n      const adicionar = (texto, classe = '') => {\n        const p = document.createElement('p')\n        p.className = classe\n        p.textContent = texto\n        mensagens.append(p)\n      }\n      ws.addEventListener('open', () => { status.textContent = 'Conectado ao chat.' })\n      ws.addEventListener('message', ({ data }) => {\n        const item = JSON.parse(data)\n        adicionar(item.tipo === 'mensagem' ? `${item.nome}: ${item.texto}` : item.texto, item.tipo)\n      })\n      ws.addEventListener('close', () => { status.textContent = 'Conexão encerrada.' })\n      ws.addEventListener('error', () => { status.textContent = 'Não foi possível conectar ao servidor.' })\n      document.querySelector('#send').addEventListener('click', () => {\n        const nome = document.querySelector('#name').value.trim()\n        const texto = document.querySelector('#message').value.trim()\n        if (!nome || !texto) return (status.textContent = 'Preencha nome e mensagem.')\n        if (ws.readyState !== WebSocket.OPEN) return (status.textContent = 'Chat desconectado.')\n        ws.send(JSON.stringify({ nome, texto }))\n        document.querySelector('#message').value = ''\n      })\n    </script>\n  </body>\n</html>\n```\n\n**Como você descreveria a importância dos WebSockets em comparação com métodos tradicionais de comunicação, como HTTP?**\n\nHTTP normalmente segue o modelo de solicitação e resposta: o cliente pede uma informação e o servidor responde. WebSocket mantém uma conexão bidirecional aberta, permitindo que o servidor envie atualizações assim que elas acontecem. Por isso é adequado para chat, acompanhamento de pedidos, jogos e notificações em tempo real. HTTP ainda é suficiente para operações pontuais, mais simples e que não exigem atualização imediata.\n\n### Discussão: monitoramento de delivery e serviços externos\n\nAlém da previsão do tempo, seriam úteis APIs de geolocalização e rotas, pagamento, notificações e mapas. A escolha precisa considerar disponibilidade, documentação, limites, custo por uso, privacidade e possibilidade de substituição. Uma API gratuita pode atender ao protótipo, mas pode ter limites e menor garantia de serviço; uma API paga tende a oferecer suporte e contrato, porém aumenta custo e dependência do fornecedor.\n\nTerceirizar desenvolvimento e contratar APIs têm em comum a dependência de uma parte externa. Em ambos os casos, é necessário definir requisitos, contrato, segurança, custos e plano de contingência. O equilíbrio consiste em terceirizar especialidades ou demandas temporárias e manter internamente conhecimento do produto, arquitetura, revisão de qualidade e decisões estratégicas. Isso reduz dependência excessiva e mantém oportunidades de formação para a equipe interna.", "fonte": "gabarito/Front-End/3° bimestre/Semana 17/semana-17-respostas.md#atividade-pratica-aula-2-criando-um-aplicativo-de-chat-com-websockets", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
