---
{"materia": "Front-End", "materiaSlug": "front-end", "bimestre": 3, "semana": 17, "ordem": 2, "aula": "a2", "titulo": "Criando um aplicativo de chat com WebSockets", "atividade": {"tipo": "pratica", "enunciado": "Roteiro de Atividade Prática\n\nNome: Turma: .\n\nAtividade: Criando um aplicativo de chat com WebSockets\n\nVocês foram contratados para desenvolver um aplicativo de chat em tempo real utilizando WebSockets. O aplicativo deve permitir que vários usuários se conectem e troquem mensagens instantaneamente.\n\nSituação fictícia produzida pela SEDUC-SP.\n\nRequisitos\n\nServidor WebSocket\n\nCrie um servidor WebSocket utilizando Node.js e a biblioteca ws.\n\nO servidor deve gerenciar as conexões dos clientes e retransmitir as mensagens recebidas para todos os usuários conectados.\n\nCliente WebSocket\n\nCrie uma interface simples em HTML/CSS/JavaScript na qual os usuários possam:\n\nInserir seu nome e mensagem.\n\nVer uma lista de mensagens enviadas por todos os usuários conectados.\n\nFuncionalidades\n\nQuando um usuário envia uma mensagem, ela deve ser exibida em tempo real para todos os usuários conectados.\n\nAdicione a funcionalidade para exibir o nome do usuário junto com a mensagem.\n\nTrate desconexões e exiba uma mensagem de aviso quando um usuário se desconectar.\n\nEstilo\n\nUtilize CSS básico para estilizar a interface e torná-la amigável.\n\nDicas\n\nUtilize WebSocket para criar a conexão no lado do cliente.\n\nNo servidor, use eventos como connection, message e close para gerenciar a comunicação.\n\nConsidere implementar um sistema simples de logs no console para monitorar as conexões e mensagens enviadas.\n\nEstrutura de arquivos sugerida\n\nchat-app/\n\n├── server.js // Servidor WebSocket\n\n├── index.html // Interface do cliente\n\n└── style.css // Estilos da interface\n\nExemplo do servidor (server.js)\n\n// server.js\n\nconst WebSocket = require('ws');\n\nconst wss = new WebSocket.Server({ port: 8081 });\n\nwss.on('connection', (ws) => {\n\nconsole.log('Novo cliente conectado');\n\nws.on('message', (message) => {\n\nconsole.log('Mensagem recebida:', message);\n\n// Envie a mensagem para todos os clientes conectados\n\nwss.clients.forEach((client) => {\n\nif (client.readyState === WebSocket.OPEN) {\n\nclient.send(message);\n\n}\n\n});\n\n});\n\nws.on('close', () => {\n\nconsole.log('Cliente desconectado');\n\n});\n\nws.onopen = () => {\n\nconsole.log('Conexão WebSocket estabelecida');\n\n};\n\nws.onerror = (error) => {\n\nconsole.error('Erro no WebSocket:', error);\n\n};\n\nws.onclose = () => {\n\nconsole.log('Conexão WebSocket fechada');\n\n};\n\n});\n\nconsole.log('Servidor WebSocket rodando na porta 8081');\n\nExemplo do cliente (index.html)\n\n<!DOCTYPE html>\n\n<html lang=\"pt-BR\">\n\n<head>\n\n<meta charset=\"UTF-8\">\n\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\">\n\n<link rel=\"stylesheet\" href=\"style.css\">\n\n<title>Chat em Tempo Real</title>\n\n</head>\n\n<body>\n\n<div id=\"chat\">\n\n<input type=\"text\" id=\"name\" placeholder=\"Seu nome\">\n\n<input type=\"text\" id=\"message\" placeholder=\"Sua mensagem\">\n\n<button onclick=\"sendMessage()\">Enviar</button>\n\n<div id=\"messages\"></div>\n\n</div>\n\n<script>\n\nconst ws = new WebSocket('ws://localhost:8081');\n\nws.onopen = () => {\n\nconsole.log('Conexão WebSocket estabelecida');\n\n};\n\nws.onmessage = (event) => {\n\nif (event.data instanceof Blob) {\n\nconsole.log('Recebido um Blob, convertendo para texto...');\n\nconst reader = new FileReader();\n\nreader.onload = function() {\n\nconst messageText = reader.result; // Aqui você terá a mensagem em texto\n\nconst messagesDiv = document.getElementById('messages');\n\nmessagesDiv.innerHTML += `<p>${messageText}</p>`;\n\n};\n\nreader.readAsText(event.data); // Converte o Blob para texto\n\n} else {\n\nconst messagesDiv = document.getElementById('messages');\n\nmessagesDiv.innerHTML += `<p>${event.data}</p>`;\n\n}\n\n};\n\nws.onerror = (error) => {\n\nconsole.error('Erro no WebSocket:', error);\n\n};\n\nws.onclose = () => {\n\nconsole.log('Conexão WebSocket fechada');\n\n};\n\nfunction sendMessage() {\n\nconst name = document.getElementById('name').value;\n\nconst message = document.getElementById('message').value;\n\nif (name && message) { // Verifica se os campos não estão vazios\n\nws.send(`${name}: ${message}`);\n\ndocument.getElementById('message').value = '';\n\n} else {\n\nalert('Por favor, preencha seu nome e a mensagem.');\n\n}\n\n}\n\n</script>\n\n</body>\n\n</html>\n\nEstilos da interface\n\nCrie um arquivo style.css para estilizar o aplicativo de chat em tempo real.\n\nRodando o arquivo HTML\n\nA maneira mais comum e segura de rodar um arquivo HTML que usa WebSockets é por meio de um servidor local. Aqui está como fazer isso usando diferentes ferramentas:\n\nUsando o Node.js com http-server\n\nInstale o http-server: se você ainda não tiver o Node.js instalado, baixe e instalado. Depois, abra o terminal e execute o seguinte comando para instalar o http-server globalmente:\n\nnpm install -g http-server\n\nNavegue até a pasta do seu projeto: no terminal, use o comando cd para mudar para o diretório onde estão seus arquivos (ex. server.js, index.html, e style.css):\n\ncd caminho/para/seu/projeto/chat-app\n\nInicie o servidor: execute o seguinte comando:\n\nhttp-server\n\nDepois abra o cmd novamente e vá para o mesmo diretório e execute o comando para executar o websocket\n\nnode server.js\n\nAcesse o aplicativo: o terminal mostrará um endereço (por padrão, http://localhost:8080). Abra esse URL no seu navegador para ver o aplicativo de chat.\n\nPergunta\n\nComo você descreveria a importância dos WebSockets em comparação com métodos tradicionais de comunicação, como HTTP?", "resposta": "O roteiro não traz uma resposta escrita pronta. A entrega solicitada é prática: siga o procedimento original e registre a sua execução.", "fonte": "Material/Front-End/3° bimestre/Semana 17 - Integracao com APIs Externas/Atividade Pratica - Aula 2- WebSockets e comunicacao em tempo real - 47413.docx"}, "quizzes": []}
---
