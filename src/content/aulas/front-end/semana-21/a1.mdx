---
{"materia": "Front-End", "materiaSlug": "front-end", "bimestre": 3, "semana": 21, "ordem": 1, "aula": "a1", "titulo": "Integração de serviços de autenticação em uma aplicação front-end", "atividade": {"tipo": "pratica", "enunciado": "Roteiro de Atividade Prática\n\nNome: Turma: .\n\nAtividade: Integração de serviços de autenticação em uma aplicação front-end\n\nVocês foram contratados por uma empresa que está desenvolvendo uma plataforma de gestão de projetos. Para garantir a segurança dos dados e a autenticidade dos usuários, é essencial implementar uma camada de autenticação. A empresa decidiu integrar o sistema a serviços de autenticação com base em OAuth e JWT, viabilizando login seguro com contas de terceiros, como Google.\n\nSituação fictícia produzida pela SEDUC-SP.\n\nParte 1 – Configuração do OAuth e obtenção do cliente ID\n\nPasso 1: configurar o projeto OAuth\n\nCriem um projeto na plataforma de autenticação desejada (ex.: Google Cloud Console https://console.cloud.google.com/welcome/new?pli=1).\n\nConfigurem as credenciais OAuth para permitir o login pela sua aplicação. Definam o URI de redirecionamento como http://localhost:3000.\n\nGuardem o client_id gerado, pois ele será utilizado para configurar a autenticação no front-end.\n\nPasso 2: explicar o processo\n\nPara integrar o Google OAuth, é necessário configurar um projeto no Google Cloud Console. Essa configuração gera as credenciais necessárias (o client_id) para autenticação segura.\n\nPasso 3: guiar a configuração\n\n3.1 Acessar o Google Cloud Console\n\nAcessem o Google Cloud Console.\n\nCaso não esteja autenticado, façam login com suas respectivas contas Google.\n\n3.2 Criar um novo projeto\n\nNo topo da página, cliquem no “Seletor de projetos”, uma lista suspensa ao lado do nome do projeto.\n\nCliquem em “Novo projeto”.\n\nEscolham um nome para o seu projeto (ex.: \"Projeto de Autenticação\").\n\nCliquem em “Criar”e aguardem. Depois, cliquem em “Selecione o projeto”.\n\n3.3 Habilitar a API Google OAuth\n\nNo menu lateral, procurem por “APIs e serviços > Painel de APIs e serviços”.\n\nCliquem em “+ Ativar APIs e serviços”, no topo da página.\n\nPesquisem por “Google Identity” ou “OAuth 2.0” e selecionem a “API Google Identity”.\n\nCliquem em “Ativar” para habilitar a API para seu projeto.\n\n3.4 Criar credenciais de OAuth 2.0\n\nNo menu lateral, procurem por “APIs e serviços > Credenciais”.\n\nCliquem em “+ Criar Credenciais” e selecionem “ID do Cliente OAuth”.\n\nSe esta for a primeira vez configurando OAuth para o projeto, vocês verão uma tela de “Configuração da Tela de Consentimento OAuth”. Completem a configuração:\n\nTipo de usuário – Selecionem “Externo” (garante que qualquer usuário do Google possa fazer login).\n\nCliquem em “Criar”.\n\nPreencham o campo “Nome do aplicativo” (ex.: \"Login de Teste\").\n\nAdicionem “Informações de contato” (seu e-mail).\n\nCliquem em “Salvar e continuar” até concluir a configuração da tela de consentimento.\n\nApós configurarem a tela de consentimento, vocês serão redirecionados para a criação de credenciais. Selecionem:\n\nTipo de aplicativo: aplicativo da Web.\n\nNome: definam um nome para as credenciais (ex.: \"Login com Google para Desenvolvimento\").\n\nURI de redirecionamento autorizado\n\nEm URIs de redirecionamento autorizados, insiram http://localhost:3000. Este é o endereço que o Google usará para redirecionar o usuário após o login durante o desenvolvimento.\n\nCliquem em “Criar”.\n\n3.5 Obter o client_id.\n\nApós a criação, vocês verão uma tela com o ID do cliente e o Segredo do cliente.\n\nCopiem o client_id (vocês precisarão dele para configurar o login no front-end).\n\nMantenham essas credenciais seguras, pois precisarão apenas do client_id no front-end (o client_secret deverá ser guardado para uso no back-end) e concluam.\n\nApós seguir esses passos, o client_id estará pronto para ser integrado ao\nfront-end. Esse client_id serve como uma identificação do aplicativo, permitindo que o Google autentique os usuários em nome da sua aplicação.\n\nAgora, ao implementarem a autenticação no React, substituam YOUR_CLIENT_ID pelo valor do client_id que acabaram de obter. Esse processo configura seu projeto para que o Google possa autenticar usuários em sua aplicação com segurança.\n\nA partir desse ponto, seguiremos com a implementação no front-end, em que vocês configurarão o login do Google em seu projeto React e, em seguida, testarão a aplicação para garantir que a autenticação funcione corretamente.\n\nParte 2 – Implementação da autenticação no front-end (React)\n\nPasso 1: configurar inicialmente o projeto\n\n1.1 Criar o projeto React.\nSe vocês ainda não têm um projeto React, criem um:\n\nnpx create-react-app oauth-google-login\n\ncd oauth-google-login\n\n1.2 Instalar as dependências\nUsem a biblioteca react-oauth/google para facilitar a integração do Google OAuth no front-end.\n\nnpm install @react-oauth/google\n\n1.3 Adicionar o client_id ao seu código\n\nSubstituam YOUR_CLIENT_ID pelo client ID que obteveram no Google Cloud Console. No arquivo src/App.js, adicionem o botão de login do Google.\n\nPasso 2: implementar a autenticação com Google OAuth\n\nDentro do arquivo src/App.js, adicionem o seguinte código:\n\nimport React, { useState, useEffect } from 'react';\n\nimport axios from 'axios';\n\nimport jwtDecode from 'jwt-decode';\n\nconst App = () => {\n\nconst [user, setUser] = useState(null);\n\nconst [jwtToken, setJwtToken] = useState(localStorage.getItem(\"jwtToken\") || \"\");\n\n// Use o seu client_id do Google Cloud Console\n\nconst clientId = \"YOUR_CLIENT_ID\";\n\nconst redirectUri = \"http://localhost:3000\";\n\nuseEffect(() => {\n\n// Se houver um token armazenado, decodifique-o\n\nif (jwtToken) {\n\nconst decodedToken = jwt_decode(jwtToken);\n\nsetUser(decodedToken);\n\n}\n\n}, [jwtToken]);\n\nconst handleGoogleLogin = () => {\n\n// Redireciona para a página de autenticação do Google\n\nwindow.location.href = `https://accounts.google.com/o/oauth2/v2/auth?client_id=${clientId}&redirect_uri=${redirectUri}&response_type=token&scope=profile email`;\n\n};\n\nconst handleTokenExchange = async (googleToken) => {\n\ntry {\n\n// Envia o token do Google para o back-end e obtém um JWT\n\nconst response = await axios.post(\"http://localhost:4000/api/auth/google\", { token: googleToken });\n\nconst { jwt } = response.data;\n\n// Armazena o JWT no localStorage e configura o estado\n\nlocalStorage.setItem(\"jwtToken\", jwt);\n\nsetJwtToken(jwt);\n\n} catch (error) {\n\nconsole.error(\"Erro ao trocar token:\", error);\n\n}\n\n};\n\nconst handleLogout = () => {\n\n// Limpa o token e o estado do usuário\n\nlocalStorage.removeItem(\"jwtToken\");\n\nsetJwtToken(\"\");\n\nsetUser(null);\n\n};\n\nreturn (\n\n<div>\n\n{user ? (\n\n<>\n\n<h2>Bem-vindo, {user.name}</h2>\n\n<button onClick={handleLogout}>Logout</button>\n\n</>\n\n) : (\n\n<button onClick={handleGoogleLogin}>Login com Google</button>\n\n)}\n\n</div>\n\n);\n\n};\n\nexport default App;\n\nExplicação: o jwtToken armazena o JWT retornado pelo back-end após a validação do token do Google.\n\nFunção handleLoginSuccess:\n\n- Após o login bem-sucedido, googleToken é obtido do Google. Foi enviado googleToken para o back-end na rota http://localhost:5000/api/auth/google.\n\n- O back-end retorna um jwtToken, que é armazenado no estado jwtToken e exibido na interface. Se o usuário estiver logado, o JWT será exibido.\n\nPasso 3: testar a aplicação\n\nExecutem o projeto React:\n\nnpm start\n\nA aplicação estará disponível em http://localhost:3000. Vocês verão um botão de login do Google. No futuro, quando o usuário fizer login com sua conta do Google, o token de autenticação será exibido no console e na interface, indicando que a autenticação foi bem-sucedida.\n\nPasso 4: enviar o token para o back-end\n\nAgora que vocês têm o token de autenticação do Google, é necessário enviá-lo ao back-end para validá-lo e gerar um JWT próprio. O código a seguir mostra como isso pode ser feito.\n\n4.1 Instalar as dependências no back-end (Node.js com Express)\n\nInstalem as dependências necessárias no back-end:\n\nnpm install express axios jsonwebtoken google-auth-library\n\nAgora, instalem o pacote jwt-decode, que é usado para decodificar o token JWT e obter informações sobre o usuário após o login.\n\nnpm install jwt-decode\n\n4.2 Criar servidor Express\n\nCriem um servidor Express simples para validar o token do Google e gerar um JWT que chamaremos de server.js. Segue código:\n\nconst express = require('express');\n\nconst jwt = require('jsonwebtoken');\n\nconst { OAuth2Client } = require('google-auth-library');\n\nconst app = express();\n\nconst client = new OAuth2Client('YOUR_CLIENT_ID');\n\napp.use(express.json());\n\napp.post('/api/auth/google', async (req, res) => {\n\nconst { token } = req.body;\n\ntry {\n\n// Verifique o token do Google\n\nconst ticket = await client.verifyIdToken({\n\nidToken: token,\n\naudience: 'YOUR_CLIENT_ID',\n\n});\n\nconst payload = ticket.getPayload();\n\nconst userId = payload['sub'];\n\n// Aqui você pode gerar seu próprio JWT\n\nconst jwtToken = jwt.sign({ userId }, 'secret_key', { expiresIn: '1h' });\n\nres.status(200).json({ jwtToken });\n\n} catch (error) {\n\nres.status(400).send('Erro na autenticação');\n\n}\n\n});\n\napp.listen(5000, () => {\n\nconsole.log('Server is running on port 5000');\n\n});\n\nParte 3 – Validação do token no back-end (Node.js/Express)\n\nNa implementação do back-end, o token de autenticação do Google é validado e um JWT próprio é gerado para uso na aplicação.\n\nPasso 1: rodar o back-end\n\nPara rodar o back-end, executem o comando abaixo:\n\nnode server.js\n\nPasso 2: testar a aplicação\n\nAgora, vocês deverão clicar no botão, que deverá solicitar a autenticação do Google.\n\nGOOGLE CLOUD. Página inicial, [s.d.]. Disponível em: https://console.cloud.google.com/welcome/new. Acesso em: 25 mar. 2025.", "resposta": "O roteiro não traz uma resposta escrita pronta. A entrega solicitada é prática: siga o procedimento original e registre a sua execução.", "fonte": "Material/Front-End/3° bimestre/Semana 21 - Servicos de Autenticacao e Monitoramento/Atividade Pratica - Aula 1- Integracao com servicos de autenticacao - 47443.docx"}, "quizzes": []}
---
