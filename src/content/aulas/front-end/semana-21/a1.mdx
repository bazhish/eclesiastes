---
{"materia": "Front-End", "materiaSlug": "front-end", "bimestre": 3, "semana": 21, "ordem": 10, "aula": "a1", "titulo": "Serviços de Autenticação e Monitoramento · Aula 1", "atividade": {"titulo": "Atividade Prática - Aula 1: Integração de serviços de autenticação em uma aplicação front-end", "enunciado": "### Configuração necessária no Google Cloud Console\n\n1. Criar o projeto e configurar a tela de consentimento OAuth.\n2. Criar uma credencial do tipo **Aplicativo da Web**.\n3. Cadastrar `http://localhost:3000` como URI de redirecionamento autorizado no desenvolvimento.\n4. Copiar somente o `client_id` para a configuração do front-end.\n5. Manter o `client_secret` exclusivamente no servidor e usar variáveis de ambiente para todos os segredos.\n\n### Instalação\n\n```bash\nnpx create-react-app oauth-google-login\ncd oauth-google-login\nnpm install @react-oauth/google axios jwt-decode\nnpm start\n```\n\n### Variável de ambiente - `.env.local`\n\n```text\nREACT_APP_GOOGLE_CLIENT_ID=SEU_CLIENT_ID.apps.googleusercontent.com\nREACT_APP_API_URL=http://localhost:5000\n```\n\nO arquivo `.env.local` deve ficar no `.gitignore` quando contiver configurações específicas de desenvolvimento. O `client_id` não é segredo, mas o `client_secret` nunca deve ser incluído no front-end.\n\n### `src/index.js`\n\n```jsx\nimport React from 'react'\nimport ReactDOM from 'react-dom/client'\nimport { GoogleOAuthProvider } from '@react-oauth/google'\nimport App from './App'\n\nconst root = ReactDOM.createRoot(document.getElementById('root'))\nroot.render(\n  <GoogleOAuthProvider clientId={process.env.REACT_APP_GOOGLE_CLIENT_ID}>\n    <App />\n  </GoogleOAuthProvider>,\n)\n```\n\n### `src/App.js`\n\n```jsx\nimport { useState } from 'react'\nimport { GoogleLogin } from '@react-oauth/google'\nimport axios from 'axios'\n\nconst api = axios.create({ baseURL: process.env.REACT_APP_API_URL })\n\nexport default function App() {\n  const [usuario, setUsuario] = useState(null)\n  const [erro, setErro] = useState('')\n\n  async function aoLogar(respostaGoogle) {\n    try {\n      setErro('')\n      const resposta = await api.post('/api/auth/google', { credential: respostaGoogle.credential })\n      setUsuario(resposta.data.usuario)\n    } catch {\n      setErro('Não foi possível concluir a autenticação. Tente novamente.')\n    }\n  }\n\n  async function sair() {\n    await api.post('/api/auth/logout')\n    setUsuario(null)\n  }\n\n  return <main>\n    <h1>Gestão de projetos</h1>\n    {usuario ? <>\n      <p>Bem-vindo, {usuario.nome}.</p>\n      <button type=\"button\" onClick={sair}>Sair</button>\n    </> : <GoogleLogin onSuccess={aoLogar} onError={() => setErro('Falha no login com Google.')} />}\n    <p role=\"alert\">{erro}</p>\n  </main>\n}\n```\n\n### Servidor de validação - `server.js`\n\n```js\nrequire('dotenv').config()\nconst express = require('express')\nconst cookieParser = require('cookie-parser')\nconst { OAuth2Client } = require('google-auth-library')\nconst jwt = require('jsonwebtoken')\n\nconst app = express()\nconst client = new OAuth2Client(process.env.GOOGLE_CLIENT_ID)\napp.use(express.json())\napp.use(cookieParser())\n\napp.post('/api/auth/google', async (req, res) => {\n  try {\n    const ticket = await client.verifyIdToken({\n      idToken: req.body.credential,\n      audience: process.env.GOOGLE_CLIENT_ID,\n    })\n    const dados = ticket.getPayload()\n    const token = jwt.sign(\n      { sub: dados.sub, email: dados.email },\n      process.env.JWT_SECRET,\n      { expiresIn: '1h' },\n    )\n    res.cookie('sessao', token, { httpOnly: true, secure: process.env.NODE_ENV === 'production', sameSite: 'lax', maxAge: 3600000 })\n    res.json({ usuario: { nome: dados.name, email: dados.email } })\n  } catch {\n    res.status(401).json({ mensagem: 'Credencial Google inválida.' })\n  }\n})\n\napp.post('/api/auth/logout', (_, res) => {\n  res.clearCookie('sessao')\n  res.status(204).end()\n})\n\napp.listen(5000, () => console.log('API em http://localhost:5000'))\n```\n\n### Variáveis do servidor - `.env`\n\n```text\nGOOGLE_CLIENT_ID=SEU_CLIENT_ID.apps.googleusercontent.com\nJWT_SECRET=UMA_CHAVE_LONGA_ALEATORIA_E_SECRETA\n```\n\nO front-end recebe a credencial do Google e a envia ao back-end. O servidor valida a assinatura e o público do token com `verifyIdToken`, gera a sessão da aplicação e a mantém em cookie `httpOnly`. O uso de cookie reduz a exposição do token a scripts em caso de XSS. O `jwt-decode` pode ser usado para ler dados de um token, mas decodificar não é validar: a validação de segurança deve ocorrer no servidor.", "resposta": "### Configuração necessária no Google Cloud Console\n\n1. Criar o projeto e configurar a tela de consentimento OAuth.\n2. Criar uma credencial do tipo **Aplicativo da Web**.\n3. Cadastrar `http://localhost:3000` como URI de redirecionamento autorizado no desenvolvimento.\n4. Copiar somente o `client_id` para a configuração do front-end.\n5. Manter o `client_secret` exclusivamente no servidor e usar variáveis de ambiente para todos os segredos.\n\n### Instalação\n\n```bash\nnpx create-react-app oauth-google-login\ncd oauth-google-login\nnpm install @react-oauth/google axios jwt-decode\nnpm start\n```\n\n### Variável de ambiente - `.env.local`\n\n```text\nREACT_APP_GOOGLE_CLIENT_ID=SEU_CLIENT_ID.apps.googleusercontent.com\nREACT_APP_API_URL=http://localhost:5000\n```\n\nO arquivo `.env.local` deve ficar no `.gitignore` quando contiver configurações específicas de desenvolvimento. O `client_id` não é segredo, mas o `client_secret` nunca deve ser incluído no front-end.\n\n### `src/index.js`\n\n```jsx\nimport React from 'react'\nimport ReactDOM from 'react-dom/client'\nimport { GoogleOAuthProvider } from '@react-oauth/google'\nimport App from './App'\n\nconst root = ReactDOM.createRoot(document.getElementById('root'))\nroot.render(\n  <GoogleOAuthProvider clientId={process.env.REACT_APP_GOOGLE_CLIENT_ID}>\n    <App />\n  </GoogleOAuthProvider>,\n)\n```\n\n### `src/App.js`\n\n```jsx\nimport { useState } from 'react'\nimport { GoogleLogin } from '@react-oauth/google'\nimport axios from 'axios'\n\nconst api = axios.create({ baseURL: process.env.REACT_APP_API_URL })\n\nexport default function App() {\n  const [usuario, setUsuario] = useState(null)\n  const [erro, setErro] = useState('')\n\n  async function aoLogar(respostaGoogle) {\n    try {\n      setErro('')\n      const resposta = await api.post('/api/auth/google', { credential: respostaGoogle.credential })\n      setUsuario(resposta.data.usuario)\n    } catch {\n      setErro('Não foi possível concluir a autenticação. Tente novamente.')\n    }\n  }\n\n  async function sair() {\n    await api.post('/api/auth/logout')\n    setUsuario(null)\n  }\n\n  return <main>\n    <h1>Gestão de projetos</h1>\n    {usuario ? <>\n      <p>Bem-vindo, {usuario.nome}.</p>\n      <button type=\"button\" onClick={sair}>Sair</button>\n    </> : <GoogleLogin onSuccess={aoLogar} onError={() => setErro('Falha no login com Google.')} />}\n    <p role=\"alert\">{erro}</p>\n  </main>\n}\n```\n\n### Servidor de validação - `server.js`\n\n```js\nrequire('dotenv').config()\nconst express = require('express')\nconst cookieParser = require('cookie-parser')\nconst { OAuth2Client } = require('google-auth-library')\nconst jwt = require('jsonwebtoken')\n\nconst app = express()\nconst client = new OAuth2Client(process.env.GOOGLE_CLIENT_ID)\napp.use(express.json())\napp.use(cookieParser())\n\napp.post('/api/auth/google', async (req, res) => {\n  try {\n    const ticket = await client.verifyIdToken({\n      idToken: req.body.credential,\n      audience: process.env.GOOGLE_CLIENT_ID,\n    })\n    const dados = ticket.getPayload()\n    const token = jwt.sign(\n      { sub: dados.sub, email: dados.email },\n      process.env.JWT_SECRET,\n      { expiresIn: '1h' },\n    )\n    res.cookie('sessao', token, { httpOnly: true, secure: process.env.NODE_ENV === 'production', sameSite: 'lax', maxAge: 3600000 })\n    res.json({ usuario: { nome: dados.name, email: dados.email } })\n  } catch {\n    res.status(401).json({ mensagem: 'Credencial Google inválida.' })\n  }\n})\n\napp.post('/api/auth/logout', (_, res) => {\n  res.clearCookie('sessao')\n  res.status(204).end()\n})\n\napp.listen(5000, () => console.log('API em http://localhost:5000'))\n```\n\n### Variáveis do servidor - `.env`\n\n```text\nGOOGLE_CLIENT_ID=SEU_CLIENT_ID.apps.googleusercontent.com\nJWT_SECRET=UMA_CHAVE_LONGA_ALEATORIA_E_SECRETA\n```\n\nO front-end recebe a credencial do Google e a envia ao back-end. O servidor valida a assinatura e o público do token com `verifyIdToken`, gera a sessão da aplicação e a mantém em cookie `httpOnly`. O uso de cookie reduz a exposição do token a scripts em caso de XSS. O `jwt-decode` pode ser usado para ler dados de um token, mas decodificar não é validar: a validação de segurança deve ocorrer no servidor.", "conteudo": "### Configuração necessária no Google Cloud Console\n\n1. Criar o projeto e configurar a tela de consentimento OAuth.\n2. Criar uma credencial do tipo **Aplicativo da Web**.\n3. Cadastrar `http://localhost:3000` como URI de redirecionamento autorizado no desenvolvimento.\n4. Copiar somente o `client_id` para a configuração do front-end.\n5. Manter o `client_secret` exclusivamente no servidor e usar variáveis de ambiente para todos os segredos.\n\n### Instalação\n\n```bash\nnpx create-react-app oauth-google-login\ncd oauth-google-login\nnpm install @react-oauth/google axios jwt-decode\nnpm start\n```\n\n### Variável de ambiente - `.env.local`\n\n```text\nREACT_APP_GOOGLE_CLIENT_ID=SEU_CLIENT_ID.apps.googleusercontent.com\nREACT_APP_API_URL=http://localhost:5000\n```\n\nO arquivo `.env.local` deve ficar no `.gitignore` quando contiver configurações específicas de desenvolvimento. O `client_id` não é segredo, mas o `client_secret` nunca deve ser incluído no front-end.\n\n### `src/index.js`\n\n```jsx\nimport React from 'react'\nimport ReactDOM from 'react-dom/client'\nimport { GoogleOAuthProvider } from '@react-oauth/google'\nimport App from './App'\n\nconst root = ReactDOM.createRoot(document.getElementById('root'))\nroot.render(\n  <GoogleOAuthProvider clientId={process.env.REACT_APP_GOOGLE_CLIENT_ID}>\n    <App />\n  </GoogleOAuthProvider>,\n)\n```\n\n### `src/App.js`\n\n```jsx\nimport { useState } from 'react'\nimport { GoogleLogin } from '@react-oauth/google'\nimport axios from 'axios'\n\nconst api = axios.create({ baseURL: process.env.REACT_APP_API_URL })\n\nexport default function App() {\n  const [usuario, setUsuario] = useState(null)\n  const [erro, setErro] = useState('')\n\n  async function aoLogar(respostaGoogle) {\n    try {\n      setErro('')\n      const resposta = await api.post('/api/auth/google', { credential: respostaGoogle.credential })\n      setUsuario(resposta.data.usuario)\n    } catch {\n      setErro('Não foi possível concluir a autenticação. Tente novamente.')\n    }\n  }\n\n  async function sair() {\n    await api.post('/api/auth/logout')\n    setUsuario(null)\n  }\n\n  return <main>\n    <h1>Gestão de projetos</h1>\n    {usuario ? <>\n      <p>Bem-vindo, {usuario.nome}.</p>\n      <button type=\"button\" onClick={sair}>Sair</button>\n    </> : <GoogleLogin onSuccess={aoLogar} onError={() => setErro('Falha no login com Google.')} />}\n    <p role=\"alert\">{erro}</p>\n  </main>\n}\n```\n\n### Servidor de validação - `server.js`\n\n```js\nrequire('dotenv').config()\nconst express = require('express')\nconst cookieParser = require('cookie-parser')\nconst { OAuth2Client } = require('google-auth-library')\nconst jwt = require('jsonwebtoken')\n\nconst app = express()\nconst client = new OAuth2Client(process.env.GOOGLE_CLIENT_ID)\napp.use(express.json())\napp.use(cookieParser())\n\napp.post('/api/auth/google', async (req, res) => {\n  try {\n    const ticket = await client.verifyIdToken({\n      idToken: req.body.credential,\n      audience: process.env.GOOGLE_CLIENT_ID,\n    })\n    const dados = ticket.getPayload()\n    const token = jwt.sign(\n      { sub: dados.sub, email: dados.email },\n      process.env.JWT_SECRET,\n      { expiresIn: '1h' },\n    )\n    res.cookie('sessao', token, { httpOnly: true, secure: process.env.NODE_ENV === 'production', sameSite: 'lax', maxAge: 3600000 })\n    res.json({ usuario: { nome: dados.name, email: dados.email } })\n  } catch {\n    res.status(401).json({ mensagem: 'Credencial Google inválida.' })\n  }\n})\n\napp.post('/api/auth/logout', (_, res) => {\n  res.clearCookie('sessao')\n  res.status(204).end()\n})\n\napp.listen(5000, () => console.log('API em http://localhost:5000'))\n```\n\n### Variáveis do servidor - `.env`\n\n```text\nGOOGLE_CLIENT_ID=SEU_CLIENT_ID.apps.googleusercontent.com\nJWT_SECRET=UMA_CHAVE_LONGA_ALEATORIA_E_SECRETA\n```\n\nO front-end recebe a credencial do Google e a envia ao back-end. O servidor valida a assinatura e o público do token com `verifyIdToken`, gera a sessão da aplicação e a mantém em cookie `httpOnly`. O uso de cookie reduz a exposição do token a scripts em caso de XSS. O `jwt-decode` pode ser usado para ler dados de um token, mas decodificar não é validar: a validação de segurança deve ocorrer no servidor.", "fonte": "gabarito/Front-End/3° bimestre/Semana 21/semana-21-respostas.md#atividade-pratica-aula-1-integracao-de-servicos-de-autenticacao-em-uma-aplicacao-front-end", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
