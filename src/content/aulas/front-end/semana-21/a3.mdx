---
{"materia": "Front-End", "materiaSlug": "front-end", "bimestre": 3, "semana": 21, "ordem": 30, "aula": "a3", "titulo": "Serviços de Autenticação e Monitoramento · Aula 3", "atividade": {"titulo": "Aula 3 - Progressive Web Apps (PWA)", "enunciado": "Para o **Jornada Milhas**, a prioridade técnica seria corrigir os requisitos identificados pelo Lighthouse em uma sequência objetiva: primeiro ícones e manifesto válidos, depois cores e tema consistentes, em seguida desempenho mobile e, por último, revisão do cache offline.\n\n### `manifest.webmanifest`\n\n```json\n{\n  \"name\": \"Jornada Milhas\",\n  \"short_name\": \"Jornada\",\n  \"start_url\": \"/\",\n  \"display\": \"standalone\",\n  \"background_color\": \"#ffffff\",\n  \"theme_color\": \"#0b4f8a\",\n  \"icons\": [\n    { \"src\": \"/icons/icon-192.png\", \"sizes\": \"192x192\", \"type\": \"image/png\", \"purpose\": \"any maskable\" },\n    { \"src\": \"/icons/icon-512.png\", \"sizes\": \"512x512\", \"type\": \"image/png\", \"purpose\": \"any maskable\" }\n  ]\n}\n```\n\n### Registro do service worker\n\n```js\nif ('serviceWorker' in navigator) {\n  window.addEventListener('load', () => {\n    navigator.serviceWorker.register('/service-worker.js')\n      .catch((erro) => console.error('Falha ao registrar service worker:', erro))\n  })\n}\n```\n\n### `service-worker.js`\n\n```js\nconst CACHE = 'jornada-milhas-v1'\nconst ARQUIVOS_INICIAIS = ['/', '/offline.html', '/manifest.webmanifest']\n\nself.addEventListener('install', (evento) => {\n  evento.waitUntil(caches.open(CACHE).then((cache) => cache.addAll(ARQUIVOS_INICIAIS)))\n  self.skipWaiting()\n})\n\nself.addEventListener('activate', (evento) => {\n  evento.waitUntil(caches.keys().then((chaves) => Promise.all(chaves.filter((chave) => chave !== CACHE).map((chave) => caches.delete(chave)))))\n  self.clients.claim()\n})\n\nself.addEventListener('fetch', (evento) => {\n  if (evento.request.method !== 'GET') return\n  evento.respondWith(fetch(evento.request).catch(() => caches.match(evento.request).then((resposta) => resposta || caches.match('/offline.html'))))\n})\n```\n\nPara reduzir o carregamento em dispositivos móveis, eu comprimiria e dimensionaria imagens, aplicaria carregamento sob demanda fora da primeira tela, reduziria JavaScript não utilizado, usaria cache de arquivos estáticos com versões e mediria novamente no Lighthouse. A estratégia de cache precisa ser definida por tipo de conteúdo: arquivos estáticos podem ser armazenados por mais tempo; dados de viagem e pontos devem usar atualização de rede para evitar informações desatualizadas.\n\nPara manter a equipe engajada, as tarefas seriam divididas em itens pequenos com impacto visível: ícones e manifesto, contraste e tema, otimização da página inicial e testes offline. A cada etapa, o time revisaria o relatório Lighthouse e a experiência em um celular real. Isso torna o benefício concreto para os usuários, reduz o retrabalho e permite negociar com o gerente um escopo priorizado sem sacrificar requisitos básicos do PWA.", "resposta": "Para o **Jornada Milhas**, a prioridade técnica seria corrigir os requisitos identificados pelo Lighthouse em uma sequência objetiva: primeiro ícones e manifesto válidos, depois cores e tema consistentes, em seguida desempenho mobile e, por último, revisão do cache offline.\n\n### `manifest.webmanifest`\n\n```json\n{\n  \"name\": \"Jornada Milhas\",\n  \"short_name\": \"Jornada\",\n  \"start_url\": \"/\",\n  \"display\": \"standalone\",\n  \"background_color\": \"#ffffff\",\n  \"theme_color\": \"#0b4f8a\",\n  \"icons\": [\n    { \"src\": \"/icons/icon-192.png\", \"sizes\": \"192x192\", \"type\": \"image/png\", \"purpose\": \"any maskable\" },\n    { \"src\": \"/icons/icon-512.png\", \"sizes\": \"512x512\", \"type\": \"image/png\", \"purpose\": \"any maskable\" }\n  ]\n}\n```\n\n### Registro do service worker\n\n```js\nif ('serviceWorker' in navigator) {\n  window.addEventListener('load', () => {\n    navigator.serviceWorker.register('/service-worker.js')\n      .catch((erro) => console.error('Falha ao registrar service worker:', erro))\n  })\n}\n```\n\n### `service-worker.js`\n\n```js\nconst CACHE = 'jornada-milhas-v1'\nconst ARQUIVOS_INICIAIS = ['/', '/offline.html', '/manifest.webmanifest']\n\nself.addEventListener('install', (evento) => {\n  evento.waitUntil(caches.open(CACHE).then((cache) => cache.addAll(ARQUIVOS_INICIAIS)))\n  self.skipWaiting()\n})\n\nself.addEventListener('activate', (evento) => {\n  evento.waitUntil(caches.keys().then((chaves) => Promise.all(chaves.filter((chave) => chave !== CACHE).map((chave) => caches.delete(chave)))))\n  self.clients.claim()\n})\n\nself.addEventListener('fetch', (evento) => {\n  if (evento.request.method !== 'GET') return\n  evento.respondWith(fetch(evento.request).catch(() => caches.match(evento.request).then((resposta) => resposta || caches.match('/offline.html'))))\n})\n```\n\nPara reduzir o carregamento em dispositivos móveis, eu comprimiria e dimensionaria imagens, aplicaria carregamento sob demanda fora da primeira tela, reduziria JavaScript não utilizado, usaria cache de arquivos estáticos com versões e mediria novamente no Lighthouse. A estratégia de cache precisa ser definida por tipo de conteúdo: arquivos estáticos podem ser armazenados por mais tempo; dados de viagem e pontos devem usar atualização de rede para evitar informações desatualizadas.\n\nPara manter a equipe engajada, as tarefas seriam divididas em itens pequenos com impacto visível: ícones e manifesto, contraste e tema, otimização da página inicial e testes offline. A cada etapa, o time revisaria o relatório Lighthouse e a experiência em um celular real. Isso torna o benefício concreto para os usuários, reduz o retrabalho e permite negociar com o gerente um escopo priorizado sem sacrificar requisitos básicos do PWA.", "conteudo": "Para o **Jornada Milhas**, a prioridade técnica seria corrigir os requisitos identificados pelo Lighthouse em uma sequência objetiva: primeiro ícones e manifesto válidos, depois cores e tema consistentes, em seguida desempenho mobile e, por último, revisão do cache offline.\n\n### `manifest.webmanifest`\n\n```json\n{\n  \"name\": \"Jornada Milhas\",\n  \"short_name\": \"Jornada\",\n  \"start_url\": \"/\",\n  \"display\": \"standalone\",\n  \"background_color\": \"#ffffff\",\n  \"theme_color\": \"#0b4f8a\",\n  \"icons\": [\n    { \"src\": \"/icons/icon-192.png\", \"sizes\": \"192x192\", \"type\": \"image/png\", \"purpose\": \"any maskable\" },\n    { \"src\": \"/icons/icon-512.png\", \"sizes\": \"512x512\", \"type\": \"image/png\", \"purpose\": \"any maskable\" }\n  ]\n}\n```\n\n### Registro do service worker\n\n```js\nif ('serviceWorker' in navigator) {\n  window.addEventListener('load', () => {\n    navigator.serviceWorker.register('/service-worker.js')\n      .catch((erro) => console.error('Falha ao registrar service worker:', erro))\n  })\n}\n```\n\n### `service-worker.js`\n\n```js\nconst CACHE = 'jornada-milhas-v1'\nconst ARQUIVOS_INICIAIS = ['/', '/offline.html', '/manifest.webmanifest']\n\nself.addEventListener('install', (evento) => {\n  evento.waitUntil(caches.open(CACHE).then((cache) => cache.addAll(ARQUIVOS_INICIAIS)))\n  self.skipWaiting()\n})\n\nself.addEventListener('activate', (evento) => {\n  evento.waitUntil(caches.keys().then((chaves) => Promise.all(chaves.filter((chave) => chave !== CACHE).map((chave) => caches.delete(chave)))))\n  self.clients.claim()\n})\n\nself.addEventListener('fetch', (evento) => {\n  if (evento.request.method !== 'GET') return\n  evento.respondWith(fetch(evento.request).catch(() => caches.match(evento.request).then((resposta) => resposta || caches.match('/offline.html'))))\n})\n```\n\nPara reduzir o carregamento em dispositivos móveis, eu comprimiria e dimensionaria imagens, aplicaria carregamento sob demanda fora da primeira tela, reduziria JavaScript não utilizado, usaria cache de arquivos estáticos com versões e mediria novamente no Lighthouse. A estratégia de cache precisa ser definida por tipo de conteúdo: arquivos estáticos podem ser armazenados por mais tempo; dados de viagem e pontos devem usar atualização de rede para evitar informações desatualizadas.\n\nPara manter a equipe engajada, as tarefas seriam divididas em itens pequenos com impacto visível: ícones e manifesto, contraste e tema, otimização da página inicial e testes offline. A cada etapa, o time revisaria o relatório Lighthouse e a experiência em um celular real. Isso torna o benefício concreto para os usuários, reduz o retrabalho e permite negociar com o gerente um escopo priorizado sem sacrificar requisitos básicos do PWA.", "fonte": "gabarito/Front-End/3° bimestre/Semana 21/semana-21-respostas.md#aula-3-progressive-web-apps-pwa", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
