---
{"materia": "Front-End", "materiaSlug": "front-end", "bimestre": 3, "semana": 21, "ordem": 20, "aula": "a2", "titulo": "Serviços de Autenticação e Monitoramento · Aula 2", "atividade": {"titulo": "Atividade Prática - Aula 2: Implementação de monitoramento e logging em aplicações front-end", "enunciado": "### Instalação\n\n```bash\nnpx create-react-app monitoramento-app\ncd monitoramento-app\nnpm install loglevel @sentry/react\nnpm start\n```\n\n### Variável de ambiente - `.env.local`\n\n```text\nREACT_APP_SENTRY_DSN=SEU_DSN_AQUI\nREACT_APP_ENVIRONMENT=development\n```\n\n### `src/App.js`\n\n```jsx\nimport { useEffect } from 'react'\nimport * as Sentry from '@sentry/react'\nimport log from 'loglevel'\n\nlog.setLevel(process.env.NODE_ENV === 'production' ? 'warn' : 'info')\n\nif (process.env.REACT_APP_SENTRY_DSN) {\n  Sentry.init({\n    dsn: process.env.REACT_APP_SENTRY_DSN,\n    environment: process.env.REACT_APP_ENVIRONMENT,\n    tracesSampleRate: process.env.NODE_ENV === 'production' ? 0.1 : 1.0,\n  })\n}\n\nfunction App() {\n  useEffect(() => {\n    log.info('Aplicação iniciada')\n  }, [])\n\n  function simularErro() {\n    try {\n      throw new Error('Erro simulado para teste de monitoramento')\n    } catch (erro) {\n      log.error('Erro capturado:', erro)\n      Sentry.captureException(erro, { tags: { origem: 'botao-simular-erro' } })\n    }\n  }\n\n  return <main>\n    <h1>Aplicação com monitoramento e logging</h1>\n    <button type=\"button\" onClick={simularErro}>Simular erro</button>\n    <p>Verifique o console e, após configurar um DSN válido, o painel do Sentry.</p>\n  </main>\n}\n\nexport default Sentry.withProfiler(App)\n```\n\n### Relatório de monitoramento\n\n| Item | Resultado esperado após a execução | Como ajuda a equipe |\n| --- | --- | --- |\n| Inicialização | Mensagem `Aplicação iniciada` no console. | Confirma que o logging está ativo. |\n| Erro simulado | Registro `Erro capturado` no console. | Permite reproduzir e localizar o ponto em que a falha ocorreu. |\n| Sentry | Evento com mensagem, stack trace, ambiente e horário no painel, quando houver DSN válido. | Centraliza falhas e ajuda a priorizar correções. |\n| Alerta | Notificação configurada para erro crítico. | Reduz o tempo para identificar incidentes reais. |\n\nLogs devem evitar dados pessoais, senhas, tokens e informações sensíveis. Em produção, a amostragem de performance deve ser reduzida de acordo com o volume de usuários e a política de privacidade do projeto.", "resposta": "### Instalação\n\n```bash\nnpx create-react-app monitoramento-app\ncd monitoramento-app\nnpm install loglevel @sentry/react\nnpm start\n```\n\n### Variável de ambiente - `.env.local`\n\n```text\nREACT_APP_SENTRY_DSN=SEU_DSN_AQUI\nREACT_APP_ENVIRONMENT=development\n```\n\n### `src/App.js`\n\n```jsx\nimport { useEffect } from 'react'\nimport * as Sentry from '@sentry/react'\nimport log from 'loglevel'\n\nlog.setLevel(process.env.NODE_ENV === 'production' ? 'warn' : 'info')\n\nif (process.env.REACT_APP_SENTRY_DSN) {\n  Sentry.init({\n    dsn: process.env.REACT_APP_SENTRY_DSN,\n    environment: process.env.REACT_APP_ENVIRONMENT,\n    tracesSampleRate: process.env.NODE_ENV === 'production' ? 0.1 : 1.0,\n  })\n}\n\nfunction App() {\n  useEffect(() => {\n    log.info('Aplicação iniciada')\n  }, [])\n\n  function simularErro() {\n    try {\n      throw new Error('Erro simulado para teste de monitoramento')\n    } catch (erro) {\n      log.error('Erro capturado:', erro)\n      Sentry.captureException(erro, { tags: { origem: 'botao-simular-erro' } })\n    }\n  }\n\n  return <main>\n    <h1>Aplicação com monitoramento e logging</h1>\n    <button type=\"button\" onClick={simularErro}>Simular erro</button>\n    <p>Verifique o console e, após configurar um DSN válido, o painel do Sentry.</p>\n  </main>\n}\n\nexport default Sentry.withProfiler(App)\n```\n\n### Relatório de monitoramento\n\n| Item | Resultado esperado após a execução | Como ajuda a equipe |\n| --- | --- | --- |\n| Inicialização | Mensagem `Aplicação iniciada` no console. | Confirma que o logging está ativo. |\n| Erro simulado | Registro `Erro capturado` no console. | Permite reproduzir e localizar o ponto em que a falha ocorreu. |\n| Sentry | Evento com mensagem, stack trace, ambiente e horário no painel, quando houver DSN válido. | Centraliza falhas e ajuda a priorizar correções. |\n| Alerta | Notificação configurada para erro crítico. | Reduz o tempo para identificar incidentes reais. |\n\nLogs devem evitar dados pessoais, senhas, tokens e informações sensíveis. Em produção, a amostragem de performance deve ser reduzida de acordo com o volume de usuários e a política de privacidade do projeto.", "conteudo": "### Instalação\n\n```bash\nnpx create-react-app monitoramento-app\ncd monitoramento-app\nnpm install loglevel @sentry/react\nnpm start\n```\n\n### Variável de ambiente - `.env.local`\n\n```text\nREACT_APP_SENTRY_DSN=SEU_DSN_AQUI\nREACT_APP_ENVIRONMENT=development\n```\n\n### `src/App.js`\n\n```jsx\nimport { useEffect } from 'react'\nimport * as Sentry from '@sentry/react'\nimport log from 'loglevel'\n\nlog.setLevel(process.env.NODE_ENV === 'production' ? 'warn' : 'info')\n\nif (process.env.REACT_APP_SENTRY_DSN) {\n  Sentry.init({\n    dsn: process.env.REACT_APP_SENTRY_DSN,\n    environment: process.env.REACT_APP_ENVIRONMENT,\n    tracesSampleRate: process.env.NODE_ENV === 'production' ? 0.1 : 1.0,\n  })\n}\n\nfunction App() {\n  useEffect(() => {\n    log.info('Aplicação iniciada')\n  }, [])\n\n  function simularErro() {\n    try {\n      throw new Error('Erro simulado para teste de monitoramento')\n    } catch (erro) {\n      log.error('Erro capturado:', erro)\n      Sentry.captureException(erro, { tags: { origem: 'botao-simular-erro' } })\n    }\n  }\n\n  return <main>\n    <h1>Aplicação com monitoramento e logging</h1>\n    <button type=\"button\" onClick={simularErro}>Simular erro</button>\n    <p>Verifique o console e, após configurar um DSN válido, o painel do Sentry.</p>\n  </main>\n}\n\nexport default Sentry.withProfiler(App)\n```\n\n### Relatório de monitoramento\n\n| Item | Resultado esperado após a execução | Como ajuda a equipe |\n| --- | --- | --- |\n| Inicialização | Mensagem `Aplicação iniciada` no console. | Confirma que o logging está ativo. |\n| Erro simulado | Registro `Erro capturado` no console. | Permite reproduzir e localizar o ponto em que a falha ocorreu. |\n| Sentry | Evento com mensagem, stack trace, ambiente e horário no painel, quando houver DSN válido. | Centraliza falhas e ajuda a priorizar correções. |\n| Alerta | Notificação configurada para erro crítico. | Reduz o tempo para identificar incidentes reais. |\n\nLogs devem evitar dados pessoais, senhas, tokens e informações sensíveis. Em produção, a amostragem de performance deve ser reduzida de acordo com o volume de usuários e a política de privacidade do projeto.", "fonte": "gabarito/Front-End/3° bimestre/Semana 21/semana-21-respostas.md#atividade-pratica-aula-2-implementacao-de-monitoramento-e-logging-em-aplicacoes-front-end", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
