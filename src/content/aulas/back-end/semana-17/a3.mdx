---
{"materia": "Back-End", "materiaSlug": "back-end", "bimestre": 3, "semana": 17, "ordem": 30, "aula": "a3", "titulo": "Introducao aos Frameworks e Integracao · Aula 3", "atividade": {"enunciado": "Documente o controle de dependências com npm e pip e as práticas de atualização segura.", "resposta": "`package.json` e o lockfile registram dependências JavaScript e suas versões resolvidas; `requirements.txt` ou `pyproject.toml` registra dependências Python. Isso torna instalação reproduzível e evita que cada integrante use versões implícitas diferentes.\n\nAtualização segura exige verificar notas de versão, CVEs e compatibilidade, alterar poucas dependências por vez, executar testes, revisar o diff do lockfile e manter rollback possível. Não se instala pacote arbitrário em produção nem se ignora lockfile. Exemplo: `npm ci` instala a árvore travada; `python -m pip install -r requirements.txt` instala a lista Python. O arquivo de exemplo abaixo não executa instalação automaticamente.", "conteudo": "## Enunciado\n\nDocumente o controle de dependências com npm e pip e as práticas de atualização segura.\n\n## Resposta\n\n`package.json` e o lockfile registram dependências JavaScript e suas versões resolvidas; `requirements.txt` ou `pyproject.toml` registra dependências Python. Isso torna instalação reproduzível e evita que cada integrante use versões implícitas diferentes.\n\nAtualização segura exige verificar notas de versão, CVEs e compatibilidade, alterar poucas dependências por vez, executar testes, revisar o diff do lockfile e manter rollback possível. Não se instala pacote arbitrário em produção nem se ignora lockfile. Exemplo: `npm ci` instala a árvore travada; `python -m pip install -r requirements.txt` instala a lista Python. O arquivo de exemplo abaixo não executa instalação automaticamente.\n\n## Artefatos\n\nConsulte [`codigo/aula-3/package.json`](codigo/aula-3/package.json), [`codigo/aula-3/requirements.txt`](codigo/aula-3/requirements.txt) e [`codigo/aula-3/README.md`](codigo/aula-3/README.md).", "fonte": "gabarito/Back-End/3° bimestre/Semana 17 - Introducao aos Frameworks e Integracao/Gabarito - Roteiro Prático Aula 3.md", "artefato": {"nome": "Artefatos da Aula 3", "href": "artefatos/back-end/semana-17/a3.zip", "arquivos": 3}}, "quizzes": []}
---
