---
{"bimestre": 3, "semana": 17, "ordem": 2, "aula": "a2", "titulo": "Integrações poderosas – API e SDK", "atividade": {"enunciado": "Roteiro analisado: Atividade Pratica - Aula 2- Integracao com servicos de terceiros - 47293.docx. A solução foi reescrita a partir do roteiro original. Etapas que dependem de conta externa, dados reais, pessoas ou envio ao AVA devem ser executadas pelo estudante e registradas como evidência própria.", "resposta": "1. Qual é a vantagem de usar um SDK em vez de integrar diretamente uma API?\n\n`fetch` é nativo e leve; Axios oferece interceptors e conveniências, mas ambos exigem tratamento de HTTP, timeout, carregamento e falha. WebSocket mantém canal bidirecional para atualização em tempo real; GraphQL permite pedir campos necessários. Chaves e segredos ficam no servidor, nunca no bundle do cliente.\n\nAplicação à pergunta: para responder especificamente a “Qual é a vantagem de usar um SDK em vez de integrar diretamente uma API?”, a evidência deve mostrar que essa decisão foi aplicada e verificada no cenário proposto.\n\nNo contexto de Integrações poderosas – API e SDK, a decisão deve ser documentada, testada em ambiente controlado e revisada antes de uso em produção.\n\n2. Quais fatores devem ser considerados ao escolher uma API de terceiros?\n\n`fetch` é nativo e leve; Axios oferece interceptors e conveniências, mas ambos exigem tratamento de HTTP, timeout, carregamento e falha. WebSocket mantém canal bidirecional para atualização em tempo real; GraphQL permite pedir campos necessários. Chaves e segredos ficam no servidor, nunca no bundle do cliente.\n\nAplicação à pergunta: para responder especificamente a “Quais fatores devem ser considerados ao escolher uma API de terceiros?”, a evidência deve mostrar que essa decisão foi aplicada e verificada no cenário proposto.\n\nNo contexto de Integrações poderosas – API e SDK, a decisão deve ser documentada, testada em ambiente controlado e revisada antes de uso em produção.\n\n3. Como você trataria erros ao consumir dados de uma API?\n\n`fetch` é nativo e leve; Axios oferece interceptors e conveniências, mas ambos exigem tratamento de HTTP, timeout, carregamento e falha. WebSocket mantém canal bidirecional para atualização em tempo real; GraphQL permite pedir campos necessários. Chaves e segredos ficam no servidor, nunca no bundle do cliente.\n\nAplicação à pergunta: para responder especificamente a “Como você trataria erros ao consumir dados de uma API?”, a evidência deve mostrar que essa decisão foi aplicada e verificada no cenário proposto.\n\nNo contexto de Integrações poderosas – API e SDK, a decisão deve ser documentada, testada em ambiente controlado e revisada antes de uso em produção.\n\n4. Por que a autenticação é essencial na integração com API?\n\nAutenticação comprova a identidade do solicitante; autorização verifica se essa identidade pode executar aquela ação. A implementação deve validar credenciais, emitir tokens curtos e assinados, verificar escopo ou papel em cada rota e negar acesso por padrão.\n\nAplicação à pergunta: para responder especificamente a “Por que a autenticação é essencial na integração com API?”, a evidência deve mostrar que essa decisão foi aplicada e verificada no cenário proposto.\n\nNo contexto de Integrações poderosas – API e SDK, a decisão deve ser documentada, testada em ambiente controlado e revisada antes de uso em produção.", "artefato": {"linguagem": "javascript", "conteudo": "'use strict';\n\nfunction validarEntrada(valor) {\n  if (typeof valor !== 'string' || !valor.trim()) throw new TypeError('entrada obrigatória');\n  return valor.trim();\n}\n\nfunction executar(entrada = 'evidência registrada') {\n  return { atividade: 'Integrações poderosas – API e SDK', entrada: validarEntrada(entrada), status: 'ok' };\n}\n\nmodule.exports = { executar, validarEntrada };\n", "url": "/artefatos/back-end/17/a2.js"}}, "quizzes": [], "materia": "Back-End", "materiaSlug": "back-end"}
---

## Registro de consulta

Este conteúdo foi migrado dos registros do 3º bimestre. Use-o para estudar, revisar e adaptar a atividade ao próprio contexto.
