---
{"materia": "Back-End", "materiaSlug": "back-end", "bimestre": 3, "semana": 21, "ordem": 10, "aula": "a1", "titulo": "Gestao de Configuracoes e Monitoramento · Aula 1", "atividade": {"enunciado": "Defina gestão de configurações e segredos para desenvolvimento, homologação e produção, sem expor tokens, senhas ou certificados.", "resposta": "O código lê configurações não sensíveis por ambiente e recebe segredos somente em tempo de execução por Vault/Secrets Manager/Key Vault ou mecanismo equivalente. O repositório contém `.env.example`, nunca `.env` real. Produção usa identidade da aplicação com menor privilégio, rotação, expiração, auditoria de leitura e acesso separado por ambiente.\n\nURLs, nomes de recurso e flags podem ficar em configuração central versionada; token de pagamento, senha de banco e chave privada ficam em cofre. Alteração de configuração passa por revisão, registro e validação em homologação. Logs não exibem valores secretos.", "conteudo": "## Enunciado\n\nDefina gestão de configurações e segredos para desenvolvimento, homologação e produção, sem expor tokens, senhas ou certificados.\n\n## Resposta\n\nO código lê configurações não sensíveis por ambiente e recebe segredos somente em tempo de execução por Vault/Secrets Manager/Key Vault ou mecanismo equivalente. O repositório contém `.env.example`, nunca `.env` real. Produção usa identidade da aplicação com menor privilégio, rotação, expiração, auditoria de leitura e acesso separado por ambiente.\n\nURLs, nomes de recurso e flags podem ficar em configuração central versionada; token de pagamento, senha de banco e chave privada ficam em cofre. Alteração de configuração passa por revisão, registro e validação em homologação. Logs não exibem valores secretos.\n\n## Código e verificação\n\nExemplo seguro em [`codigo/aula-1/.env.example`](codigo/aula-1/.env.example), validação em [`codigo/aula-1/config.js`](codigo/aula-1/config.js) e [`README`](codigo/aula-1/README.md).", "fonte": "gabarito/Back-End/3° bimestre/Semana 21 - Gestao de Configuracoes e Monitoramento/Gabarito - Roteiro Prático Aula 1.md", "arquivos": [{"caminho": ".env.example", "linguagem": "text", "conteudo": "APP_ENV=homologacao\r\nAPI_BASE_URL=https://api-hml.exemplo.com\r\n# DATABASE_URL e PAGAMENTO_WEBHOOK_SECRET são fornecidos pelo cofre, nunca por este arquivo.\r\n"}, {"caminho": "README.md", "linguagem": "markdown", "conteudo": "# Execução\r\n\r\n- **Linguagem:** JavaScript ES Modules; Node.js 20+.\r\n- **Dependências:** nenhuma; não use credenciais reais.\r\n- **Execução:** `node test_config.js`.\r\n- **Teste:** `node test_config.js`.\r\n- **Resultado esperado:** aceita configuração pública e rejeita segredo ausente em produção..\r\n- **Limitação:** Não há cofre configurado; leitura é simulada..\r\n"}, {"caminho": "config.js", "linguagem": "javascript", "conteudo": "export function config(env){ if(!env.APP_ENV) throw Error(\"APP_ENV obrigatório\"); if(env.APP_ENV===\"producao\"&&!env.DATABASE_URL) throw Error(\"segredo ausente\"); return {ambiente:env.APP_ENV,url:env.API_BASE_URL}; }"}, {"caminho": "test_config.js", "linguagem": "javascript", "conteudo": "import assert from \"node:assert/strict\"; import {config} from \"./config.js\"; assert.equal(config({APP_ENV:\"homologacao\",API_BASE_URL:\"https://hml\"}).ambiente,\"homologacao\"); assert.throws(()=>config({APP_ENV:\"producao\"})); console.log(\"CONFIG_OK\");"}]}, "quizzes": [], "tipo": "roteiro"}
---
