---
{"materia": "Back-End", "materiaSlug": "back-end", "bimestre": 3, "semana": 15, "ordem": 20, "aula": "a2", "titulo": "Fundamentos de Servidores e Seguranca · Aula 2", "atividade": {"enunciado": "Projete uma API de pedidos segura, distinguindo autenticação, autorização e criptografia; implemente uma rota protegida.", "resposta": "A API usa HTTPS em produção, autenticação por token assinado com expiração curta e autorização por papel. `POST /pedidos` exige usuário autenticado; `DELETE /pedidos/:id` exige o papel `admin`. O token identifica o sujeito, mas não substitui a verificação de permissão do recurso. Senhas não são devolvidas nem registradas em logs; segredos ficam fora do repositório. Respostas de erro são genéricas ao cliente e detalhadas apenas em log interno.\n\nA validação de entrada rejeita corpo sem `produto` ou `quantidade` positiva. O exemplo local não emite JWT real: usa token de demonstração para tornar a regra de autenticação/autorização testável sem bibliotecas ou segredo de produção.", "conteudo": "## Enunciado\n\nProjete uma API de pedidos segura, distinguindo autenticação, autorização e criptografia; implemente uma rota protegida.\n\n## Resposta\n\nA API usa HTTPS em produção, autenticação por token assinado com expiração curta e autorização por papel. `POST /pedidos` exige usuário autenticado; `DELETE /pedidos/:id` exige o papel `admin`. O token identifica o sujeito, mas não substitui a verificação de permissão do recurso. Senhas não são devolvidas nem registradas em logs; segredos ficam fora do repositório. Respostas de erro são genéricas ao cliente e detalhadas apenas em log interno.\n\nA validação de entrada rejeita corpo sem `produto` ou `quantidade` positiva. O exemplo local não emite JWT real: usa token de demonstração para tornar a regra de autenticação/autorização testável sem bibliotecas ou segredo de produção.\n\n## Código e verificação\n\nVeja [`codigo/aula-2/api_segura.js`](codigo/aula-2/api_segura.js), [`codigo/aula-2/test_api_segura.js`](codigo/aula-2/test_api_segura.js) e o [`README`](codigo/aula-2/README.md).", "fonte": "gabarito/Back-End/3° bimestre/Semana 15 - Fundamentos de Servidores e Seguranca/Gabarito - Roteiro Prático Aula 2.md", "arquivos": [{"caminho": "README.md", "linguagem": "markdown", "conteudo": "# Execução\r\n\r\n- **Linguagem:** JavaScript ES Modules; Node.js 20+.\r\n- **Dependências:** nenhuma; não use credenciais reais.\r\n- **Execução:** `node test_api_segura.js`.\r\n- **Teste:** `node test_api_segura.js`.\r\n- **Resultado esperado:** testes de autenticação, autorização e validação passam..\r\n"}, {"caminho": "api_segura.js", "linguagem": "javascript", "conteudo": "export function autorizar({ token, papel }, pedido) {\r\n  if (token !== \"token-teste\") return { status: 401, erro: \"não autenticado\" };\r\n  if (!pedido?.produto || !Number.isInteger(pedido.quantidade) || pedido.quantidade < 1) return { status: 400, erro: \"dados inválidos\" };\r\n  if (pedido.acao === \"excluir\" && papel !== \"admin\") return { status: 403, erro: \"sem permissão\" };\r\n  return { status: 201, pedido: { produto: pedido.produto, quantidade: pedido.quantidade } };\r\n}\r\n"}, {"caminho": "test_api_segura.js", "linguagem": "javascript", "conteudo": "import assert from \"node:assert/strict\"; import { autorizar } from \"./api_segura.js\";\r\nassert.equal(autorizar({token:\"x\"},{produto:\"Livro\",quantidade:1}).status,401);\r\nassert.equal(autorizar({token:\"token-teste\",papel:\"usuario\"},{produto:\"Livro\",quantidade:1}).status,201);\r\nassert.equal(autorizar({token:\"token-teste\",papel:\"usuario\"},{produto:\"Livro\",quantidade:1,acao:\"excluir\"}).status,403);\r\nconsole.log(\"API_SEGURA_OK\");\r\n"}]}, "quizzes": [], "tipo": "roteiro"}
---
