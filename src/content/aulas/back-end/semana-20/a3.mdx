---
{"materia": "Back-End", "materiaSlug": "back-end", "bimestre": 3, "semana": 20, "ordem": 30, "aula": "a3", "titulo": "Arquitetura Serverless e Pagamentos · Aula 3", "atividade": {"enunciado": "Projete integração segura com um serviço de pagamento para consultas: criação/captura, webhook, status, transação, fraude e relatórios.", "resposta": "O backend cria intenção de pagamento no provedor e armazena apenas identificador, valor, moeda e estado; nunca recebe nem armazena número de cartão. A confirmação ocorre pelo **webhook assinado** do provedor, não pelo redirecionamento do navegador. A assinatura é validada com segredo guardado em cofre; o `eventId` é idempotente e a transição de estado é transacional.\n\nFluxo: `consulta.criada → pagamento.pendente → pagamento.confirmado → consulta.confirmada`; falha ou expiração mantém a consulta não confirmada e gera comunicação apropriada. Logs registram ID e estado, sem dados de pagamento. Relatórios usam dados agregados e alertas detectam tentativas repetidas/falhas anormais. A implementação local é uma simulação de webhook, sem PayPal/Stripe/Mercado Pago real.", "conteudo": "## Enunciado\n\nProjete integração segura com um serviço de pagamento para consultas: criação/captura, webhook, status, transação, fraude e relatórios.\n\n## Resposta\n\nO backend cria intenção de pagamento no provedor e armazena apenas identificador, valor, moeda e estado; nunca recebe nem armazena número de cartão. A confirmação ocorre pelo **webhook assinado** do provedor, não pelo redirecionamento do navegador. A assinatura é validada com segredo guardado em cofre; o `eventId` é idempotente e a transição de estado é transacional.\n\nFluxo: `consulta.criada → pagamento.pendente → pagamento.confirmado → consulta.confirmada`; falha ou expiração mantém a consulta não confirmada e gera comunicação apropriada. Logs registram ID e estado, sem dados de pagamento. Relatórios usam dados agregados e alertas detectam tentativas repetidas/falhas anormais. A implementação local é uma simulação de webhook, sem PayPal/Stripe/Mercado Pago real.\n\n## Código e verificação\n\nSimulação em [`codigo/aula-3/webhook_pagamento.js`](codigo/aula-3/webhook_pagamento.js), teste e [`README`](codigo/aula-3/README.md).", "fonte": "gabarito/Back-End/3° bimestre/Semana 20 - Arquitetura Serverless e Pagamentos/Gabarito - Roteiro Prático Aula 3.md", "arquivos": [{"caminho": "README.md", "linguagem": "markdown", "conteudo": "# Execução\r\n\r\n- **Linguagem:** JavaScript ES Modules; Node.js 20+.\r\n- **Dependências:** nenhuma; não use credenciais reais.\r\n- **Execução:** `node test_webhook_pagamento.js`.\r\n- **Teste:** `node test_webhook_pagamento.js`.\r\n- **Resultado esperado:** rejeita assinatura inválida, confirma evento aprovado e ignora duplicata..\r\n- **Limitação:** Simulação local; não processa dinheiro nem chama API externa..\r\n"}, {"caminho": "test_webhook_pagamento.js", "linguagem": "javascript", "conteudo": "import assert from \"node:assert/strict\"; import {processarWebhook} from \"./webhook_pagamento.js\"; assert.equal(processarWebhook({id:\"x\",assinaturaValida:false}).status,401); assert.equal(processarWebhook({id:\"x\",assinaturaValida:true,status:\"approved\"}).consulta,\"confirmada\"); assert.equal(processarWebhook({id:\"x\",assinaturaValida:true,status:\"approved\"}).duplicado,true); console.log(\"WEBHOOK_PAGAMENTO_OK\");"}, {"caminho": "webhook_pagamento.js", "linguagem": "javascript", "conteudo": "const vistos=new Set(); export function processarWebhook(evento){ if(!evento.assinaturaValida)return{status:401}; if(vistos.has(evento.id))return{status:200,duplicado:true}; vistos.add(evento.id); return evento.status===\"approved\"?{status:200,consulta:\"confirmada\"}:{status:202,consulta:\"pendente\"}; }"}]}, "quizzes": [], "tipo": "roteiro"}
---
