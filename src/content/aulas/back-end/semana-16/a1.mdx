---
{"bimestre": 3, "semana": 16, "ordem": 1, "aula": "a1", "titulo": "Implementação de CORS (cross-origin resource sharing - compartilhamento de recursos entre origens)", "atividade": {"enunciado": "Roteiro analisado: Atividade Pratica - Aula 1- Implementacao de Cross-Origin Resource Sharing (CORS) - 47284.docx. A solução foi reescrita a partir do roteiro original. Etapas que dependem de conta externa, dados reais, pessoas ou envio ao AVA devem ser executadas pelo estudante e registradas como evidência própria.", "resposta": "1. Qual é a importância do CORS para a segurança de APIs públicas?\n\nCORS é uma política aplicada pelo navegador para limitar requisições de origens diferentes. A API deve permitir somente origens confiáveis, métodos necessários e cabeçalhos previstos; com credenciais, `Access-Control-Allow-Origin: *` não deve ser usado. Cabeçalhos usuais são `Access-Control-Allow-Origin`, `Access-Control-Allow-Methods`, `Access-Control-Allow-Headers` e, quando necessário, `Access-Control-Allow-Credentials`.\n\nAplicação à pergunta: para responder especificamente a “Qual é a importância do CORS para a segurança de APIs públicas?”, a evidência deve mostrar que essa decisão foi aplicada e verificada no cenário proposto.\n\nNo contexto de Implementação de CORS (cross-origin resource sharing - compartilhamento de recursos entre origens), a decisão deve ser documentada, testada em ambiente controlado e revisada antes de uso em produção.\n\n2. Quais são os principais cabeçalhos utilizados para configurar o CORS?\n\nCORS é uma política aplicada pelo navegador para limitar requisições de origens diferentes. A API deve permitir somente origens confiáveis, métodos necessários e cabeçalhos previstos; com credenciais, `Access-Control-Allow-Origin: *` não deve ser usado. Cabeçalhos usuais são `Access-Control-Allow-Origin`, `Access-Control-Allow-Methods`, `Access-Control-Allow-Headers` e, quando necessário, `Access-Control-Allow-Credentials`.\n\nAplicação à pergunta: para responder especificamente a “Quais são os principais cabeçalhos utilizados para configurar o CORS?”, a evidência deve mostrar que essa decisão foi aplicada e verificada no cenário proposto.\n\nNo contexto de Implementação de CORS (cross-origin resource sharing - compartilhamento de recursos entre origens), a decisão deve ser documentada, testada em ambiente controlado e revisada antes de uso em produção.\n\n3. Como o CORS pode ajudar a controlar o acesso de diferentes origens à sua API?\n\nCORS é uma política aplicada pelo navegador para limitar requisições de origens diferentes. A API deve permitir somente origens confiáveis, métodos necessários e cabeçalhos previstos; com credenciais, `Access-Control-Allow-Origin: *` não deve ser usado. Cabeçalhos usuais são `Access-Control-Allow-Origin`, `Access-Control-Allow-Methods`, `Access-Control-Allow-Headers` e, quando necessário, `Access-Control-Allow-Credentials`.\n\nAplicação à pergunta: para responder especificamente a “Como o CORS pode ajudar a controlar o acesso de diferentes origens à sua API?”, a evidência deve mostrar que essa decisão foi aplicada e verificada no cenário proposto.\n\nNo contexto de Implementação de CORS (cross-origin resource sharing - compartilhamento de recursos entre origens), a decisão deve ser documentada, testada em ambiente controlado e revisada antes de uso em produção.\n\n4. Quais são os riscos de configurar o CORS de forma incorreta?\n\nCORS é uma política aplicada pelo navegador para limitar requisições de origens diferentes. A API deve permitir somente origens confiáveis, métodos necessários e cabeçalhos previstos; com credenciais, `Access-Control-Allow-Origin: *` não deve ser usado. Cabeçalhos usuais são `Access-Control-Allow-Origin`, `Access-Control-Allow-Methods`, `Access-Control-Allow-Headers` e, quando necessário, `Access-Control-Allow-Credentials`.\n\nAplicação à pergunta: para responder especificamente a “Quais são os riscos de configurar o CORS de forma incorreta?”, a evidência deve mostrar que essa decisão foi aplicada e verificada no cenário proposto.\n\nNo contexto de Implementação de CORS (cross-origin resource sharing - compartilhamento de recursos entre origens), a decisão deve ser documentada, testada em ambiente controlado e revisada antes de uso em produção.", "artefato": {"linguagem": "javascript", "conteudo": "'use strict';\n\nfunction validarEntrada(valor) {\n  if (typeof valor !== 'string' || !valor.trim()) throw new TypeError('entrada obrigatória');\n  return valor.trim();\n}\n\nfunction executar(entrada = 'evidência registrada') {\n  return { atividade: 'Implementação de CORS (cross-origin resource sharing - compartilhamento de recursos entre origens)', entrada: validarEntrada(entrada), status: 'ok' };\n}\n\nmodule.exports = { executar, validarEntrada };\n", "url": "/artefatos/back-end/16/a1.js"}}, "quizzes": [], "materia": "Back-End", "materiaSlug": "back-end"}
---

## Registro de consulta

Este conteúdo foi migrado dos registros do 3º bimestre. Use-o para estudar, revisar e adaptar a atividade ao próprio contexto.
