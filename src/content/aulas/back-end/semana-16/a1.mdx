---
{"materia": "Back-End", "materiaSlug": "back-end", "bimestre": 3, "semana": 16, "ordem": 10, "aula": "a1", "titulo": "Configuração e Segurança de APIs · Aula 1", "atividade": {"titulo": "Atividade Prática - Aula 1: Implementação de CORS", "enunciado": "CORS controla quais origens do navegador podem consumir uma API. Para parceiros autorizados, a API deve permitir origens específicas, métodos necessários e cabeçalhos mínimos, em vez de liberar `*` em endpoints autenticados. Os cabeçalhos principais são `Access-Control-Allow-Origin`, `Access-Control-Allow-Methods` e `Access-Control-Allow-Headers`.\n\nConfiguração incorreta pode permitir que sites não autorizados façam requisições pelo navegador em nome de usuários autenticados. CORS não substitui autenticação e autorização: ele é uma defesa do navegador, enquanto o servidor ainda deve validar tokens e permissões.", "resposta": "CORS controla quais origens do navegador podem consumir uma API. Para parceiros autorizados, a API deve permitir origens específicas, métodos necessários e cabeçalhos mínimos, em vez de liberar `*` em endpoints autenticados. Os cabeçalhos principais são `Access-Control-Allow-Origin`, `Access-Control-Allow-Methods` e `Access-Control-Allow-Headers`.\n\nConfiguração incorreta pode permitir que sites não autorizados façam requisições pelo navegador em nome de usuários autenticados. CORS não substitui autenticação e autorização: ele é uma defesa do navegador, enquanto o servidor ainda deve validar tokens e permissões.", "conteudo": "CORS controla quais origens do navegador podem consumir uma API. Para parceiros autorizados, a API deve permitir origens específicas, métodos necessários e cabeçalhos mínimos, em vez de liberar `*` em endpoints autenticados. Os cabeçalhos principais são `Access-Control-Allow-Origin`, `Access-Control-Allow-Methods` e `Access-Control-Allow-Headers`.\n\nConfiguração incorreta pode permitir que sites não autorizados façam requisições pelo navegador em nome de usuários autenticados. CORS não substitui autenticação e autorização: ele é uma defesa do navegador, enquanto o servidor ainda deve validar tokens e permissões.", "fonte": "gabarito/Back-End/3° bimestre/Semana 16/semana-16-respostas.md#atividade-pratica-aula-1-implementacao-de-cors", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
