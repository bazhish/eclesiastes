---
{"materia": "Back-End", "materiaSlug": "back-end", "bimestre": 3, "semana": 16, "ordem": 20, "aula": "a2", "titulo": "Configuração e Segurança de APIs · Aula 2", "atividade": {"titulo": "Atividade Prática - Aula 2: Segurança em autenticação e autorização", "enunciado": "Para o sistema bancário, o fluxo recomendado é OAuth 2.0 Authorization Code com PKCE, pois evita expor senha a aplicações clientes. Após autenticar, o servidor emite token de acesso JWT com `sub`, `roles` ou `scope`, emissor, público e expiração curta. A assinatura deve ser validada no servidor, com checagem de algoritmo, `iss`, `aud` e `exp`; o payload não deve conter segredos.\n\nUm endpoint de transações valida o token, identifica o usuário, confere escopo como `transactions:write`, aplica regras de propriedade da conta e registra auditoria. Complementos: HTTPS obrigatório, CORS por allowlist, MFA para operações sensíveis, rate limiting, rotação/revogação de tokens, logs sem dados sensíveis e validação de entrada.", "resposta": "Para o sistema bancário, o fluxo recomendado é OAuth 2.0 Authorization Code com PKCE, pois evita expor senha a aplicações clientes. Após autenticar, o servidor emite token de acesso JWT com `sub`, `roles` ou `scope`, emissor, público e expiração curta. A assinatura deve ser validada no servidor, com checagem de algoritmo, `iss`, `aud` e `exp`; o payload não deve conter segredos.\n\nUm endpoint de transações valida o token, identifica o usuário, confere escopo como `transactions:write`, aplica regras de propriedade da conta e registra auditoria. Complementos: HTTPS obrigatório, CORS por allowlist, MFA para operações sensíveis, rate limiting, rotação/revogação de tokens, logs sem dados sensíveis e validação de entrada.", "conteudo": "Para o sistema bancário, o fluxo recomendado é OAuth 2.0 Authorization Code com PKCE, pois evita expor senha a aplicações clientes. Após autenticar, o servidor emite token de acesso JWT com `sub`, `roles` ou `scope`, emissor, público e expiração curta. A assinatura deve ser validada no servidor, com checagem de algoritmo, `iss`, `aud` e `exp`; o payload não deve conter segredos.\n\nUm endpoint de transações valida o token, identifica o usuário, confere escopo como `transactions:write`, aplica regras de propriedade da conta e registra auditoria. Complementos: HTTPS obrigatório, CORS por allowlist, MFA para operações sensíveis, rate limiting, rotação/revogação de tokens, logs sem dados sensíveis e validação de entrada.", "fonte": "gabarito/Back-End/3° bimestre/Semana 16/semana-16-respostas.md#atividade-pratica-aula-2-seguranca-em-autenticacao-e-autorizacao", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
