---
{"materia": "Versionamento de Código", "materiaSlug": "versionamento-de-codigo", "bimestre": 3, "semana": 15, "ordem": 20, "aula": "a2", "titulo": "Git básico - repositório, commit e histórico · Aula 2", "atividade": {"titulo": "Atividade Prática - Aula 2: Visualização de histórico e reversão", "enunciado": "### Relatório de incidente - Bug Report & Fix\n\n**Sistema:** módulo de prontuário eletrônico.  \n**Incidente:** corrupção da função de cálculo de dosagem e exclusão de `layout.css`.  \n**Impacto:** os médicos não conseguiam concluir prescrições.\n\nCriei o repositório `modulo-prontuario`, registrei a versão estável de `calculo_dosagem.js` e, depois, simulei uma alteração incorreta e a exclusão do arquivo de layout. Para a auditoria, usei `git log --oneline` e localizei o hash do commit em que os dois arquivos estavam corretos. O valor `HASH_ESTAVEL` abaixo deve ser substituído pelo hash exibido no histórico real.\n\n```bash\nmkdir modulo-prontuario\ncd modulo-prontuario\ngit init\n\necho \"console.log('calculo correto');\" > calculo_dosagem.js\ngit add calculo_dosagem.js\ngit commit -m \"feat: implementa cálculo base de dosagem\"\n\necho \"body { font-family: Arial; }\" > layout.css\ngit add layout.css\ngit commit -m \"style: adiciona layout inicial\"\n\necho \"console.log('calculo incorreto');\" > calculo_dosagem.js\ngit add calculo_dosagem.js\ngit commit -m \"update: ajuste na função\"\n\ngit rm layout.css\ngit commit -m \"delete: remove arquivos antigos\"\ngit log --oneline\n\ngit restore --source HASH_ESTAVEL -- calculo_dosagem.js layout.css\ngit add calculo_dosagem.js layout.css\ngit commit -m \"fix: reverte alterações incorretas e restaura layout\"\n```\n\nO `git restore --source` recuperou somente os arquivos afetados, sem descartar alterações válidas do restante do projeto. O histórico do Git funcionou como uma rede de segurança: antes de qualquer reversão, é necessário identificar o commit correto e restaurar apenas o que foi comprometido.", "resposta": "### Relatório de incidente - Bug Report & Fix\n\n**Sistema:** módulo de prontuário eletrônico.  \n**Incidente:** corrupção da função de cálculo de dosagem e exclusão de `layout.css`.  \n**Impacto:** os médicos não conseguiam concluir prescrições.\n\nCriei o repositório `modulo-prontuario`, registrei a versão estável de `calculo_dosagem.js` e, depois, simulei uma alteração incorreta e a exclusão do arquivo de layout. Para a auditoria, usei `git log --oneline` e localizei o hash do commit em que os dois arquivos estavam corretos. O valor `HASH_ESTAVEL` abaixo deve ser substituído pelo hash exibido no histórico real.\n\n```bash\nmkdir modulo-prontuario\ncd modulo-prontuario\ngit init\n\necho \"console.log('calculo correto');\" > calculo_dosagem.js\ngit add calculo_dosagem.js\ngit commit -m \"feat: implementa cálculo base de dosagem\"\n\necho \"body { font-family: Arial; }\" > layout.css\ngit add layout.css\ngit commit -m \"style: adiciona layout inicial\"\n\necho \"console.log('calculo incorreto');\" > calculo_dosagem.js\ngit add calculo_dosagem.js\ngit commit -m \"update: ajuste na função\"\n\ngit rm layout.css\ngit commit -m \"delete: remove arquivos antigos\"\ngit log --oneline\n\ngit restore --source HASH_ESTAVEL -- calculo_dosagem.js layout.css\ngit add calculo_dosagem.js layout.css\ngit commit -m \"fix: reverte alterações incorretas e restaura layout\"\n```\n\nO `git restore --source` recuperou somente os arquivos afetados, sem descartar alterações válidas do restante do projeto. O histórico do Git funcionou como uma rede de segurança: antes de qualquer reversão, é necessário identificar o commit correto e restaurar apenas o que foi comprometido.", "conteudo": "### Relatório de incidente - Bug Report & Fix\n\n**Sistema:** módulo de prontuário eletrônico.  \n**Incidente:** corrupção da função de cálculo de dosagem e exclusão de `layout.css`.  \n**Impacto:** os médicos não conseguiam concluir prescrições.\n\nCriei o repositório `modulo-prontuario`, registrei a versão estável de `calculo_dosagem.js` e, depois, simulei uma alteração incorreta e a exclusão do arquivo de layout. Para a auditoria, usei `git log --oneline` e localizei o hash do commit em que os dois arquivos estavam corretos. O valor `HASH_ESTAVEL` abaixo deve ser substituído pelo hash exibido no histórico real.\n\n```bash\nmkdir modulo-prontuario\ncd modulo-prontuario\ngit init\n\necho \"console.log('calculo correto');\" > calculo_dosagem.js\ngit add calculo_dosagem.js\ngit commit -m \"feat: implementa cálculo base de dosagem\"\n\necho \"body { font-family: Arial; }\" > layout.css\ngit add layout.css\ngit commit -m \"style: adiciona layout inicial\"\n\necho \"console.log('calculo incorreto');\" > calculo_dosagem.js\ngit add calculo_dosagem.js\ngit commit -m \"update: ajuste na função\"\n\ngit rm layout.css\ngit commit -m \"delete: remove arquivos antigos\"\ngit log --oneline\n\ngit restore --source HASH_ESTAVEL -- calculo_dosagem.js layout.css\ngit add calculo_dosagem.js layout.css\ngit commit -m \"fix: reverte alterações incorretas e restaura layout\"\n```\n\nO `git restore --source` recuperou somente os arquivos afetados, sem descartar alterações válidas do restante do projeto. O histórico do Git funcionou como uma rede de segurança: antes de qualquer reversão, é necessário identificar o commit correto e restaurar apenas o que foi comprometido.", "fonte": "gabarito/Versionamento de Código/3° bimestre/Semana 15/semana-15-respostas.md#atividade-pratica-aula-2-visualizacao-de-historico-e-reversao", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
