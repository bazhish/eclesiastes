---
{"materia": "Versionamento de Código", "materiaSlug": "versionamento-de-codigo", "bimestre": 3, "semana": 15, "ordem": 20, "aula": "a2", "titulo": "Git Basico – repositorio, commit e historico · Aula 2", "atividade": {"enunciado": "Crie um repositório simulado do módulo de prontuário, introduza corrupção em `calculo_dosagem.js` e exclusão em `layout.css`, use o histórico e restaure somente os arquivos necessários. Entregue um relatório de incidente técnico.", "resposta": "## Enunciado\n\nCrie um repositório simulado do módulo de prontuário, introduza corrupção em `calculo_dosagem.js` e exclusão em `layout.css`, use o histórico e restaure somente os arquivos necessários. Entregue um relatório de incidente técnico.\n\n## Resposta\n\n## Relatório de incidente — recuperação de versão\n\n**Impacto:** a versão publicada de `calculo_dosagem.js` substituiu o cálculo estável por uma implementação incorreta; `layout.css` também foi removido em commit posterior.\n\n**Auditoria:** `git log --oneline` identifica o commit `feat: implementa cálculo base de dosagem` como a última versão estável do cálculo e `delete: remove arquivos antigos` como a exclusão do estilo.\n\n**Correção aplicada:** `git restore --source <hash-estavel> -- calculo_dosagem.js` recupera apenas o cálculo, sem descartar as demais mudanças. `git restore --source <hash-do-layout> -- layout.css` recupera o estilo. Depois de conferir `git diff` e executar os arquivos, foi criado o commit `fix: restaura cálculo de dosagem e layout removido por engano`.\n\n**Prevenção:** alterações clínicas precisam de revisão por pares, teste automatizado e mensagem de commit que descreva a mudança; recuperação de arquivo específico é preferível a `reset --hard` em incidente, pois preserva o restante do trabalho.\n\n## Código e verificação\n\nO cenário executável está em [`codigo/aula-2/executar_cenario.ps1`](codigo/aula-2/executar_cenario.ps1). O script cria um repositório local, produz os commits, restaura os dois arquivos pelo histórico e verifica o conteúdo recuperado. Consulte [`codigo/aula-2/README.md`](codigo/aula-2/README.md).", "conteudo": "## Enunciado\n\nCrie um repositório simulado do módulo de prontuário, introduza corrupção em `calculo_dosagem.js` e exclusão em `layout.css`, use o histórico e restaure somente os arquivos necessários. Entregue um relatório de incidente técnico.\n\n## Resposta\n\n## Relatório de incidente — recuperação de versão\n\n**Impacto:** a versão publicada de `calculo_dosagem.js` substituiu o cálculo estável por uma implementação incorreta; `layout.css` também foi removido em commit posterior.\n\n**Auditoria:** `git log --oneline` identifica o commit `feat: implementa cálculo base de dosagem` como a última versão estável do cálculo e `delete: remove arquivos antigos` como a exclusão do estilo.\n\n**Correção aplicada:** `git restore --source <hash-estavel> -- calculo_dosagem.js` recupera apenas o cálculo, sem descartar as demais mudanças. `git restore --source <hash-do-layout> -- layout.css` recupera o estilo. Depois de conferir `git diff` e executar os arquivos, foi criado o commit `fix: restaura cálculo de dosagem e layout removido por engano`.\n\n**Prevenção:** alterações clínicas precisam de revisão por pares, teste automatizado e mensagem de commit que descreva a mudança; recuperação de arquivo específico é preferível a `reset --hard` em incidente, pois preserva o restante do trabalho.\n\n## Código e verificação\n\nO cenário executável está em [`codigo/aula-2/executar_cenario.ps1`](codigo/aula-2/executar_cenario.ps1). O script cria um repositório local, produz os commits, restaura os dois arquivos pelo histórico e verifica o conteúdo recuperado. Consulte [`codigo/aula-2/README.md`](codigo/aula-2/README.md).", "fonte": "gabarito/Versionamento de Código/3° bimestre/Semana 15 - Git Basico – repositorio, commit e historico/Gabarito - Roteiro Prático Aula 2.md", "arquivos": [{"caminho": "README.md", "linguagem": "markdown", "conteudo": "# Execução — cenário Git\r\n\r\n- **Linguagem:** PowerShell 7+; Git 2.x.\r\n- **Dependências:** Git disponível no PATH; nenhuma instalação de pacote.\r\n- **Execução:** `pwsh -File executar_cenario.ps1`.\r\n- **Teste:** o próprio script confere a recuperação de `calculo_dosagem.js` e `layout.css`.\r\n- **Resultado esperado:** cinco commits exibidos e a linha `CENARIO_VALIDADO`.\r\n"}, {"caminho": "cenario-git/calculo_dosagem.js", "linguagem": "javascript", "conteudo": "console.log(\"cálculo correto\")\r\n"}, {"caminho": "cenario-git/layout.css", "linguagem": "css", "conteudo": "body { color: black; }\r\n"}, {"caminho": "executar_cenario.ps1", "linguagem": "powershell", "conteudo": "$ErrorActionPreference = \"Stop\"\r\n$base = Join-Path $PSScriptRoot \"cenario-git\"\r\nif (Test-Path -LiteralPath $base) { Remove-Item -LiteralPath $base -Recurse -Force }\r\nNew-Item -ItemType Directory -Path $base | Out-Null\r\nPush-Location $base\r\ntry {\r\n  git init -q; git config user.name \"Equipe de Manutenção\"; git config user.email \"equipe@example.test\"\r\n  Set-Content calculo_dosagem.js 'console.log(\"cálculo correto\")' -Encoding utf8\r\n  git add calculo_dosagem.js; git commit -qm \"feat: implementa cálculo base de dosagem\"; $estavel = git rev-parse HEAD\r\n  Set-Content layout.css 'body { color: black; }' -Encoding utf8\r\n  git add layout.css; git commit -qm \"style: adiciona layout\"; $layout = git rev-parse HEAD\r\n  Set-Content calculo_dosagem.js 'console.log(\"cálculo incorreto\")' -Encoding utf8\r\n  git add calculo_dosagem.js; git commit -qm \"update: ajuste na função\"\r\n  Remove-Item layout.css; git add -A; git commit -qm \"delete: remove arquivos antigos\"\r\n  git restore --source $estavel -- calculo_dosagem.js; git restore --source $layout -- layout.css\r\n  if ((Get-Content calculo_dosagem.js -Raw) -notmatch \"correto\" -or -not (Test-Path layout.css)) { throw \"Recuperação não confirmada.\" }\r\n  git add calculo_dosagem.js layout.css; git commit -qm \"fix: restaura cálculo de dosagem e layout removido por engano\"\r\n  git log --oneline -5; Write-Output \"CENARIO_VALIDADO\"\r\n} finally { Pop-Location }\r\n"}]}, "quizzes": [], "tipo": "roteiro"}
---
