---
{"materia": "Versionamento de Código", "materiaSlug": "versionamento-de-codigo", "bimestre": 3, "semana": 15, "ordem": 30, "aula": "a3", "titulo": "Git básico - repositório, commit e histórico · Aula 3", "atividade": {"titulo": "Atividade Prática - Aula 3: .gitignore e boas práticas", "enunciado": "### Manual de padronização de versionamento\n\nO `.gitignore` deve ser criado no início do projeto. Devem ser ignorados segredos, dependências instaladas localmente, logs, arquivos temporários e configurações específicas da máquina, pois não fazem parte do código-fonte, podem vazar dados e aumentam o repositório sem necessidade.\n\n```gitignore\n# Credenciais e variáveis locais\n.env\n.env.*\n*.pem\n*.key\n\n# Dependências e artefatos gerados\nnode_modules/\ndist/\nbuild/\ncoverage/\n\n# Logs e arquivos temporários\n*.log\ntmp/\ntemp/\n\n# Sistema operacional e editor\n.DS_Store\nThumbs.db\n.vscode/\n```\n\nSe um segredo já tiver sido enviado ao Git, adicioná-lo ao `.gitignore` não basta. É necessário removê-lo do rastreamento com `git rm --cached nome_do_arquivo`, trocar a credencial exposta e avaliar a limpeza do histórico.\n\nAs mensagens de commit devem ser claras e indicar a intenção da alteração. A equipe adotará o formato `tipo: verbo no imperativo + descrição`, por exemplo: `feat: adiciona agendamento de consultas`, `fix: corrige cálculo de dosagem`, `docs: atualiza instruções de instalação` e `chore: atualiza arquivo .gitignore`. Mensagens como “teste”, “arrumando” e “final” não devem ser usadas.\n\nA regra de atomicidade é: **um commit corresponde a uma tarefa lógica e completa**. Não devem ser misturados, no mesmo commit, correção de bug, alteração visual e documentação. Antes de confirmar, o desenvolvedor deve revisar `git status` e `git diff --staged`.", "resposta": "### Manual de padronização de versionamento\n\nO `.gitignore` deve ser criado no início do projeto. Devem ser ignorados segredos, dependências instaladas localmente, logs, arquivos temporários e configurações específicas da máquina, pois não fazem parte do código-fonte, podem vazar dados e aumentam o repositório sem necessidade.\n\n```gitignore\n# Credenciais e variáveis locais\n.env\n.env.*\n*.pem\n*.key\n\n# Dependências e artefatos gerados\nnode_modules/\ndist/\nbuild/\ncoverage/\n\n# Logs e arquivos temporários\n*.log\ntmp/\ntemp/\n\n# Sistema operacional e editor\n.DS_Store\nThumbs.db\n.vscode/\n```\n\nSe um segredo já tiver sido enviado ao Git, adicioná-lo ao `.gitignore` não basta. É necessário removê-lo do rastreamento com `git rm --cached nome_do_arquivo`, trocar a credencial exposta e avaliar a limpeza do histórico.\n\nAs mensagens de commit devem ser claras e indicar a intenção da alteração. A equipe adotará o formato `tipo: verbo no imperativo + descrição`, por exemplo: `feat: adiciona agendamento de consultas`, `fix: corrige cálculo de dosagem`, `docs: atualiza instruções de instalação` e `chore: atualiza arquivo .gitignore`. Mensagens como “teste”, “arrumando” e “final” não devem ser usadas.\n\nA regra de atomicidade é: **um commit corresponde a uma tarefa lógica e completa**. Não devem ser misturados, no mesmo commit, correção de bug, alteração visual e documentação. Antes de confirmar, o desenvolvedor deve revisar `git status` e `git diff --staged`.", "conteudo": "### Manual de padronização de versionamento\n\nO `.gitignore` deve ser criado no início do projeto. Devem ser ignorados segredos, dependências instaladas localmente, logs, arquivos temporários e configurações específicas da máquina, pois não fazem parte do código-fonte, podem vazar dados e aumentam o repositório sem necessidade.\n\n```gitignore\n# Credenciais e variáveis locais\n.env\n.env.*\n*.pem\n*.key\n\n# Dependências e artefatos gerados\nnode_modules/\ndist/\nbuild/\ncoverage/\n\n# Logs e arquivos temporários\n*.log\ntmp/\ntemp/\n\n# Sistema operacional e editor\n.DS_Store\nThumbs.db\n.vscode/\n```\n\nSe um segredo já tiver sido enviado ao Git, adicioná-lo ao `.gitignore` não basta. É necessário removê-lo do rastreamento com `git rm --cached nome_do_arquivo`, trocar a credencial exposta e avaliar a limpeza do histórico.\n\nAs mensagens de commit devem ser claras e indicar a intenção da alteração. A equipe adotará o formato `tipo: verbo no imperativo + descrição`, por exemplo: `feat: adiciona agendamento de consultas`, `fix: corrige cálculo de dosagem`, `docs: atualiza instruções de instalação` e `chore: atualiza arquivo .gitignore`. Mensagens como “teste”, “arrumando” e “final” não devem ser usadas.\n\nA regra de atomicidade é: **um commit corresponde a uma tarefa lógica e completa**. Não devem ser misturados, no mesmo commit, correção de bug, alteração visual e documentação. Antes de confirmar, o desenvolvedor deve revisar `git status` e `git diff --staged`.", "fonte": "gabarito/Versionamento de Código/3° bimestre/Semana 15/semana-15-respostas.md#atividade-pratica-aula-3-gitignore-e-boas-praticas", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
