---
{"materia": "Versionamento de Código", "materiaSlug": "versionamento-de-codigo", "bimestre": 3, "semana": 18, "ordem": 20, "aula": "a2", "titulo": "Tagging, releases e versionamento semântico · Aula 2", "atividade": {"titulo": "Atividade Prática - Aula 2: Versionamento semântico (SemVer)", "enunciado": "A versão inicial da API é `1.2.3`. A correção de arredondamento não altera o contrato público, portanto muda apenas PATCH: `1.2.4`. A inclusão de `calcular_desconto_fixo` é compatível com a API existente, então muda MINOR: `1.3.0`. A alteração de `calcular_desconto` para exigir `imposto` quebra chamadas antigas; por isso, muda MAJOR: `2.0.0`.\n\n```bash\ngit tag -a v2.0.0 -m \"release: versão 2.0.0 com novo contrato de desconto\"\ngit push origin v2.0.0\n```\n\nNão se deve alterar MAJOR sem necessidade porque isso informa uma quebra de compatibilidade e exige adaptação de quem consome a API. Uma versão errada pode causar falhas em sistemas integrados. SemVer comunica risco e facilita o planejamento de atualizações.", "resposta": "A versão inicial da API é `1.2.3`. A correção de arredondamento não altera o contrato público, portanto muda apenas PATCH: `1.2.4`. A inclusão de `calcular_desconto_fixo` é compatível com a API existente, então muda MINOR: `1.3.0`. A alteração de `calcular_desconto` para exigir `imposto` quebra chamadas antigas; por isso, muda MAJOR: `2.0.0`.\n\n```bash\ngit tag -a v2.0.0 -m \"release: versão 2.0.0 com novo contrato de desconto\"\ngit push origin v2.0.0\n```\n\nNão se deve alterar MAJOR sem necessidade porque isso informa uma quebra de compatibilidade e exige adaptação de quem consome a API. Uma versão errada pode causar falhas em sistemas integrados. SemVer comunica risco e facilita o planejamento de atualizações.", "conteudo": "A versão inicial da API é `1.2.3`. A correção de arredondamento não altera o contrato público, portanto muda apenas PATCH: `1.2.4`. A inclusão de `calcular_desconto_fixo` é compatível com a API existente, então muda MINOR: `1.3.0`. A alteração de `calcular_desconto` para exigir `imposto` quebra chamadas antigas; por isso, muda MAJOR: `2.0.0`.\n\n```bash\ngit tag -a v2.0.0 -m \"release: versão 2.0.0 com novo contrato de desconto\"\ngit push origin v2.0.0\n```\n\nNão se deve alterar MAJOR sem necessidade porque isso informa uma quebra de compatibilidade e exige adaptação de quem consome a API. Uma versão errada pode causar falhas em sistemas integrados. SemVer comunica risco e facilita o planejamento de atualizações.", "fonte": "gabarito/Versionamento de Código/3° bimestre/Semana 18/semana-18-respostas.md#atividade-pratica-aula-2-versionamento-semantico-semver", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
