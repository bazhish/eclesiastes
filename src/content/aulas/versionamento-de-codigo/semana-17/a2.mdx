---
{"materia": "Versionamento de Código", "materiaSlug": "versionamento-de-codigo", "bimestre": 3, "semana": 17, "ordem": 20, "aula": "a2", "titulo": "Colaboracao remota – Pull Request, Code Review · Aula 2", "atividade": {"enunciado": "Revise o código Python de cálculo de média, avalie correção, legibilidade e riscos, apresente versão melhorada e um comentário construtivo de Code Review.", "resposta": "O algoritmo original produz a média para uma lista não vazia, mas `s` é um nome pouco informativo e ocorre divisão por zero quando não há notas. A versão revisada valida a entrada e usa nomes que comunicam a intenção:\n\n```python\ndef calcular_media(notas: list[float]) -> float:\nif not notas:\nraise ValueError(\"A lista de notas não pode estar vazia.\")\nreturn sum(notas) / len(notas)\n```\n\nCom `[7, 8, 6, 10, 5]`, o resultado é `7.2`. Com `[]`, a função informa um erro explícito em vez de falhar com `ZeroDivisionError`.\n\n**Comentário de revisão:** “Sugestão: substituir `s` por uma função `calcular_media` com `sum` e validar lista vazia. Isso torna a regra clara e evita divisão por zero em entradas sem notas.”\n\nO objetivo do Code Review é elevar qualidade e compartilhar conhecimento; não é atribuir culpa.", "conteudo": "## Enunciado\n\nRevise o código Python de cálculo de média, avalie correção, legibilidade e riscos, apresente versão melhorada e um comentário construtivo de Code Review.\n\n## Resposta\n\nO algoritmo original produz a média para uma lista não vazia, mas `s` é um nome pouco informativo e ocorre divisão por zero quando não há notas. A versão revisada valida a entrada e usa nomes que comunicam a intenção:\n\n```python\ndef calcular_media(notas: list[float]) -> float:\n    if not notas:\n        raise ValueError(\"A lista de notas não pode estar vazia.\")\n    return sum(notas) / len(notas)\n```\n\nCom `[7, 8, 6, 10, 5]`, o resultado é `7.2`. Com `[]`, a função informa um erro explícito em vez de falhar com `ZeroDivisionError`.\n\n**Comentário de revisão:** “Sugestão: substituir `s` por uma função `calcular_media` com `sum` e validar lista vazia. Isso torna a regra clara e evita divisão por zero em entradas sem notas.”\n\nO objetivo do Code Review é elevar qualidade e compartilhar conhecimento; não é atribuir culpa.\n\n## Código e verificação\n\nArquivos executáveis: [`codigo/aula-2/media_notas.py`](codigo/aula-2/media_notas.py) e [`codigo/aula-2/test_media_notas.py`](codigo/aula-2/test_media_notas.py). Instruções em [`codigo/aula-2/README.md`](codigo/aula-2/README.md).", "fonte": "gabarito/Versionamento de Código/3° bimestre/Semana 17 - Colaboracao remota – Pull Request, Code Review/Gabarito - Roteiro Prático Aula 2.md", "artefato": {"nome": "Artefatos da Aula 2", "href": "artefatos/versionamento-de-codigo/semana-17/a2.zip", "arquivos": 5}}, "quizzes": []}
---
