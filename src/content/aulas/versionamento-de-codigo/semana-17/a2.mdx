---
{"materia": "Versionamento de Código", "materiaSlug": "versionamento-de-codigo", "bimestre": 3, "semana": 17, "ordem": 20, "aula": "a2", "titulo": "Colaboração remota - Pull Request e Code Review · Aula 2", "atividade": {"titulo": "Atividade Prática - Aula 2: Técnicas de Code Review", "enunciado": "O código calcula a média quando a lista possui notas, mas os nomes `s` e `i` reduzem a legibilidade e uma lista vazia causa divisão por zero. A versão revisada usa nomes claros, uma função reutilizável e validação de entrada.\n\n```python\ndef calcular_media(notas):\n    if not notas:\n        raise ValueError(\"A lista de notas não pode estar vazia.\")\n    return sum(notas) / len(notas)\n\nnotas_turma = [7, 8, 6, 10, 5]\nmedia_final = calcular_media(notas_turma)\nprint(f\"Média final: {media_final:.2f}\")\n```\n\nComentário de revisão: “Sugestão: substituir as variáveis `s` e `i` por nomes que expressem seu propósito e validar a lista vazia antes da divisão. Isso evita erro de execução e torna a manutenção mais simples.” O Code Review não serve para apontar culpados; ele reduz riscos, melhora padrões e dissemina conhecimento técnico.", "resposta": "O código calcula a média quando a lista possui notas, mas os nomes `s` e `i` reduzem a legibilidade e uma lista vazia causa divisão por zero. A versão revisada usa nomes claros, uma função reutilizável e validação de entrada.\n\n```python\ndef calcular_media(notas):\n    if not notas:\n        raise ValueError(\"A lista de notas não pode estar vazia.\")\n    return sum(notas) / len(notas)\n\nnotas_turma = [7, 8, 6, 10, 5]\nmedia_final = calcular_media(notas_turma)\nprint(f\"Média final: {media_final:.2f}\")\n```\n\nComentário de revisão: “Sugestão: substituir as variáveis `s` e `i` por nomes que expressem seu propósito e validar a lista vazia antes da divisão. Isso evita erro de execução e torna a manutenção mais simples.” O Code Review não serve para apontar culpados; ele reduz riscos, melhora padrões e dissemina conhecimento técnico.", "conteudo": "O código calcula a média quando a lista possui notas, mas os nomes `s` e `i` reduzem a legibilidade e uma lista vazia causa divisão por zero. A versão revisada usa nomes claros, uma função reutilizável e validação de entrada.\n\n```python\ndef calcular_media(notas):\n    if not notas:\n        raise ValueError(\"A lista de notas não pode estar vazia.\")\n    return sum(notas) / len(notas)\n\nnotas_turma = [7, 8, 6, 10, 5]\nmedia_final = calcular_media(notas_turma)\nprint(f\"Média final: {media_final:.2f}\")\n```\n\nComentário de revisão: “Sugestão: substituir as variáveis `s` e `i` por nomes que expressem seu propósito e validar a lista vazia antes da divisão. Isso evita erro de execução e torna a manutenção mais simples.” O Code Review não serve para apontar culpados; ele reduz riscos, melhora padrões e dissemina conhecimento técnico.", "fonte": "gabarito/Versionamento de Código/3° bimestre/Semana 17/semana-17-respostas.md#atividade-pratica-aula-2-tecnicas-de-code-review", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
