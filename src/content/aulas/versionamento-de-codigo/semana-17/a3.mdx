---
{"materia": "Versionamento de Código", "materiaSlug": "versionamento-de-codigo", "bimestre": 3, "semana": 17, "ordem": 30, "aula": "a3", "titulo": "Colaboração remota - Pull Request e Code Review · Aula 3", "atividade": {"titulo": "Atividade Prática - Aula 3: Resolvendo conflitos complexos", "enunciado": "O desenvolvedor A adicionou uma variável intermediária, impressão do total e retorno da variável. O desenvolvedor B adicionou a validação de valor negativo. Escolher apenas uma versão perderia uma funcionalidade; portanto, a resolução deve integrar as duas melhorias.\n\n```python\ndef calcular_total(valor, taxa):\n    if valor < 0:\n        return 0\n\n    total = valor + (valor * taxa)\n    print(\"Total calculado:\", total)\n    return total\n```\n\nJustificativa: a validação impede que valores negativos gerem cálculo inválido, e a variável `total` torna o resultado explícito antes da exibição e do retorno. O conflito foi resolvido editando os marcadores `<<<<<<<`, `=======` e `>>>>>>>`, executando testes e registrando a decisão.\n\nCommit sugerido: `fix: integra validação de valor e exibição do total`.  \nComentário de Pull Request: “Foram preservadas a validação para valores negativos e a exibição do total calculado. A função continua retornando o valor final e foi mantida uma estrutura legível para testes futuros.”\n\nConflitos são parte normal do trabalho paralelo. Um checklist com branches curtos, atualização frequente da branch base, commits atômicos e revisão antes do merge reduz a chance de conflitos difíceis.", "resposta": "O desenvolvedor A adicionou uma variável intermediária, impressão do total e retorno da variável. O desenvolvedor B adicionou a validação de valor negativo. Escolher apenas uma versão perderia uma funcionalidade; portanto, a resolução deve integrar as duas melhorias.\n\n```python\ndef calcular_total(valor, taxa):\n    if valor < 0:\n        return 0\n\n    total = valor + (valor * taxa)\n    print(\"Total calculado:\", total)\n    return total\n```\n\nJustificativa: a validação impede que valores negativos gerem cálculo inválido, e a variável `total` torna o resultado explícito antes da exibição e do retorno. O conflito foi resolvido editando os marcadores `<<<<<<<`, `=======` e `>>>>>>>`, executando testes e registrando a decisão.\n\nCommit sugerido: `fix: integra validação de valor e exibição do total`.  \nComentário de Pull Request: “Foram preservadas a validação para valores negativos e a exibição do total calculado. A função continua retornando o valor final e foi mantida uma estrutura legível para testes futuros.”\n\nConflitos são parte normal do trabalho paralelo. Um checklist com branches curtos, atualização frequente da branch base, commits atômicos e revisão antes do merge reduz a chance de conflitos difíceis.", "conteudo": "O desenvolvedor A adicionou uma variável intermediária, impressão do total e retorno da variável. O desenvolvedor B adicionou a validação de valor negativo. Escolher apenas uma versão perderia uma funcionalidade; portanto, a resolução deve integrar as duas melhorias.\n\n```python\ndef calcular_total(valor, taxa):\n    if valor < 0:\n        return 0\n\n    total = valor + (valor * taxa)\n    print(\"Total calculado:\", total)\n    return total\n```\n\nJustificativa: a validação impede que valores negativos gerem cálculo inválido, e a variável `total` torna o resultado explícito antes da exibição e do retorno. O conflito foi resolvido editando os marcadores `<<<<<<<`, `=======` e `>>>>>>>`, executando testes e registrando a decisão.\n\nCommit sugerido: `fix: integra validação de valor e exibição do total`.  \nComentário de Pull Request: “Foram preservadas a validação para valores negativos e a exibição do total calculado. A função continua retornando o valor final e foi mantida uma estrutura legível para testes futuros.”\n\nConflitos são parte normal do trabalho paralelo. Um checklist com branches curtos, atualização frequente da branch base, commits atômicos e revisão antes do merge reduz a chance de conflitos difíceis.", "fonte": "gabarito/Versionamento de Código/3° bimestre/Semana 17/semana-17-respostas.md#atividade-pratica-aula-3-resolvendo-conflitos-complexos", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
