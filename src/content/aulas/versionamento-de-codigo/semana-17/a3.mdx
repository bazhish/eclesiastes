---
{"materia": "Versionamento de Código", "materiaSlug": "versionamento-de-codigo", "bimestre": 3, "semana": 17, "ordem": 30, "aula": "a3", "titulo": "Colaboracao remota – Pull Request, Code Review · Aula 3", "atividade": {"enunciado": "Resolva o conflito de merge preservando validação de valor negativo, exibição do total e clareza do código; documente decisão, commit e comentário de Pull Request.", "resposta": "A versão final deve integrar as duas contribuições, não escolher apenas uma delas:\n\n```python\ndef calcular_total(valor: float, taxa: float) -> float:\nif valor < 0:\nreturn 0\ntotal = valor + (valor * taxa)\nprint(f\"Total calculado: {total}\")\nreturn total\n```\n\nA validação deve ocorrer antes do cálculo para impedir total negativo; a variável `total` mantém o valor calculado claro e permite a saída solicitada.\n\n**Commit:** `fix: resolve conflito preservando validação e exibição do total`.\n\n**Comentário no Pull Request:** “Integrei a validação para valores negativos e a variável `total` com mensagem de saída. Assim evitamos retorno indevido e preservamos a informação de diagnóstico. Incluí testes para valor positivo e negativo.”\n\nConflitos indicam edições concorrentes na mesma região, não falha pessoal. Um registro claro permite entender por que a combinação foi escolhida e facilita manutenção futura.", "conteudo": "## Enunciado\n\nResolva o conflito de merge preservando validação de valor negativo, exibição do total e clareza do código; documente decisão, commit e comentário de Pull Request.\n\n## Resposta\n\nA versão final deve integrar as duas contribuições, não escolher apenas uma delas:\n\n```python\ndef calcular_total(valor: float, taxa: float) -> float:\n    if valor < 0:\n        return 0\n    total = valor + (valor * taxa)\n    print(f\"Total calculado: {total}\")\n    return total\n```\n\nA validação deve ocorrer antes do cálculo para impedir total negativo; a variável `total` mantém o valor calculado claro e permite a saída solicitada.\n\n**Commit:** `fix: resolve conflito preservando validação e exibição do total`.\n\n**Comentário no Pull Request:** “Integrei a validação para valores negativos e a variável `total` com mensagem de saída. Assim evitamos retorno indevido e preservamos a informação de diagnóstico. Incluí testes para valor positivo e negativo.”\n\nConflitos indicam edições concorrentes na mesma região, não falha pessoal. Um registro claro permite entender por que a combinação foi escolhida e facilita manutenção futura.\n\n## Código e verificação\n\nA implementação e os testes estão em [`codigo/aula-3/calculo_total.py`](codigo/aula-3/calculo_total.py), [`codigo/aula-3/test_calculo_total.py`](codigo/aula-3/test_calculo_total.py) e [`codigo/aula-3/README.md`](codigo/aula-3/README.md).", "fonte": "gabarito/Versionamento de Código/3° bimestre/Semana 17 - Colaboracao remota – Pull Request, Code Review/Gabarito - Roteiro Prático Aula 3.md", "arquivos": [{"caminho": "README.md", "linguagem": "markdown", "conteudo": "# Execução — resolução de conflito\r\n\r\n- **Linguagem:** Python 3.11+.\n- **Dependências:** nenhuma.\n- **Execução:** `python -c \"from calculo_total import calcular_total; print(calcular_total(100, 0.1))\"`.\n- **Teste:** `python -m unittest test_calculo_total.py`.\n- **Resultado esperado:** valor 100 com taxa 0,1 retorna 110; valor negativo retorna 0.\r\n"}, {"caminho": "calculo_total.py", "linguagem": "python", "conteudo": "def calcular_total(valor: float, taxa: float) -> float:\r\n    if valor < 0:\r\n        return 0\r\n    total = valor + (valor * taxa)\r\n    print(f\"Total calculado: {total}\")\r\n    return total\r\n"}, {"caminho": "test_calculo_total.py", "linguagem": "python", "conteudo": "import unittest\r\nfrom calculo_total import calcular_total\r\n\r\nclass TestCalculoTotal(unittest.TestCase):\r\n    def test_total_positivo(self): self.assertEqual(calcular_total(100, 0.1), 110)\r\n    def test_valor_negativo(self): self.assertEqual(calcular_total(-1, 0.1), 0)\r\n\r\nif __name__ == \"__main__\": unittest.main()\r\n"}]}, "quizzes": [], "tipo": "roteiro"}
---
