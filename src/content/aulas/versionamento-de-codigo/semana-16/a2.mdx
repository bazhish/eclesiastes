---
{"materia": "Versionamento de Código", "materiaSlug": "versionamento-de-codigo", "bimestre": 3, "semana": 16, "ordem": 20, "aula": "a2", "titulo": "Branching e estrategias de fluxo (Git Flow, Trunk Based) · Aula 2", "atividade": {"enunciado": "Resolva os desafios de Git Flow: função de `main` e `develop`, origem de funcionalidades, preparação de release, hotfix em produção e boas práticas de commit.", "resposta": "**`main`** representa o código liberado em produção; **`develop`** integra o trabalho já aceito para a próxima versão. Alterações não entram diretamente em `main` porque precisam de revisão, teste e preparação controlada.\n\nUma funcionalidade nasce de `develop` em `feature/nome-da-funcionalidade`, é desenvolvida e testada isoladamente e retorna a `develop` por revisão. Essa separação preserva a estabilidade de produção.\n\nPara preparar uma entrega, cria-se `release/2.4.0` a partir de `develop`; nessa branch são permitidos ajustes finais, correções e atualização de versão, não novas funcionalidades. Após validação, ela é integrada em `main`, marcada com tag de release e também integrada de volta em `develop` se recebeu correções.\n\nUm erro crítico em produção exige `hotfix/corrige-login` criado a partir de `main`: corrigir, testar, integrar em `main`, publicar a tag corretiva e propagar a correção para `develop`. O hotfix não espera a próxima release porque usuários já estão afetados.\n\nCommits devem ser pequenos e descritivos, por exemplo `fix: impede login com sessão expirada`.", "conteudo": "## Enunciado\n\nResolva os desafios de Git Flow: função de `main` e `develop`, origem de funcionalidades, preparação de release, hotfix em produção e boas práticas de commit.\n\n## Resposta\n\n**`main`** representa o código liberado em produção; **`develop`** integra o trabalho já aceito para a próxima versão. Alterações não entram diretamente em `main` porque precisam de revisão, teste e preparação controlada.\n\nUma funcionalidade nasce de `develop` em `feature/nome-da-funcionalidade`, é desenvolvida e testada isoladamente e retorna a `develop` por revisão. Essa separação preserva a estabilidade de produção.\n\nPara preparar uma entrega, cria-se `release/2.4.0` a partir de `develop`; nessa branch são permitidos ajustes finais, correções e atualização de versão, não novas funcionalidades. Após validação, ela é integrada em `main`, marcada com tag de release e também integrada de volta em `develop` se recebeu correções.\n\nUm erro crítico em produção exige `hotfix/corrige-login` criado a partir de `main`: corrigir, testar, integrar em `main`, publicar a tag corretiva e propagar a correção para `develop`. O hotfix não espera a próxima release porque usuários já estão afetados.\n\nCommits devem ser pequenos e descritivos, por exemplo `fix: impede login com sessão expirada`.", "fonte": "gabarito/Versionamento de Código/3° bimestre/Semana 16 - Branching e estrategias de fluxo (Git Flow, Trunk Based)/Gabarito - Roteiro Prático Aula 2.md"}, "quizzes": []}
---
