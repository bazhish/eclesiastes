---
{"materia": "Versionamento de Código", "materiaSlug": "versionamento-de-codigo", "bimestre": 3, "semana": 16, "ordem": 20, "aula": "a2", "titulo": "Branching e estratégias de fluxo · Aula 2", "atividade": {"titulo": "Atividade Prática - Aula 2: Estratégia Git Flow", "enunciado": "No Git Flow, `main` representa a versão estável já publicada em produção. O branch `develop` concentra as funcionalidades que estão sendo integradas para a próxima versão. Nem toda alteração deve ir diretamente para `main`, pois esse branch precisa permanecer estável, testado e pronto para entrega.\n\nUma nova funcionalidade deve surgir de `develop` em um branch como `feature/agendamento-online`. Ela não deve ser criada diretamente em `main`, pois pode estar incompleta, gerar falhas e comprometer usuários em produção. Depois de pronta e revisada, a feature retorna para `develop` por merge ou Pull Request.\n\nPara preparar uma versão, a equipe cria um branch `release/1.2.0` a partir de `develop`. Nessa etapa são feitos testes finais, correções pequenas, atualização de versão e documentação. Quando a release estiver aprovada, ela é integrada em `main` e também em `develop`, evitando a perda de ajustes feitos durante a preparação.\n\nSe um erro crítico aparecer em produção, o fluxo correto é criar `hotfix/corrige-dosagem` a partir de `main`. A correção é testada, integrada em `main`, publicada e depois integrada em `develop`. Ela não deve aguardar a próxima release porque o sistema em produção já está impactando usuários.\n\n```bash\ngit switch develop\ngit switch -c feature/agendamento-online\n# desenvolver, testar e commitar\ngit switch develop\ngit merge --no-ff feature/agendamento-online\n\ngit switch -c release/1.2.0\n# testes finais e ajustes de release\ngit switch main\ngit merge --no-ff release/1.2.0\ngit tag -a v1.2.0 -m \"release: versão 1.2.0\"\ngit switch develop\ngit merge --no-ff release/1.2.0\n\ngit switch main\ngit switch -c hotfix/corrige-dosagem\n# corrigir e testar\ngit switch main\ngit merge --no-ff hotfix/corrige-dosagem\ngit switch develop\ngit merge --no-ff hotfix/corrige-dosagem\n```\n\nOs commits devem ser curtos e objetivos para facilitar revisão, auditoria e reversão. Feature flags permitem integrar código ainda não liberado sem expor a funcionalidade a todos os usuários, reduzindo o risco de uma entrega.", "resposta": "No Git Flow, `main` representa a versão estável já publicada em produção. O branch `develop` concentra as funcionalidades que estão sendo integradas para a próxima versão. Nem toda alteração deve ir diretamente para `main`, pois esse branch precisa permanecer estável, testado e pronto para entrega.\n\nUma nova funcionalidade deve surgir de `develop` em um branch como `feature/agendamento-online`. Ela não deve ser criada diretamente em `main`, pois pode estar incompleta, gerar falhas e comprometer usuários em produção. Depois de pronta e revisada, a feature retorna para `develop` por merge ou Pull Request.\n\nPara preparar uma versão, a equipe cria um branch `release/1.2.0` a partir de `develop`. Nessa etapa são feitos testes finais, correções pequenas, atualização de versão e documentação. Quando a release estiver aprovada, ela é integrada em `main` e também em `develop`, evitando a perda de ajustes feitos durante a preparação.\n\nSe um erro crítico aparecer em produção, o fluxo correto é criar `hotfix/corrige-dosagem` a partir de `main`. A correção é testada, integrada em `main`, publicada e depois integrada em `develop`. Ela não deve aguardar a próxima release porque o sistema em produção já está impactando usuários.\n\n```bash\ngit switch develop\ngit switch -c feature/agendamento-online\n# desenvolver, testar e commitar\ngit switch develop\ngit merge --no-ff feature/agendamento-online\n\ngit switch -c release/1.2.0\n# testes finais e ajustes de release\ngit switch main\ngit merge --no-ff release/1.2.0\ngit tag -a v1.2.0 -m \"release: versão 1.2.0\"\ngit switch develop\ngit merge --no-ff release/1.2.0\n\ngit switch main\ngit switch -c hotfix/corrige-dosagem\n# corrigir e testar\ngit switch main\ngit merge --no-ff hotfix/corrige-dosagem\ngit switch develop\ngit merge --no-ff hotfix/corrige-dosagem\n```\n\nOs commits devem ser curtos e objetivos para facilitar revisão, auditoria e reversão. Feature flags permitem integrar código ainda não liberado sem expor a funcionalidade a todos os usuários, reduzindo o risco de uma entrega.", "conteudo": "No Git Flow, `main` representa a versão estável já publicada em produção. O branch `develop` concentra as funcionalidades que estão sendo integradas para a próxima versão. Nem toda alteração deve ir diretamente para `main`, pois esse branch precisa permanecer estável, testado e pronto para entrega.\n\nUma nova funcionalidade deve surgir de `develop` em um branch como `feature/agendamento-online`. Ela não deve ser criada diretamente em `main`, pois pode estar incompleta, gerar falhas e comprometer usuários em produção. Depois de pronta e revisada, a feature retorna para `develop` por merge ou Pull Request.\n\nPara preparar uma versão, a equipe cria um branch `release/1.2.0` a partir de `develop`. Nessa etapa são feitos testes finais, correções pequenas, atualização de versão e documentação. Quando a release estiver aprovada, ela é integrada em `main` e também em `develop`, evitando a perda de ajustes feitos durante a preparação.\n\nSe um erro crítico aparecer em produção, o fluxo correto é criar `hotfix/corrige-dosagem` a partir de `main`. A correção é testada, integrada em `main`, publicada e depois integrada em `develop`. Ela não deve aguardar a próxima release porque o sistema em produção já está impactando usuários.\n\n```bash\ngit switch develop\ngit switch -c feature/agendamento-online\n# desenvolver, testar e commitar\ngit switch develop\ngit merge --no-ff feature/agendamento-online\n\ngit switch -c release/1.2.0\n# testes finais e ajustes de release\ngit switch main\ngit merge --no-ff release/1.2.0\ngit tag -a v1.2.0 -m \"release: versão 1.2.0\"\ngit switch develop\ngit merge --no-ff release/1.2.0\n\ngit switch main\ngit switch -c hotfix/corrige-dosagem\n# corrigir e testar\ngit switch main\ngit merge --no-ff hotfix/corrige-dosagem\ngit switch develop\ngit merge --no-ff hotfix/corrige-dosagem\n```\n\nOs commits devem ser curtos e objetivos para facilitar revisão, auditoria e reversão. Feature flags permitem integrar código ainda não liberado sem expor a funcionalidade a todos os usuários, reduzindo o risco de uma entrega.", "fonte": "gabarito/Versionamento de Código/3° bimestre/Semana 16/semana-16-respostas.md#atividade-pratica-aula-2-estrategia-git-flow", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
