---
{"materia": "Programação Mobile", "materiaSlug": "programacao-mobile", "bimestre": 3, "semana": 16, "ordem": 40, "aula": "a4", "titulo": "Acessibilidade e Usabilidade · Aula 4", "atividade": {"titulo": "Atividade Prática - Aula 4: Interface adaptativa", "enunciado": "No aplicativo de saúde, o layout deve reorganizar informações, não apenas diminuir tudo. Em smartphone, os campos ficam em uma coluna, botões ocupam a largura disponível e o texto conserva tamanho legível. Em tablet, os mesmos blocos podem ficar lado a lado, sem reduzir a área de toque.\n\n```kotlin\n@Composable\nfun HealthForm(windowWidth: Dp) {\n    val telaAmpla = windowWidth >= 600.dp\n    val conteudo: @Composable () -> Unit = {\n        OutlinedTextField(value = \"\", onValueChange = {}, label = { Text(\"Nome do paciente\") }, modifier = Modifier.fillMaxWidth())\n        OutlinedTextField(value = \"\", onValueChange = {}, label = { Text(\"Data da consulta\") }, modifier = Modifier.fillMaxWidth())\n    }\n    if (telaAmpla) Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.spacedBy(16.dp)) {\n        Box(Modifier.weight(1f)) { conteudo() }\n        Box(Modifier.weight(1f)) { Text(\"Resumo do agendamento\") }\n    } else Column(Modifier.fillMaxWidth().padding(16.dp), verticalArrangement = Arrangement.spacedBy(12.dp)) {\n        conteudo()\n        Button(onClick = {}, modifier = Modifier.fillMaxWidth().heightIn(min = 48.dp)) { Text(\"Confirmar agendamento\") }\n    }\n}\n```\n\n**Qual é a importância de um design responsivo em aplicativos mobile?**\n\nEle mantém o conteúdo compreensível, os controles utilizáveis e a navegação previsível em telefones e tablets. Uma interface que funciona somente em uma resolução exclui usuários e aumenta erros de uso.\n\n**Como as media queries ajudam a criar interfaces adaptativas?**\n\nElas permitem definir regras diferentes conforme largura, altura ou orientação. Assim, é possível usar uma coluna em telas pequenas e mais de uma coluna em telas amplas. Em interfaces nativas, a mesma ideia é aplicada com classes de tamanho de janela e layouts condicionais.\n\n**Por que é importante testar o aplicativo em vários dispositivos?**\n\nEmuladores ajudam, mas aparelhos reais revelam diferenças de densidade, tamanho, teclado, recortes de tela, desempenho e recursos de acessibilidade. Testar uma variedade reduz o risco de elementos cortados ou difíceis de tocar em dispositivos específicos.", "resposta": "No aplicativo de saúde, o layout deve reorganizar informações, não apenas diminuir tudo. Em smartphone, os campos ficam em uma coluna, botões ocupam a largura disponível e o texto conserva tamanho legível. Em tablet, os mesmos blocos podem ficar lado a lado, sem reduzir a área de toque.\n\n```kotlin\n@Composable\nfun HealthForm(windowWidth: Dp) {\n    val telaAmpla = windowWidth >= 600.dp\n    val conteudo: @Composable () -> Unit = {\n        OutlinedTextField(value = \"\", onValueChange = {}, label = { Text(\"Nome do paciente\") }, modifier = Modifier.fillMaxWidth())\n        OutlinedTextField(value = \"\", onValueChange = {}, label = { Text(\"Data da consulta\") }, modifier = Modifier.fillMaxWidth())\n    }\n    if (telaAmpla) Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.spacedBy(16.dp)) {\n        Box(Modifier.weight(1f)) { conteudo() }\n        Box(Modifier.weight(1f)) { Text(\"Resumo do agendamento\") }\n    } else Column(Modifier.fillMaxWidth().padding(16.dp), verticalArrangement = Arrangement.spacedBy(12.dp)) {\n        conteudo()\n        Button(onClick = {}, modifier = Modifier.fillMaxWidth().heightIn(min = 48.dp)) { Text(\"Confirmar agendamento\") }\n    }\n}\n```\n\n**Qual é a importância de um design responsivo em aplicativos mobile?**\n\nEle mantém o conteúdo compreensível, os controles utilizáveis e a navegação previsível em telefones e tablets. Uma interface que funciona somente em uma resolução exclui usuários e aumenta erros de uso.\n\n**Como as media queries ajudam a criar interfaces adaptativas?**\n\nElas permitem definir regras diferentes conforme largura, altura ou orientação. Assim, é possível usar uma coluna em telas pequenas e mais de uma coluna em telas amplas. Em interfaces nativas, a mesma ideia é aplicada com classes de tamanho de janela e layouts condicionais.\n\n**Por que é importante testar o aplicativo em vários dispositivos?**\n\nEmuladores ajudam, mas aparelhos reais revelam diferenças de densidade, tamanho, teclado, recortes de tela, desempenho e recursos de acessibilidade. Testar uma variedade reduz o risco de elementos cortados ou difíceis de tocar em dispositivos específicos.", "conteudo": "No aplicativo de saúde, o layout deve reorganizar informações, não apenas diminuir tudo. Em smartphone, os campos ficam em uma coluna, botões ocupam a largura disponível e o texto conserva tamanho legível. Em tablet, os mesmos blocos podem ficar lado a lado, sem reduzir a área de toque.\n\n```kotlin\n@Composable\nfun HealthForm(windowWidth: Dp) {\n    val telaAmpla = windowWidth >= 600.dp\n    val conteudo: @Composable () -> Unit = {\n        OutlinedTextField(value = \"\", onValueChange = {}, label = { Text(\"Nome do paciente\") }, modifier = Modifier.fillMaxWidth())\n        OutlinedTextField(value = \"\", onValueChange = {}, label = { Text(\"Data da consulta\") }, modifier = Modifier.fillMaxWidth())\n    }\n    if (telaAmpla) Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.spacedBy(16.dp)) {\n        Box(Modifier.weight(1f)) { conteudo() }\n        Box(Modifier.weight(1f)) { Text(\"Resumo do agendamento\") }\n    } else Column(Modifier.fillMaxWidth().padding(16.dp), verticalArrangement = Arrangement.spacedBy(12.dp)) {\n        conteudo()\n        Button(onClick = {}, modifier = Modifier.fillMaxWidth().heightIn(min = 48.dp)) { Text(\"Confirmar agendamento\") }\n    }\n}\n```\n\n**Qual é a importância de um design responsivo em aplicativos mobile?**\n\nEle mantém o conteúdo compreensível, os controles utilizáveis e a navegação previsível em telefones e tablets. Uma interface que funciona somente em uma resolução exclui usuários e aumenta erros de uso.\n\n**Como as media queries ajudam a criar interfaces adaptativas?**\n\nElas permitem definir regras diferentes conforme largura, altura ou orientação. Assim, é possível usar uma coluna em telas pequenas e mais de uma coluna em telas amplas. Em interfaces nativas, a mesma ideia é aplicada com classes de tamanho de janela e layouts condicionais.\n\n**Por que é importante testar o aplicativo em vários dispositivos?**\n\nEmuladores ajudam, mas aparelhos reais revelam diferenças de densidade, tamanho, teclado, recortes de tela, desempenho e recursos de acessibilidade. Testar uma variedade reduz o risco de elementos cortados ou difíceis de tocar em dispositivos específicos.", "fonte": "gabarito/Programação Mobile/3° bimestre/Semana 16/semana-16-respostas.md#atividade-pratica-aula-4-interface-adaptativa", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
