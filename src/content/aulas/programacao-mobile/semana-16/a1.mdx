---
{"materia": "Programação Mobile", "materiaSlug": "programacao-mobile", "bimestre": 3, "semana": 16, "ordem": 10, "aula": "a1", "titulo": "Acessibilidade e Usabilidade · Aula 1", "atividade": {"titulo": "Atividade Prática - Aula 1: Princípios de acessibilidade", "enunciado": "No aplicativo **Cozinha fácil**, os controles devem ter nomes claros para leitores de tela e comandos de voz. Em uma interface móvel nativa, a semântica equivalente aos rótulos ARIA é obtida com descrições de conteúdo. O estado de um elemento expansível também precisa ser informado.\n\n```kotlin\n@Composable\nfun RecipeActions(onProxima: () -> Unit, onSalvar: () -> Unit, onCompartilhar: () -> Unit) {\n    Row {\n        IconButton(onClick = onProxima) {\n            Icon(Icons.AutoMirrored.Filled.ArrowForward, contentDescription = \"Próxima receita\")\n        }\n        IconButton(onClick = onSalvar) {\n            Icon(Icons.Filled.Bookmark, contentDescription = \"Salvar receita\")\n        }\n        IconButton(onClick = onCompartilhar) {\n            Icon(Icons.Filled.Share, contentDescription = \"Compartilhar receita\")\n        }\n    }\n}\n\n@Composable\nfun IngredientesExpansiveis() {\n    var expandido by rememberSaveable { mutableStateOf(false) }\n    Button(onClick = { expandido = !expandido }) {\n        Text(if (expandido) \"Ocultar ingredientes\" else \"Mostrar ingredientes\")\n    }\n    if (expandido) Text(\"Lista de ingredientes da receita\")\n}\n```\n\n**Como os atributos ARIA podem melhorar a acessibilidade do aplicativo “Cozinha fácil”?**\n\nEm uma versão web do aplicativo, `aria-label` daria nome a botões somente com ícone, como “próxima receita”, “salvar receita” e “compartilhar”. `aria-expanded` informaria se uma seção de ingredientes está aberta ou fechada. Esses dados permitem que leitores de tela descrevam corretamente a função e o estado dos controles. Em Android e iOS nativos, o mesmo objetivo é alcançado com rótulos e descrições de conteúdo adequados.\n\n**Quais são os desafios ao se implementar navegação por voz em um aplicativo?**\n\nOs comandos precisam corresponder a rótulos claros e distintos; termos genéricos como “OK” ou “mais” geram ambiguidade. Também é necessário testar variações de fala, ruído, idioma, nomes parecidos e fluxos que exigem confirmação, para evitar ações involuntárias.\n\n**Quais ajustes você faria para melhorar a experiência de usuários que dependem de leitores de tela?**\n\nEu adicionaria descrições significativas a ícones e imagens, organizaria a ordem de foco, usaria componentes nativos sempre que possível, anunciaria mensagens de sucesso e erro e garantiria que campos tenham rótulo. Elementos decorativos não devem ser anunciados; ações críticas precisam de confirmação clara.\n\n**Por que é importante testar o aplicativo com usuários reais que possuem deficiências?**\n\nFerramentas e revisões técnicas não mostram todos os obstáculos do uso real. Pessoas que usam TalkBack, VoiceOver, comandos de voz ou recursos de ampliação revelam problemas de linguagem, foco, ordem de navegação e interação que podem passar despercebidos pela equipe.", "resposta": "No aplicativo **Cozinha fácil**, os controles devem ter nomes claros para leitores de tela e comandos de voz. Em uma interface móvel nativa, a semântica equivalente aos rótulos ARIA é obtida com descrições de conteúdo. O estado de um elemento expansível também precisa ser informado.\n\n```kotlin\n@Composable\nfun RecipeActions(onProxima: () -> Unit, onSalvar: () -> Unit, onCompartilhar: () -> Unit) {\n    Row {\n        IconButton(onClick = onProxima) {\n            Icon(Icons.AutoMirrored.Filled.ArrowForward, contentDescription = \"Próxima receita\")\n        }\n        IconButton(onClick = onSalvar) {\n            Icon(Icons.Filled.Bookmark, contentDescription = \"Salvar receita\")\n        }\n        IconButton(onClick = onCompartilhar) {\n            Icon(Icons.Filled.Share, contentDescription = \"Compartilhar receita\")\n        }\n    }\n}\n\n@Composable\nfun IngredientesExpansiveis() {\n    var expandido by rememberSaveable { mutableStateOf(false) }\n    Button(onClick = { expandido = !expandido }) {\n        Text(if (expandido) \"Ocultar ingredientes\" else \"Mostrar ingredientes\")\n    }\n    if (expandido) Text(\"Lista de ingredientes da receita\")\n}\n```\n\n**Como os atributos ARIA podem melhorar a acessibilidade do aplicativo “Cozinha fácil”?**\n\nEm uma versão web do aplicativo, `aria-label` daria nome a botões somente com ícone, como “próxima receita”, “salvar receita” e “compartilhar”. `aria-expanded` informaria se uma seção de ingredientes está aberta ou fechada. Esses dados permitem que leitores de tela descrevam corretamente a função e o estado dos controles. Em Android e iOS nativos, o mesmo objetivo é alcançado com rótulos e descrições de conteúdo adequados.\n\n**Quais são os desafios ao se implementar navegação por voz em um aplicativo?**\n\nOs comandos precisam corresponder a rótulos claros e distintos; termos genéricos como “OK” ou “mais” geram ambiguidade. Também é necessário testar variações de fala, ruído, idioma, nomes parecidos e fluxos que exigem confirmação, para evitar ações involuntárias.\n\n**Quais ajustes você faria para melhorar a experiência de usuários que dependem de leitores de tela?**\n\nEu adicionaria descrições significativas a ícones e imagens, organizaria a ordem de foco, usaria componentes nativos sempre que possível, anunciaria mensagens de sucesso e erro e garantiria que campos tenham rótulo. Elementos decorativos não devem ser anunciados; ações críticas precisam de confirmação clara.\n\n**Por que é importante testar o aplicativo com usuários reais que possuem deficiências?**\n\nFerramentas e revisões técnicas não mostram todos os obstáculos do uso real. Pessoas que usam TalkBack, VoiceOver, comandos de voz ou recursos de ampliação revelam problemas de linguagem, foco, ordem de navegação e interação que podem passar despercebidos pela equipe.", "conteudo": "No aplicativo **Cozinha fácil**, os controles devem ter nomes claros para leitores de tela e comandos de voz. Em uma interface móvel nativa, a semântica equivalente aos rótulos ARIA é obtida com descrições de conteúdo. O estado de um elemento expansível também precisa ser informado.\n\n```kotlin\n@Composable\nfun RecipeActions(onProxima: () -> Unit, onSalvar: () -> Unit, onCompartilhar: () -> Unit) {\n    Row {\n        IconButton(onClick = onProxima) {\n            Icon(Icons.AutoMirrored.Filled.ArrowForward, contentDescription = \"Próxima receita\")\n        }\n        IconButton(onClick = onSalvar) {\n            Icon(Icons.Filled.Bookmark, contentDescription = \"Salvar receita\")\n        }\n        IconButton(onClick = onCompartilhar) {\n            Icon(Icons.Filled.Share, contentDescription = \"Compartilhar receita\")\n        }\n    }\n}\n\n@Composable\nfun IngredientesExpansiveis() {\n    var expandido by rememberSaveable { mutableStateOf(false) }\n    Button(onClick = { expandido = !expandido }) {\n        Text(if (expandido) \"Ocultar ingredientes\" else \"Mostrar ingredientes\")\n    }\n    if (expandido) Text(\"Lista de ingredientes da receita\")\n}\n```\n\n**Como os atributos ARIA podem melhorar a acessibilidade do aplicativo “Cozinha fácil”?**\n\nEm uma versão web do aplicativo, `aria-label` daria nome a botões somente com ícone, como “próxima receita”, “salvar receita” e “compartilhar”. `aria-expanded` informaria se uma seção de ingredientes está aberta ou fechada. Esses dados permitem que leitores de tela descrevam corretamente a função e o estado dos controles. Em Android e iOS nativos, o mesmo objetivo é alcançado com rótulos e descrições de conteúdo adequados.\n\n**Quais são os desafios ao se implementar navegação por voz em um aplicativo?**\n\nOs comandos precisam corresponder a rótulos claros e distintos; termos genéricos como “OK” ou “mais” geram ambiguidade. Também é necessário testar variações de fala, ruído, idioma, nomes parecidos e fluxos que exigem confirmação, para evitar ações involuntárias.\n\n**Quais ajustes você faria para melhorar a experiência de usuários que dependem de leitores de tela?**\n\nEu adicionaria descrições significativas a ícones e imagens, organizaria a ordem de foco, usaria componentes nativos sempre que possível, anunciaria mensagens de sucesso e erro e garantiria que campos tenham rótulo. Elementos decorativos não devem ser anunciados; ações críticas precisam de confirmação clara.\n\n**Por que é importante testar o aplicativo com usuários reais que possuem deficiências?**\n\nFerramentas e revisões técnicas não mostram todos os obstáculos do uso real. Pessoas que usam TalkBack, VoiceOver, comandos de voz ou recursos de ampliação revelam problemas de linguagem, foco, ordem de navegação e interação que podem passar despercebidos pela equipe.", "fonte": "gabarito/Programação Mobile/3° bimestre/Semana 16/semana-16-respostas.md#atividade-pratica-aula-1-principios-de-acessibilidade", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
