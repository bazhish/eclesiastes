---
{"materia": "Programação Mobile", "materiaSlug": "programacao-mobile", "bimestre": 3, "semana": 15, "ordem": 30, "aula": "a3", "titulo": "Componentes Avançados de Interface · Aula 3", "atividade": {"titulo": "Atividade Prática - Aula 3: Gerenciamento de estados complexos", "enunciado": "Utilizei um estado centralizado para a lista de tarefas. As telas de lista, detalhes e resumo consultam a mesma fonte de verdade, portanto qualquer adição, remoção ou conclusão atualiza a interface sem recarregar a aplicação.\n\n```kotlin\ndata class Tarefa(val id: Long, val titulo: String, val concluida: Boolean = false)\n\nclass TarefasViewModel : ViewModel() {\n    private val _tarefas = MutableStateFlow<List<Tarefa>>(emptyList())\n    val tarefas: StateFlow<List<Tarefa>> = _tarefas.asStateFlow()\n\n    fun adicionar(titulo: String) {\n        if (titulo.isBlank()) return\n        _tarefas.update { it + Tarefa(id = System.currentTimeMillis(), titulo = titulo.trim()) }\n    }\n\n    fun alternarConclusao(id: Long) {\n        _tarefas.update { lista -> lista.map { tarefa -> if (tarefa.id == id) tarefa.copy(concluida = !tarefa.concluida) else tarefa } }\n    }\n\n    fun remover(id: Long) {\n        _tarefas.update { lista -> lista.filterNot { it.id == id } }\n    }\n}\n```\n\nAo concluir uma tarefa, a tela pode exibir uma mensagem breve, como “Tarefa concluída”, e a lista observada pelo Compose é recomposta automaticamente.\n\n**Qual é o principal benefício de utilizar um gerenciamento de estado centralizado em aplicativos complexos?**\n\nO principal benefício é ter uma fonte única e previsível para os dados compartilhados. Isso reduz divergências entre telas e facilita entender onde e como o estado é alterado.\n\n**Como o gerenciamento de estados pode afetar a performance de um aplicativo móvel?**\n\nUm estado mal dividido pode atualizar mais componentes do que o necessário, consumindo memória e processamento. Para evitar isso, é importante observar somente os dados necessários, manter o estado imutável quando apropriado e evitar objetos grandes ou cálculos pesados na interface.\n\n**Explique por que a sincronização de estado é crucial em aplicativos com múltiplas telas.**\n\nEla garante que uma alteração feita em uma tela apareça imediatamente nas outras. Em uma lista de tarefas, marcar um item como concluído deve atualizar a lista, a tela de detalhes e qualquer contador de pendências com o mesmo valor.\n\n**Quando seria mais apropriado usar um estado local em vez de um estado global?**\n\nO estado local é mais adequado para informações temporárias e restritas a um componente, como o texto digitado em um campo, a abertura de um menu ou a visibilidade de uma senha. Não há necessidade de centralizar dados que outra tela não usa.", "resposta": "Utilizei um estado centralizado para a lista de tarefas. As telas de lista, detalhes e resumo consultam a mesma fonte de verdade, portanto qualquer adição, remoção ou conclusão atualiza a interface sem recarregar a aplicação.\n\n```kotlin\ndata class Tarefa(val id: Long, val titulo: String, val concluida: Boolean = false)\n\nclass TarefasViewModel : ViewModel() {\n    private val _tarefas = MutableStateFlow<List<Tarefa>>(emptyList())\n    val tarefas: StateFlow<List<Tarefa>> = _tarefas.asStateFlow()\n\n    fun adicionar(titulo: String) {\n        if (titulo.isBlank()) return\n        _tarefas.update { it + Tarefa(id = System.currentTimeMillis(), titulo = titulo.trim()) }\n    }\n\n    fun alternarConclusao(id: Long) {\n        _tarefas.update { lista -> lista.map { tarefa -> if (tarefa.id == id) tarefa.copy(concluida = !tarefa.concluida) else tarefa } }\n    }\n\n    fun remover(id: Long) {\n        _tarefas.update { lista -> lista.filterNot { it.id == id } }\n    }\n}\n```\n\nAo concluir uma tarefa, a tela pode exibir uma mensagem breve, como “Tarefa concluída”, e a lista observada pelo Compose é recomposta automaticamente.\n\n**Qual é o principal benefício de utilizar um gerenciamento de estado centralizado em aplicativos complexos?**\n\nO principal benefício é ter uma fonte única e previsível para os dados compartilhados. Isso reduz divergências entre telas e facilita entender onde e como o estado é alterado.\n\n**Como o gerenciamento de estados pode afetar a performance de um aplicativo móvel?**\n\nUm estado mal dividido pode atualizar mais componentes do que o necessário, consumindo memória e processamento. Para evitar isso, é importante observar somente os dados necessários, manter o estado imutável quando apropriado e evitar objetos grandes ou cálculos pesados na interface.\n\n**Explique por que a sincronização de estado é crucial em aplicativos com múltiplas telas.**\n\nEla garante que uma alteração feita em uma tela apareça imediatamente nas outras. Em uma lista de tarefas, marcar um item como concluído deve atualizar a lista, a tela de detalhes e qualquer contador de pendências com o mesmo valor.\n\n**Quando seria mais apropriado usar um estado local em vez de um estado global?**\n\nO estado local é mais adequado para informações temporárias e restritas a um componente, como o texto digitado em um campo, a abertura de um menu ou a visibilidade de uma senha. Não há necessidade de centralizar dados que outra tela não usa.", "conteudo": "Utilizei um estado centralizado para a lista de tarefas. As telas de lista, detalhes e resumo consultam a mesma fonte de verdade, portanto qualquer adição, remoção ou conclusão atualiza a interface sem recarregar a aplicação.\n\n```kotlin\ndata class Tarefa(val id: Long, val titulo: String, val concluida: Boolean = false)\n\nclass TarefasViewModel : ViewModel() {\n    private val _tarefas = MutableStateFlow<List<Tarefa>>(emptyList())\n    val tarefas: StateFlow<List<Tarefa>> = _tarefas.asStateFlow()\n\n    fun adicionar(titulo: String) {\n        if (titulo.isBlank()) return\n        _tarefas.update { it + Tarefa(id = System.currentTimeMillis(), titulo = titulo.trim()) }\n    }\n\n    fun alternarConclusao(id: Long) {\n        _tarefas.update { lista -> lista.map { tarefa -> if (tarefa.id == id) tarefa.copy(concluida = !tarefa.concluida) else tarefa } }\n    }\n\n    fun remover(id: Long) {\n        _tarefas.update { lista -> lista.filterNot { it.id == id } }\n    }\n}\n```\n\nAo concluir uma tarefa, a tela pode exibir uma mensagem breve, como “Tarefa concluída”, e a lista observada pelo Compose é recomposta automaticamente.\n\n**Qual é o principal benefício de utilizar um gerenciamento de estado centralizado em aplicativos complexos?**\n\nO principal benefício é ter uma fonte única e previsível para os dados compartilhados. Isso reduz divergências entre telas e facilita entender onde e como o estado é alterado.\n\n**Como o gerenciamento de estados pode afetar a performance de um aplicativo móvel?**\n\nUm estado mal dividido pode atualizar mais componentes do que o necessário, consumindo memória e processamento. Para evitar isso, é importante observar somente os dados necessários, manter o estado imutável quando apropriado e evitar objetos grandes ou cálculos pesados na interface.\n\n**Explique por que a sincronização de estado é crucial em aplicativos com múltiplas telas.**\n\nEla garante que uma alteração feita em uma tela apareça imediatamente nas outras. Em uma lista de tarefas, marcar um item como concluído deve atualizar a lista, a tela de detalhes e qualquer contador de pendências com o mesmo valor.\n\n**Quando seria mais apropriado usar um estado local em vez de um estado global?**\n\nO estado local é mais adequado para informações temporárias e restritas a um componente, como o texto digitado em um campo, a abertura de um menu ou a visibilidade de uma senha. Não há necessidade de centralizar dados que outra tela não usa.", "fonte": "gabarito/Programação Mobile/3° bimestre/Semana 15/semana-15-respostas.md#atividade-pratica-aula-3-gerenciamento-de-estados-complexos", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
