---
{"materia": "Programação Mobile", "materiaSlug": "programacao-mobile", "bimestre": 3, "semana": 15, "ordem": 20, "aula": "a2", "titulo": "Componentes Avançados de Interface · Aula 2", "atividade": {"titulo": "Atividade Prática - Aula 2: Animações avançadas", "enunciado": "Para o aplicativo de receitas, aplicaria animações curtas e relacionadas à ação do usuário. Um cartão de receita pode expandir com efeito de mola; ao favoritar, o ícone recebe um pequeno rebote; na tela de detalhes, a imagem cresce suavemente e o texto entra lateralmente. No retorno à lista, um `fade-out` curto evita mudança brusca de contexto.\n\n```kotlin\n@Composable\nfun RecipeCard(nome: String, favorita: Boolean, onFavoritar: () -> Unit) {\n    var expandido by rememberSaveable { mutableStateOf(false) }\n    val escala by animateFloatAsState(\n        targetValue = if (favorita) 1.12f else 1f,\n        animationSpec = spring(dampingRatio = Spring.DampingRatioMediumBouncy),\n        label = \"escalaFavorito\",\n    )\n\n    Card(\n        modifier = Modifier.fillMaxWidth().animateContentSize(\n            animationSpec = spring(stiffness = Spring.StiffnessMediumLow),\n        ),\n    ) {\n        Column(Modifier.padding(16.dp)) {\n            Text(nome, style = MaterialTheme.typography.titleMedium)\n            IconButton(onClick = onFavoritar, modifier = Modifier.scale(escala)) {\n                Icon(\n                    imageVector = if (favorita) Icons.Filled.Favorite else Icons.Outlined.FavoriteBorder,\n                    contentDescription = if (favorita) \"Remover receita dos favoritos\" else \"Adicionar receita aos favoritos\",\n                )\n            }\n            TextButton(onClick = { expandido = !expandido }) { Text(if (expandido) \"Ocultar detalhes\" else \"Ver detalhes\") }\n            AnimatedVisibility(visible = expandido) { Text(\"Ingredientes e modo de preparo da receita.\") }\n        }\n    }\n}\n```\n\n**Qual é a importância das animações baseadas em Física para a experiência do usuário em um aplicativo móvel?**\n\nElas tornam o retorno visual mais natural, porque simulam aceleração, desaceleração e mola. Quando usadas com moderação, deixam claro que um item abriu, foi movido ou foi favoritado, reforçando a relação entre a ação e o resultado.\n\n**Como as transições personalizadas podem impactar a percepção de um usuário ao navegar pelo aplicativo?**\n\nElas preservam o contexto entre telas e fazem a navegação parecer contínua. Por exemplo, ampliar a imagem de uma receita ao abrir os detalhes mostra visualmente que o conteúdo detalhado pertence ao cartão selecionado.\n\n**Cite um exemplo de quando uma animação poderia ser excessiva e prejudicar a experiência do usuário.**\n\nSeria excessivo fazer todos os itens de uma lista girarem ou saltarem sempre que a tela é aberta. Isso aumenta distração, pode reduzir desempenho e dificulta o uso para pessoas sensíveis a movimento.\n\n**Qual é o benefício de utilizar animações que simulam o comportamento de mola ao interagir com elementos da interface?**\n\nO efeito de mola cria feedback imediato e perceptível, sem precisar de mensagem extra. Um rebote discreto no ícone de favorito confirma a ação de forma visual e torna a interação mais agradável.", "resposta": "Para o aplicativo de receitas, aplicaria animações curtas e relacionadas à ação do usuário. Um cartão de receita pode expandir com efeito de mola; ao favoritar, o ícone recebe um pequeno rebote; na tela de detalhes, a imagem cresce suavemente e o texto entra lateralmente. No retorno à lista, um `fade-out` curto evita mudança brusca de contexto.\n\n```kotlin\n@Composable\nfun RecipeCard(nome: String, favorita: Boolean, onFavoritar: () -> Unit) {\n    var expandido by rememberSaveable { mutableStateOf(false) }\n    val escala by animateFloatAsState(\n        targetValue = if (favorita) 1.12f else 1f,\n        animationSpec = spring(dampingRatio = Spring.DampingRatioMediumBouncy),\n        label = \"escalaFavorito\",\n    )\n\n    Card(\n        modifier = Modifier.fillMaxWidth().animateContentSize(\n            animationSpec = spring(stiffness = Spring.StiffnessMediumLow),\n        ),\n    ) {\n        Column(Modifier.padding(16.dp)) {\n            Text(nome, style = MaterialTheme.typography.titleMedium)\n            IconButton(onClick = onFavoritar, modifier = Modifier.scale(escala)) {\n                Icon(\n                    imageVector = if (favorita) Icons.Filled.Favorite else Icons.Outlined.FavoriteBorder,\n                    contentDescription = if (favorita) \"Remover receita dos favoritos\" else \"Adicionar receita aos favoritos\",\n                )\n            }\n            TextButton(onClick = { expandido = !expandido }) { Text(if (expandido) \"Ocultar detalhes\" else \"Ver detalhes\") }\n            AnimatedVisibility(visible = expandido) { Text(\"Ingredientes e modo de preparo da receita.\") }\n        }\n    }\n}\n```\n\n**Qual é a importância das animações baseadas em Física para a experiência do usuário em um aplicativo móvel?**\n\nElas tornam o retorno visual mais natural, porque simulam aceleração, desaceleração e mola. Quando usadas com moderação, deixam claro que um item abriu, foi movido ou foi favoritado, reforçando a relação entre a ação e o resultado.\n\n**Como as transições personalizadas podem impactar a percepção de um usuário ao navegar pelo aplicativo?**\n\nElas preservam o contexto entre telas e fazem a navegação parecer contínua. Por exemplo, ampliar a imagem de uma receita ao abrir os detalhes mostra visualmente que o conteúdo detalhado pertence ao cartão selecionado.\n\n**Cite um exemplo de quando uma animação poderia ser excessiva e prejudicar a experiência do usuário.**\n\nSeria excessivo fazer todos os itens de uma lista girarem ou saltarem sempre que a tela é aberta. Isso aumenta distração, pode reduzir desempenho e dificulta o uso para pessoas sensíveis a movimento.\n\n**Qual é o benefício de utilizar animações que simulam o comportamento de mola ao interagir com elementos da interface?**\n\nO efeito de mola cria feedback imediato e perceptível, sem precisar de mensagem extra. Um rebote discreto no ícone de favorito confirma a ação de forma visual e torna a interação mais agradável.", "conteudo": "Para o aplicativo de receitas, aplicaria animações curtas e relacionadas à ação do usuário. Um cartão de receita pode expandir com efeito de mola; ao favoritar, o ícone recebe um pequeno rebote; na tela de detalhes, a imagem cresce suavemente e o texto entra lateralmente. No retorno à lista, um `fade-out` curto evita mudança brusca de contexto.\n\n```kotlin\n@Composable\nfun RecipeCard(nome: String, favorita: Boolean, onFavoritar: () -> Unit) {\n    var expandido by rememberSaveable { mutableStateOf(false) }\n    val escala by animateFloatAsState(\n        targetValue = if (favorita) 1.12f else 1f,\n        animationSpec = spring(dampingRatio = Spring.DampingRatioMediumBouncy),\n        label = \"escalaFavorito\",\n    )\n\n    Card(\n        modifier = Modifier.fillMaxWidth().animateContentSize(\n            animationSpec = spring(stiffness = Spring.StiffnessMediumLow),\n        ),\n    ) {\n        Column(Modifier.padding(16.dp)) {\n            Text(nome, style = MaterialTheme.typography.titleMedium)\n            IconButton(onClick = onFavoritar, modifier = Modifier.scale(escala)) {\n                Icon(\n                    imageVector = if (favorita) Icons.Filled.Favorite else Icons.Outlined.FavoriteBorder,\n                    contentDescription = if (favorita) \"Remover receita dos favoritos\" else \"Adicionar receita aos favoritos\",\n                )\n            }\n            TextButton(onClick = { expandido = !expandido }) { Text(if (expandido) \"Ocultar detalhes\" else \"Ver detalhes\") }\n            AnimatedVisibility(visible = expandido) { Text(\"Ingredientes e modo de preparo da receita.\") }\n        }\n    }\n}\n```\n\n**Qual é a importância das animações baseadas em Física para a experiência do usuário em um aplicativo móvel?**\n\nElas tornam o retorno visual mais natural, porque simulam aceleração, desaceleração e mola. Quando usadas com moderação, deixam claro que um item abriu, foi movido ou foi favoritado, reforçando a relação entre a ação e o resultado.\n\n**Como as transições personalizadas podem impactar a percepção de um usuário ao navegar pelo aplicativo?**\n\nElas preservam o contexto entre telas e fazem a navegação parecer contínua. Por exemplo, ampliar a imagem de uma receita ao abrir os detalhes mostra visualmente que o conteúdo detalhado pertence ao cartão selecionado.\n\n**Cite um exemplo de quando uma animação poderia ser excessiva e prejudicar a experiência do usuário.**\n\nSeria excessivo fazer todos os itens de uma lista girarem ou saltarem sempre que a tela é aberta. Isso aumenta distração, pode reduzir desempenho e dificulta o uso para pessoas sensíveis a movimento.\n\n**Qual é o benefício de utilizar animações que simulam o comportamento de mola ao interagir com elementos da interface?**\n\nO efeito de mola cria feedback imediato e perceptível, sem precisar de mensagem extra. Um rebote discreto no ícone de favorito confirma a ação de forma visual e torna a interação mais agradável.", "fonte": "gabarito/Programação Mobile/3° bimestre/Semana 15/semana-15-respostas.md#atividade-pratica-aula-2-animacoes-avancadas", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
