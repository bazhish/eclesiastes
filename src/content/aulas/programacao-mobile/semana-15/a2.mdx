---
{"bimestre": 3, "semana": 15, "ordem": 2, "aula": "a2", "titulo": "Gerenciamento de estados complexos", "atividade": {"enunciado": "Roteiro analisado: Atividade Pratica - Aula 3- Gerenciamento de estados complexos - 47335.docx. A solução foi reescrita a partir do roteiro original. Etapas que dependem de conta externa, dados reais, pessoas ou envio ao AVA devem ser executadas pelo estudante e registradas como evidência própria.", "resposta": "1. Qual é o principal benefício de utilizar um gerenciamento de estado centralizado em aplicativos complexos?\n\nRedux é apropriado para estado compartilhado e previsível, não para todo estado local. Actions descrevem eventos, reducers são puros e efeitos assíncronos ficam em thunk, saga ou camada equivalente. Seletoras e testes evitam acoplamento da interface à estrutura interna.\n\nAplicação à pergunta: para responder especificamente a “Qual é o principal benefício de utilizar um gerenciamento de estado centralizado em aplicativos complexos?”, a evidência deve mostrar que essa decisão foi aplicada e verificada no cenário proposto.\n\nNo contexto de Gerenciamento de estados complexos, a decisão deve ser documentada, testada em ambiente controlado e revisada antes de uso em produção.\n\n2. Como o gerenciamento de estados pode afetar a performance de um aplicativo móvel?\n\nA solução deve ser medida e reversível: use `EXPLAIN` para validar índices, mantenha backup testado com retenção, valide migração por contagens e checksums e prepare rollback. Índices seguem consultas frequentes e seletivas; índices em excesso também degradam escrita.\n\nAplicação à pergunta: para responder especificamente a “Como o gerenciamento de estados pode afetar a performance de um aplicativo móvel?”, a evidência deve mostrar que essa decisão foi aplicada e verificada no cenário proposto.\n\nNo contexto de Gerenciamento de estados complexos, a decisão deve ser documentada, testada em ambiente controlado e revisada antes de uso em produção.\n\n3. Quando seria mais apropriado usar um estado local em vez de um estado global?\n\nRedux é apropriado para estado compartilhado e previsível, não para todo estado local. Actions descrevem eventos, reducers são puros e efeitos assíncronos ficam em thunk, saga ou camada equivalente. Seletoras e testes evitam acoplamento da interface à estrutura interna.\n\nAplicação à pergunta: para responder especificamente a “Quando seria mais apropriado usar um estado local em vez de um estado global?”, a evidência deve mostrar que essa decisão foi aplicada e verificada no cenário proposto.\n\nNo contexto de Gerenciamento de estados complexos, a decisão deve ser documentada, testada em ambiente controlado e revisada antes de uso em produção.", "artefato": {"linguagem": "jsx", "conteudo": "import { useState } from 'react';\n\nexport default function Atividade() {\n  const [status, setStatus] = useState('Aguardando validação');\n  return (\n    <main>\n      <h1>Gerenciamento de estados complexos</h1>\n      <p aria-live=\"polite\">{status}</p>\n      <button type=\"button\" onClick={() => setStatus('Evidência registrada em ambiente controlado')}>\n        Registrar evidência\n      </button>\n    </main>\n  );\n}\n", "url": "/artefatos/programacao-mobile/15/a2.jsx"}}, "quizzes": [], "materia": "Programação Mobile", "materiaSlug": "programacao-mobile"}
---

## Registro de consulta

Este conteúdo foi migrado dos registros do 3º bimestre. Use-o para estudar, revisar e adaptar a atividade ao próprio contexto.
