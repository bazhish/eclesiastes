---
{"materia": "Programação Mobile", "materiaSlug": "programacao-mobile", "bimestre": 3, "semana": 15, "ordem": 10, "aula": "a1", "titulo": "Componentes Avançados de Interface · Aula 1", "atividade": {"titulo": "Atividade Prática - Aula 1: Componentes personalizados", "enunciado": "Para o aplicativo de produtos artesanais, eu criaria componentes pequenos, com uma responsabilidade clara e parâmetros para dados, aparência e ações. Assim, o mesmo cartão pode exibir produtos, notificações ou perfis sem duplicação de código. Também utilizaria o tema do aplicativo em vez de fixar cores e tamanhos dentro do componente.\n\n```kotlin\n@Composable\nfun ProductCard(\n    nome: String,\n    preco: String,\n    imagem: Painter,\n    favorito: Boolean,\n    onFavoritar: () -> Unit,\n    onAdicionarAoCarrinho: () -> Unit,\n    modifier: Modifier = Modifier,\n) {\n    Card(modifier = modifier.padding(8.dp)) {\n        Column(Modifier.padding(16.dp)) {\n            Image(\n                painter = imagem,\n                contentDescription = \"Imagem do produto $nome\",\n                modifier = Modifier.fillMaxWidth().height(160.dp),\n            )\n            Text(nome, style = MaterialTheme.typography.titleMedium)\n            Text(preco, style = MaterialTheme.typography.bodyLarge)\n            Row(horizontalArrangement = Arrangement.SpaceBetween, modifier = Modifier.fillMaxWidth()) {\n                IconButton(onClick = onFavoritar) {\n                    Icon(\n                        imageVector = if (favorito) Icons.Filled.Favorite else Icons.Outlined.FavoriteBorder,\n                        contentDescription = if (favorito) \"Remover dos favoritos\" else \"Adicionar aos favoritos\",\n                    )\n                }\n                Button(onClick = onAdicionarAoCarrinho) { Text(\"Adicionar\") }\n            }\n        }\n    }\n}\n```\n\nNa situação do aviso urgente, eu reutilizaria um `NotificationCard` já existente e adicionaria como parâmetro uma ação opcional de inscrição. Eu priorizaria título, mensagem, botão acessível e integração com o tema. Antes de publicar, verificaria o fluxo do botão e o contraste no modo claro e escuro. Isso entrega a função necessária sem criar um componente isolado e difícil de manter em apenas 30 minutos.", "resposta": "Para o aplicativo de produtos artesanais, eu criaria componentes pequenos, com uma responsabilidade clara e parâmetros para dados, aparência e ações. Assim, o mesmo cartão pode exibir produtos, notificações ou perfis sem duplicação de código. Também utilizaria o tema do aplicativo em vez de fixar cores e tamanhos dentro do componente.\n\n```kotlin\n@Composable\nfun ProductCard(\n    nome: String,\n    preco: String,\n    imagem: Painter,\n    favorito: Boolean,\n    onFavoritar: () -> Unit,\n    onAdicionarAoCarrinho: () -> Unit,\n    modifier: Modifier = Modifier,\n) {\n    Card(modifier = modifier.padding(8.dp)) {\n        Column(Modifier.padding(16.dp)) {\n            Image(\n                painter = imagem,\n                contentDescription = \"Imagem do produto $nome\",\n                modifier = Modifier.fillMaxWidth().height(160.dp),\n            )\n            Text(nome, style = MaterialTheme.typography.titleMedium)\n            Text(preco, style = MaterialTheme.typography.bodyLarge)\n            Row(horizontalArrangement = Arrangement.SpaceBetween, modifier = Modifier.fillMaxWidth()) {\n                IconButton(onClick = onFavoritar) {\n                    Icon(\n                        imageVector = if (favorito) Icons.Filled.Favorite else Icons.Outlined.FavoriteBorder,\n                        contentDescription = if (favorito) \"Remover dos favoritos\" else \"Adicionar aos favoritos\",\n                    )\n                }\n                Button(onClick = onAdicionarAoCarrinho) { Text(\"Adicionar\") }\n            }\n        }\n    }\n}\n```\n\nNa situação do aviso urgente, eu reutilizaria um `NotificationCard` já existente e adicionaria como parâmetro uma ação opcional de inscrição. Eu priorizaria título, mensagem, botão acessível e integração com o tema. Antes de publicar, verificaria o fluxo do botão e o contraste no modo claro e escuro. Isso entrega a função necessária sem criar um componente isolado e difícil de manter em apenas 30 minutos.", "conteudo": "Para o aplicativo de produtos artesanais, eu criaria componentes pequenos, com uma responsabilidade clara e parâmetros para dados, aparência e ações. Assim, o mesmo cartão pode exibir produtos, notificações ou perfis sem duplicação de código. Também utilizaria o tema do aplicativo em vez de fixar cores e tamanhos dentro do componente.\n\n```kotlin\n@Composable\nfun ProductCard(\n    nome: String,\n    preco: String,\n    imagem: Painter,\n    favorito: Boolean,\n    onFavoritar: () -> Unit,\n    onAdicionarAoCarrinho: () -> Unit,\n    modifier: Modifier = Modifier,\n) {\n    Card(modifier = modifier.padding(8.dp)) {\n        Column(Modifier.padding(16.dp)) {\n            Image(\n                painter = imagem,\n                contentDescription = \"Imagem do produto $nome\",\n                modifier = Modifier.fillMaxWidth().height(160.dp),\n            )\n            Text(nome, style = MaterialTheme.typography.titleMedium)\n            Text(preco, style = MaterialTheme.typography.bodyLarge)\n            Row(horizontalArrangement = Arrangement.SpaceBetween, modifier = Modifier.fillMaxWidth()) {\n                IconButton(onClick = onFavoritar) {\n                    Icon(\n                        imageVector = if (favorito) Icons.Filled.Favorite else Icons.Outlined.FavoriteBorder,\n                        contentDescription = if (favorito) \"Remover dos favoritos\" else \"Adicionar aos favoritos\",\n                    )\n                }\n                Button(onClick = onAdicionarAoCarrinho) { Text(\"Adicionar\") }\n            }\n        }\n    }\n}\n```\n\nNa situação do aviso urgente, eu reutilizaria um `NotificationCard` já existente e adicionaria como parâmetro uma ação opcional de inscrição. Eu priorizaria título, mensagem, botão acessível e integração com o tema. Antes de publicar, verificaria o fluxo do botão e o contraste no modo claro e escuro. Isso entrega a função necessária sem criar um componente isolado e difícil de manter em apenas 30 minutos.", "fonte": "gabarito/Programação Mobile/3° bimestre/Semana 15/semana-15-respostas.md#atividade-pratica-aula-1-componentes-personalizados", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
