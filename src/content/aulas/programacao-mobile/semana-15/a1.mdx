---
{"bimestre": 3, "semana": 15, "ordem": 1, "aula": "a1", "titulo": "Animações avançadas", "atividade": {"enunciado": "Roteiro analisado: Atividade Pratica - Aula 2- Animacoes avancadas - 47333.docx. A solução foi reescrita a partir do roteiro original. Etapas que dependem de conta externa, dados reais, pessoas ou envio ao AVA devem ser executadas pelo estudante e registradas como evidência própria.", "resposta": "1. Qual é a importância das animações baseadas em Física para a experiência do usuário em um aplicativo móvel?\n\nAnimações devem comunicar estado e respeitar `prefers-reduced-motion`. Prefira `transform` e `opacity`, evite animar propriedades de layout e limite quantidade simultânea. CSS é suficiente para transições declarativas; JavaScript é indicado quando a animação depende de dados, física ou interação complexa.\n\nAplicação à pergunta: para responder especificamente a “Qual é a importância das animações baseadas em Física para a experiência do usuário em um aplicativo móvel?”, a evidência deve mostrar que essa decisão foi aplicada e verificada no cenário proposto.\n\nNo contexto de Animações avançadas, a decisão deve ser documentada, testada em ambiente controlado e revisada antes de uso em produção.\n\n2. Como as transições personalizadas podem impactar a percepção de um usuário ao navegar pelo aplicativo?\n\nA resposta deve considerar o requisito expresso na pergunta — Como as transições personalizadas podem impactar a percepção de um usuário ao navegar pelo aplicativo? —, definir um critério verificável, registrar a evidência produzida e apontar limites da execução. No contexto de Animações avançadas, a decisão deve ser documentada, testada em ambiente controlado e revisada antes de uso em produção.\n\n3. Qual é o benefício de utilizar animações que simulam o comportamento de \"mola\" ao interagir com elementos da interface?\n\nAnimações devem comunicar estado e respeitar `prefers-reduced-motion`. Prefira `transform` e `opacity`, evite animar propriedades de layout e limite quantidade simultânea. CSS é suficiente para transições declarativas; JavaScript é indicado quando a animação depende de dados, física ou interação complexa.\n\nAplicação à pergunta: para responder especificamente a “Qual é o benefício de utilizar animações que simulam o comportamento de \"mola\" ao interagir com elementos da interface?”, a evidência deve mostrar que essa decisão foi aplicada e verificada no cenário proposto.\n\nNo contexto de Animações avançadas, a decisão deve ser documentada, testada em ambiente controlado e revisada antes de uso em produção.", "artefato": {"linguagem": "jsx", "conteudo": "import { useState } from 'react';\n\nexport default function Atividade() {\n  const [status, setStatus] = useState('Aguardando validação');\n  return (\n    <main>\n      <h1>Animações avançadas</h1>\n      <p aria-live=\"polite\">{status}</p>\n      <button type=\"button\" onClick={() => setStatus('Evidência registrada em ambiente controlado')}>\n        Registrar evidência\n      </button>\n    </main>\n  );\n}\n", "url": "/artefatos/programacao-mobile/15/a1.jsx"}}, "quizzes": [], "materia": "Programação Mobile", "materiaSlug": "programacao-mobile"}
---

## Registro de consulta

Este conteúdo foi migrado dos registros do 3º bimestre. Use-o para estudar, revisar e adaptar a atividade ao próprio contexto.
