---
{"materia": "Programação Mobile", "materiaSlug": "programacao-mobile", "bimestre": 3, "semana": 17, "ordem": 20, "aula": "a2", "titulo": "Banco de Dados Avançados · Aula 2", "atividade": {"titulo": "Atividade Prática - Aula 2: Explorando o Firebase - armazenamento dinâmico em ação", "enunciado": "Para o aplicativo de receitas, criaria a coleção `receitas` no Cloud Firestore. Cada documento teria autor, título, ingredientes, modo de preparo, URL da imagem, data de criação e atualização. O Firebase Authentication identificaria o autor e as regras do Firestore impediriam que uma pessoa alterasse ou apagasse receitas de outras pessoas.\n\n### Modelo de documento\n\n```text\nreceitas/{receitaId}\n  autorId: \"uid-do-usuario\"\n  titulo: \"Bolo de cenoura\"\n  ingredientes: [\"cenoura\", \"farinha\", \"ovos\"]\n  preparo: \"Misturar os ingredientes e assar.\"\n  imagemUrl: \"https://...\"\n  criadoEm: timestamp\n  atualizadoEm: timestamp\n```\n\n### CRUD com Firestore\n\n```kotlin\ndata class Receita(\n    val id: String = \"\",\n    val autorId: String = \"\",\n    val titulo: String = \"\",\n    val ingredientes: List<String> = emptyList(),\n    val preparo: String = \"\",\n)\n\nclass ReceitasRepository(private val db: FirebaseFirestore) {\n    fun criar(receita: Receita) = db.collection(\"receitas\").add(receita)\n\n    fun observar(onAtualizar: (List<Receita>) -> Unit) =\n        db.collection(\"receitas\").orderBy(\"titulo\").addSnapshotListener { dados, erro ->\n            if (erro == null) onAtualizar(dados?.toObjects(Receita::class.java).orEmpty())\n        }\n\n    fun atualizar(id: String, titulo: String, preparo: String) =\n        db.collection(\"receitas\").document(id).update(mapOf(\"titulo\" to titulo, \"preparo\" to preparo))\n\n    fun excluir(id: String) = db.collection(\"receitas\").document(id).delete()\n}\n```\n\nO Firebase acelera o protótipo por disponibilizar autenticação, banco e sincronização sem administrar servidor próprio. Os cuidados são regras de segurança, limites e custo por operação, necessidade de índices em consultas e evitar carregar coleções inteiras quando basta uma paginação.", "resposta": "Para o aplicativo de receitas, criaria a coleção `receitas` no Cloud Firestore. Cada documento teria autor, título, ingredientes, modo de preparo, URL da imagem, data de criação e atualização. O Firebase Authentication identificaria o autor e as regras do Firestore impediriam que uma pessoa alterasse ou apagasse receitas de outras pessoas.\n\n### Modelo de documento\n\n```text\nreceitas/{receitaId}\n  autorId: \"uid-do-usuario\"\n  titulo: \"Bolo de cenoura\"\n  ingredientes: [\"cenoura\", \"farinha\", \"ovos\"]\n  preparo: \"Misturar os ingredientes e assar.\"\n  imagemUrl: \"https://...\"\n  criadoEm: timestamp\n  atualizadoEm: timestamp\n```\n\n### CRUD com Firestore\n\n```kotlin\ndata class Receita(\n    val id: String = \"\",\n    val autorId: String = \"\",\n    val titulo: String = \"\",\n    val ingredientes: List<String> = emptyList(),\n    val preparo: String = \"\",\n)\n\nclass ReceitasRepository(private val db: FirebaseFirestore) {\n    fun criar(receita: Receita) = db.collection(\"receitas\").add(receita)\n\n    fun observar(onAtualizar: (List<Receita>) -> Unit) =\n        db.collection(\"receitas\").orderBy(\"titulo\").addSnapshotListener { dados, erro ->\n            if (erro == null) onAtualizar(dados?.toObjects(Receita::class.java).orEmpty())\n        }\n\n    fun atualizar(id: String, titulo: String, preparo: String) =\n        db.collection(\"receitas\").document(id).update(mapOf(\"titulo\" to titulo, \"preparo\" to preparo))\n\n    fun excluir(id: String) = db.collection(\"receitas\").document(id).delete()\n}\n```\n\nO Firebase acelera o protótipo por disponibilizar autenticação, banco e sincronização sem administrar servidor próprio. Os cuidados são regras de segurança, limites e custo por operação, necessidade de índices em consultas e evitar carregar coleções inteiras quando basta uma paginação.", "conteudo": "Para o aplicativo de receitas, criaria a coleção `receitas` no Cloud Firestore. Cada documento teria autor, título, ingredientes, modo de preparo, URL da imagem, data de criação e atualização. O Firebase Authentication identificaria o autor e as regras do Firestore impediriam que uma pessoa alterasse ou apagasse receitas de outras pessoas.\n\n### Modelo de documento\n\n```text\nreceitas/{receitaId}\n  autorId: \"uid-do-usuario\"\n  titulo: \"Bolo de cenoura\"\n  ingredientes: [\"cenoura\", \"farinha\", \"ovos\"]\n  preparo: \"Misturar os ingredientes e assar.\"\n  imagemUrl: \"https://...\"\n  criadoEm: timestamp\n  atualizadoEm: timestamp\n```\n\n### CRUD com Firestore\n\n```kotlin\ndata class Receita(\n    val id: String = \"\",\n    val autorId: String = \"\",\n    val titulo: String = \"\",\n    val ingredientes: List<String> = emptyList(),\n    val preparo: String = \"\",\n)\n\nclass ReceitasRepository(private val db: FirebaseFirestore) {\n    fun criar(receita: Receita) = db.collection(\"receitas\").add(receita)\n\n    fun observar(onAtualizar: (List<Receita>) -> Unit) =\n        db.collection(\"receitas\").orderBy(\"titulo\").addSnapshotListener { dados, erro ->\n            if (erro == null) onAtualizar(dados?.toObjects(Receita::class.java).orEmpty())\n        }\n\n    fun atualizar(id: String, titulo: String, preparo: String) =\n        db.collection(\"receitas\").document(id).update(mapOf(\"titulo\" to titulo, \"preparo\" to preparo))\n\n    fun excluir(id: String) = db.collection(\"receitas\").document(id).delete()\n}\n```\n\nO Firebase acelera o protótipo por disponibilizar autenticação, banco e sincronização sem administrar servidor próprio. Os cuidados são regras de segurança, limites e custo por operação, necessidade de índices em consultas e evitar carregar coleções inteiras quando basta uma paginação.", "fonte": "gabarito/Programação Mobile/3° bimestre/Semana 17/semana-17-respostas.md#atividade-pratica-aula-2-explorando-o-firebase-armazenamento-dinamico-em-acao", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
