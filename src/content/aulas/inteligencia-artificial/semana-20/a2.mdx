---
{"materia": "Inteligência Artificial", "materiaSlug": "inteligencia-artificial", "bimestre": 3, "semana": 20, "ordem": 20, "aula": "a2", "titulo": "Agentes com RAG e memória · Aula 2", "atividade": {"titulo": "Atividade Prática - Aula 2: Bancos de vetores", "enunciado": "| Etapa | Entrada | Processamento | Saída |\n|---|---|---|---|\n| Indexação | Manuais técnicos | Texto convertido em embedding e armazenado com metadados. | Vetores pesquisáveis e textos originais. |\n| Busca | Consulta do técnico | Consulta convertida em embedding e comparada por similaridade. | Documentos semanticamente mais próximos. |\n\n```text\nfunção buscar(consulta, documentos):\n    vetor_consulta = gerar_embedding(consulta)\n    resultados = []\n    para cada doc em documentos:\n        vetor_doc = doc.embedding\n        similaridade = calcular_similaridade(vetor_consulta, vetor_doc)\n        se similaridade > limite:\n            adicionar (doc, similaridade) aos resultados\n    retornar ordenar_por_similaridade(resultados)\n```\n\n“Falha elétrica” e “defeito no circuito” podem ficar próximos no espaço vetorial porque os embeddings representam contexto e significado, não apenas palavras idênticas. Agrupamentos: `falha elétrica` com `defeito no circuito`; `manutenção preventiva` com `revisão periódica`; `motor superaquecido` com `sobreaquecimento do conjunto motriz`.", "resposta": "| Etapa | Entrada | Processamento | Saída |\n|---|---|---|---|\n| Indexação | Manuais técnicos | Texto convertido em embedding e armazenado com metadados. | Vetores pesquisáveis e textos originais. |\n| Busca | Consulta do técnico | Consulta convertida em embedding e comparada por similaridade. | Documentos semanticamente mais próximos. |\n\n```text\nfunção buscar(consulta, documentos):\n    vetor_consulta = gerar_embedding(consulta)\n    resultados = []\n    para cada doc em documentos:\n        vetor_doc = doc.embedding\n        similaridade = calcular_similaridade(vetor_consulta, vetor_doc)\n        se similaridade > limite:\n            adicionar (doc, similaridade) aos resultados\n    retornar ordenar_por_similaridade(resultados)\n```\n\n“Falha elétrica” e “defeito no circuito” podem ficar próximos no espaço vetorial porque os embeddings representam contexto e significado, não apenas palavras idênticas. Agrupamentos: `falha elétrica` com `defeito no circuito`; `manutenção preventiva` com `revisão periódica`; `motor superaquecido` com `sobreaquecimento do conjunto motriz`.", "conteudo": "| Etapa | Entrada | Processamento | Saída |\n|---|---|---|---|\n| Indexação | Manuais técnicos | Texto convertido em embedding e armazenado com metadados. | Vetores pesquisáveis e textos originais. |\n| Busca | Consulta do técnico | Consulta convertida em embedding e comparada por similaridade. | Documentos semanticamente mais próximos. |\n\n```text\nfunção buscar(consulta, documentos):\n    vetor_consulta = gerar_embedding(consulta)\n    resultados = []\n    para cada doc em documentos:\n        vetor_doc = doc.embedding\n        similaridade = calcular_similaridade(vetor_consulta, vetor_doc)\n        se similaridade > limite:\n            adicionar (doc, similaridade) aos resultados\n    retornar ordenar_por_similaridade(resultados)\n```\n\n“Falha elétrica” e “defeito no circuito” podem ficar próximos no espaço vetorial porque os embeddings representam contexto e significado, não apenas palavras idênticas. Agrupamentos: `falha elétrica` com `defeito no circuito`; `manutenção preventiva` com `revisão periódica`; `motor superaquecido` com `sobreaquecimento do conjunto motriz`.", "fonte": "gabarito/Inteligência Artificial/3° bimestre/Semana 20/semana-20-respostas.md#atividade-pratica-aula-2-bancos-de-vetores", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
