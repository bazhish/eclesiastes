---
{"materia": "Inteligência Artificial", "materiaSlug": "inteligencia-artificial", "bimestre": 3, "semana": 16, "ordem": 30, "aula": "a3", "titulo": "Aprendizado supervisionado - pipeline de ML · Aula 3", "atividade": {"titulo": "Atividade Prática - Aula 3: Ferramenta scikit-learn - fluxo básico", "enunciado": "```python\nfrom sklearn.model_selection import train_test_split\nfrom sklearn.tree import DecisionTreeClassifier\nfrom sklearn.metrics import accuracy_score\n\n# X contém as características: horas de estudo e número de faltas.\n# y contém o rótulo conhecido: 0 = reprovado e 1 = aprovado.\nX = [[2, 1], [4, 0], [1, 3], [3, 1], [5, 0], [2, 2]]\ny = [0, 1, 0, 1, 1, 0]\n\n# O teste usa dados não vistos no treino para verificar generalização.\nX_treino, X_teste, y_treino, y_teste = train_test_split(\n    X, y, test_size=0.33, random_state=42, stratify=y\n)\n\n# A árvore de decisão aprende regras de separação a partir dos exemplos.\nmodelo = DecisionTreeClassifier(random_state=42, max_depth=2)\nmodelo.fit(X_treino, y_treino)\n\nprevisoes = modelo.predict(X_teste)\nacuracia = accuracy_score(y_teste, previsoes)\nprint(f\"Acurácia: {acuracia:.2f}\")\n```\n\nO modelo recebe exemplos de alunos com resultado já conhecido e aprende padrões entre horas de estudo, faltas e aprovação. A árvore de decisão foi escolhida por ser simples de interpretar e adequada a um conjunto pequeno. A acurácia mede a proporção de previsões corretas no teste, mas este resultado não é suficiente para concluir que o modelo é confiável: a base possui apenas seis exemplos e pode não representar a realidade. O primeiro passo em ML é melhorar a quantidade, qualidade e diversidade dos dados, além de comparar modelos e métricas.", "resposta": "```python\nfrom sklearn.model_selection import train_test_split\nfrom sklearn.tree import DecisionTreeClassifier\nfrom sklearn.metrics import accuracy_score\n\n# X contém as características: horas de estudo e número de faltas.\n# y contém o rótulo conhecido: 0 = reprovado e 1 = aprovado.\nX = [[2, 1], [4, 0], [1, 3], [3, 1], [5, 0], [2, 2]]\ny = [0, 1, 0, 1, 1, 0]\n\n# O teste usa dados não vistos no treino para verificar generalização.\nX_treino, X_teste, y_treino, y_teste = train_test_split(\n    X, y, test_size=0.33, random_state=42, stratify=y\n)\n\n# A árvore de decisão aprende regras de separação a partir dos exemplos.\nmodelo = DecisionTreeClassifier(random_state=42, max_depth=2)\nmodelo.fit(X_treino, y_treino)\n\nprevisoes = modelo.predict(X_teste)\nacuracia = accuracy_score(y_teste, previsoes)\nprint(f\"Acurácia: {acuracia:.2f}\")\n```\n\nO modelo recebe exemplos de alunos com resultado já conhecido e aprende padrões entre horas de estudo, faltas e aprovação. A árvore de decisão foi escolhida por ser simples de interpretar e adequada a um conjunto pequeno. A acurácia mede a proporção de previsões corretas no teste, mas este resultado não é suficiente para concluir que o modelo é confiável: a base possui apenas seis exemplos e pode não representar a realidade. O primeiro passo em ML é melhorar a quantidade, qualidade e diversidade dos dados, além de comparar modelos e métricas.", "conteudo": "```python\nfrom sklearn.model_selection import train_test_split\nfrom sklearn.tree import DecisionTreeClassifier\nfrom sklearn.metrics import accuracy_score\n\n# X contém as características: horas de estudo e número de faltas.\n# y contém o rótulo conhecido: 0 = reprovado e 1 = aprovado.\nX = [[2, 1], [4, 0], [1, 3], [3, 1], [5, 0], [2, 2]]\ny = [0, 1, 0, 1, 1, 0]\n\n# O teste usa dados não vistos no treino para verificar generalização.\nX_treino, X_teste, y_treino, y_teste = train_test_split(\n    X, y, test_size=0.33, random_state=42, stratify=y\n)\n\n# A árvore de decisão aprende regras de separação a partir dos exemplos.\nmodelo = DecisionTreeClassifier(random_state=42, max_depth=2)\nmodelo.fit(X_treino, y_treino)\n\nprevisoes = modelo.predict(X_teste)\nacuracia = accuracy_score(y_teste, previsoes)\nprint(f\"Acurácia: {acuracia:.2f}\")\n```\n\nO modelo recebe exemplos de alunos com resultado já conhecido e aprende padrões entre horas de estudo, faltas e aprovação. A árvore de decisão foi escolhida por ser simples de interpretar e adequada a um conjunto pequeno. A acurácia mede a proporção de previsões corretas no teste, mas este resultado não é suficiente para concluir que o modelo é confiável: a base possui apenas seis exemplos e pode não representar a realidade. O primeiro passo em ML é melhorar a quantidade, qualidade e diversidade dos dados, além de comparar modelos e métricas.", "fonte": "gabarito/Inteligência Artificial/3° bimestre/Semana 16/semana-16-respostas.md#atividade-pratica-aula-3-ferramenta-scikit-learn-fluxo-basico", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
