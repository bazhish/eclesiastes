---
{"materia": "Inteligência Artificial", "materiaSlug": "inteligencia-artificial", "bimestre": 3, "semana": 17, "ordem": 30, "aula": "a3", "titulo": "Pré-Processamento e Feature Engineering · Aula 3", "atividade": {"titulo": "Atividade Prática - Aula 3: Seleção e criação de features", "enunciado": "```python\nimport numpy as np\nimport pandas as pd\nfrom sklearn.preprocessing import StandardScaler\nfrom sklearn.decomposition import PCA\n\nnp.random.seed(42)\ndados = pd.DataFrame({\n    \"horas_estudo\": np.random.randint(1, 6, 50),\n    \"exercicios\": np.random.randint(10, 100, 50),\n    \"frequencia\": np.random.randint(60, 100, 50),\n    \"participacao\": np.random.randint(1, 10, 50),\n})\ndados[\"nota_anterior\"] = dados[\"horas_estudo\"] * 1.5 + np.random.normal(0, 1, 50)\n\ncorrelacao = dados.corr(numeric_only=True)\nprint(correlacao)\n\ndados[\"engajamento_total\"] = dados[\"frequencia\"] + dados[\"participacao\"]\n\n# nota_anterior tende a ser redundante com horas_estudo nesta base simulada.\nfeatures = dados.drop(columns=[\"nota_anterior\"])\nfeatures_padronizadas = StandardScaler().fit_transform(features)\npca = PCA(n_components=0.95)\ncomponentes = pca.fit_transform(features_padronizadas)\nprint(\"Variância explicada:\", pca.explained_variance_ratio_)\nprint(\"Número de componentes:\", pca.n_components_)\n```\n\nA matriz de correlação identifica variáveis que se movem juntas. Nesta base, `nota_anterior` foi criada a partir de `horas_estudo`, portanto pode introduzir redundância. A feature `engajamento_total` combina frequência e participação e representa envolvimento do estudante. A padronização é necessária antes do PCA para que variáveis com escalas maiores não dominem os componentes. PCA deve ser usado quando há muitas features correlacionadas e a redução de dimensionalidade preserva variância suficiente, mas reduz a interpretação direta das variáveis originais.", "resposta": "```python\nimport numpy as np\nimport pandas as pd\nfrom sklearn.preprocessing import StandardScaler\nfrom sklearn.decomposition import PCA\n\nnp.random.seed(42)\ndados = pd.DataFrame({\n    \"horas_estudo\": np.random.randint(1, 6, 50),\n    \"exercicios\": np.random.randint(10, 100, 50),\n    \"frequencia\": np.random.randint(60, 100, 50),\n    \"participacao\": np.random.randint(1, 10, 50),\n})\ndados[\"nota_anterior\"] = dados[\"horas_estudo\"] * 1.5 + np.random.normal(0, 1, 50)\n\ncorrelacao = dados.corr(numeric_only=True)\nprint(correlacao)\n\ndados[\"engajamento_total\"] = dados[\"frequencia\"] + dados[\"participacao\"]\n\n# nota_anterior tende a ser redundante com horas_estudo nesta base simulada.\nfeatures = dados.drop(columns=[\"nota_anterior\"])\nfeatures_padronizadas = StandardScaler().fit_transform(features)\npca = PCA(n_components=0.95)\ncomponentes = pca.fit_transform(features_padronizadas)\nprint(\"Variância explicada:\", pca.explained_variance_ratio_)\nprint(\"Número de componentes:\", pca.n_components_)\n```\n\nA matriz de correlação identifica variáveis que se movem juntas. Nesta base, `nota_anterior` foi criada a partir de `horas_estudo`, portanto pode introduzir redundância. A feature `engajamento_total` combina frequência e participação e representa envolvimento do estudante. A padronização é necessária antes do PCA para que variáveis com escalas maiores não dominem os componentes. PCA deve ser usado quando há muitas features correlacionadas e a redução de dimensionalidade preserva variância suficiente, mas reduz a interpretação direta das variáveis originais.", "conteudo": "```python\nimport numpy as np\nimport pandas as pd\nfrom sklearn.preprocessing import StandardScaler\nfrom sklearn.decomposition import PCA\n\nnp.random.seed(42)\ndados = pd.DataFrame({\n    \"horas_estudo\": np.random.randint(1, 6, 50),\n    \"exercicios\": np.random.randint(10, 100, 50),\n    \"frequencia\": np.random.randint(60, 100, 50),\n    \"participacao\": np.random.randint(1, 10, 50),\n})\ndados[\"nota_anterior\"] = dados[\"horas_estudo\"] * 1.5 + np.random.normal(0, 1, 50)\n\ncorrelacao = dados.corr(numeric_only=True)\nprint(correlacao)\n\ndados[\"engajamento_total\"] = dados[\"frequencia\"] + dados[\"participacao\"]\n\n# nota_anterior tende a ser redundante com horas_estudo nesta base simulada.\nfeatures = dados.drop(columns=[\"nota_anterior\"])\nfeatures_padronizadas = StandardScaler().fit_transform(features)\npca = PCA(n_components=0.95)\ncomponentes = pca.fit_transform(features_padronizadas)\nprint(\"Variância explicada:\", pca.explained_variance_ratio_)\nprint(\"Número de componentes:\", pca.n_components_)\n```\n\nA matriz de correlação identifica variáveis que se movem juntas. Nesta base, `nota_anterior` foi criada a partir de `horas_estudo`, portanto pode introduzir redundância. A feature `engajamento_total` combina frequência e participação e representa envolvimento do estudante. A padronização é necessária antes do PCA para que variáveis com escalas maiores não dominem os componentes. PCA deve ser usado quando há muitas features correlacionadas e a redução de dimensionalidade preserva variância suficiente, mas reduz a interpretação direta das variáveis originais.", "fonte": "gabarito/Inteligência Artificial/3° bimestre/Semana 17/semana-17-respostas.md#atividade-pratica-aula-3-selecao-e-criacao-de-features", "arquivos": []}, "tipo": "roteiro", "quizzes": []}
---
