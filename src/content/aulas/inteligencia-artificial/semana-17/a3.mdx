---
{"materia": "Inteligência Artificial", "materiaSlug": "inteligencia-artificial", "bimestre": 3, "semana": 17, "ordem": 30, "aula": "a3", "titulo": "Pre‑Processamento e Feature Engineering · Aula 3", "atividade": {"enunciado": "Analisar correlação, reduzir colinearidade, criar uma feature de engajamento, aplicar PCA e explicar os efeitos na generalização.", "resposta": "`nota_anterior` tende a se correlacionar fortemente com `horas_estudo`, pois foi construída a partir dela. Para reduzir redundância, a análise pode removê-la ou comparar essa escolha por validação. A feature `engajamento_total = frequencia + participacao` representa uma hipótese de envolvimento. O PCA cria componentes não correlacionados; a variância explicada orienta quantos manter. Ele pode reduzir ruído e dimensionalidade, mas reduz a interpretação direta das variáveis originais.", "conteudo": "## Seleção e criação de features\n\n## Enunciado\n\nAnalisar correlação, reduzir colinearidade, criar uma feature de engajamento, aplicar PCA e explicar os efeitos na generalização.\n\n## Resposta\n\n`nota_anterior` tende a se correlacionar fortemente com `horas_estudo`, pois foi construída a partir dela. Para reduzir redundância, a análise pode removê-la ou comparar essa escolha por validação. A feature `engajamento_total = frequencia + participacao` representa uma hipótese de envolvimento. O PCA cria componentes não correlacionados; a variância explicada orienta quantos manter. Ele pode reduzir ruído e dimensionalidade, mas reduz a interpretação direta das variáveis originais.\n\n## Código e verificação\n\nArquivos entregues: [`codigo/aula-3/features.py`](codigo/aula-3/features.py) e [`README.md`](codigo/aula-3/README.md).", "fonte": "gabarito/Inteligência Artificial/3° bimestre/Semana 17 - Pre‑Processamento e Feature Engineering/Gabarito - Roteiro Prático Aula 3.md", "arquivos": [{"caminho": "README.md", "linguagem": "markdown", "conteudo": "# Código — Engenharia de atributos\n\n## Linguagem e versão\n\nPython 3.11+.\n\n## Dependências\n\n`numpy`, `pandas` e `scikit-learn`.\n\n## Instalação e execução\n\n`python -m pip install numpy pandas scikit-learn` e `python features.py`.\n\n## Teste\n\nConfira a matriz de correlação e a variância explicada.\n\n## Resultado esperado\n\nO script mostra a relação entre variáveis e o número de componentes necessário para manter 95% da variância.\n"}, {"caminho": "features.py", "linguagem": "python", "conteudo": "import numpy as np\nimport pandas as pd\nfrom sklearn.decomposition import PCA\nnp.random.seed(42)\ndados=pd.DataFrame({'horas_estudo':np.random.randint(1,6,50),'exercicios':np.random.randint(10,100,50),'frequencia':np.random.randint(60,100,50),'participacao':np.random.randint(1,10,50)})\ndados['nota_anterior']=dados.horas_estudo*1.5+np.random.normal(0,1,50)\ndados['engajamento_total']=dados.frequencia+dados.participacao\nprint(dados.corr(numeric_only=True).round(2))\npca=PCA(n_components=.95,random_state=42).fit(dados.drop(columns='nota_anterior'))\nprint('componentes:',pca.n_components_,'variância:',pca.explained_variance_ratio_.sum())\n"}]}, "quizzes": [], "tipo": "roteiro"}
---
